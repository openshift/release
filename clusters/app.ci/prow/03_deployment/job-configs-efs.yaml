apiVersion: v1
kind: PersistentVolume
metadata:
  name: job-configs-efs-ci
spec:
  capacity:
    storage: 100Gi
  volumeMode: Filesystem
  accessModes:
  - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  csi:
    driver: efs.csi.aws.com
    volumeHandle: fs-04ff518a9f9158510
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: job-configs-nfs
  namespace: ci
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: ""
  volumeName: job-configs-efs-ci
  resources:
    requests:
      storage: 100Gi
