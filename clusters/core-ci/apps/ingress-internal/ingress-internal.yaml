apiVersion: operator.openshift.io/v1
kind: IngressController
metadata:
  namespace: openshift-ingress-operator
  name: internal
spec:
  domain: apps-int.master.ci.devcluster.openshift.com
  routeSelector:
    matchLabels:
      internal: "true"
  endpointPublishingStrategy:
    type: LoadBalancerService
    loadBalancer:
      scope: Internal
