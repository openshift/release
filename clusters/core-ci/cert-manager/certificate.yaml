# !!! WARNING - DO NOT MODIFY !!!
# Generated by cluster-init: https://github.com/openshift/ci-tools/tree/main/cmd/cluster-init
# Modifying this file manually might break some tests in both openshift/ci-tools and openshift/release repositories.
# Please consider, instead, writing a yaml patch in one of the cluster-install.yaml into clusters/_cluster-install/
# or, alternatively, modifying the cluster-init tool itself.

apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  labels:
    aws-project: openshift-ci-infra
  name: apiserver-tls
  namespace: openshift-config
spec:
  dnsNames:
  - api.master.ci.devcluster.openshift.com
  issuerRef:
    kind: ClusterIssuer
    name: cert-issuer-aws
  secretName: apiserver-tls
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  labels:
    aws-project: openshift-ci-infra
  name: apps-tls
  namespace: openshift-ingress
spec:
  dnsNames:
  - '*.apps.master.ci.devcluster.openshift.com'
  issuerRef:
    kind: ClusterIssuer
    name: cert-issuer-aws
  secretName: apps-tls
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  labels:
    gcp-project: openshift-ci-infra
  name: registry-tls
  namespace: openshift-image-registry
spec:
  dnsNames:
  - registry.core.ci.openshift.org
  issuerRef:
    kind: ClusterIssuer
    name: cert-issuer
  secretName: public-route-tls
