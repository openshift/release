apiVersion: imageregistry.operator.openshift.io/v1
kind: Config
metadata:
  finalizers:
  - imageregistry.operator.openshift.io/finalizer
  name: cluster
spec:
  logLevel: Normal
  logging: 2
  managementState: Managed
  observedConfig: null
  operatorLogLevel: Normal
  proxy: {}
  replicas: 3
  requests:
    read:
      maxWaitInQueue: 0s
    write:
      maxWaitInQueue: 0s
  routes:
  - hostname: registry.build01.ci.openshift.org
    name: public-routes
    secretName: public-route-tls
  storage:
    managementState: Managed
    s3:
      bucket: build01-9hdwj-image-registry-us-east-1-nucqrmelsxtgndkbvchwdkw
      encrypt: true
      region: us-east-1
      virtualHostedStyle: false
  unsupportedConfigOverrides: null
