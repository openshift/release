# !!! WARNING - DO NOT MODIFY !!!
# Generated by cluster-init: https://github.com/openshift/ci-tools/tree/main/cmd/cluster-init
# Modifying this file manually might break some tests in both openshift/ci-tools and openshift/release repositories.
# Please consider, instead, writing a yaml patch in one of the cluster-install.yaml into clusters/_cluster-install/
# or, alternatively, modifying the cluster-init tool itself.

apiVersion: imageregistry.operator.openshift.io/v1
kind: Config
metadata:
  name: cluster
spec:
  affinity:
    podAntiAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
      - podAffinityTerm:
          labelSelector:
            matchExpressions:
            - key: docker-registry
              operator: In
              values:
              - default
          topologyKey: kubernetes.io/hostname
        weight: 100
  logLevel: Normal
  managementState: Managed
  nodeSelector:
    node-role.kubernetes.io/infra: ""
  operatorLogLevel: Normal
  replicas: 5
  routes:
  - hostname: registry.build04.ci.openshift.org
    name: public-routes
    secretName: public-route-tls
  storage:
    managementState: Managed
    s3:
      bucket: build04-image-registry
      encrypt: true
      region: us-east-1
      regionEndpoint: https://2209d6dda25891dee55b086a469130de.r2.cloudflarestorage.com
      trustedCA:
        name: ""
      virtualHostedStyle: false
  tolerations:
  - effect: NoSchedule
    key: node-role.kubernetes.io/infra
    operator: Exists
