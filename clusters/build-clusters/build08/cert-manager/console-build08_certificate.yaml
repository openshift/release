apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  labels:
    gcp-project: openshift-ci-infra
  name: console-build08
  namespace: openshift-config
spec:
  secretName: console-build08-tls
  issuerRef:
    name: cert-issuer
    kind: ClusterIssuer
  dnsNames:
  - console.build08.ci.openshift.org
