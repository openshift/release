apiVersion: operator.openshift.io/v1
kind: Console
metadata:
  annotations:
    release.openshift.io/create-only: "true"
  name: cluster
spec:
  managementState: Managed
  route:
    hostname: console.build13.ci.openshift.org
    secret:
      name: console-build13-tls
