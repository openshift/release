##################################################################################
#                                DO NOT EDIT
# File generated during execution of: generate-release-controllers.py
##################################################################################


#---------------------------------------------------------------------------------
# art_rpm_mirroring_services.py added the following resource
#---------------------------------------------------------------------------------

apiVersion: v1
kind: Service
metadata:
  name: base-4-20
  namespace: ocp
spec:
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: base-4-20
  type: ClusterIP
---
#---------------------------------------------------------------------------------
# art_rpm_mirroring_services.py added the following resource
#---------------------------------------------------------------------------------

apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    keel.sh/matchTag: 'true'
    keel.sh/policy: force
    keel.sh/pollSchedule: '@every 5m'
    keel.sh/trigger: poll
  labels:
    app: base-4-20
  name: base-4-20
  namespace: ocp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: base-4-20
  template:
    metadata:
      annotations:
        cluster-autoscaler.kubernetes.io/safe-to-evict: 'true'
      labels:
        app: base-4-20
    spec:
      containers:
      - command:
        - content-mirror
        - --path=/tmp/config
        - --max-size=5g
        - --timeout=30m
        - /tmp/repos
        - /tmp/key
        - /tmp/mirror-enterprise-basic-auth
        image: quay-proxy.ci.openshift.org/openshift/ci:ci_content-mirror_latest
        imagePullPolicy: Always
        name: mirror
        ports:
        - containerPort: 8080
          name: http
        resources:
          requests:
            memory: 500Mi
        volumeMounts:
        - mountPath: /tmp/repos
          name: repos
          readOnly: true
        - mountPath: /tmp/key
          name: key
          readOnly: true
        - mountPath: /tmp/mirror-enterprise-basic-auth
          name: mirror-enterprise-basic-auth
          readOnly: true
        - mountPath: /tmp/cache
          name: cache
        workingDir: /tmp/repos
      nodeSelector:
        kubernetes.io/arch: amd64
        kubernetes.io/os: linux
      volumes:
      - configMap:
          items:
          - key: ocp-4.20-default.repo
            path: ocp-4.20-default.repo
          name: base-repos
        name: repos
      - name: key
        secret:
          secretName: mirror.openshift.com
      - name: mirror-enterprise-basic-auth
        secret:
          defaultMode: 420
          secretName: mirror-enterprise-basic-auth
      - emptyDir: {}
        name: cache
      - emptyDir: {}
        name: run
---
#---------------------------------------------------------------------------------
# art_rpm_mirroring_services.py added the following resource
#---------------------------------------------------------------------------------

apiVersion: v1
kind: Service
metadata:
  name: openstack-beta-4-20
  namespace: ocp
spec:
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: openstack-beta-4-20
  type: ClusterIP
---
#---------------------------------------------------------------------------------
# art_rpm_mirroring_services.py added the following resource
#---------------------------------------------------------------------------------

apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    keel.sh/matchTag: 'true'
    keel.sh/policy: force
    keel.sh/pollSchedule: '@every 5m'
    keel.sh/trigger: poll
  labels:
    app: openstack-beta-4-20
  name: openstack-beta-4-20
  namespace: ocp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: openstack-beta-4-20
  template:
    metadata:
      annotations:
        cluster-autoscaler.kubernetes.io/safe-to-evict: 'true'
      labels:
        app: openstack-beta-4-20
    spec:
      containers:
      - command:
        - content-mirror
        - --path=/tmp/config
        - --max-size=5g
        - --timeout=30m
        - /tmp/repos
        - /tmp/key
        - /tmp/mirror-enterprise-basic-auth
        image: quay-proxy.ci.openshift.org/openshift/ci:ci_content-mirror_latest
        imagePullPolicy: Always
        name: mirror
        ports:
        - containerPort: 8080
          name: http
        resources:
          requests:
            memory: 500Mi
        volumeMounts:
        - mountPath: /tmp/repos
          name: repos
          readOnly: true
        - mountPath: /tmp/key
          name: key
          readOnly: true
        - mountPath: /tmp/mirror-enterprise-basic-auth
          name: mirror-enterprise-basic-auth
          readOnly: true
        - mountPath: /tmp/cache
          name: cache
        workingDir: /tmp/repos
      nodeSelector:
        kubernetes.io/arch: amd64
        kubernetes.io/os: linux
      volumes:
      - configMap:
          items:
          - key: ocp-4.20-openstack-beta.repo
            path: ocp-4.20-openstack-beta.repo
          name: base-repos
        name: repos
      - name: key
        secret:
          secretName: mirror.openshift.com
      - name: mirror-enterprise-basic-auth
        secret:
          defaultMode: 420
          secretName: mirror-enterprise-basic-auth
      - emptyDir: {}
        name: cache
      - emptyDir: {}
        name: run
---
#---------------------------------------------------------------------------------
# art_rpm_mirroring_services.py added the following resource
#---------------------------------------------------------------------------------

apiVersion: v1
kind: Service
metadata:
  name: base-openstack-4-20
  namespace: ocp
spec:
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: base-openstack-4-20
  type: ClusterIP
---
#---------------------------------------------------------------------------------
# art_rpm_mirroring_services.py added the following resource
#---------------------------------------------------------------------------------

apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    keel.sh/matchTag: 'true'
    keel.sh/policy: force
    keel.sh/pollSchedule: '@every 5m'
    keel.sh/trigger: poll
  labels:
    app: base-openstack-4-20
  name: base-openstack-4-20
  namespace: ocp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: base-openstack-4-20
  template:
    metadata:
      annotations:
        cluster-autoscaler.kubernetes.io/safe-to-evict: 'true'
      labels:
        app: base-openstack-4-20
    spec:
      containers:
      - command:
        - content-mirror
        - --path=/tmp/config
        - --max-size=5g
        - --timeout=30m
        - /tmp/repos
        - /tmp/key
        - /tmp/mirror-enterprise-basic-auth
        image: quay-proxy.ci.openshift.org/openshift/ci:ci_content-mirror_latest
        imagePullPolicy: Always
        name: mirror
        ports:
        - containerPort: 8080
          name: http
        resources:
          requests:
            memory: 500Mi
        volumeMounts:
        - mountPath: /tmp/repos
          name: repos
          readOnly: true
        - mountPath: /tmp/key
          name: key
          readOnly: true
        - mountPath: /tmp/mirror-enterprise-basic-auth
          name: mirror-enterprise-basic-auth
          readOnly: true
        - mountPath: /tmp/cache
          name: cache
        workingDir: /tmp/repos
      nodeSelector:
        kubernetes.io/arch: amd64
        kubernetes.io/os: linux
      volumes:
      - configMap:
          items:
          - key: ocp-4.20-openstack.repo
            path: ocp-4.20-openstack.repo
          name: base-repos
        name: repos
      - name: key
        secret:
          secretName: mirror.openshift.com
      - name: mirror-enterprise-basic-auth
        secret:
          defaultMode: 420
          secretName: mirror-enterprise-basic-auth
      - emptyDir: {}
        name: cache
      - emptyDir: {}
        name: run
---
#---------------------------------------------------------------------------------
# art_rpm_mirroring_services.py added the following resource
#---------------------------------------------------------------------------------

apiVersion: v1
kind: Service
metadata:
  name: base-4-20-ppc64le
  namespace: ocp
spec:
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: base-4-20-ppc64le
  type: ClusterIP
---
#---------------------------------------------------------------------------------
# art_rpm_mirroring_services.py added the following resource
#---------------------------------------------------------------------------------

apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    keel.sh/matchTag: 'true'
    keel.sh/policy: force
    keel.sh/pollSchedule: '@every 5m'
    keel.sh/trigger: poll
  labels:
    app: base-4-20-ppc64le
  name: base-4-20-ppc64le
  namespace: ocp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: base-4-20-ppc64le
  template:
    metadata:
      annotations:
        cluster-autoscaler.kubernetes.io/safe-to-evict: 'true'
      labels:
        app: base-4-20-ppc64le
    spec:
      containers:
      - command:
        - content-mirror
        - --path=/tmp/config
        - --max-size=5g
        - --timeout=30m
        - /tmp/repos
        - /tmp/key
        - /tmp/mirror-enterprise-basic-auth
        image: quay-proxy.ci.openshift.org/openshift/ci:ci_content-mirror_latest
        imagePullPolicy: Always
        name: mirror
        ports:
        - containerPort: 8080
          name: http
        resources:
          requests:
            memory: 500Mi
        volumeMounts:
        - mountPath: /tmp/repos
          name: repos
          readOnly: true
        - mountPath: /tmp/key
          name: key
          readOnly: true
        - mountPath: /tmp/mirror-enterprise-basic-auth
          name: mirror-enterprise-basic-auth
          readOnly: true
        - mountPath: /tmp/cache
          name: cache
        workingDir: /tmp/repos
      nodeSelector:
        kubernetes.io/arch: amd64
        kubernetes.io/os: linux
      volumes:
      - configMap:
          items:
          - key: ocp-4.20-ppc64le.repo
            path: ocp-4.20-ppc64le.repo
          name: base-repos
        name: repos
      - name: key
        secret:
          secretName: mirror.openshift.com
      - name: mirror-enterprise-basic-auth
        secret:
          defaultMode: 420
          secretName: mirror-enterprise-basic-auth
      - emptyDir: {}
        name: cache
      - emptyDir: {}
        name: run
---
#---------------------------------------------------------------------------------
# art_rpm_mirroring_services.py added the following resource
#---------------------------------------------------------------------------------

apiVersion: v1
kind: Service
metadata:
  name: base-4-20-rhel-8-ironic-prevalidation
  namespace: ocp
spec:
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: base-4-20-rhel-8-ironic-prevalidation
  type: ClusterIP
---
#---------------------------------------------------------------------------------
# art_rpm_mirroring_services.py added the following resource
#---------------------------------------------------------------------------------

apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    keel.sh/matchTag: 'true'
    keel.sh/policy: force
    keel.sh/pollSchedule: '@every 5m'
    keel.sh/trigger: poll
  labels:
    app: base-4-20-rhel-8-ironic-prevalidation
  name: base-4-20-rhel-8-ironic-prevalidation
  namespace: ocp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: base-4-20-rhel-8-ironic-prevalidation
  template:
    metadata:
      annotations:
        cluster-autoscaler.kubernetes.io/safe-to-evict: 'true'
      labels:
        app: base-4-20-rhel-8-ironic-prevalidation
    spec:
      containers:
      - command:
        - content-mirror
        - --path=/tmp/config
        - --max-size=5g
        - --timeout=30m
        - /tmp/repos
        - /tmp/key
        - /tmp/mirror-enterprise-basic-auth
        image: quay-proxy.ci.openshift.org/openshift/ci:ci_content-mirror_latest
        imagePullPolicy: Always
        name: mirror
        ports:
        - containerPort: 8080
          name: http
        resources:
          requests:
            memory: 500Mi
        volumeMounts:
        - mountPath: /tmp/repos
          name: repos
          readOnly: true
        - mountPath: /tmp/key
          name: key
          readOnly: true
        - mountPath: /tmp/mirror-enterprise-basic-auth
          name: mirror-enterprise-basic-auth
          readOnly: true
        - mountPath: /tmp/cache
          name: cache
        workingDir: /tmp/repos
      nodeSelector:
        kubernetes.io/arch: amd64
        kubernetes.io/os: linux
      volumes:
      - configMap:
          items:
          - key: ocp-4.20-rhel-8-ironic-prevalidation.repo
            path: ocp-4.20-rhel-8-ironic-prevalidation.repo
          name: base-repos
        name: repos
      - name: key
        secret:
          secretName: mirror.openshift.com
      - name: mirror-enterprise-basic-auth
        secret:
          defaultMode: 420
          secretName: mirror-enterprise-basic-auth
      - emptyDir: {}
        name: cache
      - emptyDir: {}
        name: run
---
#---------------------------------------------------------------------------------
# art_rpm_mirroring_services.py added the following resource
#---------------------------------------------------------------------------------

apiVersion: v1
kind: Service
metadata:
  name: base-4-20-rhel-8-server-ironic
  namespace: ocp
spec:
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: base-4-20-rhel-8-server-ironic
  type: ClusterIP
---
#---------------------------------------------------------------------------------
# art_rpm_mirroring_services.py added the following resource
#---------------------------------------------------------------------------------

apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    keel.sh/matchTag: 'true'
    keel.sh/policy: force
    keel.sh/pollSchedule: '@every 5m'
    keel.sh/trigger: poll
  labels:
    app: base-4-20-rhel-8-server-ironic
  name: base-4-20-rhel-8-server-ironic
  namespace: ocp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: base-4-20-rhel-8-server-ironic
  template:
    metadata:
      annotations:
        cluster-autoscaler.kubernetes.io/safe-to-evict: 'true'
      labels:
        app: base-4-20-rhel-8-server-ironic
    spec:
      containers:
      - command:
        - content-mirror
        - --path=/tmp/config
        - --max-size=5g
        - --timeout=30m
        - /tmp/repos
        - /tmp/key
        - /tmp/mirror-enterprise-basic-auth
        image: quay-proxy.ci.openshift.org/openshift/ci:ci_content-mirror_latest
        imagePullPolicy: Always
        name: mirror
        ports:
        - containerPort: 8080
          name: http
        resources:
          requests:
            memory: 500Mi
        volumeMounts:
        - mountPath: /tmp/repos
          name: repos
          readOnly: true
        - mountPath: /tmp/key
          name: key
          readOnly: true
        - mountPath: /tmp/mirror-enterprise-basic-auth
          name: mirror-enterprise-basic-auth
          readOnly: true
        - mountPath: /tmp/cache
          name: cache
        workingDir: /tmp/repos
      nodeSelector:
        kubernetes.io/arch: amd64
        kubernetes.io/os: linux
      volumes:
      - configMap:
          items:
          - key: ocp-4.20-rhel-8-server-ironic.repo
            path: ocp-4.20-rhel-8-server-ironic.repo
          name: base-repos
        name: repos
      - name: key
        secret:
          secretName: mirror.openshift.com
      - name: mirror-enterprise-basic-auth
        secret:
          defaultMode: 420
          secretName: mirror-enterprise-basic-auth
      - emptyDir: {}
        name: cache
      - emptyDir: {}
        name: run
---
#---------------------------------------------------------------------------------
# art_rpm_mirroring_services.py added the following resource
#---------------------------------------------------------------------------------

apiVersion: v1
kind: Service
metadata:
  name: base-4-20-rhel-9-ironic-prevalidation
  namespace: ocp
spec:
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: base-4-20-rhel-9-ironic-prevalidation
  type: ClusterIP
---
#---------------------------------------------------------------------------------
# art_rpm_mirroring_services.py added the following resource
#---------------------------------------------------------------------------------

apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    keel.sh/matchTag: 'true'
    keel.sh/policy: force
    keel.sh/pollSchedule: '@every 5m'
    keel.sh/trigger: poll
  labels:
    app: base-4-20-rhel-9-ironic-prevalidation
  name: base-4-20-rhel-9-ironic-prevalidation
  namespace: ocp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: base-4-20-rhel-9-ironic-prevalidation
  template:
    metadata:
      annotations:
        cluster-autoscaler.kubernetes.io/safe-to-evict: 'true'
      labels:
        app: base-4-20-rhel-9-ironic-prevalidation
    spec:
      containers:
      - command:
        - content-mirror
        - --path=/tmp/config
        - --max-size=5g
        - --timeout=30m
        - /tmp/repos
        - /tmp/key
        - /tmp/mirror-enterprise-basic-auth
        image: quay-proxy.ci.openshift.org/openshift/ci:ci_content-mirror_latest
        imagePullPolicy: Always
        name: mirror
        ports:
        - containerPort: 8080
          name: http
        resources:
          requests:
            memory: 500Mi
        volumeMounts:
        - mountPath: /tmp/repos
          name: repos
          readOnly: true
        - mountPath: /tmp/key
          name: key
          readOnly: true
        - mountPath: /tmp/mirror-enterprise-basic-auth
          name: mirror-enterprise-basic-auth
          readOnly: true
        - mountPath: /tmp/cache
          name: cache
        workingDir: /tmp/repos
      nodeSelector:
        kubernetes.io/arch: amd64
        kubernetes.io/os: linux
      volumes:
      - configMap:
          items:
          - key: ocp-4.20-rhel-9-ironic-prevalidation.repo
            path: ocp-4.20-rhel-9-ironic-prevalidation.repo
          name: base-repos
        name: repos
      - name: key
        secret:
          secretName: mirror.openshift.com
      - name: mirror-enterprise-basic-auth
        secret:
          defaultMode: 420
          secretName: mirror-enterprise-basic-auth
      - emptyDir: {}
        name: cache
      - emptyDir: {}
        name: run
---
#---------------------------------------------------------------------------------
# art_rpm_mirroring_services.py added the following resource
#---------------------------------------------------------------------------------

apiVersion: v1
kind: Service
metadata:
  name: base-4-20-rhel-9-server-ironic
  namespace: ocp
spec:
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: base-4-20-rhel-9-server-ironic
  type: ClusterIP
---
#---------------------------------------------------------------------------------
# art_rpm_mirroring_services.py added the following resource
#---------------------------------------------------------------------------------

apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    keel.sh/matchTag: 'true'
    keel.sh/policy: force
    keel.sh/pollSchedule: '@every 5m'
    keel.sh/trigger: poll
  labels:
    app: base-4-20-rhel-9-server-ironic
  name: base-4-20-rhel-9-server-ironic
  namespace: ocp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: base-4-20-rhel-9-server-ironic
  template:
    metadata:
      annotations:
        cluster-autoscaler.kubernetes.io/safe-to-evict: 'true'
      labels:
        app: base-4-20-rhel-9-server-ironic
    spec:
      containers:
      - command:
        - content-mirror
        - --path=/tmp/config
        - --max-size=5g
        - --timeout=30m
        - /tmp/repos
        - /tmp/key
        - /tmp/mirror-enterprise-basic-auth
        image: quay-proxy.ci.openshift.org/openshift/ci:ci_content-mirror_latest
        imagePullPolicy: Always
        name: mirror
        ports:
        - containerPort: 8080
          name: http
        resources:
          requests:
            memory: 500Mi
        volumeMounts:
        - mountPath: /tmp/repos
          name: repos
          readOnly: true
        - mountPath: /tmp/key
          name: key
          readOnly: true
        - mountPath: /tmp/mirror-enterprise-basic-auth
          name: mirror-enterprise-basic-auth
          readOnly: true
        - mountPath: /tmp/cache
          name: cache
        workingDir: /tmp/repos
      nodeSelector:
        kubernetes.io/arch: amd64
        kubernetes.io/os: linux
      volumes:
      - configMap:
          items:
          - key: ocp-4.20-rhel-9-server-ironic.repo
            path: ocp-4.20-rhel-9-server-ironic.repo
          name: base-repos
        name: repos
      - name: key
        secret:
          secretName: mirror.openshift.com
      - name: mirror-enterprise-basic-auth
        secret:
          defaultMode: 420
          secretName: mirror-enterprise-basic-auth
      - emptyDir: {}
        name: cache
      - emptyDir: {}
        name: run
---
#---------------------------------------------------------------------------------
# art_rpm_mirroring_services.py added the following resource
#---------------------------------------------------------------------------------

apiVersion: v1
kind: Service
metadata:
  name: base-4-20-rhel10
  namespace: ocp
spec:
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: base-4-20-rhel10
  type: ClusterIP
---
#---------------------------------------------------------------------------------
# art_rpm_mirroring_services.py added the following resource
#---------------------------------------------------------------------------------

apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    keel.sh/matchTag: 'true'
    keel.sh/policy: force
    keel.sh/pollSchedule: '@every 5m'
    keel.sh/trigger: poll
  labels:
    app: base-4-20-rhel10
  name: base-4-20-rhel10
  namespace: ocp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: base-4-20-rhel10
  template:
    metadata:
      annotations:
        cluster-autoscaler.kubernetes.io/safe-to-evict: 'true'
      labels:
        app: base-4-20-rhel10
    spec:
      containers:
      - command:
        - content-mirror
        - --path=/tmp/config
        - --max-size=5g
        - --timeout=30m
        - /tmp/repos
        - /tmp/key
        - /tmp/mirror-enterprise-basic-auth
        image: quay-proxy.ci.openshift.org/openshift/ci:ci_content-mirror_latest
        imagePullPolicy: Always
        name: mirror
        ports:
        - containerPort: 8080
          name: http
        resources:
          requests:
            memory: 500Mi
        volumeMounts:
        - mountPath: /tmp/repos
          name: repos
          readOnly: true
        - mountPath: /tmp/key
          name: key
          readOnly: true
        - mountPath: /tmp/mirror-enterprise-basic-auth
          name: mirror-enterprise-basic-auth
          readOnly: true
        - mountPath: /tmp/cache
          name: cache
        workingDir: /tmp/repos
      nodeSelector:
        kubernetes.io/arch: amd64
        kubernetes.io/os: linux
      volumes:
      - configMap:
          items:
          - key: ocp-4.20-rhel10.repo
            path: ocp-4.20-rhel10.repo
          name: base-repos
        name: repos
      - name: key
        secret:
          secretName: mirror.openshift.com
      - name: mirror-enterprise-basic-auth
        secret:
          defaultMode: 420
          secretName: mirror-enterprise-basic-auth
      - emptyDir: {}
        name: cache
      - emptyDir: {}
        name: run
---
#---------------------------------------------------------------------------------
# art_rpm_mirroring_services.py added the following resource
#---------------------------------------------------------------------------------

apiVersion: v1
kind: Service
metadata:
  name: base-4-20-rhel8-aarch64
  namespace: ocp
spec:
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: base-4-20-rhel8-aarch64
  type: ClusterIP
---
#---------------------------------------------------------------------------------
# art_rpm_mirroring_services.py added the following resource
#---------------------------------------------------------------------------------

apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    keel.sh/matchTag: 'true'
    keel.sh/policy: force
    keel.sh/pollSchedule: '@every 5m'
    keel.sh/trigger: poll
  labels:
    app: base-4-20-rhel8-aarch64
  name: base-4-20-rhel8-aarch64
  namespace: ocp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: base-4-20-rhel8-aarch64
  template:
    metadata:
      annotations:
        cluster-autoscaler.kubernetes.io/safe-to-evict: 'true'
      labels:
        app: base-4-20-rhel8-aarch64
    spec:
      containers:
      - command:
        - content-mirror
        - --path=/tmp/config
        - --max-size=5g
        - --timeout=30m
        - /tmp/repos
        - /tmp/key
        - /tmp/mirror-enterprise-basic-auth
        image: quay-proxy.ci.openshift.org/openshift/ci:ci_content-mirror_latest
        imagePullPolicy: Always
        name: mirror
        ports:
        - containerPort: 8080
          name: http
        resources:
          requests:
            memory: 500Mi
        volumeMounts:
        - mountPath: /tmp/repos
          name: repos
          readOnly: true
        - mountPath: /tmp/key
          name: key
          readOnly: true
        - mountPath: /tmp/mirror-enterprise-basic-auth
          name: mirror-enterprise-basic-auth
          readOnly: true
        - mountPath: /tmp/cache
          name: cache
        workingDir: /tmp/repos
      nodeSelector:
        kubernetes.io/arch: amd64
        kubernetes.io/os: linux
      volumes:
      - configMap:
          items:
          - key: ocp-4.20-rhel8-aarch64.repo
            path: ocp-4.20-rhel8-aarch64.repo
          name: base-repos
        name: repos
      - name: key
        secret:
          secretName: mirror.openshift.com
      - name: mirror-enterprise-basic-auth
        secret:
          defaultMode: 420
          secretName: mirror-enterprise-basic-auth
      - emptyDir: {}
        name: cache
      - emptyDir: {}
        name: run
---
#---------------------------------------------------------------------------------
# art_rpm_mirroring_services.py added the following resource
#---------------------------------------------------------------------------------

apiVersion: v1
kind: Service
metadata:
  name: base-4-20-rhel8
  namespace: ocp
spec:
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: base-4-20-rhel8
  type: ClusterIP
---
#---------------------------------------------------------------------------------
# art_rpm_mirroring_services.py added the following resource
#---------------------------------------------------------------------------------

apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    keel.sh/matchTag: 'true'
    keel.sh/policy: force
    keel.sh/pollSchedule: '@every 5m'
    keel.sh/trigger: poll
  labels:
    app: base-4-20-rhel8
  name: base-4-20-rhel8
  namespace: ocp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: base-4-20-rhel8
  template:
    metadata:
      annotations:
        cluster-autoscaler.kubernetes.io/safe-to-evict: 'true'
      labels:
        app: base-4-20-rhel8
    spec:
      containers:
      - command:
        - content-mirror
        - --path=/tmp/config
        - --max-size=5g
        - --timeout=30m
        - /tmp/repos
        - /tmp/key
        - /tmp/mirror-enterprise-basic-auth
        image: quay-proxy.ci.openshift.org/openshift/ci:ci_content-mirror_latest
        imagePullPolicy: Always
        name: mirror
        ports:
        - containerPort: 8080
          name: http
        resources:
          requests:
            memory: 500Mi
        volumeMounts:
        - mountPath: /tmp/repos
          name: repos
          readOnly: true
        - mountPath: /tmp/key
          name: key
          readOnly: true
        - mountPath: /tmp/mirror-enterprise-basic-auth
          name: mirror-enterprise-basic-auth
          readOnly: true
        - mountPath: /tmp/cache
          name: cache
        workingDir: /tmp/repos
      nodeSelector:
        kubernetes.io/arch: amd64
        kubernetes.io/os: linux
      volumes:
      - configMap:
          items:
          - key: ocp-4.20-rhel8.repo
            path: ocp-4.20-rhel8.repo
          name: base-repos
        name: repos
      - name: key
        secret:
          secretName: mirror.openshift.com
      - name: mirror-enterprise-basic-auth
        secret:
          defaultMode: 420
          secretName: mirror-enterprise-basic-auth
      - emptyDir: {}
        name: cache
      - emptyDir: {}
        name: run
---
#---------------------------------------------------------------------------------
# art_rpm_mirroring_services.py added the following resource
#---------------------------------------------------------------------------------

apiVersion: v1
kind: Service
metadata:
  name: base-4-20-rhel86
  namespace: ocp
spec:
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: base-4-20-rhel86
  type: ClusterIP
---
#---------------------------------------------------------------------------------
# art_rpm_mirroring_services.py added the following resource
#---------------------------------------------------------------------------------

apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    keel.sh/matchTag: 'true'
    keel.sh/policy: force
    keel.sh/pollSchedule: '@every 5m'
    keel.sh/trigger: poll
  labels:
    app: base-4-20-rhel86
  name: base-4-20-rhel86
  namespace: ocp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: base-4-20-rhel86
  template:
    metadata:
      annotations:
        cluster-autoscaler.kubernetes.io/safe-to-evict: 'true'
      labels:
        app: base-4-20-rhel86
    spec:
      containers:
      - command:
        - content-mirror
        - --path=/tmp/config
        - --max-size=5g
        - --timeout=30m
        - /tmp/repos
        - /tmp/key
        - /tmp/mirror-enterprise-basic-auth
        image: quay-proxy.ci.openshift.org/openshift/ci:ci_content-mirror_latest
        imagePullPolicy: Always
        name: mirror
        ports:
        - containerPort: 8080
          name: http
        resources:
          requests:
            memory: 500Mi
        volumeMounts:
        - mountPath: /tmp/repos
          name: repos
          readOnly: true
        - mountPath: /tmp/key
          name: key
          readOnly: true
        - mountPath: /tmp/mirror-enterprise-basic-auth
          name: mirror-enterprise-basic-auth
          readOnly: true
        - mountPath: /tmp/cache
          name: cache
        workingDir: /tmp/repos
      nodeSelector:
        kubernetes.io/arch: amd64
        kubernetes.io/os: linux
      volumes:
      - configMap:
          items:
          - key: ocp-4.20-rhel86.repo
            path: ocp-4.20-rhel86.repo
          name: base-repos
        name: repos
      - name: key
        secret:
          secretName: mirror.openshift.com
      - name: mirror-enterprise-basic-auth
        secret:
          defaultMode: 420
          secretName: mirror-enterprise-basic-auth
      - emptyDir: {}
        name: cache
      - emptyDir: {}
        name: run
---
#---------------------------------------------------------------------------------
# art_rpm_mirroring_services.py added the following resource
#---------------------------------------------------------------------------------

apiVersion: v1
kind: Service
metadata:
  name: base-4-20-rhel9
  namespace: ocp
spec:
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: base-4-20-rhel9
  type: ClusterIP
---
#---------------------------------------------------------------------------------
# art_rpm_mirroring_services.py added the following resource
#---------------------------------------------------------------------------------

apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    keel.sh/matchTag: 'true'
    keel.sh/policy: force
    keel.sh/pollSchedule: '@every 5m'
    keel.sh/trigger: poll
  labels:
    app: base-4-20-rhel9
  name: base-4-20-rhel9
  namespace: ocp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: base-4-20-rhel9
  template:
    metadata:
      annotations:
        cluster-autoscaler.kubernetes.io/safe-to-evict: 'true'
      labels:
        app: base-4-20-rhel9
    spec:
      containers:
      - command:
        - content-mirror
        - --path=/tmp/config
        - --max-size=5g
        - --timeout=30m
        - /tmp/repos
        - /tmp/key
        - /tmp/mirror-enterprise-basic-auth
        image: quay-proxy.ci.openshift.org/openshift/ci:ci_content-mirror_latest
        imagePullPolicy: Always
        name: mirror
        ports:
        - containerPort: 8080
          name: http
        resources:
          requests:
            memory: 500Mi
        volumeMounts:
        - mountPath: /tmp/repos
          name: repos
          readOnly: true
        - mountPath: /tmp/key
          name: key
          readOnly: true
        - mountPath: /tmp/mirror-enterprise-basic-auth
          name: mirror-enterprise-basic-auth
          readOnly: true
        - mountPath: /tmp/cache
          name: cache
        workingDir: /tmp/repos
      nodeSelector:
        kubernetes.io/arch: amd64
        kubernetes.io/os: linux
      volumes:
      - configMap:
          items:
          - key: ocp-4.20-rhel9.repo
            path: ocp-4.20-rhel9.repo
          name: base-repos
        name: repos
      - name: key
        secret:
          secretName: mirror.openshift.com
      - name: mirror-enterprise-basic-auth
        secret:
          defaultMode: 420
          secretName: mirror-enterprise-basic-auth
      - emptyDir: {}
        name: cache
      - emptyDir: {}
        name: run
---
#---------------------------------------------------------------------------------
# art_rpm_mirroring_services.py added the following resource
#---------------------------------------------------------------------------------

apiVersion: v1
kind: Service
metadata:
  name: base-4-20-rhel92
  namespace: ocp
spec:
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: base-4-20-rhel92
  type: ClusterIP
---
#---------------------------------------------------------------------------------
# art_rpm_mirroring_services.py added the following resource
#---------------------------------------------------------------------------------

apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    keel.sh/matchTag: 'true'
    keel.sh/policy: force
    keel.sh/pollSchedule: '@every 5m'
    keel.sh/trigger: poll
  labels:
    app: base-4-20-rhel92
  name: base-4-20-rhel92
  namespace: ocp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: base-4-20-rhel92
  template:
    metadata:
      annotations:
        cluster-autoscaler.kubernetes.io/safe-to-evict: 'true'
      labels:
        app: base-4-20-rhel92
    spec:
      containers:
      - command:
        - content-mirror
        - --path=/tmp/config
        - --max-size=5g
        - --timeout=30m
        - /tmp/repos
        - /tmp/key
        - /tmp/mirror-enterprise-basic-auth
        image: quay-proxy.ci.openshift.org/openshift/ci:ci_content-mirror_latest
        imagePullPolicy: Always
        name: mirror
        ports:
        - containerPort: 8080
          name: http
        resources:
          requests:
            memory: 500Mi
        volumeMounts:
        - mountPath: /tmp/repos
          name: repos
          readOnly: true
        - mountPath: /tmp/key
          name: key
          readOnly: true
        - mountPath: /tmp/mirror-enterprise-basic-auth
          name: mirror-enterprise-basic-auth
          readOnly: true
        - mountPath: /tmp/cache
          name: cache
        workingDir: /tmp/repos
      nodeSelector:
        kubernetes.io/arch: amd64
        kubernetes.io/os: linux
      volumes:
      - configMap:
          items:
          - key: ocp-4.20-rhel92.repo
            path: ocp-4.20-rhel92.repo
          name: base-repos
        name: repos
      - name: key
        secret:
          secretName: mirror.openshift.com
      - name: mirror-enterprise-basic-auth
        secret:
          defaultMode: 420
          secretName: mirror-enterprise-basic-auth
      - emptyDir: {}
        name: cache
      - emptyDir: {}
        name: run
---
#---------------------------------------------------------------------------------
# art_rpm_mirroring_services.py added the following resource
#---------------------------------------------------------------------------------

apiVersion: v1
kind: Service
metadata:
  name: base-4-20-rhel94
  namespace: ocp
spec:
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: base-4-20-rhel94
  type: ClusterIP
---
#---------------------------------------------------------------------------------
# art_rpm_mirroring_services.py added the following resource
#---------------------------------------------------------------------------------

apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    keel.sh/matchTag: 'true'
    keel.sh/policy: force
    keel.sh/pollSchedule: '@every 5m'
    keel.sh/trigger: poll
  labels:
    app: base-4-20-rhel94
  name: base-4-20-rhel94
  namespace: ocp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: base-4-20-rhel94
  template:
    metadata:
      annotations:
        cluster-autoscaler.kubernetes.io/safe-to-evict: 'true'
      labels:
        app: base-4-20-rhel94
    spec:
      containers:
      - command:
        - content-mirror
        - --path=/tmp/config
        - --max-size=5g
        - --timeout=30m
        - /tmp/repos
        - /tmp/key
        - /tmp/mirror-enterprise-basic-auth
        image: quay-proxy.ci.openshift.org/openshift/ci:ci_content-mirror_latest
        imagePullPolicy: Always
        name: mirror
        ports:
        - containerPort: 8080
          name: http
        resources:
          requests:
            memory: 500Mi
        volumeMounts:
        - mountPath: /tmp/repos
          name: repos
          readOnly: true
        - mountPath: /tmp/key
          name: key
          readOnly: true
        - mountPath: /tmp/mirror-enterprise-basic-auth
          name: mirror-enterprise-basic-auth
          readOnly: true
        - mountPath: /tmp/cache
          name: cache
        workingDir: /tmp/repos
      nodeSelector:
        kubernetes.io/arch: amd64
        kubernetes.io/os: linux
      volumes:
      - configMap:
          items:
          - key: ocp-4.20-rhel94.repo
            path: ocp-4.20-rhel94.repo
          name: base-repos
        name: repos
      - name: key
        secret:
          secretName: mirror.openshift.com
      - name: mirror-enterprise-basic-auth
        secret:
          defaultMode: 420
          secretName: mirror-enterprise-basic-auth
      - emptyDir: {}
        name: cache
      - emptyDir: {}
        name: run
---
#---------------------------------------------------------------------------------
# art_rpm_mirroring_services.py added the following resource
#---------------------------------------------------------------------------------

apiVersion: v1
kind: Service
metadata:
  name: base-4-20-rhel96
  namespace: ocp
spec:
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: base-4-20-rhel96
  type: ClusterIP
---
#---------------------------------------------------------------------------------
# art_rpm_mirroring_services.py added the following resource
#---------------------------------------------------------------------------------

apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    keel.sh/matchTag: 'true'
    keel.sh/policy: force
    keel.sh/pollSchedule: '@every 5m'
    keel.sh/trigger: poll
  labels:
    app: base-4-20-rhel96
  name: base-4-20-rhel96
  namespace: ocp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: base-4-20-rhel96
  template:
    metadata:
      annotations:
        cluster-autoscaler.kubernetes.io/safe-to-evict: 'true'
      labels:
        app: base-4-20-rhel96
    spec:
      containers:
      - command:
        - content-mirror
        - --path=/tmp/config
        - --max-size=5g
        - --timeout=30m
        - /tmp/repos
        - /tmp/key
        - /tmp/mirror-enterprise-basic-auth
        image: quay-proxy.ci.openshift.org/openshift/ci:ci_content-mirror_latest
        imagePullPolicy: Always
        name: mirror
        ports:
        - containerPort: 8080
          name: http
        resources:
          requests:
            memory: 500Mi
        volumeMounts:
        - mountPath: /tmp/repos
          name: repos
          readOnly: true
        - mountPath: /tmp/key
          name: key
          readOnly: true
        - mountPath: /tmp/mirror-enterprise-basic-auth
          name: mirror-enterprise-basic-auth
          readOnly: true
        - mountPath: /tmp/cache
          name: cache
        workingDir: /tmp/repos
      nodeSelector:
        kubernetes.io/arch: amd64
        kubernetes.io/os: linux
      volumes:
      - configMap:
          items:
          - key: ocp-4.20-rhel96.repo
            path: ocp-4.20-rhel96.repo
          name: base-repos
        name: repos
      - name: key
        secret:
          secretName: mirror.openshift.com
      - name: mirror-enterprise-basic-auth
        secret:
          defaultMode: 420
          secretName: mirror-enterprise-basic-auth
      - emptyDir: {}
        name: cache
      - emptyDir: {}
        name: run
---
#---------------------------------------------------------------------------------
# art_rpm_mirroring_services.py added the following resource
#---------------------------------------------------------------------------------

apiVersion: v1
kind: Service
metadata:
  name: base-4-20-s390x
  namespace: ocp
spec:
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: base-4-20-s390x
  type: ClusterIP
---
#---------------------------------------------------------------------------------
# art_rpm_mirroring_services.py added the following resource
#---------------------------------------------------------------------------------

apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    keel.sh/matchTag: 'true'
    keel.sh/policy: force
    keel.sh/pollSchedule: '@every 5m'
    keel.sh/trigger: poll
  labels:
    app: base-4-20-s390x
  name: base-4-20-s390x
  namespace: ocp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: base-4-20-s390x
  template:
    metadata:
      annotations:
        cluster-autoscaler.kubernetes.io/safe-to-evict: 'true'
      labels:
        app: base-4-20-s390x
    spec:
      containers:
      - command:
        - content-mirror
        - --path=/tmp/config
        - --max-size=5g
        - --timeout=30m
        - /tmp/repos
        - /tmp/key
        - /tmp/mirror-enterprise-basic-auth
        image: quay-proxy.ci.openshift.org/openshift/ci:ci_content-mirror_latest
        imagePullPolicy: Always
        name: mirror
        ports:
        - containerPort: 8080
          name: http
        resources:
          requests:
            memory: 500Mi
        volumeMounts:
        - mountPath: /tmp/repos
          name: repos
          readOnly: true
        - mountPath: /tmp/key
          name: key
          readOnly: true
        - mountPath: /tmp/mirror-enterprise-basic-auth
          name: mirror-enterprise-basic-auth
          readOnly: true
        - mountPath: /tmp/cache
          name: cache
        workingDir: /tmp/repos
      nodeSelector:
        kubernetes.io/arch: amd64
        kubernetes.io/os: linux
      volumes:
      - configMap:
          items:
          - key: ocp-4.20-s390x.repo
            path: ocp-4.20-s390x.repo
          name: base-repos
        name: repos
      - name: key
        secret:
          secretName: mirror.openshift.com
      - name: mirror-enterprise-basic-auth
        secret:
          defaultMode: 420
          secretName: mirror-enterprise-basic-auth
      - emptyDir: {}
        name: cache
      - emptyDir: {}
        name: run
