This directory is supposed to contain files that are needed when onboarding a new cluster.
Most likely those files need extra modifications (`sed 's/buildXX/buildYY/g'`) before placing them into the final directory, like `clusters/build-clusters/build01`.
