ref:
  as: enable-qe-catalogsource
  from_image:
    namespace: ocp
    name: "4.14"
    tag: upi-installer
  grace_period: 10m
  commands: enable-qe-catalogsource-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 100Mi
  env:
  - name: CATALOGSOURCE_NAME
    default: qe-app-registry
    documentation: |-
      System environment variable used to create catalogsource with customized name
  - name: SKIP_QE_APP_REGISTRY
    default: "false"
    documentation: |-
      Set to true to skip QE catalogsource installation
  credentials:
  - collection: openshift-qe
    group: mirror-registry
    mount_path: /var/run/vault/mirror-registry
  - collection: deploy-konflux-operator
    group: art-image-share
    mount_path: /var/run/vault/deploy-konflux-operator-art-image-share
  documentation: |-
    Install QE catalog source for OLM operators in connected env.
