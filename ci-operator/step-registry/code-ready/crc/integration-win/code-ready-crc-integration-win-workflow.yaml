workflow:
  as: code-ready-crc-integration-win
  steps:
    pre:
      - chain: upi-azure-nested-pre
    test:
      - ref: code-ready-crc-integration-test
    post:
      - ref: gather-crc
      - chain: upi-azure-nested-post
  documentation: |-
    The crc integration workflow executes the common end-to-end test suite on CRC with a Azure nested cluster configuration.
