ref:
  as: ccm-gather-service-aws
  #optional_on_success: true
  from: upi-installer
  commands: ccm-gather-service-aws-commands.sh
  resources:
    requests:
      cpu: 300m
      memory: 300Mi
  documentation: |-
    The ccm-gather-service-aws step runs the additional step to collect Load Balancer information from AWS API.
