ref:
  as: eap-deploy-resources
  from_image:
    namespace: ocp
    name: cli-jq
    tag: latest
  commands: eap-deploy-resources-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 1000Mi
  documentation: |-
    This ref step is used for deploying secrets, configmaps, deployment-config, persistent-volumes of EAP test-suite.
