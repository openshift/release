workflow:
  as: redhat-et-microshift-e2e-copr-rpm-validation
  steps:
    pre:
      - chain: upi-gcp-rhel8-pre
    test:
      - chain: redhat-et-microshift-e2e-copr-rpm-validation
    post:
      - chain: upi-gcp-rhel8-post