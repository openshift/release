ref:
  as: network-conf-ingress-avoid-worker
  from: cli
  commands: network-conf-ingress-avoid-worker-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 100Mi
  documentation: |-
    This steps place the ingressVIP virtual IP address exclusively with the control plane nodes
