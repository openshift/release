ref:
  as: network-conf-enable-nm-trace
  from: cli
  commands: network-conf-enable-nm-trace-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 100Mi
  documentation: |-
    This step sets level=TRACE for NetworkManager to debug network issues on boot
