chain:
  as: network-mtu-migration-increase
  steps:
  - ref: network-mtu-migration-increase-conf
  - ref: network-mtu-migration-increase
  env:
  - name: MTU_INCREASE
    default: "200"
    documentation: |-
      The increase offset to apply with respect to the current cluster MTU.
  documentation: |-
    The chain configures and performs an increase MTU migration procedure.
