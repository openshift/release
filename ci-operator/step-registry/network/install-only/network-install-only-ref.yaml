ref:
  as: network-install-only
  from: cli
  commands: network-install-only-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 100Mi
  documentation: |-
    This step does nothing; it is used to override the test step in
    other e2e workflows to turn them into tests of simply installing a
    cluster with a given configuration.
