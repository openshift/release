ref:
  as: test-hypershift-crd-validation
  from: hypershift-tests
  cli: latest
  commands: test-hypershift-crd-validation-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 200Mi
  documentation: |-
    Test to verify CRD validation error when installing hypershift on OpenShift 4.15.
    This test specifically checks for the unsupported syntax '.?' error in CRD validation rules.