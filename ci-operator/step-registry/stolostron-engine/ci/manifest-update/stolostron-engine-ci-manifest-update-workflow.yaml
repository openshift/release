workflow:
  as: stolostron-engine-ci-manifest-update
  steps:
    pre:
    - ref: stolostron-engine-ci-rbac
    - ref: stolostron-engine-ci-image-mirror
    - ref: stolostron-engine-ci-manifest-update
  documentation: |-
    Mirrors an image from the CI Registry to a given image ref and
    updates the stolostron-engine image manifest accordingly.
