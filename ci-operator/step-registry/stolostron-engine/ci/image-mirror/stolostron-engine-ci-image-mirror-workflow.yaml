workflow:
  as: stolostron-engine-ci-image-mirror
  steps:
    pre:
    - ref: stolostron-engine-ci-rbac
    - ref: stolostron-engine-ci-image-mirror
  documentation: |-
    Mirrors an image from the CI Registry to a given image ref.
