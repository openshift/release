workflow:
  as: stolostron-engine-ci-rbac
  steps:
    pre:
    - ref: stolostron-engine-ci-rbac
  documentation: |-
    Adds the necessary privileges to the Prow job's namespace so that
    images can be accessed outside the Prow clusters.
