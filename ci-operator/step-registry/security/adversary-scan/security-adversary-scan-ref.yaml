ref:
  as: security-adversary-scan
  from: claude-ai-helpers
  commands: security-adversary-scan-commands.sh
  credentials:
  - namespace: test-credentials
    name: sa-claude-openshift-ci
    mount_path: /var/run/claude-code-service-accoun
  env:
  - name: ADVERSARY_SCAN_MODE
    default: "merge-ref-scan"
    documentation: |-
      Scan mode. Options:
        merge-ref-scan  Scan the merge branch / test merge (default for presubmits).
        full-scan       Scan the entire repo on main (default for periodics).
        groundwork      Full scan with deep codebase analysis (Phases 0 + 0.5).
  - name: CLAUDE_MODEL
    default: "claude-opus-4-6"
    documentation: "Claude model to use for the scan."
  - name: MAX_TURNS
    default: "100"
    documentation: "Maximum conversation turns for the Claude session."
  - name: CLAUDE_CODE_USE_VERTEX
    default: "1"
    documentation: "Set to 1 to route Claude API calls through Vertex AI."
  - name: CLOUD_ML_REGION
    default: "global"
    documentation: "Vertex AI region for Claude API requests."
  - name: ANTHROPIC_VERTEX_PROJECT_ID
    default: "openshift-ci-prow-agents"
    documentation: "GCP project ID for Vertex AI Claude endpoint."
  - name: GOOGLE_APPLICATION_CREDENTIALS
    default: "/var/run/claude-code-service-account/google-token"
    documentation: "Path to GCP service account credentials for Vertex AI authentication."
  - name: GITHUB_PAT_PATH
    default: "/var/run/claude-code-service-account/gh-pat"
    documentation: |-
      Path to a file containing a GitHub PAT for an account with
      openshift-online org membership, used to authenticate
      `claude plugin marketplace add`/`install` agains
      openshift-online/rosa-claude-plugins (which requires org membership
      to read). Expected as an additional key (`gh-pat`) in the existing
      sa-claude-openshift-ci secret — see ONBOARDING.md for how to add it.
  resources:
    requests:
      cpu: 1000m
      memory: 4Gi
  timeout: 3h0m0s
  grace_period: 1m0s
  documentation: |-
    Runs the adversary security scanner (from rosa-claude-plugins/security)
    against the repo source code. It covers many security domains, including:
    SAST, IaC, containers, K8s, CI/CD, secrets, supply chain, web, API, auth,
    database, mobile, cloud, performance, git, agent/skill, and critical
    workflows.

    Three scan modes are available via ADVERSARY_SCAN_MODE:
      merge-ref-scan  Scans the merge branch / test merge.
      full-scan       Scan the entire repo on main.
      groundwork      Full scan with deep architecture and code analysis.

    Results are written to ARTIFACT_DIR as JUnit XML and a full scan log.
    Groundwork mode also produces an interactive HTML report. A severity
    summary is written to SHARED_DIR for downstream notification steps.

    See ONBOARDING.md in this directory for setup instructions.
