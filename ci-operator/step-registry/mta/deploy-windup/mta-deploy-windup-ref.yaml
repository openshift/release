ref:
  as: mta-deploy-windup
  from: cli
  commands: mta-deploy-windup-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    Deploy the Windup CRD and Windup
