ref:
  as: mta-execute-tests
  from: mta-runner
  commands: mta-execute-tests-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  credentials:
    - namespace: test-credentials
      name: mta-ftp-credentials
      mount_path: /tmp/secrets
  documentation: |-
    Executes MTA tests
