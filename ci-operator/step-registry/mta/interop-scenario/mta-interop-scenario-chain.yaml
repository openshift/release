chain:
  as: mta-interop-scenario
  steps:
  - ref: interop-tooling-operator-install
  - ref: mta-prepare
  - ref: mta-execute-tests
  documentation: |-
        Install MTA operator and execute the inerop test scenario