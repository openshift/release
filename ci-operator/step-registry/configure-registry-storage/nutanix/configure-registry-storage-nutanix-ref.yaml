ref:
  as: configure-registry-storage-nutanix
  from: cli
  commands: configure-registry-storage-nutanix-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    Configure image registry to use empty directory.
