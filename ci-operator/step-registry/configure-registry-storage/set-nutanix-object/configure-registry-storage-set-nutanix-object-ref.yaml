ref:
  as: configure-registry-storage-set-nutanix-object 
  from_image:
    namespace: ocp
    name: "4.12"
    tag: upi-installer
  cli: latest
  grace_period: 10m
  commands: configure-registry-storage-set-nutanix-object-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    Configure image registry to use nutanix object storage.
