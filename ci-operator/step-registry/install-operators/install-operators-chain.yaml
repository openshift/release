chain:
  as: install-operators
  steps:
  - ref: install-operators
  - ref: cucushift-installer-check-cluster-health
  documentation: |-
    Used to install multiple operators on a cluster, after the installation of the operators is finished, 
    a cluster health check will be executed.
    Please see the README.md document associated with the `install-operators` ref for more information.