chain:
  as: install-operators
  steps:
  - ref: install-operators
  - ref: cucushift-installer-check-operators
  documentation: |-
    Used to install multiple operators on a cluster.
    Please see the README.md document associated with the `install-operators` ref for more information.
    After the installation of the operators is finished, a cluster health check will be executed.