ref:
  as: hypershift-verify-crd-validation-fix
  from: hypershift-tests
  cli: latest
  commands: hypershift-verify-crd-validation-fix-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 200Mi
  documentation: |-
    Verify that the CRD validation bug with unsupported syntax '.?' is fixed.
    This step checks the hostedclusters CRD for the problematic validation rule
    and attempts to create a test hostedcluster to verify the fix.