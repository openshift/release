chain:
  as: hypershift-hive-create
  steps:
  - ref: hypershift-hive-create-hostedcluster
  env:
    - name: HYPERSHIFT_NODE_COUNT
      default: "1"
    - name: HYPERSHIFT_GUEST_INFRA_OCP_ACCOUNT
      default: true
