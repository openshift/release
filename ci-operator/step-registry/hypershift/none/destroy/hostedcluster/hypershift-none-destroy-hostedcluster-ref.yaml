chain:
  as: hypershift-none-delete-hostedclsuter
  steps:
  - as: destroy
    cli: latest
    env:
    commands: |-
      set -exuo pipefail
      # TO DO : Finish the destruction of HC.
      echo "$(date) Finished deleting cluster"
    from_image:
      namespace: ocp
      name: "4.12"
      tag: hypershift-operator
    grace_period: 5m0s
    resources:
      requests:
        cpu: 100m
        memory: 100Mi
    timeout: 45m0s
