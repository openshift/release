chain:
  as: hypershift-must-gather
  steps:
  - as: must-gather
    from: cli
    commands: |-
      export KUBECONFIG=${SHARED_DIR}/nested_kubeconfig
      oc adm must-gather --dest-dir "${ARTIFACT_DIR}/gather-openshift"
    resources:
      requests:
        cpu: 100m