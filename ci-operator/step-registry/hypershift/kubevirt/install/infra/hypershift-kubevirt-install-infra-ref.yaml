ref:
  as: hypershift-kubevirt-install-infra
  from: upi-installer
  cli: latest
  grace_period: 10m
  env:
  - name: INSTALL_KUBECONFIG
    default: ""
    documentation: |
      Path to a kubeconfig to use for this step. Must be set by the caller to
      $SHARED_DIR/infra-kubeconfig (env var defaults in ref YAMLs are not
      shell-expanded, so the path cannot be defaulted here).
  - name: CNV_SUBSCRIPTION_SOURCE
    default: "cnv-prerelease-catalog-source"
    documentation: |
      subscription source for kubevirt, pre release catalog source by default that
      can be changed to redhat-operators to install stable version, other values
      are not accepted.
  - name: BREW_IMAGE_REGISTRY_USERNAME
    default: '|7820aea2-0d75-11e7-9259-28d244ea5a6d.hhav.f63e13'
  - name: BREW_IMAGE_REGISTRY_TOKEN_PATH
    default: /var/run/cnv-ci-brew-pull-secret/token
  commands: hypershift-kubevirt-install-infra-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 100Mi
  credentials:
  - mount_path: /etc/cnv-nightly-pull-credentials
    name: openshift-cnv-quay-credentials
    namespace: test-credentials
  - collection: ""
    mount_path: /var/run/cnv-ci-brew-pull-secret
    name: cnv-ci-brew-pull-secret
    namespace: test-credentials
  documentation: |-
    install kubevirt operator on the infra cluster.
