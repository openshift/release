chain:
  as: hypershift-mce-ibmz-test
  steps:
    - as: test-ibmz-hostedcluster
      cli: latest
      commands: |-
        set -x
        echo "$(date) Checking for the cluster-operators status to be ready"
        export KUBECONFIG=${SHARED_DIR}/nested_kubeconfig
        while true; do
            count=$(oc get co --insecure-skip-tls-verify --no-headers --request-timeout=300s | awk '$3 != "True" {print $1}' | wc -l)
            if [ "$count" -eq 0 ]
            then
                echo "$(date) All the cluster operators are ready"
                break
            else
                echo "All the cluster operators are not ready yet"
                sleep 5
            fi
            oc get co --insecure-skip-tls-verify --request-timeout=300s
        done
        oc get co --insecure-skip-tls-verify --request-timeout=300s
        oc get no --insecure-skip-tls-verify --request-timeout=300s
      from: tests
      grace_period: 15m0s
      resources:
        requests:
          cpu: 100m
          memory: 100Mi
      timeout: 30m0s