workflow:
  as: hypershift-mce-agent-disconnected-conformance
  documentation: |-
    The HyperShift agent conformance workflow executes tests against a new ephemeral x 1
  steps:
    allow_best_effort_post_steps: true
    allow_skip_on_success: true
    post:
    - ref: wait
    - chain: hypershift-mce-agent-destroy
    - chain: baremetalds-ofcir-post
    test:
    - ref: hypershift-mce-agent-info
    - chain: hypershift-conformance
    pre:
    - chain: baremetalds-ofcir-pre
    - ref: hypershift-install
    - ref: enable-qe-catalogsource-baremetalds-proxy-server
    - ref: enable-qe-catalogsource-disconnected
    - chain: hypershift-mce-agent-lvm
    - ref: hypershift-agent-create-config-dns
    - ref: hypershift-agent-create-hostedcluster
    env:
      DISCONNECTED: "true"
      ENABLE_ICSP: "true"
      CLOUD_PROVIDER: "BM"
      LVM_OPERATOR_SUB_SOURCE: qe-app-registry
      IP_STACK: v6
      DEVSCRIPTS_CONFIG: |
        MIRROR_IMAGES=true
        IP_STACK=v6