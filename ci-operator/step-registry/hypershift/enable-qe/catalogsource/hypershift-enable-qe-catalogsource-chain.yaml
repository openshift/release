chain:
  as: hypershift-enable-qe-catalogsource
  steps:
  - ref: hypershift-enable-qe-pull-secret
  - ref: hypershift-enable-qe-catalogsource
  documentation: |-
    Updates the pull secret and installs the QE catalog source on
    a HyperShift hosted cluster.
