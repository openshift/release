ref:
  as: hypershift-dependabot-triage-report
  from: claude-ai-helpers
  commands: hypershift-dependabot-triage-report-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
  documentation: |-
    Generates an HTML report from the dependabot-triage processing output.
    Parses stream-json token data and produces a readable report in ${ARTIFACT_DIR}.
