ref:
  as: hypershift-self-managed-azure-oadp-destroy
  from: upi-installer
  cli: latest
  commands: hypershift-self-managed-azure-oadp-destroy-commands.sh
  best_effort: true
  timeout: 5m
  resources:
    requests:
      cpu: 300m
      memory: 300Mi
  credentials:
  - mount_path: /etc/hypershift-ci-jobs-self-managed-azure
    name: hypershift-ci-jobs-self-managed-azure
    namespace: test-credentials
  documentation: |-
    Cleans up Azure OADP resources created by hypershift-self-managed-azure-oadp-setup.
    Deletes the dynamically created storage account used for OADP backups.
