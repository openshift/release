chain:
  as: hypershift-standalone-aws-conf
  steps:
  - ref: ipi-conf
  - ref: ipi-conf-telemetry
  - ref: hypershift-standalone-aws-conf
  - ref: ipi-conf-aws-byo-ipv4-pool-public
  - ref: ipi-install-monitoringpvc
  documentation: |-
    The HyperShift standalone AWS configure step chain generates the install-config.yaml file 
    using fixed AWS region and zones with custom AWS credentials instead of cluster profile leasing.
