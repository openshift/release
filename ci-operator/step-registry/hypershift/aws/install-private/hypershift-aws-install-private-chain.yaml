chain:
  as: hypershift-aws-install-private
  steps:
  - ref: hypershift-aws-install-private-config
  - ref: hypershift-aws-install-private
  documentation: |-
    This chain performs the following steps:
    - (AWS only) Create an S3 bucket to hold the OIDC documents
    - Install Hypershift operator
