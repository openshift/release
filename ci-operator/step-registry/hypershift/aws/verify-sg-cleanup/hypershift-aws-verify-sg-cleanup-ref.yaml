ref:
  as: hypershift-aws-verify-sg-cleanup
  cli: latest
  commands: hypershift-aws-verify-sg-cleanup-commands.sh
  credentials:
    - mount_path: /etc/hypershift-pool-aws-credentials
      name: hypershift-pool-aws-credentials
      namespace: test-credentials
  env:
    - name: HYPERSHIFT_AWS_REGION
      default: "us-east-1"
      documentation: "The AWS region to check for security groups."
  from: upi-installer
  best_effort: true
  grace_period: 1m0s
  resources:
    requests:
      cpu: 100m
      memory: 100Mi
  timeout: 5m0s
