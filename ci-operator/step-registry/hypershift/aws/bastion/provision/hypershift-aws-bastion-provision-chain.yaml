chain:
  as: hypershift-aws-bastion-provision
  steps:
    - ref: hypershift-aws-bastion-metadata
    - ref: hypershift-aws-bastion-provision
  documentation: |-
    Create a bastion host on AWS for proxy and mirror registry.
