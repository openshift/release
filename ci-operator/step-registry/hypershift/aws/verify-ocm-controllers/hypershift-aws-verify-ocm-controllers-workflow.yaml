workflow:
  as: hypershift-aws-verify-ocm-controllers
  documentation: |-
    Verifies that CPO v2 preserves HCCO modifications to the OCM Controllers
    field when Image Registry managementState is set to Removed. This ensures
    pull secrets are not created when the registry is disabled.

    Bug: OCPBUGS-81836 / OCPBUGS-79539
  steps:
    pre:
    - ref: ipi-install-rbac
    - chain: hypershift-setup-root-management-cluster
    test:
    - ref: hypershift-aws-verify-ocm-controllers
    post: []
