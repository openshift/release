chain:
  as: hypershift-hostedcluster-healthcheck
  steps:
  - ref: hypershift-hostedcluster-healthcheck-disable-caps
  documentation: |
    Do some configurations and health checks after hosted cluster is created
