ref:
  as: hypershift-docs-build
  from: custom-verify-src
  commands: hypershift-docs-build-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 512Mi
  documentation: |-
    Builds HyperShift MkDocs documentation from the docs/ directory.
    The built site is archived to SHARED_DIR for subsequent steps.
