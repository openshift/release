chain:
  as: agent-appliance-pre
  steps:
  - ref: ofcir-acquire
  - ref: assisted-ofcir-setup
  - ref: ipi-install-rbac
  - ref: baremetalds-devscripts-proxy
  - ref: ipi-install-hosted-loki
  - ref: baremetalds-devscripts-setup
  documentation: |-
    This chain contains all the steps required to setup a cluster using the agent appliance installer
