ref:
  as: ovn-utils
  from_image:
    name: cli-yq
    namespace: ocp
    tag: latest
  commands: ovn-utils-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    This step provides some common bash function utilities to use in different scripts used in the ovn
    step registry workflows.
