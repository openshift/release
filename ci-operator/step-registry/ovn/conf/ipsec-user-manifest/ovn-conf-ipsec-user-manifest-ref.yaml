ref:
  as: ovn-conf-ipsec-user-manifest
  from: cli
  commands: ovn-conf-ipsec-user-manifest-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    The ovn ipsec user manifest step adds the configuration for ipsec libreswan to set of manifests for installation
 

