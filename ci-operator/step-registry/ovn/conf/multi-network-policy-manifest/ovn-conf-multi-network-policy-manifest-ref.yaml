ref:
  as: ovn-conf-multi-network-policy-manifest
  from_image:
   namespace: origin
   name: centos
   tag: '8'
  commands: ovn-conf-multi-network-policy-manifest-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    The ovn ipsec manifest step adds the configuration for ipsec to set of manifests for installation
