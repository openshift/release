ref:
  as: rosa-e2e-record-start-time
  from_image:
    namespace: ocp
    name: "4.18"
    tag: cli
  commands: rosa-e2e-record-start-time-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 50Mi
  timeout: 1m0s
  documentation: |-
    Records the job start time (Unix epoch) to SHARED_DIR/job-start-time.
    Used by rosa-e2e-collect-cs-telemetry post-step to bound RHOBS queries.
