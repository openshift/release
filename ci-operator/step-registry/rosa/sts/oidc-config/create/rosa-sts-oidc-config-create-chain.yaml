chain:
  as: rosa-sts-oidc-config-create
  steps:
  - ref: rosa-sts-account-roles-create
  - ref: rosa-sts-oidc-config-create
  - ref: rosa-sts-operator-roles-create
  documentation: >-
    This chain creates a byo oidc.
