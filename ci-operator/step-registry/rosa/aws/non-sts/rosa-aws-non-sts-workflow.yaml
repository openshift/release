workflow:
  as: rosa-aws-non-sts
  steps:
    env:
      STS: "false"
      COMPUTE_MACHINE_TYPE: "m5.2xlarge"
      MULTI_AZ: "false"
      ENABLE_AUTOSCALING: "true"
      MIN_REPLICAS: "3"
      MAX_REPLICAS: "6"
      ETCD_ENCRYPTION: "true"
      DISABLE_WORKLOAD_MONITORING: "false"
    pre:
    - chain: rosa-cluster-provision
    - ref: osd-ccs-conf-idp-htpasswd-multi-users
    post:
    - chain: rosa-cluster-deprovision
  documentation: |-
    This workflow installs a rosa cluster. The cluster is set with htpasswd idp, and the login informations are stored under $SHARED_DIR/api.login.
    After finish testing, the cluster will be deprovsioned.
