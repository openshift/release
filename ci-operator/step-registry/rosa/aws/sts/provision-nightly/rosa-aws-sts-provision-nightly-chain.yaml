chain:
  as: rosa-aws-sts-provision-nightly
  steps:
  - chain: rosa-aws-sts-provision
  - ref: rosa-cluster-allow-unsigned
  documentation: >-
    Same as rosa-aws-sts-provision but allows unsigned nightly images
    on the cluster under test. Use for Classic STS conformance jobs that
    test nightly OCP payloads on 4.22+ clusters.
