chain:
  as: rosa-aws-sts-provision-no-idp
  steps:
  - ref: rosa-sts-account-roles-create
  - ref: rosa-sts-cluster-provision
  - ref: rosa-operators-wait-ready
  documentation: |-
    This chain installs a rosa sts cluster.
