chain:
  as: rosa-gather-hcp-diagnostics
  steps:
  - ref: rosa-gather-rhobs-logs
  documentation: |-
    Collects RHOBS diagnostic logs for HCP clusters. Wraps rosa-gather-rhobs-logs
    as a single entry point for workflows and inline FVT job post steps.
