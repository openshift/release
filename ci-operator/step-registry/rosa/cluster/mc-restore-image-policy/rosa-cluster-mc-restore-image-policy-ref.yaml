ref:
  as: rosa-cluster-mc-restore-image-policy
  best_effort: true
  from: rosa-aws-cli
  commands: rosa-cluster-mc-restore-image-policy-commands.sh
  timeout: 5m0s
  resources:
    requests:
      cpu: 100m
      memory: 300Mi
  documentation: |-
    Restore the management cluster's ClusterImagePolicy to its original state
    after HCP nightly testing. Removes the CVO override and restores the original
    scopes. Paired with rosa-cluster-mc-allow-unsigned pre step.
