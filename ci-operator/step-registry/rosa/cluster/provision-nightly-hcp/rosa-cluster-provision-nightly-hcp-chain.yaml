chain:
  as: rosa-cluster-provision-nightly-hcp
  steps:
  - ref: rosa-cluster-provision
  - ref: rosa-cluster-mc-allow-unsigned
  - ref: rosa-cluster-wait-ready-cluster
  - ref: rosa-cluster-notify-error
  - ref: rosa-conf-idp-htpasswd
  - ref: rosa-cluster-wait-ready-operators
  documentation: |-
    Same as rosa-cluster-provision but patches the management cluster's
    ClusterImagePolicy after cluster creation to allow unsigned nightly
    images. Required for HCP nightly conformance testing on 4.22+ MCs.
