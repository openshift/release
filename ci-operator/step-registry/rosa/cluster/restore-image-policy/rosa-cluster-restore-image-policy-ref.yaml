ref:
  as: rosa-cluster-restore-image-policy
  best_effort: true
  from: cli
  commands: rosa-cluster-restore-image-policy-commands.sh
  timeout: 5m0s
  resources:
    requests:
      cpu: 100m
      memory: 300Mi
  documentation: |-
    Restore the cluster's ClusterImagePolicy to its original state after
    nightly testing. Removes the CVO override and restores the original
    scopes. Paired with rosa-cluster-allow-unsigned pre step.
