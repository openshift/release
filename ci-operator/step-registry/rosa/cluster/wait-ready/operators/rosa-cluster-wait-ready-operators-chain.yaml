chain:
  as: rosa-cluster-wait-ready-operators
  steps:
  - ref: rosa-cluster-wait-ready-operators
  documentation: |-
    This chain waits for cluster operators to be Ready. It currently is labeled as ROSA but there is nothing here that is specific to ROSA and can be used on self-managed too.