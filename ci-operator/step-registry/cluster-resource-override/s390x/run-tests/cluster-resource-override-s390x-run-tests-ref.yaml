ref:
  as: cluster-resource-override-s390x-run-tests
  from_image:
    namespace: ocp
    name: cli-jq
    tag: latest
  commands: cluster-resource-override-s390x-run-tests-commands.sh
  grace_period: 1m0s
  resources:
    requests:
      cpu: 100m
      memory: 200Mi
  timeout: 30m0s
  env:
  - name: CRO_NAMESPACE
    default: "openshift-cluster-resource-override"
    documentation: |-
      Namespace where the ClusterResourceOverride operator is installed.
  documentation: |-
    Placeholder test step for ClusterResourceOverride on s390x. Currently verifies
    the operator Deployment is Available and dumps basic status for artifacts.
    Replace with the real testsuite when ready.
