ref:
  as: openstack-conf-externallb-verify
  from: openstack-installer
  commands: openstack-conf-externallb-verify-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  env:
    - name: CONFIG_TYPE
      documentation: |
        The type of config for the environment to deploy.

        * 'minimal' - Configure the install-config with the minimal options to make it work on
                      our tests, which requires the usage of floating IPs. This config type doesn't
                      require a proxy server to be deployed.
        * 'proxy' - Configure the install-config to use a pre-created restricted network (BYON) and
                   a Cluster Proxy.
  documentation: |-
    For externallb jobs, verify the cluster Infrastructure status reflects external DNS
    (dnsRecordsType: External) and a user-managed load balancer.
