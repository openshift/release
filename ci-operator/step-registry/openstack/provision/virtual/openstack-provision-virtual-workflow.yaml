workflow:
  as: openstack-provision-virtual
  steps:
    pre:
    test:
      - ref: openstack-provision-virtual
    post:
  documentation: |-
    THIS IS A PLACEHOLDER.
    This workflow provisions a virtual machine where to test the
    OpenStack-provisioning scripts.
