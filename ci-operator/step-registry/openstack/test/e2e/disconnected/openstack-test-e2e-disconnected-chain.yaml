chain:
  as: openstack-test-e2e-disconnected
  steps:
  - ref: openstack-test-e2e-images
  - ref: mirror-images-qe-test-images
  - ref: openshift-e2e-test
  - ref: openstack-test-openstack
  documentation: |-
    This chain runs the e2e test suites for OpenShift on OpenStack in a disconnected
    environment.
