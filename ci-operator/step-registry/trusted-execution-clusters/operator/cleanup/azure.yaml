chain:
  as: trusted-execution-clusters-operator-cleanup-azure
  steps:
  - ref: trusted-execution-clusters-ref-operator-beaker-deprovision
  documentation: |-
    Azure tests create a Kind VM. Remove its resource group.
