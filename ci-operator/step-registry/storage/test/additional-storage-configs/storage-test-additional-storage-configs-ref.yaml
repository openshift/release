ref:
  as: storage-test-additional-storage-configs
  from_image:
    namespace: ocp
    name: "4.22"
    tag: cli
  cli: latest
  commands: storage-test-additional-storage-configs-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 200Mi
  documentation: |-
    Tests the Additional Storage Configurations feature (Tech Preview).
    Creates ContainerRuntimeConfigs for:
    - additionalLayerStores
    - additionalImageStores
    - additionalArtifactStores
    Verifies MCO processes the configurations correctly.
