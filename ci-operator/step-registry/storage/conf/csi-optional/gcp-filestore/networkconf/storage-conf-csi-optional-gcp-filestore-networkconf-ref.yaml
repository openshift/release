ref:
  as: storage-conf-csi-optional-gcp-filestore-networkconf
  from: cli
  # inject oc binary
  cli: latest
  commands: storage-conf-csi-optional-gcp-filestore-networkconf-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    The csi-gcp-filestore step creates ${SHARED_DIR}/${TEST_CSI_DRIVER_MANIFEST} for
    GCP Filestore CSI driver, which is consumed by openshift-e2e-test step to
    run CSI driver certification suite.
