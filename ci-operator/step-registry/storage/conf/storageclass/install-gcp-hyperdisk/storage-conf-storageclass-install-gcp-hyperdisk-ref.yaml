ref:
  as: storage-conf-storageclass-install-gcp-hyperdisk
  from: cli
  cli: latest
  commands: storage-conf-storageclass-install-gcp-hyperdisk-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    The storage-conf-storageclass-install-gcp-hyperdisk step changes the cluster default storageclass to the specified one.
