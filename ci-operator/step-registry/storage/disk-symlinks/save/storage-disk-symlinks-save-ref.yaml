ref:
  as: storage-disk-symlinks-save
  from: tests
  commands: storage-disk-symlinks-save-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    This step saves root disk symlinks info (symlink names and correspondent target names).
