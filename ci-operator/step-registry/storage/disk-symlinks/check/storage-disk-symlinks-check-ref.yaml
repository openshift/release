ref:
  as: storage-disk-symlinks-check
  from: tests
  commands: storage-disk-symlinks-check-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    This step checks that symlinks saved in the "save" step are still present and point to the root disk.
