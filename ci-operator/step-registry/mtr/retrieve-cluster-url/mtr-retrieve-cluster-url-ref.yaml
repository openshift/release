ref:
  as: mtr-retrieve-cluster-url
  from: cli
  commands: mtr-retrieve-cluster-url-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    Retrieve the cluster URL of the test cluster and write it to the "cluster_url" file in the SHARED_DIR for use later.