workflow:
  as: origin-e2e-gcp
  steps:
    pre:
    - chain: ipi-gcp-pre
    test:
    - ref: origin-e2e-test
    post:
    - chain: ipi-gcp-post
  documentation: |-
    The Origin E2E GCP workflow executes the common end-to-end test suite on GCP with a default cluster configuration.
