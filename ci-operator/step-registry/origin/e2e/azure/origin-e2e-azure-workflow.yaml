workflow:
  as: origin-e2e-azure
  steps:
    pre:
    - chain: ipi-azure-pre
    test:
    - ref: origin-e2e-test
    post:
    - chain: ipi-azure-post
  documentation: |-
    The Origin E2E Azure workflow executes the common end-to-end test suite on Azure with a default cluster configuration.
