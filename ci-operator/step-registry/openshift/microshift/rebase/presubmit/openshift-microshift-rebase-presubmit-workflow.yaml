workflow:
  as: openshift-microshift-rebase-presubmit
  steps:
    test:
      - chain: openshift-microshift-rebase-presubmit
  documentation: |-
    This workflow verifies that assets and rebase scripts are in sync.
