workflow:
  as: openshift-microshift-rebase-cryptoscan
  steps:
    test:
      - ref: openshift-microshift-rebase-cryptoscan
  documentation: |-
    This workflow rebases MicroShift and runs the Red Hat crypto scanner.
    Uses a custom image with podman and jq to support crypto scanning.
