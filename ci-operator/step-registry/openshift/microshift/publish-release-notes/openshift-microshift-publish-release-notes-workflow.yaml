workflow:
  as: openshift-microshift-publish-release-notes
  steps:
    test:
      - chain: openshift-microshift-publish-release-notes
  documentation: |-
    This workflow publishes release notes for pre-release versions of MicroShift.
