workflow:
    as: openshift-microshift-manage-versions-releases
    steps:
        pre:
            - chain: openshift-microshift-infra-aws-ec2
        test:
            - ref: openshift-microshift-manage-versions-releases
        post:
            - ref: openshift-microshift-infra-aws-deprovision
    documentation: |-
        This workflow manages versions and releases for MicroShift.
