ref:
  as: openshift-cluster-bot-rbac
  from: cli
  commands: openshift-cluster-bot-rbac-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 100Mi
  documentation: |-
    Creates policy allowing cluster-bot the ability to poll for status of cluster operations in this namespace.
