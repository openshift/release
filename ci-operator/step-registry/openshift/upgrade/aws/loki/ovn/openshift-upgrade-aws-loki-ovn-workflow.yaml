workflow:
  as: openshift-upgrade-aws-loki-ovn
  steps:
    pre:
    - ref: ipi-install-monitoringpvc
    - ref: ipi-install-loki
    - chain: ipi-conf-aws
    - ref: ovn-conf
    - chain: ipi-install-stableinitial
    test:
    - ref: openshift-e2e-test
    post:
    - ref: gather-loki
    - chain: gather-network
    - chain: gather-core-dump
    - chain: ipi-aws-post
    dependencies:
      OPENSHIFT_INSTALL_RELEASE_IMAGE_OVERRIDE: "release:initial"
      OPENSHIFT_UPGRADE_RELEASE_IMAGE_OVERRIDE: "release:latest"
    env:
      TEST_COMMAND: "run-upgrade"
      TEST_SUITE: "all"
      TEST_OPTIONS: "abort-at=100"
  documentation: |-
    The Openshift E2E AWS workflow executes the upgrade end-to-end test suite on AWS with OVN cluster configuration and loki as log collector, exercising an upgrade during the process.
