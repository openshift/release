workflow:
  as: openshift-upgrade-aws-pausedworkers
  steps:
    allow_best_effort_post_steps: true
    pre:
    - ref: ipi-install-hosted-loki
    - chain: ipi-aws-pre-stableinitial
    test:
    - ref: openshift-e2e-test-pausedworkers
    post:
    - chain: ipi-aws-post
    dependencies:
      OPENSHIFT_INSTALL_RELEASE_IMAGE_OVERRIDE: "release:initial"
      OPENSHIFT_UPGRADE0_RELEASE_IMAGE_OVERRIDE: "release:intermediate"
      OPENSHIFT_UPGRADE1_RELEASE_IMAGE_OVERRIDE: "release:latest"
    env:
      TEST_TYPE: upgrade
      TEST_SUITE: ""
      TEST_UPGRADE_OPTIONS: ""
  documentation: |-
    The Openshift E2E AWS workflow executes the upgrade from OPENSHIFT_INSTALL_RELEASE_IMAGE_OVERRIDE to OPENSHIFT_UPGRADE0_RELEASE_IMAGE_OVERRIDE and OPENSHIFT_UPGRADE1_RELEASE_IMAGE_OVERRIDE with minimal worker pool distruption by pausing the worker pool until the final upgrade.
