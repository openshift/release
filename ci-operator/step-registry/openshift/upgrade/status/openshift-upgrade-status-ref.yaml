ref:
  as: openshift-upgrade-status
  from: cli
  commands: openshift-upgrade-status-commands.sh
  resources:
    requests:
      cpu: "100m"
      memory: "200Mi"
  documentation: |-
    This step displays the OpenShift upgrade status using:
    OC_ENABLE_CMD_UPGRADE_STATUS=true oc adm upgrade status
