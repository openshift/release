workflow:
  as: openshift-release-art-gomod
  documentation: |-
    This workflow ensures that repositories onboarded with ART have gomod configured in a way that complies with ART
    requirements
  steps:
    test:
    - chain: openshift-release-art-gomod
