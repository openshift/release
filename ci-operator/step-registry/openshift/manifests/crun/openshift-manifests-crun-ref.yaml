ref:
  as: openshift-manifests-crun
  from: cli
  commands: openshift-manifests-crun-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 100Mi
  documentation: |-
    This steps adds MachineConfigs which configure nodes to use crun
