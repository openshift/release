ref:
  as: openshift-manifests-kubelet-logs-to-console
  from: cli
  commands: openshift-manifests-kubelet-logs-to-console-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 100Mi
  documentation: |-
    This steps adds kubelet configuration to post logs to console, so that hanging kubelet logs could be recovered in gather-*-console step
