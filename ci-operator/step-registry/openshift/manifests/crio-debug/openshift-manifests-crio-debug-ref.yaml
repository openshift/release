ref:
  as: openshift-manifests-crio-debug
  from: tools
  commands: openshift-manifests-crio-debug-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 100Mi
  documentation: |-
    Creates a manifest to enable cri-o debug log level.
