ref:
  as: openshift-lightspeed-service-install-disconnected
  from: upi-installer
  timeout: 3h0m0s
  cli: latest
  grace_period: 10m
  commands: openshift-lightspeed-service-install-disconnected-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 100Mi
  credentials:
  - namespace: test-credentials
    name: openshift-custom-mirror-registry
    mount_path: /var/run/vault/mirror-registry
  documentation: |-
    Install Openshift Lightspeed catalog source for running operators in disconnected env.