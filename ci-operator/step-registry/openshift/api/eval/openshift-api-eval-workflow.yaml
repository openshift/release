workflow:
  as: openshift-api-eval
  steps:
    pre:
    - ref: openshift-api-eval-setup
    test:
    - ref: openshift-api-eval-run
  documentation: |-
    OpenShift API review eval workflow.
    1. Setup: Verifies Claude Code CLI and Go toolchain are available
    2. Run: Clones the API repo and executes the eval suite via make
