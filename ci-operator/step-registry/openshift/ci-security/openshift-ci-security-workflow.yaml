workflow:
  as: openshift-ci-security
  documentation: |-
    This workflow executes security test on the source code
    and report the results to a centralised database.

  steps:
    test:
    - chain: openshift-ci-security-snyk
