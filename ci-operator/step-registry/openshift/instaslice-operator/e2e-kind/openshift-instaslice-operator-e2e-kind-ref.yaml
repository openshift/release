ref:
  as: openshift-instaslice-operator-e2e-kind
  from: instaslice-base-src
  commands: openshift-instaslice-operator-e2e-kind-commands.sh
  resources:
    requests:
      cpu: "200m"
      memory: 1Gi
  documentation: |-
    Creates a GCP VM and installs all the required dependencies
