ref:
  as: openshift-instaslice-operator-e2e-bundle
  from: instaslice-base-src
  cli: latest
  commands: openshift-instaslice-operator-e2e-bundle-commands.sh
  resources:
    requests:
      cpu: "200m"
      memory: 1Gi
  documentation: |-
    Sets up prerequisites to run Instaslice operator e2e test suite using bundle deployment on a GPU cluster
