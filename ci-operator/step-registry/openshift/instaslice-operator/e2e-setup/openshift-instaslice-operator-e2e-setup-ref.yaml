ref:
  as: openshift-instaslice-operator-e2e-setup
  from: src
  commands: openshift-instaslice-operator-e2e-setup-commands.sh
  resources:
    requests:
      cpu: "200m"
      memory: 1Gi
  dependencies:
    - name: "bundle-image"
      env: "BUNDLE_IMAGE"
  documentation: |-
    Sets up prerequisites to run Instaslice operator e2e test suite
