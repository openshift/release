workflow:
  as: openshift-claude-sippy-pr-followup-agent
  steps:
    test:
    - ref: openshift-claude-sippy-pr-followup-agent
  documentation: |-
    Follows up on an existing sippy PR for a Jira issue. Finds the PR,
    checks out the branch, addresses review comments, and pushes fixes.
