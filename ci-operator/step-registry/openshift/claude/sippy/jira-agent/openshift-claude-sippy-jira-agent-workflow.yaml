workflow:
  as: openshift-claude-sippy-jira-agent
  steps:
    test:
    - ref: openshift-claude-sippy-jira-agent
  documentation: |-
    Solves a Jira issue for the sippy repo using Claude AI. Fetches
    the issue, implements a fix, and opens a draft PR.
