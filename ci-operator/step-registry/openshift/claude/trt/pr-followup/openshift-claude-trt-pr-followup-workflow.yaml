workflow:
  as: openshift-claude-trt-pr-followup
  steps:
    pre:
    - ref: openshift-claude-trt-init
    test:
    - ref: openshift-claude-trt-pr-followup
  documentation: |-
    Generic TRT workflow: addresses review comments on an existing PR.
    Pre-step validates credentials, fetches the issue, and writes
    common functions to SHARED_DIR. Test step runs Claude.
