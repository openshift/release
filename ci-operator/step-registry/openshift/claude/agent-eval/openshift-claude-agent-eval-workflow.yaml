workflow:
  as: openshift-claude-agent-eval
  steps:
    test:
    - ref: openshift-claude-agent-eval
    post:
    - ref: openshift-claude-post
  documentation: |-
    Runs skill evaluations using the agent-eval-harness and generates
    a continue-session page in artifacts for resuming locally.
