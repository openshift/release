workflow:
  as: openshift-claude-agent-eval-manifest
  steps:
    test:
    - ref: openshift-claude-agent-eval-manifest
    post:
    - ref: openshift-claude-post
  documentation: |-
    Runs manifest-selected PR evals; see hack/claude-agent-eval/README.md in openshift/release.
