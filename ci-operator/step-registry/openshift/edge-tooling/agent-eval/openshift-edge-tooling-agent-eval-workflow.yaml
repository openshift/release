workflow:
  as: openshift-edge-tooling-agent-eval
  steps:
    test:
    - ref: openshift-edge-tooling-agent-eval
    post:
    - ref: openshift-claude-post
  documentation: |-
    Runs skill evaluations for edge-tooling plugins using the
    agent-eval-harness. Generates JUnit XML and eval reports.
