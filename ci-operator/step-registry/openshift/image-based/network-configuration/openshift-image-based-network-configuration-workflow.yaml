workflow:
  as: openshift-image-based-network-configuration
  documentation: |-
    The Openshift Image Based Network Configuration workflow performs the network configuration flow for the IBI installed cluster.
  steps:
    allow_best_effort_post_steps: true
    pre:
    - chain: openshift-image-based-infra-aws-ec2
    - ref: ipi-install-rbac
    - ref: openshift-image-based-upgrade-metal-config
    - ref: openshift-image-based-upgrade-seed-create
    - ref: openshift-image-based-install
    test:
    - ref: openshift-image-based-network-configuration-configure
    post:
    - ref: openshift-image-based-infra-sos
    - ref: openshift-image-based-upgrade-seed-gather-cluster
    - ref: openshift-image-based-upgrade-seed-gather-lca
    - ref: openshift-image-based-network-configuration-gather
    - ref: aws-deprovision-stacks

