workflow:
  as: openshift-e2e-powervs-upi-cluster
  steps:
    pre:
    - ref: upi-install-powervs-cluster
    post:
    - ref: upi-deprovision-powervs-cluster
  documentation: |-
    Wraps the Openshift E2E workflow and executes the common end-to-end test suite running on a cluster at IBM Cloud on PowerVS.
