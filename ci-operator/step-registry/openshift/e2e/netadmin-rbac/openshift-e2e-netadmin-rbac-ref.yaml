ref:
  as: openshift-e2e-netadmin-rbac
  from: cli
  commands: openshift-e2e-netadmin-rbac-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 100Mi
  documentation: |-
    Creates binding allowing service accounts in the test namespace rights to use net_admin & net_raw SCC. All pods
    in the namespace will receive these capabilities. 
