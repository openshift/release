ref:
  as: openshift-e2e-cluster-kube-apiserver-operator-test
  from: src
  commands: openshift-e2e-cluster-kube-apiserver-operator-test-commands.sh
  timeout: 4h0m0s
  resources:
    requests:
      cpu: 100m
  documentation: |-
    Runs the cluster-kube-apiserver-operator e2e test suite using the operator's
    Makefile target.
