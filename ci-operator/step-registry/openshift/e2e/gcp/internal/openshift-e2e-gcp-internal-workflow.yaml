workflow:
  as: openshift-e2e-gcp-internal
  steps:
    pre:
    - chain: ipi-gcp-pre-internal
    test:
    - ref: openshift-e2e-test-jump
    post:
    - chain: ipi-gcp-post-jump
  documentation: |-
    The Openshift E2E Azure workflow executes the common end-to-end test suite on Azure in a internal configuration.
