workflow:
  as: openshift-e2e-gcp-csi-migration
  steps:
    pre:
    - chain: ipi-gcp-pre
    - ref: storage-pv-save
    test:
    - ref: openshift-e2e-test
    post:
    - ref: storage-pv-check
    - chain: ipi-gcp-post
  documentation: |-
    This workflow enables CSI migration for all volume plugins and runs e2e tests (which include GCP PD in-tree tests).
