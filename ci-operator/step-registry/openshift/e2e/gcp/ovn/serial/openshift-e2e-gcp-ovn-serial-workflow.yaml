workflow:
  as: openshift-e2e-gcp-ovn-serial
  steps:
    allow_best_effort_post_steps: true
    pre:
    - ref: ipi-install-hosted-loki
    - chain: ipi-conf-gcp
    - ref: ovn-conf
    - chain: ipi-install
    test:
    - ref: openshift-e2e-test
    post:
    - chain: gather-network
    - chain: gather-core-dump
    - chain: ipi-deprovision
    env:
      TEST_SUITE: openshift/conformance/serial
  documentation: |-
    The Openshift E2E GCP OVN `serial` workflow executes the `openshift/serial` end-to-end test suite on GCP with a cluster configuration with OVNKubernetes.
