workflow:
  as: openshift-e2e-aws-localzone
  steps:
    pre:
    - chain: ipi-aws-pre-localzone
    test:
    - ref: openshift-e2e-test
    post:
    - chain: ipi-aws-post-localzone
  documentation: |-
    The origin E2E AWS localzones workflow executes the common end-to-end test suite with OVNKubernetes network
    plugin on an AWS cluster with localzone compute nodes.
