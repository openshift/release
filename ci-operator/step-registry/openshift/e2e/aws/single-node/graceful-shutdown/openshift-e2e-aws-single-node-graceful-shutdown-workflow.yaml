workflow:
  as: openshift-e2e-aws-single-node-graceful-shutdown
  steps:
    pre:
    - chain: ipi-conf-aws
    - ref: single-node-conf-aws
    - ref: single-node-conf-e2e
    - ref: single-node-conf-e2e-graceful-shutdown
    - chain: ipi-install
    test:
    - ref: single-node-add-worker-1
    - ref: single-node-e2e-test
    post:
    - chain: gather-network
    - chain: gather-core-dump
    - chain: ipi-deprovision
  documentation: |-
    The Openshift E2E Single Node graceful shutdown workflow tests graceful shutdown with a single node cluster with one worker node.
