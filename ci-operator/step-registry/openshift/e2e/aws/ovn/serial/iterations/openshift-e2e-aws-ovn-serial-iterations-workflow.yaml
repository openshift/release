workflow:
  as: openshift-e2e-aws-ovn-serial-iterations
  steps:
    allow_best_effort_post_steps: true
    pre:
    - chain: ipi-conf-aws
    - ref: ovn-conf
    - ref: ovn-conf-multi-network-policy-manifest
    - ref: rhcos-conf-osstream
    - chain: ipi-install
    test:
    - ref: openshift-e2e-test-iterations
    post:
      - chain: gather-network
      - chain: gather-core-dump
      - chain: ipi-deprovision
    env:
      TEST_SUITE: openshift/conformance/serial
  documentation: |-
    The Openshift E2E AWS `serial iterations` workflow executes the `openshift/serial` end-to-end test suite
    on AWS with the OVNKubernetes network plugin with useMultiNetworkPolicy set to true.
    The test suite can be run multiple times using the TEST_ITERATIONS environment variable.
