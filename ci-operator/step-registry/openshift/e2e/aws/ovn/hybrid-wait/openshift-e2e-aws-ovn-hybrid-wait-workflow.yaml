workflow:
  as: openshift-e2e-aws-ovn-hybrid-wait
  steps:
    pre:
    - chain: ipi-conf-aws
    - ref: ovn-conf-custom-cidr
    - chain: ipi-install
    test:
    - ref: baremetal-lab-wait
    post:
    - chain: gather-network
    - chain: gather-core-dump
    - chain: ipi-deprovision
  documentation: |-
    Builds an AWS OVN hybrid cluster then holds it for CLUSTER_DURATION seconds (e.g. for manual upgrade testing).
    Set CLUSTER_NETWORK_CIDR, CLUSTER_NETWORK_HOST_PREFIX, HYBRID_CLUSTER_NETWORK_CIDR, HYBRID_CLUSTER_NETWORK_HOST_PREFIX for network config; CLUSTER_DURATION for hold time in seconds.
