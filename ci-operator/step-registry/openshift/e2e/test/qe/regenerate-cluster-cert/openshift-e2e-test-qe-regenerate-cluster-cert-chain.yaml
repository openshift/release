chain:
  as: openshift-e2e-test-qe-regenerate-cluster-cert
  steps:
  - ref: openshift-e2e-test-qe-regenerate-cluster-cert
  documentation: |-
    Following the doc link https://access.redhat.com/articles/regenerating_cluster_certificates based on fips enabled cluster.
    The chain executes regenerating certificates of the cluster;
    then execute e2e tests which checks if the new certificates work fine.
