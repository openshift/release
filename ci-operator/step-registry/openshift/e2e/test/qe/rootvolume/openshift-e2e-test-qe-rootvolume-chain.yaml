chain:
  as: openshift-e2e-test-qe-rootvolume
  steps:
  - ref: cucushift-installer-check-aws-rootvolume
  - chain: cucushift-installer-check-cluster-health
  - ref: idp-htpasswd
  - ref: fips-check-fips-or-die
  - ref: fips-check-node-scan
  - ref: cucushift-pre
  - ref: openshift-extended-test
  - ref: cucushift-e2e
  - ref: openshift-extended-web-tests
  - ref: openshift-extended-test-supplementary
  - ref: openshift-e2e-test-clusterinfra-qe
  - ref: openshift-e2e-test-qe-report
  documentation: |-
    Execute e2e tests from QE with AWS root volume configuration validation.
    This chain includes root volume verification before running e2e tests.
    It validates that gp3 throughput configuration is correctly applied to
    worker and control plane nodes before proceeding with e2e test suite.
