chain:
  as: openshift-e2e-test-netobserv-prepare-bm-cluster
  steps:
  - ref: netobserv-apply-redhat-operators
  - ref: netobserv-deploy-nfs-provisioner
  documentation: |-
    Prepare baremetal cluster to make it possible to run netobserv e2e tests (openshift-e2e-test-netobserv-qe).
