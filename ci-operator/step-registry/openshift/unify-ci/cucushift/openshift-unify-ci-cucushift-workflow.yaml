workflow:
  as: openshift-unify-ci-cucushift
  steps:
    pre:
    - ref: oc status
    test:
    - ref: oc get clusterversion
    post:
    - ref: oc get pods
  documentation: |-
        The workflow of unify upgrade ci.