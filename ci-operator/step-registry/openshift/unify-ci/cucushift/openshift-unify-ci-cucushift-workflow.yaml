workflow:
  as: unify-ci
  steps:
    pre:
    - ref: cucushift-upgrade-prepare
    test:
    - ref: run-upgrade
    post:
    - ref: cucushift-upgrade-check
  env:
  - name: TO_VERSION
  documentation: |-
        The workflow of unify upgrade ci.