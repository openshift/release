workflow:
  as: openshift-agentic-trt-pr-followup
  steps:
    pre:
    - ref: openshift-agentic-trt-init
    test:
    - ref: openshift-agentic-trt-pr-followup
    - ref: openshift-agentic-trt-review-responder
  documentation: |-
    Finds the PR for a Jira issue, checks out the branch, then polls
    for review comments and addresses them using Claude.
