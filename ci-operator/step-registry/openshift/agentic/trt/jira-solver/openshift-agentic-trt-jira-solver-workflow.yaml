workflow:
  as: openshift-agentic-trt-jira-solver
  steps:
    pre:
    - ref: openshift-agentic-trt-init
    test:
    - ref: openshift-agentic-trt-jira-solver
  documentation: |-
    Generic TRT workflow: solves a Jira issue using Claude AI.
    Pre-step validates credentials, fetches the issue, and writes
    common functions to SHARED_DIR. Test step runs Claude.
