workflow:
  as: openshift-agentic-trt-jira-solver
  steps:
    pre:
    - ref: trt-github-app-auth
    - ref: openshift-agentic-trt-init
    test:
    - ref: openshift-agentic-trt-jira-solver
    - ref: openshift-agentic-trt-review-responder
  documentation: |-
    Solves a Jira issue using Claude, creates a PR, then polls for
    review comments and addresses them.
