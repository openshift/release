ref:
  as: openshift-agentic-trt-review-responder-eval-init
  from: agentic-dev
  commands: openshift-agentic-trt-review-responder-eval-init-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
  timeout: 15m0s
  documentation: |-
    Review-responder eval init step. Creates a PR from fixture branches
    (deliberately flawed implementation), posts seeded review comments
    from comments.json, and writes PR metadata to SHARED_DIR.
