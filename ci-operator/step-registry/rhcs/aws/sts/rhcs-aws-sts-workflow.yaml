workflow:
  as: rhcs-aws-sts
  steps:
    allow_best_effort_post_steps: true
    pre:
    - ref: rhcs-e2e-setup
    post:
    - ref: rhcs-e2e-teardown
      best_effort: true
  documentation: |-
    This workflow installs an advanced rosa sts cluster with unmanaged oidc config. 

