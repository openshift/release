ref:
  as: openshift-mcp-server-install-gateway
  from_image:
    namespace: ocp
    name: cli-jq
    tag: latest
  dependencies:
  - env: MCP_SERVER_IMAGE
    name: openshift-mcp-server
  grace_period: 15m0s
  timeout: 120m0s
  commands: openshift-mcp-server-install-gateway-commands.sh
  resources:
    requests:
      cpu: 200m
      memory: 512Mi
  documentation: |-
    Installs MCPServer behind the gateway.
