ref:
  as: workers-rhel7-aws-provision
  from: ansible
  commands: workers-rhel7-aws-provision-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 200Mi
  documentation: |-
    The post-installation provision step creates new machinesets which provision new RHEL instances for worker nodes.
