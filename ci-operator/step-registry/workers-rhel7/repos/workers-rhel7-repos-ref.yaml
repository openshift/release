ref:
  as: workers-rhel7-repos
  from: ansible
  commands: workers-rhel7-repos-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 200Mi
  documentation: |-
    The post-installation repos step configures repos and host settings for new RHEL instances.
