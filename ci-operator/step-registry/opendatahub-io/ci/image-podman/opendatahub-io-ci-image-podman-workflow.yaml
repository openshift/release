workflow:
  as: opendatahub-io-ci-image-podman
  steps:
    pre:
    - ref: opendatahub-io-ci-image-podman
  documentation: |-
    This workflow mirrors an image from the CI Registry to a given
    image ref.
