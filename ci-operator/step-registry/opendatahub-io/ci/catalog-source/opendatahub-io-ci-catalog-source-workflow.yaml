workflow:
  as: opendatahub-io-ci-catalog-source
  steps:
    pre:
    - ref: opendatahub-io-ci-catalog-source
  documentation: |-
    This workflow takes an image from a given registry (quay.io) and creates a bundle and 
    a catalog image to be used in a cluster.
