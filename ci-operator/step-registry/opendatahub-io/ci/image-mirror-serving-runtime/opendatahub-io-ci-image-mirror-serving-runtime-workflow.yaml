workflow:
  as: opendatahub-io-ci-image-mirror-serving-runtime
  steps:
    pre:
    - ref: opendatahub-io-ci-image-mirror-serving-runtime
  documentation: |-
    This workflow mirrors an serving runtime image from the CI Registry to a given
    image ref.
