chain:
  as: kueue-operator-test-ocp-upgrade
  steps:
  - ref: kueue-operator-test-upgrade-smoke-setup
  - ref: kueue-operator-test-upgrade-ocp
  - ref: kueue-operator-test-upgrade-smoke
  documentation: |-
    Creates Kueue resources before the OCP upgrade to verify they survive,
    then performs the OCP cluster upgrade, and runs a post-upgrade smoke
    test to verify kueue admission still works.
