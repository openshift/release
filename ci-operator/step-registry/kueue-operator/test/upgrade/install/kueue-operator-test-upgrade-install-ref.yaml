ref:
  as: kueue-operator-test-upgrade-install
  from: operator-sdk
  commands: kueue-operator-test-upgrade-install-commands.sh
  resources:
    requests:
      cpu: 200m
      memory: 300Mi
  documentation: |-
    Installs the kueue operator bundle on the cluster using operator-sdk.
