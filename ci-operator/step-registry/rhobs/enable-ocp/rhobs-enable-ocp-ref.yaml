ref:
  as: rhobs-enable-ocp
  from_image:
    namespace: ocp
    name: cli-yq
    tag: latest
  cli: latest
  commands: rhobs-enable-ocp-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    The step modify csv of COO/ObO to enable OCP mode
