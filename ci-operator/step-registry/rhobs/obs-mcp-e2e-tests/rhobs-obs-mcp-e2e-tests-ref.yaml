ref:
  as: rhobs-obs-mcp-e2e-tests
  from: src
  cli: latest
  commands: rhobs-obs-mcp-e2e-tests-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 400Mi
  documentation: |-
    Runs obs-mcp e2e tests against OpenShift cluster.
    Sets up port-forward to obs-mcp service and runs make test-e2e.
