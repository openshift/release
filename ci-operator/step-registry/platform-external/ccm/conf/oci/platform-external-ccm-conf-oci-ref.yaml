ref:
  as: platform-external-ccm-conf-oci
  from: upi-installer
  commands: platform-external-ccm-conf-oci-commands.sh
  grace_period: 5m
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    The IPI AWS configure step generates the AWS-specific install-config.yaml contents based on the cluster profile and optional input files.
  env:
  - name: PROVIDER_NAME
    default: ""
    documentation: |-
      The Provider Name the cluster will be provisioned.
  - name: PROVIDER_REGION
    default: ""
    documentation: |-
      The Provider Name the cluster will be provisioned.