chain:
  as: platform-external-ccm-deploy-aws
  steps:
  - ref: platform-external-cluster-wait-for-api-bootstrap
  - ref: platform-external-ccm-conf-aws
  - ref: platform-external-ccm-deploy
  documentation: >-
    Wait for dependencies and deploy Cloud Controller Manager to AWS on temporary control plane (day-0).
