chain:
  as: platform-external-pre-conf-stableinitial
  steps:
  - ref: platform-external-pre-conf
  - ref: platform-external-pre-conf-manifests-stableinitial
  documentation: >-
    Platform External pre-conf chain for upgrade jobs. Generates install-config then creates
    manifests/ignition with the stable-initial installer so bootstrap CVO flags match the
    initial release payload.
