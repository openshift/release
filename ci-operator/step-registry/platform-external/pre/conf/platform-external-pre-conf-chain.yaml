chain:
  as: platform-external-pre-conf
  steps:
  - ref: platform-external-pre-conf
  - ref: platform-external-pre-conf-manifests
  documentation: >-
    This chain contains all of the steps to provision an OpenShift cluster using the AWS UPI workflow.
