ref:
  as: platform-external-cluster-task-image-registry-emptydir
  from: upi-installer
  grace_period: 25m
  commands: platform-external-cluster-task-image-registry-emptydir-commands.sh
  resources:
    requests:
      cpu: 300m
      memory: 300Mi
  documentation: |-
    Setup Image Registry to use storage with EmptyDir.