workflow:
  as: mpiit-re-trigger-poc
  steps:
    pre:
    - ref: mpiit-re-trigger-poc-fail-pre-phase
    post:
    - ref: job-re-trigger
  documentation: |-
    Temporary workflow to test the new re-trigger mechinism