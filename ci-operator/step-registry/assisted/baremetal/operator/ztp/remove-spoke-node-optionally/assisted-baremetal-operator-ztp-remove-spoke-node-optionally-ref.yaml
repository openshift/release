ref:
  as: assisted-baremetal-operator-ztp-remove-spoke-node-optionally
  from: assisted-service-scripts
  commands: assisted-baremetal-operator-ztp-remove-spoke-node-optionally-commands.sh
  resources:
    requests:
      cpu: 1000m
      memory: 500Mi
  timeout: 1h30m0s
  grace_period: 5m0s
  documentation: |-
    Given a spoke cluster, remove a worker node from it using the hub resources. REMOVE_SPOKE_NODE is unset by default, setting it triggers this step to remove a node after installation.
