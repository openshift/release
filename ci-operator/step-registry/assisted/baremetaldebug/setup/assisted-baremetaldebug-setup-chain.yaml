chain:
  as: assisted-baremetaldebug-setup
  steps:
    - ref: baremetalds-packet-setup
    - ref: ipi-install-rbac
    - ref: assisted-baremetaldebug-setup
  documentation: |-
    Steps for installing an assisted-installer cluster on a packet server.
