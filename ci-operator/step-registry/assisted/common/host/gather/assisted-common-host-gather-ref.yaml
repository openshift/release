ref:
  as: assisted-common-host-gather
  from: dev-scripts
  commands: assisted-common-host-gather-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 100Mi
  documentation: |-
    Generic host artifact gathering step that delegates to the appropriate host provider's gather implementation.
    This abstraction layer allows provider-agnostic workflows to gather provider-specific artifacts.