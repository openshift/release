chain:
  as: assisted-common-host-post
  steps:
    - ref: baremetalds-devscripts-gather
    - chain: gather
    - ref: assisted-common-host-release
  documentation: |-
    Collect dev-scripts artifacts, run standard gather routines, and release the acquired host via the host contract.
