ref:
  as: assisted-common-lib
  from: cli
  commands: assisted-common-lib-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 32Mi
  documentation: |
    Library helper that loads and validates the assisted host contract. Intended to
    be sourced by other steps, not executed directly.
