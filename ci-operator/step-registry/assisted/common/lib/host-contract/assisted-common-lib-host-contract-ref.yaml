ref:
  as: assisted-common-lib-host-contract
  from: cli
  commands: assisted-common-lib-host-contract-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 10Mi
  documentation: |-
    Helper ref exposing the host contract library to satisfy registry tooling.
    This ref is not intended to be executed directly.
