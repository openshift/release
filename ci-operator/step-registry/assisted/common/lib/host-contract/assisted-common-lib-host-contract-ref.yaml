ref:
  as: assisted-common-lib-host-contract
  from: cli
  commands: assisted-common-lib-host-contract-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 32Mi
  documentation: |
    Derives ci-machine-config.sh from existing provider artifacts (packet-conf.sh).
    No-op when the contract already exists. Intended to run immediately after
    provider acquisition steps.
