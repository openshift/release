ref:
  as: assisted-common-lib-host-providers
  from: cli
  commands: assisted-common-lib-host-providers-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 10Mi
  documentation: |-
    Helper ref that exposes the assisted host provider bridge scripts for
    registry tooling. Not intended for direct execution.
