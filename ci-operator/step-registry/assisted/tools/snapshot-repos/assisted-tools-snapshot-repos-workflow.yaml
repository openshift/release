workflow:
  as: assisted-tools-snapshot-repos
  steps:
    test:
      - ref: assisted-tools-snapshot-repos
  documentation: |-
    Update assisted-installer.yaml file with repositories' current revisions.
