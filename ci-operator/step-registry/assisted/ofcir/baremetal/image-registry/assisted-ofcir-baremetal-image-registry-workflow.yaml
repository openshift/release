workflow:
  as: assisted-ofcir-baremetal-image-registry
  steps:
    cluster_profile: packet-assisted
    allow_best_effort_post_steps: true
    allow_skip_on_success: true
    pre:
      - ref: ofcir-acquire
      - ref: assisted-ofcir-setup
      - chain: assisted-common-pre
      - ref: assisted-ofcir-baremetal-image-registry-setup
    test:
      - ref: assisted-baremetal-test
    post:
      - ref: assisted-common-gather
      - ref: ofcir-gather
      - ref: ofcir-release
    env:
      TEST_TYPE: none
      CLUSTERTYPE: assisted_medium_el9
  documentation: |-
    Bare-metal OFCIR workflow that enables the cluster image registry (Managed + emptyDir)
    after install, then runs assisted baremetal conformance tests.
