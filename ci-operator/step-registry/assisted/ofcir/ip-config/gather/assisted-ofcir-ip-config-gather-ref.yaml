ref:
  as: assisted-ofcir-ip-config-gather
  from: dev-scripts
  commands: assisted-ofcir-ip-config-gather-commands.sh
  optional_on_success: false
  best_effort: true
  timeout: 10m
  resources:
    requests:
      cpu: 1000m
      memory: 2Gi
  documentation: |-
    Gathers logs from the lifecycle-agent ip-config flow.

