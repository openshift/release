ref:
  as: assisted-host-contract-release
  from: dev-scripts
  grace_period: 1m0s
  commands: assisted-host-contract-release-commands.sh
  resources:
    requests:
      cpu: 1000m
      memory: 600Mi
    limits:
      memory: 2Gi
  documentation: |-
    Wraps `ofcir-release` while cleaning up the host contract metadata file.
