chain:
  as: assisted-host-contract-agent-post
  steps:
  - ref: agent-gather
  - chain: assisted-host-contract-baremetalds-post
  documentation: |-
    Matches the agent post chain but relies on the host contract cleanup so assisted jobs stay
    provider-neutral.
