chain:
  as: assisted-host-contract-baremetalds-pre
  steps:
  - ref: assisted-host-contract-acquire
  - ref: ipi-install-rbac
  - ref: baremetalds-devscripts-ibm
  - ref: baremetalds-devscripts-proxy
  - ref: ipi-install-hosted-loki
  - ref: baremetalds-devscripts-setup
  documentation: |-
    Mirrors `baremetalds-ofcir-pre` but starts with the host contract acquire step so assisted
    workflows can target OFCIR or local hosts using the same metadata interface.
