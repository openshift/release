chain:
  as: assisted-host-contract-baremetalds-post
  steps:
  - ref: baremetalds-devscripts-gather
  - chain: gather
  - ref: assisted-host-contract-release
  documentation: |-
    Mirrors `baremetalds-ofcir-post` but uses the host contract release step so assisted
    workflows can run with either OFCIR or local hosts.
