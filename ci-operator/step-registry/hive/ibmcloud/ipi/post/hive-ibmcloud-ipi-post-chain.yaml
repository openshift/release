chain:
  as: hive-ibmcloud-ipi-post
  steps:
  - ref: hive-ibmcloud-ipi-post-deprovision-spoke
  - chain: ipi-ibmcloud-post
  documentation: |-
    Cleans up the spoke cluster by deleting the ClusterDeployment (which
    triggers Hive to deprovision the cluster), then deprovisions the hub
    cluster using the standard IPI deprovision chain.
