chain:
  as: redhat-lp-chaos-lp-cnv-vm-verify
  steps:
  - ref: redhat-lp-chaos-lp-cnv-vm-check
  - ref: redhat-lp-chaos-lp-cnv-vm-snapshot
  - ref: redhat-lp-chaos-lp-cnv-vm-create-after-chaos
  documentation: |-
    This chain combines several verification steps (check, snapshot, create) into a single, reusable post-chaos verification flow for CNV.
