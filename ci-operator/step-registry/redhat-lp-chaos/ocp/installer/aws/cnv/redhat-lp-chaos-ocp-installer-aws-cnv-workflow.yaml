workflow:
  as: redhat-lp-chaos-ocp-installer-aws-cnv
  steps:
    allow_best_effort_post_steps: true
    post:
    - ref: mpiit-data-router-reporter
    - chain: ipi-aws-post 
    pre:
    - chain: ipi-aws-pre
    - ref: interop-tests-deploy-cnv
  documentation: |-
    This workflow provisions an OCP Cluster (using IPI) on AWS Cloud and also
    deploys the OpenShift Virtualization on the cluster.
