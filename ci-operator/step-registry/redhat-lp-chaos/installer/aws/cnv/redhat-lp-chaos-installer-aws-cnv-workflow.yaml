workflow:
  as: redhat-lp-chaos-installer-aws-cnv
  steps:
    allow_best_effort_post_steps: true
    post:
    - chain: ipi-aws-post 
    pre:
    - chain: ipi-aws-pre
  documentation: |-
    This is the workflow to install cluster and install cnv operator
