ref:
  as: quay-omr-v2-seed-migration-sentinel
  from: upi-installer
  commands: quay-omr-v2-seed-migration-sentinel-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
  timeout: 20m0s
  grace_period: 2m0s
  documentation: |-
    Append a unique marker layer to the mirrored CLI image, push it into a
    dedicated repository through OMR v2, and publish its tag and digest for
    post-migration persistence validation.
