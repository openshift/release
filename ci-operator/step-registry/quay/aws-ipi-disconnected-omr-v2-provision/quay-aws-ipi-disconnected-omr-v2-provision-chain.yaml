chain:
  as: quay-aws-ipi-disconnected-omr-v2-provision
  steps:
  - ref: quay-omr-v2-prepare-pipeline
  - chain: quay-aws-ipi-disconnected-omr-v2-upgrade-v3-provision
  documentation: |-
    Prepare the PR-built OMR v2 pipeline image, then reuse the proven OMR v2
    disconnected AWS provisioning, mirroring, and cluster installation path
    from the v2-to-v3 migration workflow. The install step extracts and
    consumes the offline archive without passing it through the shared secret.
