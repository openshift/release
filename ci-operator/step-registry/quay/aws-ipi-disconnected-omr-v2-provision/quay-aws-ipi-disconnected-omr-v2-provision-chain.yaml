chain:
  as: quay-aws-ipi-disconnected-omr-v2-provision
  steps:
  - ref: quay-omr-v2-extract-pipeline
  - chain: quay-aws-ipi-disconnected-omr-v2-upgrade-v3-provision
  documentation: |-
    Extract the offline OMR v2 installer built from the pull request, then
    reuse the proven OMR v2 disconnected AWS provisioning, mirroring, and
    cluster installation path from the v2-to-v3 migration workflow.
