ref:
  as: quay-pr-smoke-result
  from: cli
  commands: quay-pr-smoke-result-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    Returns the stored Playwright exit status after diagnostics and cleanup
    have run. Pre-test failures remain attributed to their originating step.
