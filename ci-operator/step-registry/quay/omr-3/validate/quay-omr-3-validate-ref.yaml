ref:
  as: quay-omr-3-validate
  from: cli
  commands: quay-omr-3-validate-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
  timeout: 20m0s
  grace_period: 2m0s
  documentation: |-
    Validates cluster availability, non-degraded operators, Ready nodes, both
    OpenShift IDMS mappings, OMR requests after release mirroring completed,
    and a pod started from the mirrored CLI digest.
