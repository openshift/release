ref:
  as: quay-enable-quay-bridge-operator
  from: cli
  commands: quay-enable-quay-bridge-operator-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    Enable Quay Bridge Operator
