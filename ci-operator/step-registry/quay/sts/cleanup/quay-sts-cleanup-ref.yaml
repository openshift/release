ref:
  as: quay-sts-cleanup
  from: quay-sts-tools
  commands: quay-sts-cleanup-commands.sh
  timeout: 30m
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
  documentation: Delete run-owned Quay STS AWS resources after ephemeral cluster teardown.
