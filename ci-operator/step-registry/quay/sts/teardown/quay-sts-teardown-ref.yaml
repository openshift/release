ref:
  as: quay-sts-teardown
  from: quay-sts-tools
  cli: latest
  commands: quay-sts-teardown-commands.sh
  timeout: 35m
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
  documentation: Remove the run-owned Quay registry and namespace before destroying the ephemeral cluster.
