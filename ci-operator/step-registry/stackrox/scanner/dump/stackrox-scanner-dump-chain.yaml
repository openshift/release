chain:
  as: stackrox-scanner-dump
  steps:
  - ref: stackrox-scanner-build-updater
  - ref: stackrox-scanner-generate-genesis-dump
  documentation: |-
    A chain that generates the StackRox Scanner's genesis dump artifacts.
