ref:
  as: stackrox-operator-e2e
  commands: stackrox-operator-e2e-commands.sh
  credentials:
  - mount_path: /tmp/vault/stackrox-stackrox-e2e-tests
    group: credentials
    collection: stackrox-stackrox-e2e-tests
  - mount_path: /tmp/vault/stackrox-automation-flavors
    group: credentials
    collection: stackrox-automation-flavors
  resources:
    requests:
      cpu: 2000m
      memory: 4000Mi
  from: test-bin
  timeout: 5h0m0s
  env:
  - name: GKE_CLUSTER_VERSION
    default: ''
  - name: TEST_SUITE
    default: ocp-operator-e2e-tests
  - name: QA_TEST_TARGET
    default: ''
  documentation: |-
    A step that runs a standard stackrox e2e test with mounted credentials, etc.
    Executes .openshift-ci/dispatch.sh in the target repo and passes it the short form test name (JOB_NAME_SAFE), which can be overriden by specifying TEST_SUITE.
