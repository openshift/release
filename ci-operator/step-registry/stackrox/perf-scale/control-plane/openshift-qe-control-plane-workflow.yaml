
workflow:
  as: stackrox-perf-scale-control-plane
  steps:
    test:
      - chain: stackrox-perf-scale-control-plane
  documentation: |-
    This workflow executes cluster density v2, node density smoketest workloads.
