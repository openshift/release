ref:
- as: stackrox-collector
  commands: stackrox-collector-commands.sh "$ARTIFACT_DIR/shared-env"
  from: src
  timeout: 3h0m
  resources:
    requests:
      cpu: 2000m
      memory: 4000Mi
  documentation: |-
    Sets environment variables needed by collector
