ref:
  as: cocl-e2e-build-image-for-qemu-vm
  from_image:
    namespace: ocp
    name: "4.12"
    tag: upi-installer
  commands: cocl-e2e-build-image-for-qemu-vm-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 200Mi
  documentation: |-
    This step builds the QEMU image for the confidential clusters e2e tests.