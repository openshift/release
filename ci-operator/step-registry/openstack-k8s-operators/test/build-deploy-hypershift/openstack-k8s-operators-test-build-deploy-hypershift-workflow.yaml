workflow:
  as: openstack-k8s-operators-test-build-deploy-hypershift
  steps:
    pre:
    - ref: openshift-cluster-bot-rbac
    - chain: hypershift-hostedcluster-create
    test:
    #- ref: openstack-k8s-operators-build
    - ref: openstack-k8s-operators-deploy
    post:
    - ref: openstack-k8s-operators-gather
    - chain: hypershift-hostedcluster-dump
    - chain: hypershift-hostedcluster-destroy
  documentation: |-
    Test build and deploy of an openstack-operator in hypershift cluster
