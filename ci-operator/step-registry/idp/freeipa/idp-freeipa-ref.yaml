ref:
  as: idp-freeipa
  from_image:
    namespace: ci
    name: verification-tests
    tag: latest
  commands: idp-freeipa-commands.sh
  cli: latest
  resources:
    limits:
      cpu: 500m
      memory: 500Mi
    requests:
      cpu: 300m
      memory: 200Mi
  documentation: |-
    Setting users via freeipa Identity.
