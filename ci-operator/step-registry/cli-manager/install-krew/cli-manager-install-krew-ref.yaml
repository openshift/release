ref:
  as: cli-manager-install-krew
  from: tests
  commands: cli-manager-install-krew-commands.sh
  grace_period: 5m
  resources:
    requests:
      cpu: 100m
      memory: 200Mi
  documentation: |-
    Installs Krew (kubectl plugin manager) required for cli-manager-operator tests.
