chain:
  as: ipi-vsphere-pre-externallb
  steps:
  - chain: ipi-conf-vsphere-lb-external  
  - chain: ipi-install-vsphere
  documentation: |-
    Deploys an IPI vSphere cluster which installs with an external LB at day 0.
    Additionally, the on-prem infra will not be deployed.
