chain:
  as: ipi-vsphere-pre-hive
  steps:
  - chain: ipi-conf-vsphere-hive
  - chain: ipi-install-vsphere # Does this need to change?
  documentation: |-
    The IPI setup step contains all steps that provision an OpenShift cluster
    with a default configuration on vSphere.