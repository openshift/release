chain:
  as: ipi-vsphere-proxy
  steps:
  - chain: ipi-conf-vsphere
  - ref: ipi-conf-vsphere-proxy
  - chain: ipi-install-vsphere
  documentation: |-
    The IPI behind proxy setup step contains all steps that provision an OpenShift cluster
    with a proxy cluster configuration on vSphere.
