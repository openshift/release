chain:
  as: ipi-powervc-pre
  steps:
  - chain: ipi-conf-powervc
  - chain: ipi-install-powervc
  env:
  documentation: |-
    The IPI setup step contains all steps that provision an OpenShift cluster
    with a default configuration in PowerVC environment on IBM Cloud.
