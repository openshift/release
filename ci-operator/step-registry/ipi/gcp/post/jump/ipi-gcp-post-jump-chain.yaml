chain:
  as: ipi-gcp-post-jump
  steps:
  - chain: ipi-deprovision-jump
  documentation: |-
    The IPI cleanup step contains all steps that deprovision an OpenShift
    cluster on GCP, provisioned by the `ipi-gcp-pre-jump` chain.
