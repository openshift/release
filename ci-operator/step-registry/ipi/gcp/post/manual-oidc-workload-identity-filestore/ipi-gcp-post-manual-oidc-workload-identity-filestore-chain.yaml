chain:
  as: ipi-gcp-post-manual-oidc-workload-identity-filestore
  steps:
  - chain: ipi-gcp-post
  - ref: ipi-conf-gcp-filestore-workload-identity-federation-cleanup
  documentation: |-
    The IPI cleanup step contains all steps that removes a user defined OIDC
    provider and the service accounts on GCP for operators.
