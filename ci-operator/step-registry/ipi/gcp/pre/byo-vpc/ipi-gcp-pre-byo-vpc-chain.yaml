chain:
  as: ipi-gcp-pre-byo-vpc
  steps:
  - chain: ipi-conf-gcp-byo-vpc
  - chain: ipi-install
  documentation: |-
    The IPI setup step contains all steps that provision an OpenShift cluster
    in a bring-your-own network configuration on GCP.
