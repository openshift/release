chain:
  as: ipi-gcp-pre-sharednetwork
  steps:
  - chain: ipi-conf-gcp-sharednetwork
  - chain: ipi-install
  - ref: manifests
  documentation: |-
    The IPI setup step contains all steps that provision an OpenShift cluster
    in a shared network configuration on GCP.
