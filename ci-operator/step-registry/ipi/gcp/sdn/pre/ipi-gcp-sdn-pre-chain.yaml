chain:
  as: ipi-gcp-sdn-pre
  steps:
  - chain: ipi-conf-gcp
  - ref: sdn-conf
  - chain: ipi-install
  documentation: |-
    The IPI setup step contains all steps that provision an OpenShift cluster
    with with OpenShiftSDN as the CNI and otherwise default configuration on GCP.
