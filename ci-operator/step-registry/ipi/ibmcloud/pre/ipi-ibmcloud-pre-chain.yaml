chain:
  as: ipi-ibmcloud-pre
  steps:
  - chain: ipi-conf-ibmcloud
  - chain: ipi-install
  documentation: |-
    The IPI setup step contains all steps that provision an OpenShift cluster
    with a default configuration on IBM Cloud.
