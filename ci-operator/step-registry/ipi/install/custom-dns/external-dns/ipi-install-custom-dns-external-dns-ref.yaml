ref:
  as: ipi-install-custom-dns-external-dns
  from: cli
  commands: ipi-install-custom-dns-external-dns-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 100Mi
  documentation: |-
    The post-installation step that configures external DNS records for api and ingress. To be used when custom-dns is configured.
