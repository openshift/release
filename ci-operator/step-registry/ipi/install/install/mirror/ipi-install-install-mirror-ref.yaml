ref:
  as: ipi-install-install-mirror
  from: installer
  commands: ipi-install-install-mirror-commands.sh
  resources:
    requests:
      cpu: 1000m
      memory: 2Gi
  documentation: |-
    The IPI install-mirror step reads install payload ref from SHARED_DIR
