ref:
  as: ipi-install-ovirt-patch-image-registry
  from: cli
  commands: ipi-install-ovirt-patch-image-registry-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 10Mi
  documentation: >-
    On 4.4 and 4.5 we don't have CSI Driver so we need to patch that image registry to use emptydir.
