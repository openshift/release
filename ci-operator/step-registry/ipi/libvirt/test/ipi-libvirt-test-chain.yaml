chain:
  as: ipi-libvirt-test
  steps:
  - ref: ipi-libvirt-conf
  - ref: ipi-libvirt-test
  documentation: |-
    The IPI setup step contains all steps that provision an OpenShift cluster
    with a default configuration on multi-arch(s390x & ppc64le)
