chain:
  as: ipi-libvirt-post
  steps:
  - chain: ipi-deprovision-libvirt
  documentation: |-
    The IPI cleanup step contains all steps that gather and deprovision an Openshift cluster on multi-arch, provisioned by the 'ipi-libvirt-pre' chain.
