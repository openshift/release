chain:
  as: ipi-nutanix-post
  steps:
  - chain: ipi-deprovision
  - ref: ipi-deprovision-nutanix-dns
  documentation: |-
    The IPI cleanup step contains all steps that deprovision an OpenShift
    cluster on Nutanix cluster, provisioned by the `ipi-nutanix-pre` chain.
