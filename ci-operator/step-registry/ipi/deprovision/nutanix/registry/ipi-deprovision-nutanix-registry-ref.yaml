ref:
  as: ipi-deprovision-nutanix-registry
  from: cli
  commands: ipi-deprovision-nutanix-registry-commands.sh
  resources:
    requests:
      cpu: 1000m
      memory: 100Mi
  documentation: >-
    Delete image registry pvc via `${SHARED_DIR}/image-registry-pvc.yaml` which generated in ipi-install-nutanix-registry-ref step.
