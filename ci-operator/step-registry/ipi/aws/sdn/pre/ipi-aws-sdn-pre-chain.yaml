chain:
  as: ipi-aws-sdn-pre
  steps:
  - chain: ipi-conf-aws
  - ref: sdn-conf
  - chain: ipi-install
  documentation: |-
    The IPI setup step contains all steps that provision an OpenShift cluster using
    OpenShiftSDN as the CNI and otherwise default cluster configuration on AWS.
