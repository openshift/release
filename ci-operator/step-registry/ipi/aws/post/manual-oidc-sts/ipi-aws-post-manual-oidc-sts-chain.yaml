chain:
  as: ipi-aws-post-manual-oidc-sts
  steps:
  - chain: ipi-aws-post
  - ref: ipi-conf-aws-oidc-creds-deprovision
  documentation: |-
    The IPI cleanup step contains all steps that removes a user defined OIDC
    provider and the service accounts on AWS for operators.
