chain:
  as: ipi-aws-pre-publicsubnets
  steps:
  - chain: ipi-install
  documentation: |-
    The IPI setup step contains all steps that provision an OpenShift cluster
    in a VPC configuration without NAT GWs on AWS.
