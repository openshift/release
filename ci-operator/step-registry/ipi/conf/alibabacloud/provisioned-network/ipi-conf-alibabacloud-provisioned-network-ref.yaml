ref:
  as: ipi-conf-alibabacloud-provisioned-network
  from_image:
    name: "4.12"
    namespace: ocp
    tag: upi-installer
  commands: ipi-conf-alibabacloud-provisioned-network-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  credentials:
  documentation: |-
    The step inserts GCP network settings into the install-config.yaml
