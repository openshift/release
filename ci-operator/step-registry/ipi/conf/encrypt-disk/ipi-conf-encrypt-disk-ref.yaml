ref:
  as: ipi-conf-encrypt-disk
  commands: ipi-conf-encrypt-disk-commands.sh
  from: tools
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: >-
    Uses ${SHARED_DIR}/encrypt_disk.sh to create manifests to encrypt disk root partition on all nodes. It's one configuration for Confidential cluster which needed for attestation.
