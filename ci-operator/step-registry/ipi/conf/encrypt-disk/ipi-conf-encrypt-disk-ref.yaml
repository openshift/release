ref:
  as: ipi-conf-encrypt-disk
  commands: ipi-conf-encrypt-disk-commands.sh
  from: tools
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: >-
    Create manifests to encrypt disk root partition on all nodes. It's one configuration for Confidential cluster which needed for attestation.
