ref:
  as: ipi-conf-internal
  from: base
  commands: ipi-conf-internal-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    The IPI internal configure step generates install config for Internal cluster configuration.
