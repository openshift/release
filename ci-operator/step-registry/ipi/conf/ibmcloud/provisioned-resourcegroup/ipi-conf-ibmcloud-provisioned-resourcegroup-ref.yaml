ref:
  as: ipi-conf-ibmcloud-provisioned-resourcegroup
  from_image:
    namespace: ocp
    name: "4.12"
    tag: upi-installer
  commands: ipi-conf-ibmcloud-provisioned-resourcegroup-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  credentials:
  documentation: |-
     Adds provisioned resource group section in the install-config.yaml
