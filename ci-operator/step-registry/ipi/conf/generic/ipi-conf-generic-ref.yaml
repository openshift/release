ref:
  as: ipi-conf-generic
  from: centos:7
  commands: ipi-conf-generic-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    The IPI generic configure step generates the platform-agnostic portions of the install-config.yaml file based on optional input files.
