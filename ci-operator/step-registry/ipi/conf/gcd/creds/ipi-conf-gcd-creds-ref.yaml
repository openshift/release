ref:
  as: ipi-conf-gcd-creds
  from_image:
    namespace: ocp
    name: "5.0"
    tag: upi-installer
  commands: ipi-conf-gcd-creds-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    Validates the GCD credential configuration in the cluster profile.
