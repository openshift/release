ref:
  as: ipi-conf-manual-creds
  from_image:
   namespace: origin
   name: centos
   tag: '8'
  commands: ipi-conf-manual-creds-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    The IPI manual-creds configure step updates install-config.yaml to set credentialsMode to Manual.
