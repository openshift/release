ref:
  as: ipi-conf-azure-user-tags
  from_image:
    name: "4.12"
    namespace: ocp
    tag: upi-installer
  commands: ipi-conf-azure-user-tags-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    The user-tags step configures platform.azure.userTags in the install-config.yaml
