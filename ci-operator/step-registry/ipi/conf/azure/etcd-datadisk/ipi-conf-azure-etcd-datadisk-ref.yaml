ref:
  as: ipi-conf-azure-etcd-datadisk
  from_image:
    namespace: ocp
    name: "4.14"
    tag: upi-installer
  commands: ipi-conf-azure-etcd-datadisk-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    (EXPERIMENTAL step to monitor stability and costs)
    The IPI configure step creates the manifest to mount etcd on data disk.
