chain:
  as: ipi-conf-azure-resourcegroup
  steps:
  - ref: ipi-conf
  - ref: ipi-conf-azure
  - ref: ipi-conf-azure-resourcegroup
  - ref: ipi-install-monitoringpvc
  documentation: |-
    The IPI configure step chain generates the install-config.yaml file based on the cluster profile and optional input files. In this case, we add the needed configuration to deploy in an existing Resource Group.
