ref:
  as: ipi-conf-azure-proxy
  from: 
  from_image:
   namespace: origin
   name: centos
   tag: '8'
  commands: ipi-conf-azure-proxy-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  credentials:
  documentation: |-
    The proxy setting step adds proxy section in the install-config.yaml
