ref:
  as: ipi-conf-azure-provisionednetwork
  from_image:
    name: "4.12"
    namespace: ocp
    tag: upi-installer
  commands: ipi-conf-azure-provisionednetwork-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  credentials:
  documentation: |-
    The vnet setting step adds vnet section in the install-config.yaml
