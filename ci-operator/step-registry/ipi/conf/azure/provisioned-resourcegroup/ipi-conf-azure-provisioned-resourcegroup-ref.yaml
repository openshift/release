ref:
  as: ipi-conf-azure-provisioned-resourcegroup
  from: 
  from_image:
   namespace: origin
   name: centos
   tag: '8'
  commands: ipi-conf-azure-provisioned-resourcegroup-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  credentials:
  documentation: |-
     Adds provisioned resource group section in the install-config.yaml
