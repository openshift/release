ref:
  as: ipi-conf-azure-jump
  from: base
  commands: ipi-conf-azure-jump-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    The IPI Azure configure step generates a Jump host location based on the install-config.
