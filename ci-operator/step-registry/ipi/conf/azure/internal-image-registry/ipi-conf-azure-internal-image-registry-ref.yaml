ref:
  as: ipi-conf-azure-internal-image-registry
  from_image:
    namespace: ocp
    name: "4.12"
    tag: upi-installer
  commands: ipi-conf-azure-internal-image-registry-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    Manually config private endpoint for image registry storage account
