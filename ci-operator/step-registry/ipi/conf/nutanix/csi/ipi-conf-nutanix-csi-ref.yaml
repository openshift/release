ref:
  as: ipi-conf-nutanix-csi
  from: tools
  commands: ipi-conf-nutanix-csi-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: >-
    Uses ${SHARED_DIR}/nutanix_context.sh to create manifests for Nutanix CSI driver.
    Saves manifests in ${SHARED_DIR}.