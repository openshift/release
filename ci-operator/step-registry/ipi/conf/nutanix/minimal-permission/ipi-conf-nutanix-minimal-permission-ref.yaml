ref:
  as: ipi-conf-nutanix-minimal-permission
  from_image:
    namespace: ocp
    name: "4.12"
    tag: upi-installer
  commands: ipi-conf-nutanix-minimal-permission-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: >-
    Replace user to the one which is created to test minimal permission in install-config.yaml
