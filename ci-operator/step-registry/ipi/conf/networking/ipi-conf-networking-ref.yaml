ref:
  as: ipi-conf-networking
  from: centos:7
  commands: ipi-conf-networking-commands.sh
  resources:
    requests:
      cpu: 1000m
      memory: 100Mi
  documentation: |-
    The IPI networking configure step generates the networking-specific install-config.yaml contents based on optional input files.
