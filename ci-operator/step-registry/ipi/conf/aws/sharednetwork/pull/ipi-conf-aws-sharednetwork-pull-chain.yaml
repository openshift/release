chain:
  as: ipi-conf-aws-sharednetwork-pull
  steps:
  - ref: ipi-conf
  - ref: ipi-conf-telemetry
  - ref: ipi-conf-aws-sharednetwork-pull
  - ref: ipi-conf-aws-sharednetwork
  - ref: ipi-conf-aws
  - ref: ipi-install-monitoringpvc
  documentation: |-
    The IPI configure step chain generates the install-config.yaml file based on the cluster profile and optional input files.
    Warning: this step must not be used with payload jobs, to do so,
    use ipi-conf-aws-sharednetwork instead.