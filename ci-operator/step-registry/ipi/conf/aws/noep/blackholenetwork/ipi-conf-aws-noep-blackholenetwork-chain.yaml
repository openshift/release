chain:
  as: ipi-conf-aws-noep-blackholenetwork
  steps:
  - ref: ipi-conf
  - ref: ipi-conf-aws-noep-blackholenetwork
  - ref: ipi-conf-aws
  documentation: |-
    The IPI configure step chain generates the install-config.yaml file based on the cluster profile and optional input files.
    Leaves out VPC endpoints for EC2, S3, ELB
