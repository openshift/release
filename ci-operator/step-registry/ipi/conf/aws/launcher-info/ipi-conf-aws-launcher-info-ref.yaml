ref:
  as: ipi-conf-aws-launcher-info
  from_image:
    namespace: ocp
    name: "4.16"
    tag: upi-installer
  commands: ipi-conf-aws-launcher-info-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  env:
  - name: CI_CLUSTER_TYPE
    default: "classic"
