chain:
  as: ipi-conf-aws-upgrade
  steps:
  - ref: ipi-conf
  - ref: ipi-conf-aws
  documentation: |-
    The IPI configure step chain generates the install-config.yaml file based on the cluster profile and optional input files. This chain doesn't set a PVC for monitoring operator
