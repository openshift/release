ref:
  as: ipi-conf-aws-outpost
  from_image:
    namespace: ocp
    name: "4.11"
    tag: upi-installer
  grace_period: 10m
  commands: ipi-conf-aws-outpost-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    This step fixes the workers network manifests to adjust the outpost.


  # credentials:
  # - namespace: test-credentials # this entry injects the custom credential
  #   name: aws-outpost
  #   mount_path: /var/run/secrets/ci.openshift.io/cluster-profile/.awscred

  # credentials:
  # - namespace: ci
  #   name: cluster-secrets-aws-outpost
  #   mount_path: /var/run/secrets/ci.openshift.io/cluster-profile/.awscred
