chain:
  as: ipi-conf-aws-outpost
  steps:
  - ref: ipi-conf-aws-outpost
  documentation: |-
    The IPI configure aws outpost step chain spins up an outpost in a separate ec2 instance and appends the outpost info to the install-config.yaml file.
