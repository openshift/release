chain:
  as: ipi-conf-aws-disconnectedprivatecluster
  steps:
  - chain: ipi-conf-aws
  - ref: aws-provision-vpc-disconnected
  - ref: aws-provision-bastionhost
  - ref: aws-provision-disconnectedusers
  - ref: ipi-conf-aws-disconnectedprivatecluster
  documentation: |-
    The IPI configure aws proxy step chain spins up a squid proxy in a separate ec2 instance and appends the proxy info to the install-config.yaml file.
