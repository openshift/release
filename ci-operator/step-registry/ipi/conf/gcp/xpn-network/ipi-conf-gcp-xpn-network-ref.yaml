ref:
  as: ipi-conf-gcp-xpn-network
  from_image:
   namespace: ocp
   name: "4.12"
   tag: upi-installer
  commands: ipi-conf-gcp-xpn-network-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: >-
    The IPI configure step updates install-config.yaml to insert GCP XPN network settings. 
