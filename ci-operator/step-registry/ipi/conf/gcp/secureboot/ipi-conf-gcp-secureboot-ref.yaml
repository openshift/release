ref:
  as: ipi-conf-gcp-secureboot
  from_image:
    namespace: ocp
    name: "4.12"
    tag: upi-installer
  commands: ipi-conf-gcp-secureboot-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    The IPI configure step updates install-config.yaml with the secureboot option.