ref:
  as: ipi-conf-gcp-persistent-disk-workload-identity-federation-cleanup
  from_image:
    namespace: ocp
    name: "4.12"
    tag: upi-installer
  # inject oc binary
  cli: latest
  commands: ipi-conf-gcp-persistent-disk-workload-identity-federation-cleanup-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    The script clean up the role of GCP Workload Identity Federation for OpenShift's Persistent Disk service. 
