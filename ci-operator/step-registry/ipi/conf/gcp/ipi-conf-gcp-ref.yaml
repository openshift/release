ref:
  as: ipi-conf-gcp
  from: centos:7
  commands: ipi-conf-gcp-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    The IPI GCP configure step generates the GCP-specific install-config.yaml contents based on the cluster profile and optional input files.
