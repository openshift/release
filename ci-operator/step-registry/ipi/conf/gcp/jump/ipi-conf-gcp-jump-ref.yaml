ref:
  as: ipi-conf-gcp-jump
  from: base
  commands: ipi-conf-gcp-jump-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: >-
    The IPI GCP configure step generates a Jump host location based on the install-config.
