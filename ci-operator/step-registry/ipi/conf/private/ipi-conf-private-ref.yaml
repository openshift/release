ref:
  as: ipi-conf-private
  from_image:
    namespace: ocp
    name: "4.5"
    tag: upi-installer
  commands: ipi-conf-private-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    The IPI AWS configure step generates the AWS-specific install-config.yaml contents based on the cluster profile and optional input files.
