ref:
  as: ipi-conf-additional-ca-azurestack
  from_image:
    namespace: ocp
    name: "4.12"
    tag: upi-installer
  commands: ipi-conf-additional-ca-azurestack-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    Configure CA certificate if Azure Stack Hub environment is using an internal Certificate Authority (CA)
