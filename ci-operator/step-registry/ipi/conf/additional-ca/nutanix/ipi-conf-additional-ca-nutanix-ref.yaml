ref:
  as: ipi-conf-additional-ca-nutanix
  from_image:
    namespace: ocp
    name: "4.12"
    tag: upi-installer
  commands: ipi-conf-additional-ca-nutanix-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    Configure CA certificate if nutanix environment is using an internal Certificate Authority (CA)
