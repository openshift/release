chain:
  as: ipi-azure-post-manual-oidc-workload-identity
  steps:
  - chain: ipi-azure-post
  - ref: ipi-conf-azure-oidc-creds-deprovision
  documentation: |-
    The IPI cleanup step contains all steps that removes a user defined OIDC
    provider and the service accounts on Azure for operators.
