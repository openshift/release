chain:
  as: ipi-azure-sdn-pre
  steps:
  - chain: ipi-conf-azure
  - ref: sdn-conf
  - chain: ipi-install
  - ref: ipi-azure-rbac
  documentation: |-
    The IPI setup step contains all steps that provision an OpenShift cluster
    with OpenShiftSDN as the CNI and otherwise default configuration on Azure.
