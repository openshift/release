chain:
  as: ipi-azure-pre-hypershift
  steps:
  - chain: ipi-conf-azure
  - chain: ipi-install-hypershift-azure
  - ref: ipi-azure-rbac
  documentation: |-
    The IPI setup step contains all steps that provision an OpenShift cluster
    with a default configuration on Azure.
