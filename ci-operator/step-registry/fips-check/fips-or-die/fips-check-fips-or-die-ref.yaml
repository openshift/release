ref:
  as: fips-check-fips-or-die
  from: cli
  commands: fips-check-fips-or-die-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: >-
    This step checks if the FIPS works well.
