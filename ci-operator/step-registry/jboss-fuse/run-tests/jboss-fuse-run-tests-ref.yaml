ref:
  as: jboss-fuse-run-tests
  from_image:
    namespace: ocp
    name: cli-jq
    tag: latest
  commands: jboss-fuse-run-tests-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 1000Mi
  documentation: |-
    This ref step is used for running the tests of Fuse On Openshify product.

