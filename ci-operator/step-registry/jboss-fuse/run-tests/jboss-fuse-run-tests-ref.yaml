ref:
  as: jboss-fuse-run-tests
  timeout: 4h
  from_image:
    namespace: ocp
    name: cli-jq
    tag: latest
  commands: jboss-fuse-run-tests-commands.sh
  resources:
    requests:
      cpu: '2'
      memory: 4Gi
  documentation: |-
    This ref step is used for running the tests of Fuse On Openshift product.

