ref:
  as: operator-pipelines-preflight-preprod-approve-csrs
  from: tools
  commands: operator-pipelines-preflight-preprod-approve-csrs-commands.sh
  resources:
    requests:
      cpu: 1000m
      memory: 400Mi
  documentation: |-
    This step checks to makes sure there are no
    Certificate Signing Requests(CSR) that need approval,
    if their are, this script will this script will approve them.
    Ensuring that the nodes and cluster are healthy.
