workflow:
  as: crio-test-integration
  steps:
    pre:
      - chain: upi-gcp-rhel8-pre
    test:
      - ref: crio-test-integration-test
    post:
      - ref: crio-test-integration-gather
      - chain: upi-gcp-rhel8-post
  documentation: |-
    This workflow executes the subsystem test suite on a cluster provisioned by running assisted-installer-crio-test on a packet server.
