workflow:
  as: crio-test-critest-fedora
  steps:
    cluster_profile: gcp
    test:
      - ref: crio-test-critest-fedora-test
  documentation: |-
    This workflow executes the subsystem test suite on a cluster provisioned by running assisted-installer-crio-test on a packet server.
