ref:
  as: rhcos-conf-rhcos10
  from: installer
  commands: rhcos-conf-rhcos10-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  env:
  documentation: |-
    Creates manifests for worker and master to set osImageURL to latest RHCOS 10 version
