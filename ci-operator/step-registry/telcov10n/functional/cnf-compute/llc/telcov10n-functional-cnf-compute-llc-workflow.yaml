workflow:
  as: telcov10n-functional-cnf-compute-llc
  steps:
    # https://docs.ci.openshift.org/docs/architecture/step-registry/#marking-post-steps-best-effort
    allow_best_effort_post_steps: true
    pre:
      - ref: telco5g-jobs-router
      - ref: telco5g-cluster-setup
    test:
      - ref: telcov10n-functional-cnf-compute-llc-tests
    post:
      - ref: telco5g-log-gather
      - chain: gather
      - ref: telco5g-release-cluster
  documentation: |-
    Install a cluster through an ssh for telco5g testing
    and run Last Level Cache related functional tests on it. 
