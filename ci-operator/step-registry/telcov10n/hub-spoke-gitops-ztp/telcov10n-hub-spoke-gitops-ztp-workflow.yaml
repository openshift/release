workflow:
  as: telcov10n-hub-spoke-gitops-ztp
  steps:
    pre:
     - chain: telcov10n-hub-spoke-gitops-ztp-deploy-hub
     - ref: telcov10n-hub-spoke-gitops-ztp-debug
    # test:
    # - ref: telcov10n-functional-tests
    # post:
    # - ref: telco5g-log-gather
    # - chain: gather
    # - ref: telco5g-release-cluster
  documentation: |-
    Install a hub cluster via agent install and spoke via GitOps ZTP, configure both to run telco tests, 
    run the tests and deprovision the cluster.
