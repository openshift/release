ref:
  as: telcov10n-system-telco-kpis-reporting-generate-report
  from: root
  commands: telcov10n-system-telco-kpis-reporting-generate-report-commands.sh
  grace_period: 10s
  timeout: 2h
  resources:
    requests:
      cpu: 100m
      memory: 200Mi
  env:
    - name: SPOKE_CLUSTER
      default: "spree-02"
      documentation: Spoke cluster to generate report for
    - name: HUB_CLUSTER
      default: "kni-qe-71"
      documentation: Hub cluster managing the spoke
    - name: REPORTING_SETTINGS
      default: '{}'
      documentation: >-
        JSON object for reporting configuration overrides from CI config.
        Values here take precedence over REPORTING_SETTINGS_DEFAULTS.
    - name: REPORTING_SETTINGS_DEFAULTS
      default: |-
        {
          "generate_report": {
            "skip": false,
            "continue_on_fail": false,
            "debug_on_fail": false,
            "debug_timeout": "+10 min",
            "test_filter": "",
            "output_filename": "",
            "report_publish_mode": "prod",
            "report_repo_url": "https://gitlab.cee.redhat.com/telcov10n/kpi",
            "report_repo_branch": "telco-kpis-reports",
            "force_report": false,
            "debug": false
          },
          "splunk": {
            "push_enabled": true,
            "formal_test": false,
            "hec_url": "https://splunk-hec.corp.redhat.com:8088/services/collector"
          }
        }
      documentation: >-
        JSON object for reporting configuration. The "generate_report" key supports:
        skip (bool) - when true, skip this step entirely and exit 0;
        continue_on_fail (bool) - when true, failures exit 0 so the pipeline continues;
        test_filter (string) - filter tests to include in report (empty for all);
        output_filename (string) - output filename for report (empty to auto-generate);
        report_publish_mode (string) - controls where reports are published: "prod" pushes to report_repo_url (production GitLab); "dev-bastion" deploys a local Gitea on the bastion for quick format checks; "dev-hypervisor" deploys a shared Gitea on the hypervisor, emulating prod multi-spoke accumulation;
        report_repo_url (string) - production git repo URL for publishing reports (used when report_publish_mode is "prod");
        report_repo_branch (string) - branch in the production repo to publish to;
        force_report (bool) - when true, bypass freshness check and regenerate report even if no new test data;
        debug (bool) - when true, enables Ansible -vvv verbose output.
        The "splunk" key supports:
        push_enabled (bool) - when true, push test results to Splunk HEC (default true);
        formal_test (bool) - when true, marks events as formal test results;
        hec_url (string) - Splunk HEC endpoint URL.
        Values are exported as uppercase env vars, overriding ref.yaml defaults.

  credentials:
    # Splunk HEC credentials
    - collection: telcov10n-ci
      group: teams/telco-kpis/splunk
      mount_path: /var/splunk

    # Production report repository token
    - collection: telcov10n-ci
      group: teams/telco-kpis/reports-repo
      mount_path: /var/reports-repo

    # Common group variables
    - collection: telcov10n-ci
      group: ansible
      mount_path: /var/common_variables

    # Spoke cluster credentials
    - collection: telcov10n-ci
      group: teams/telco-kpis/clusters/spree-01
      mount_path: /var/clusters/spree-01
    - collection: telcov10n-ci
      group: teams/telco-kpis/clusters/spree-02
      mount_path: /var/clusters/spree-02
    - collection: telcov10n-ci
      group: teams/telco-kpis/clusters/spree-03
      mount_path: /var/clusters/spree-03
    - collection: telcov10n-ci
      group: teams/telco-kpis/clusters/spree-04
      mount_path: /var/clusters/spree-04
    - collection: telcov10n-ci
      group: teams/telco-kpis/clusters/cnfdg46
      mount_path: /var/clusters/cnfdg46
    - collection: telcov10n-ci
      group: teams/telco-kpis/clusters/kni-qe-100
      mount_path: /var/clusters/kni-qe-100

    # Hub cluster credentials
    - collection: telcov10n-ci
      group: teams/telco-kpis/clusters/kni-qe-70
      mount_path: /var/clusters/kni-qe-70
    - collection: telcov10n-ci
      group: teams/telco-kpis/clusters/kni-qe-71
      mount_path: /var/clusters/kni-qe-71
    - collection: telcov10n-ci
      group: teams/telco-kpis/clusters/dev-kpi-01
      mount_path: /var/clusters/dev-kpi-01
    - collection: telcov10n-ci
      group: teams/telco-kpis/clusters/dev-kpi-02
      mount_path: /var/clusters/dev-kpi-02
    - collection: telcov10n-ci
      group: teams/telco-kpis/clusters/dev-kpi-03
      mount_path: /var/clusters/dev-kpi-03
    - collection: telcov10n-ci
      group: teams/telco-kpis/clusters/dev-kpi-04
      mount_path: /var/clusters/dev-kpi-04
  documentation: |-
    Generate comprehensive Markdown reports aggregating all test artifacts.
    Includes node info, test results, BIOS validation, RDS comparison, and ZTP timeline.
