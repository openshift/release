ref:
  as: telcov10n-sno-spoke-gitops-verify
  commands: telcov10n-sno-spoke-gitops-verify-commands.sh
  from: src
  resources:
    requests:
      cpu: 1000m
      memory: 500Mi
  env:
  credentials:
  documentation: |-
    This step allows to verify the Giteops k8s service into the associated OCP Hub cluster
    through its kubeconfig.
