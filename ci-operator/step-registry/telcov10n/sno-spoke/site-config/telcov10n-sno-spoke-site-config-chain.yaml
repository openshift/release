chain:
  as: telcov10n-sno-spoke-site-config
  steps:
    - ref: telcov10n-sno-spoke-site-config-clean-up
    - ref: telcov10n-sno-spoke-site-config-deploy
    - ref: telcov10n-sno-spoke-site-config-verify
  documentation: |-
    These chainned steps allow to generate, push and verify the Siteconfig CR
    to be used later for the SNO Spoke cluster deployment
