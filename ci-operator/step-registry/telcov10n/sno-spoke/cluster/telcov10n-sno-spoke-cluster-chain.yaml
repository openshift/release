chain:
  as: telcov10n-sno-spoke-cluster
  steps:
    - ref: telcov10n-sno-spoke-cluster-clean-up
    - ref: telcov10n-sno-spoke-cluster-install
    - ref: telcov10n-sno-spoke-cluster-verify
  documentation: |-
    These chainned steps allow to both deploy and verify the SNO Spoke cluster via ZTP
