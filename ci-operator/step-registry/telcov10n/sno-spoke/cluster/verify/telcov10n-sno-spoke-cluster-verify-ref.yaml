ref:
  as: telcov10n-sno-spoke-cluster-verify
  commands: telcov10n-sno-spoke-cluster-verify-commands.sh
  from: src
  resources:
    requests:
      cpu: 1000m
      memory: 500Mi
  env:
  documentation: |-
    This step allows to verify Gitea deployment in the Hub cluster as a k8s service
