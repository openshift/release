ref:
  as: telcov10n-sno-spoke-cluster-verify
  commands: telcov10n-sno-spoke-cluster-verify-commands.sh
  from: src
  resources:
    requests:
      cpu: 1000m
      memory: 500Mi
  documentation: |-
    This step allows to both check the progress and verify the SNO Spoke cluster installation
