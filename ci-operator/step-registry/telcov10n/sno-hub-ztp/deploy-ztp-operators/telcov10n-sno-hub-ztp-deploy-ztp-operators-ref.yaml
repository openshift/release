ref:
  as: telcov10n-sno-hub-ztp-deploy-ztp-operators
  commands: telcov10n-sno-hub-ztp-deploy-ztp-operators-commands.sh
  from: src
  resources:
    requests:
      cpu: 1000m
      memory: 500Mi
  credentials:
  - namespace: test-credentials
    name: telcov10n-ztp-left-shifting
    mount_path: /var/run/telcov10n/ztp-left-shifting
  documentation: |-
    This step will deploy ZTP operators in the OCP Hub cluster
