ref:
  as: workers-scale
  steps: 
  - ref: workers-scale
  env:
  - name: WORKER_COUNT
    default: 0
    documentation: |-
      Default is 0, which means workers won't scale at all
  documentation: |-
    The post-installation provision step increases the machinesets which provision new instances for worker nodes.
