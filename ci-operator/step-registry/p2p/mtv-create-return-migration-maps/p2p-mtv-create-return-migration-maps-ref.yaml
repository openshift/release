ref:
  as: p2p-mtv-create-return-migration-maps
  from: cli
  commands: p2p-mtv-create-return-migration-maps-commands.sh
  timeout: 30m0s
  grace_period: 10m0s
  resources:
    requests:
      cpu: 200m
      memory: 400Mi
  documentation: |-
    Create MTV NetworkMap (pod→pod) and StorageMap for the return migration leg (spoke-2 → spoke-1)
    on the hub. Run after p2p-mtv-create-migration-maps (which creates the forward maps) and after
    ODF is Available on both spokes; creating StorageMap too early causes StorageRefNotValid.

    This is the return-leg companion to p2p-mtv-create-migration-maps. Default source/destination
    are reversed (spoke-2 as source, spoke-1 as destination) and map names carry a "-return" suffix
    to coexist alongside the forward maps in the same openshift-mtv namespace.

    Annotates both providers for inventory refresh (unless MTV_SKIP_INVENTORY_REFRESH=true),
    applies maps, waits for Ready, and appends to ${ARTIFACT_DIR}/mtv-migration-maps-status.txt.

    Hub kubeconfig is KUBECONFIG from ci-operator.
  env:
  - name: MTV_NAMESPACE
    default: "openshift-mtv"
    documentation: Namespace where MTV operator and map CRs live.
  - name: MTV_SOURCE_PROVIDER
    default: "spoke-2"
    documentation: Source MTV Provider CR name for the return leg (migration origin).
  - name: MTV_DESTINATION_PROVIDER
    default: "spoke-1"
    documentation: Destination MTV Provider CR name for the return leg (migration target).
  - name: MTV_NETWORK_MAP_NAME
    default: "spoke-network-map-return"
    documentation: NetworkMap metadata.name for the return leg.
  - name: MTV_STORAGE_MAP_NAME
    default: "spoke-storage-map-return"
    documentation: StorageMap metadata.name for the return leg.
  - name: MTV_SOURCE_STORAGE_NAME
    default: "ocs-storagecluster-ceph-rbd-virtualization"
    documentation: StorageClass name on spoke-2 (the return-leg source; spec.map[].source.name).
  - name: MTV_DESTINATION_STORAGE_CLASS
    default: "ocs-storagecluster-ceph-rbd-virtualization"
    documentation: Destination StorageClass name on spoke-1 (spec.map[].destination.storageClass).
  - name: MTV_PROVIDER_READY_TIMEOUT
    default: "15m"
    documentation: Timeout for oc wait provider/<name> --for=condition=Ready.
  - name: MTV_MAP_READY_TIMEOUT
    default: "15m"
    documentation: Timeout for NetworkMap/StorageMap Ready waits.
  - name: MTV_SKIP_INVENTORY_REFRESH
    default: "false"
    documentation: Set to "true" to skip forklift.konveyor.io/inventory-refresh annotation.
