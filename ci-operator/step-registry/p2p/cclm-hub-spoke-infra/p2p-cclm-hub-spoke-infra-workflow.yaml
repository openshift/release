workflow:
  as: p2p-cclm-hub-spoke-infra
  steps:
    pre:
    - ref: ipi-conf
    - ref: ipi-conf-telemetry
    - ref: ipi-conf-aws-custom-az
    - ref: ipi-conf-aws
    - ref: ipi-install-monitoringpvc
    - chain: ipi-install
    test:
    - chain: cucushift-installer-check-cluster-health
    - ref: install-operators
    - ref: acm-mch
    - ref: p2p-install-odf-hub
    - ref: acm-interop-p2p-cluster-install
    - ref: acm-fetch-managed-clusters
    - ref: p2p-install-cnv-hub
    - ref: p2p-acm-cnv-install-policy
    - ref: p2p-install-odf-spokes
    - chain: acm-interop-p2p-submariner-install
    - ref: p2p-mtv-additional-config
    - ref: p2p-mtv-register-providers
    - ref: p2p-cnv-exchange-kubevirt-ca
    post:
    - ref: acm-fetch-operator-versions
    - ref: acm-must-gather
    - ref: gather-aws-console
    - ref: acm-interop-p2p-cluster-uninstall
    - chain: ipi-deprovision
  documentation: |-
    Hub-spoke cluster infrastructure setup for CCLM testing.
    
    Provisions:
      - Hub cluster: IPI on AWS (m6a.2xlarge control-plane, configurable workers)
      - Spoke cluster(s): Hive/ACM-provisioned on AWS
    
    Installs:
      - ACM MultiClusterHub
      - MTV (Migration Toolkit for Virtualization)
      - ODF on hub and spoke
      - CNV on hub and spoke
      - Submariner for pod-to-pod connectivity
      - MTV provider registration
    
    This workflow prepares infrastructure for CCLM migration testing.
    Migration tests are added in subsequent PRs.
