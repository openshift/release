ref:
  as: instaslice-operator-bundle
  from: src
  commands: instaslice-operator-bundle-commands.sh
  resources:
    requests:
      cpu: "200m"
      memory: 1Gi
  documentation: |-
    Sets up prerequisites to run Instaslice operator e2e test suite
