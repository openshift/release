ref:
  as: single-node-conf-aws-graceful-shutdown
  from: installer
  commands: single-node-conf-aws-graceful-shutdown-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    Modifies install-config.yaml to use a single control-plane node and without any workers with graceful shutdown enabled.
