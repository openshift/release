ref:
  as: single-node-conf-e2e-graceful-shutdown
  from: installer
  commands: single-node-conf-e2e-graceful-shutdown-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
      Configurations required to run E2E tests on a single node cluster.
