workflow:
  as: oape-review-handler-trigger
  steps:
    test:
      - ref: oape-review-handler-trigger
  documentation: |-
    Lightweight workflow that triggers the periodic oape-review-handler job via
    Gangway instead of running the agent inline. This avoids the presubmit
    aborting when the agent pushes to the PR branch.
