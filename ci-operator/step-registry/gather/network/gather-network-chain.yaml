chain:
  as: gather-network
  steps:
  - ref: gather-network
  documentation: |-
    The gather-network step chain contains all the individual steps necessary to gather network artifacts from a cluster.
