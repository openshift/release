ref:
  as: gather-disconnected
  from: upi-installer
  commands: gather-disconnected-commands.sh
  resources:
    requests:
      cpu: 300m
      memory: 300Mi
  documentation: |-
    The gather step collects journal logs from the proxy.