ref:
  as: gather-must-gather-investigator
  optional_on_success: true
  from: okd-camgi
  commands: gather-must-gather-investigator-commands.sh
  resources:
    requests:
      cpu: 300m
      memory: 300Mi
  documentation: |-
    okd-camgi generates an HTML view of must-gather parts relating to cluster
    autoscaler behavior and configuration
