ref:
  as: gather-symptoms
  optional_on_success: true
  from: cli
  commands: gather-symptoms-commands.py
  timeout: 600s
  resources:
    requests:
      cpu: 300m
      memory: 300Mi
  documentation: >-
    This step checks logs for known issues and creates JUnit files.
