ref:
  as: gather-install-status
  best_effort: true
  from: cli
  commands: gather-install-status-commands.sh
  resources:
    requests:
      cpu: 300m
      memory: 300Mi
  timeout: 15m0s
  documentation: |-
    The install-status step creates the junit file with install test results.
