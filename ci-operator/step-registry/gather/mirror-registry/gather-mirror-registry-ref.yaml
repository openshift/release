ref:
  as: gather-mirror-registry
  from: tools
  grace_period: 10m
  commands: gather-mirror-registry-commands.sh
  resources:
    requests:
      cpu: 300m
      memory: 300Mi
  documentation: |-
    The gather step collects journal logs for registry service from bation host.
