ref:
  as: gather-azure-resource
  from_image:
    namespace: ocp
    name: "4.13"
    tag: upi-installer
  commands: gather-azure-resource-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: >-
    This step gathers the set of Azure resources after an installation.
