ref:
  as: gather-must-gather-jump
  from: tests
  commands: gather-must-gather-jump-commands.sh
  resources:
    requests:
      cpu: 300m
      memory: 300Mi
  documentation: |-
    The must-gather step runs the must-gather tool to gather artifacts.
