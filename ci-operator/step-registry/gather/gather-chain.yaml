chain:
  as: gather
  steps:
  - ref: gather-must-gather
  - ref: gather-extra
  - ref: gather-audit-logs
  - ref: cucushift-installer-gather-aws-resources
  documentation: |-
    The gather step chain contains all the individual steps necessary to gather artifacts from a cluster.
