ref:
  as: gather-installer-gather
  optional_on_success: true
  best_effort: true
  timeout: 10m
  from: installer
  commands: gather-installer-gather-commands.sh
  resources:
    requests:
      cpu: 300m
      memory: 300Mi
  documentation: |-
    Gather installation logs in case of failed installation.
