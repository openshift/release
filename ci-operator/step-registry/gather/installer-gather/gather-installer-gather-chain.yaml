chain:
  as: gather-installer-gather
  steps:
  - ref: gather-installer-gather
  documentation: |-
    The gather-installer-gather step chain contains all the individual steps necessary to gather the instlalation logs from the cluster.
