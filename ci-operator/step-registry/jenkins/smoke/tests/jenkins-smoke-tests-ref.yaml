ref:
  as: jenkins-smoke-tests
  cli: latest
  from: jenkins-runner
  commands: jenkins-smoke-tests-commands.sh
  resources:
    requests:
      cpu: 50m
      memory: 100Mi
  documentation: |
    Run smoke tests for OpenShift Jenkins.
