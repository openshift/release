workflow:
  as: identitatem-ci-image-mirror
  steps:
    pre:
    - ref: identitatem-ci-rbac
    - ref: identitatem-ci-image-mirror
  documentation: |-
    This workflow mirrors an image from the CI Registry to a given
    image ref.
