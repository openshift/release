workflow:
  as: ship-status-dash-e2e
  steps:
    pre:
    - ref: ship-status-dash-e2e-setup
    test:
    - ref: ship-status-dash-e2e-test
  documentation: |-
    This workflow runs end-to-end tests for the ship-status-dash application.

    The workflow includes:
    - Setup: Prepare test environment (postgres database, dependencies)
    - Test: Execute the e2e test suite

    This is a stub workflow that will be expanded with actual test implementation.
