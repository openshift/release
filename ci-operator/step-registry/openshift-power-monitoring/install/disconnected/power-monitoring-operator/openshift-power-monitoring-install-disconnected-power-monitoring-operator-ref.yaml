ref:
  as: openshift-power-monitoring-install-disconnected-power-monitoring-operator
  from: upi-installer
  commands: openshift-power-monitoring-install-disconnected-power-monitoring-operator-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 100Mi
  credentials:
  - namespace: test-credentials
    name: openshift-custom-mirror-registry
    mount_path: /var/run/vault/mirror-registry
  documentation: |-
    Install PowerMonitoring Operator catalog source for running operators in disconnected env.
