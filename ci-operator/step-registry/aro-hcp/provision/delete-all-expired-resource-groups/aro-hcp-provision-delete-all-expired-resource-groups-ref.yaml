ref:
  as: aro-hcp-provision-delete-all-expired-resource-groups
  from: src-plus-az
  commands: aro-hcp-provision-delete-all-expired-resource-groups-commands.sh
  credentials:
    - namespace: test-credentials
      name: hcp-integration-credentials
      mount_path: /var/run/hcp-integration-credentials
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  env:
  - name: CUSTOMER_SUBSCRIPTION
    default: ""
    documentation: Azure subscription to delete resources in.  For example, "ARO SRE Team - INT (EA Subscription 3)"
  documentation: |-
    Delete ARO HCP cluster.