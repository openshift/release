ref:
  as: aro-hcp-exporter
  from: src
  commands: aro-hcp-exporter-commands.sh
  grace_period: 1m0s
  timeout: 15m0s
  env:
  - name: DEPLOY_ENV
    default: "dev"
    documentation: The ARO HCP deploy environment used to resolve ACR image coordinates via templatize.
  dependencies:
  - name: aro-hcp-exporter
    env: ARO_HCP_EXPORTER
  resources:
    requests:
      cpu: "1"
      memory: 2Gi
  documentation: |-
    Pushes the ci-operator built aro-hcp-exporter image to ACR.
    Runs only on postsubmit (merge to main).
