ref:
  as: aro-hcp-deprovision-environment
  from: aro-hcp-e2e-tools
  commands: aro-hcp-deprovision-environment-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 300Mi
  credentials:
    - namespace: test-credentials
      name: cluster-secrets-aro-hcp-dev
      mount_path: /var/run/aro-hcp-dev
  env:
    - name: VAULT_SECRET_PROFILE
      default: "dev"
      documentation: |-
        Selects which environment's cluster secrets to use (dev, int, stg, prod).
    - name: ARO_HCP_DEPLOY_ENV
      default: "ci00"
      documentation: |-
        Config environment name. Also used to select the infrastructure
        subscription from Vault (infra-{ARO_HCP_DEPLOY_ENV}-subscription-id).
    - name: SKIP_DEPROVISION
      default: "false"
      documentation: |-
        When set to "true", the deprovision step becomes a no-op, leaving
        infrastructure intact for debugging purposes.
  documentation: |-
    Delete an ARO HCP development environment
