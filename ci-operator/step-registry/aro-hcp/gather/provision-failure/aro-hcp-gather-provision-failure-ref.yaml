ref:
  as: aro-hcp-gather-provision-failure
  from: aro-hcp-e2e-tools
  commands: aro-hcp-gather-provision-failure-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  credentials:
    - namespace: test-credentials
      name: cluster-secrets-aro-hcp-dev
      mount_path: /var/run/aro-hcp-dev
  env:
    - name: VAULT_SECRET_PROFILE
      default: "dev"
      documentation: |-
        Selects which environment's cluster secrets to use (dev, int, stg, prod).
  best_effort: true
  timeout: 5m
