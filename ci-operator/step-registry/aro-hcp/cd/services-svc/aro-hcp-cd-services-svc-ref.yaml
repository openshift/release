ref:
  as: aro-hcp-cd-services-svc
  from: aro-hcp-e2e-tools
  commands: aro-hcp-cd-services-svc-commands.sh
  timeout: 30m0s
  env:
  - name: DEPLOY_ENV
    default: "cspr"
    documentation: The ARO HCP deploy environment for CSPR SVC cluster service deployment.
  resources:
    requests:
      cpu: "1"
      memory: 2Gi
  documentation: |-
    Deploys services to the SVC cluster: backend, frontend, admin, sessiongate
    (via record-override + pipeline), maestro, tracing, and CS PR env deps.
