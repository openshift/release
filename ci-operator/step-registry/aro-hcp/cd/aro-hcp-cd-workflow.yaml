workflow:
  as: aro-hcp-cd
  steps:
    test:
    - ref: aro-hcp-images-push
    - ref: aro-hcp-cd-infra
  documentation: |-
    CD workflow for CSPR environment: pushes service images to ACR,
    then deploys infrastructure.
    Steps execute sequentially in the order listed.
