ref:
  as: aro-hcp-sessiongate-push
  from: src
  commands: aro-hcp-sessiongate-push-commands.sh
  grace_period: 1m0s
  timeout: 15m0s
  env:
  - name: DEPLOY_ENV
    default: "dev"
    documentation: The ARO HCP deploy environment used to resolve ACR image coordinates via templatize.
  dependencies:
  - name: aro-hcp-sessiongate
    env: ARO_HCP_SESSIONGATE
  resources:
    requests:
      cpu: "1"
      memory: 2Gi
  documentation: |-
    Pushes the ci-operator built aro-hcp-sessiongate image to ACR.
    Runs only on postsubmit (merge to main).
