workflow:
  as: aro-hcp-cspr
  steps:
    test:
    - ref: aro-hcp-cspr-deploy
  documentation: |-
    Deploys the CSPR environment in topology order while intentionally
    omitting the Cluster Service pipeline. CI-built images are supplied
    through one shared override and mirrored by each owning pipeline.
