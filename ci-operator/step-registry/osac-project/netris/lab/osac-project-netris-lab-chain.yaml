chain:
  as: osac-project-netris-lab
  steps:
  - ref: ipi-install-rbac
  - ref: osac-project-netris-lab-setup
  - ref: osac-project-netris-lab-deploy
  - ref: osac-project-netris-lab-snapshot
  documentation: |-
    Sets up the OFCIR bare-metal host, deploys the Netris lab
    network simulation, restores OCP + OSAC from snapshot with
    recert, then refreshes OSAC component images under test.
