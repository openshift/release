ref:
  as: ingress-aws-conf-lbtype-classic
  from_image:
    namespace: ocp
    name: cli-yq
    tag: latest
  commands: ingress-aws-conf-lbtype-classic-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    The aws-conf-lbtype-classic sets the platform.aws.lbType to Classic in the install-config.yaml
