ref:
  as: ingress-aws-nlb-manifest
  from_image:
   namespace: origin
   name: centos
   tag: '8'
  commands: ingress-aws-nlb-manifest-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    The AWS NLB manifest step adds the configuration for enabling AWS NLB as default for installation
