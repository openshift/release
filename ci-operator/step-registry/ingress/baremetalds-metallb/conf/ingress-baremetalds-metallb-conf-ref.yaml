ref:
  as: ingress-baremetalds-metallb-conf
  from: cli
  grace_period: 10m
  commands: ingress-baremetalds-metallb-conf-commands.sh
  resources:
    requests:
      cpu: 1000m
      memory: 500Mi
  timeout: 20m0s
  documentation: |-
    This step installs the MetalLB operator and configures it for ingress testing.
    It creates MetalLB instance, IPAddressPool with hardcoded IP range, and L2Advertisement
    resources to enable load balancer services for ingress testing.
