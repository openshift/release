ref:
  as: ingress-gcp-glb-manifest
  from_image:
   namespace: origin
   name: centos
   tag: '8'
  commands: ingress-gcp-glb-manifest-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    The GCP glb manifest step adds the configuration for enabling global access to GCP internal LB as default for installation
