ref:
  as: terraform-aws-cluster-create
  from: ocm-tf-e2e
  grace_period: 10m
  commands: terraform-aws-cluster-create-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 300Mi
  timeout: 2h0m0s
  env:
  - name: ART_NAME
    default: "cluster"
  - name: TF_FOLDER
    default: ci/e2e/terraform_provider_ocm_files
  - name: TF_VARS
    default: |
      url = "https://api.stage.openshift.com"

  documentation: |-
    Using rosa cli to create a rosa sts cluster with the provided cluster profile. The cluster profile should include the offline token ocm-token to login and .awscred to init the rosa environment.
