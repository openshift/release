workflow:
  as: terraform-aws-e2e
  steps: 
    pre:
    - ref: terraform-aws-e2e-test
  documentation: |-
    This is a demo workflow for install rehearse/test only.
    The workflow shows how to provision/deprovision an AWS cluster via ocm terraform provider.
