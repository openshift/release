ref:
  as: terraform-aws-destroy
  from: ocm-tf-cli
  grace_period: 10m
  commands: terraform-aws-destroy-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 300Mi
  timeout: 2h0m0s
  env:
  - name: ART_NAME
    default: "cluster1"

  documentation: |-
    Using rosa cli to create a rosa sts cluster with the provided cluster profile. The cluster profile should include the offline token ocm-token to login and .awscred to init the rosa environment.
