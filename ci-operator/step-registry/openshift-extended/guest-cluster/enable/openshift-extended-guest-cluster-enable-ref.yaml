ref:
  as: openshift-extended-guest-cluster-enable
  from: tests-private
  grace_period: 30m
  cli: latest
  commands: openshift-extended-guest-cluster-enable-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 100Mi
  documentation: |-
    enable Hypershift hostedcluster in cluster.
