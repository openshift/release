ref:
  as: openshift-extended-guest-cluster-disable
  from: tests-private
  grace_period: 30m
  cli: latest
  commands: openshift-extended-guest-cluster-disable-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 100Mi
  documentation: |-
    disable Hypershift hostedcluster in cluster.
