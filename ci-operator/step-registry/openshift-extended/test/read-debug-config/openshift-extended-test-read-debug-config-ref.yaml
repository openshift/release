ref:
  as: openshift-extended-test-read-debug-config
  from_image:
    namespace: ocp
    name: "4.22"
    tag: cli
  grace_period: 5m
  commands: openshift-extended-test-read-debug-config-commands.sh
  timeout: 10m0s
  resources:
    requests:
      cpu: 100m
      memory: 100Mi
  documentation: |-
    Reads .debug-cluster-config if present and exports RELEASE_IMAGE as
    CUSTOM_OPENSHIFT_INSTALL_RELEASE_IMAGE_OVERRIDE.
