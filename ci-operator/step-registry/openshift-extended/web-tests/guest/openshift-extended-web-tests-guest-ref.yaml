ref:
  as: openshift-extended-web-tests-guest
  from: tests-private-ui
  cli: latest
  commands: openshift-extended-web-tests-guest-commands.sh
  timeout: 2h0m0s
  resources:
    requests:
      cpu: "2"
      memory: 2Gi
    limits:
      memory: 4Gi
  documentation: |-
    This workflow will run OCP web testing scenarios for hypershift guest cluster
