ref:
  as: interop-tests-deploy-portworx
  from: cnv-ci
  commands: interop-tests-deploy-portworx-commands.sh
  credentials:
    - namespace: test-credentials
      name: test-secret
      mount_path: /tmp/secrets/ci
  resources:
    requests:
      cpu: 100m
      memory: 200Mi
  documentation: |-
    Deploy Portworx