ref:
  as: azure-provision-sharednetwork
  from_image:
    namespace: ocp
    name: "4.5"
    tag: upi-installer
  grace_period: 10m
  commands: azure-provision-sharednetwork-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    The IPI configure step chain generates the install-config.yaml file based on the cluster profile and optional input files.
