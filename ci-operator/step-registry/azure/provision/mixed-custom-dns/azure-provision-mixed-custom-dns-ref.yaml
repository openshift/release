ref:
  as: azure-provision-mixed-custom-dns
  from_image:
    namespace: ocp
    name: "4.17"
    tag: upi-installer
  grace_period: 10m
  commands: azure-provision-mixed-custom-dns-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  env:
  - name: BASE_DOMAIN
    default: ""
    documentation: |-
      The base domain of custom dns
  documentation: |-
    On running cluster, query frontend IP for api and ingress lb rules from public/internal load balancer
    based on api server and ingress publish strategy, and saved along with api/ingress dns.
