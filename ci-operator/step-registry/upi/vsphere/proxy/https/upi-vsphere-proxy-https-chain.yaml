chain:
  as: upi-vsphere-proxy-https
  steps:
  - ref: ipi-install-rbac
  - chain: upi-conf-vsphere-proxy-https
  - ref: upi-install-vsphere
  - ref: ipi-install-times-collection
  documentation: >-
    This chain contains all of the steps to provision an OpenShift cluster using the vSphere UPI workflow with https proxy configuration.
