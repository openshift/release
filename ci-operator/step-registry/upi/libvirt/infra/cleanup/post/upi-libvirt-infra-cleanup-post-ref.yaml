ref:
  as: upi-libvirt-infra-cleanup-post
  from: libvirt-installer
  grace_period: 10m
  commands: upi-libvirt-infra-cleanup-post-commands.sh
  env:
  - name: CLUSTER_ROLE
    default: "infra"
    documentation: Fixed to "infra". Uses $LEASED_RESOURCE_INFRA to clean up the infra cluster host.
  - name: POOL_NAME
    default: "multiarch-ci-pool"
  - name: HTTPD_POOL_NAME
    default: "httpd"
  resources:
    requests:
      cpu: 1000m
      memory: 300Mi
  dnsConfig:
    nameservers:
    - 172.30.38.188
    searches:
    - "bastion-z.svc.cluster.local"
    - "bastion-ppc64le-libvirt.svc.cluster.local"
  documentation: |-
    Infra variant of upi-libvirt-cleanup-post. Destroys infra cluster libvirt resources
    identified by $LEASED_RESOURCE_INFRA after the job completes.
