ref:
  as: upi-libvirt-infra-rbac
  from: cli
  commands: upi-libvirt-infra-rbac-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 100Mi
  documentation: Infra variant of ipi-install-rbac. Grants RBAC for the infra cluster against the build farm.
