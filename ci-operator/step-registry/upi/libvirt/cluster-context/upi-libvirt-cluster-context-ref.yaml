ref:
  as: upi-libvirt-cluster-context
  from: libvirt-installer
  commands: upi-libvirt-cluster-context-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 10Mi
  documentation: |-
    Internal library for UPI libvirt dual-cluster installs. Sourced by conf/install
    steps; do not reference directly in workflows.
