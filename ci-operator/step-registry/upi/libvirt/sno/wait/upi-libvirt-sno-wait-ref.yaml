ref:
  as: upi-libvirt-sno-wait
  from: cli
  commands: upi-libvirt-sno-wait-commands.sh
  timeout: 60m
  resources:
    requests:
      cpu: 100m
      memory: 100Mi
  documentation: |-
    Install health check for libvirt SNO: wait for nodes Ready and
    clusteroperators Available. This is not openshift-tests / conformance.
