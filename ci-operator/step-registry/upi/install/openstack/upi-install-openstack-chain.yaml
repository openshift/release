chain:
  as: upi-install-openstack
  steps:
  - ref: ipi-install-rbac
  - ref: openshift-cluster-bot-rbac
  - ref: upi-install-openstack-inventory
  - ref: upi-install-openstack
  - ref: ipi-install-times-collection
  documentation: >-
    This chain follows the documentation for an UPI installation on OpenStack.
