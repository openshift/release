chain:
  as: upi-install-aws-usgov-private
  steps:
    - ref: upi-install-aws-usgov-private-cluster
    - ref: ipi-install-times-collection
  documentation: |-
    Deploys a private OpenShift cluster on AWS GovCloud using UPI with
    CloudFormation. Creates VPC, internal-only NLB, and all cluster nodes
    on private subnets.
