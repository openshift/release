chain:
  as: upi-install-aws
  steps:
  - ref: upi-install-aws-cluster
  - ref: ipi-install-times-collection
  documentation: >-
    This chain generates the files necessary to deploy a AWS UPI cluster.
    It creates a cluster in AWS and then informs of the time it took for
    the cluster to install.
