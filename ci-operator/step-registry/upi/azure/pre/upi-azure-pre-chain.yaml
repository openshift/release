chain:
  as: upi-azure-pre
  steps:
  - ref: ipi-install-rbac
  - chain: ipi-conf-azure
  - ref: upi-install-azure
  documentation: >-
    This chain contains all of the steps to provision an OpenShift cluster using the Azure UPI workflow.
