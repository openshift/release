ref:
  as: ipi-install-rbac-infra
  from: cli
  commands: ipi-install-rbac-infra-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 100Mi
  documentation: |-
    Infra-cluster variant of ipi-install-rbac. Runs the same RBAC setup for the
    infra cluster against the build farm.
