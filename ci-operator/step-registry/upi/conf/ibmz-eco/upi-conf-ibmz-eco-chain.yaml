chain:
  as: upi-conf-ibmz-eco
  steps:
  - ref: upi-conf-ibmz-eco
  documentation: >-
    This chain generates the files necessary to deploy an IBM Z UPI cluster.
    It creates a terraform.tfvars file that is used to generate install yaml and ignition files
