ref:
  as: upi-conf-vsphere-s3
  from: upi-installer
  commands: upi-conf-vsphere-s3-commands.sh
  resources:
    requests:
      cpu: 1000m
      memory: 512Mi
  documentation: >-
    Creates S3 bucket for bootstrap ignition and then provides a presign URL to the bootstrap.ign file

