chain:
  as: upi-conf-vsphere-clusterbot-post
  steps:
  - ref: ipi-deprovision-vsphere-lb
  documentation: >-
    This chain destroys the load balancer provisioned to offer ingress for clusterbot provisioned clusters
