ref:
  as: upi-conf-vsphere-ova-windows
  from: upi-installer
  grace_period: 10m
  commands: upi-conf-vsphere-ova-windows-commands.sh
  resources:
    requests:
      cpu: 1000m
      memory: 512Mi
  documentation: >-
    This step checks if the Windows OS template exists, otherwise fails.
