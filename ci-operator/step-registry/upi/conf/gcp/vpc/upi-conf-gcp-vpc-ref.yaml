ref:
  as: upi-conf-gcp-vpc
  from_image:
    namespace: ocp
    name: "4.5"
    tag: upi-installer
  commands: upi-conf-gcp-vpc-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    The UPI GCP configuration step creates VPC network/subnets/router/nat based on the cluster profile, and export VPC info for subsequent consumers. 
