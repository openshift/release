ref:
  as: upi-gcp-vpc-post
  from_image:
    namespace: ocp
    name: "4.5"
    tag: upi-installer
  commands: upi-gcp-vpc-post-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    The UPI GCP step destroys VPC network/subnets/router/nat resources. 
