ref:
  as: upi-gcp-vpc-pre
  from_image:
    namespace: ocp
    name: "4.5"
    tag: upi-installer
  commands: upi-gcp-vpc-pre-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    The UPI GCP step creates VPC network/subnets/router/nat resources, based on the cluster profile, and export VPC info for subsequent consumers. 
