chain:
  as: upi-metal-pre
  steps:
  - ref: ipi-install-rbac
  - ref: upi-install-metal
  - ref: ipi-install-times-collection
  documentation: >-
    This chain contains all of the steps to provision an OpenShift cluster using the baremetal UPI workflow.
