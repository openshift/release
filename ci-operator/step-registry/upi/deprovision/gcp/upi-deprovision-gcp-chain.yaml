chain:
  as: upi-deprovision-gcp
  steps:
  - ref: upi-deprovision-gcp
  - ref: upi-deprovision-gcp-by-infra-manager
  documentation: >-
    This chain deprovisions the resources created by upi-install-gcp.
    It requires the ipi-deprovision already be executed against the cluster.
