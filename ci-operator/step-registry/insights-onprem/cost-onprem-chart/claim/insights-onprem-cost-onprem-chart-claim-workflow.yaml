workflow:
  as: insights-onprem-cost-onprem-chart-claim
  steps:
    allow_skip_on_success: true
    pre:
    - ref: ipi-install-rbac
    post:
    - ref: insights-onprem-cost-onprem-chart-send-reportportal
      optional_on_success: false
    - chain: gather
      optional_on_success: true
  documentation: |-
    This workflow performs shared pre/post actions on clusters obtained from
    preinstalled pools. It extends generic-claim but skips gather steps on success.

