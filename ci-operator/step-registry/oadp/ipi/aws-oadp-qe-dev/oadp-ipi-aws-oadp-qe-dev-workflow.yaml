workflow:
  as: oadp-ipi-aws-oadp-qe-dev
  steps:
    env:
        RE_TRIGGER_ON_FAILURE: "true"
    pre:
    - chain: ipi-aws-pre
    - ref: optional-operators-subscribe
      from: oadp-operator
    - ref: oadp-s3-create
    post:
    - ref: oadp-s3-destroy
    - chain: ipi-aws-post
    - ref: job-re-trigger
    - ref: send-results-to-reportportal

  documentation: |-
    Workflow that adds a ability to deploy unreleased operator from dev, S3 bucket creation and destruction step specific to the OADP interop scenario.
