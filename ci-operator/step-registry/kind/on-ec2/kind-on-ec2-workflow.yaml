workflow:
  as: kind-on-ec2
  steps:
    pre:
    - ref: kind-on-ec2-provision
    test:
    - ref: kind-on-ec2-test
    post:
    - ref: aws-deprovision-stacks
  documentation: |-
    Provision an EC2 instance, create a kind cluster on it,
    run a hello-world verification, and tear down the infrastructure.
