workflow:
  as: amd-gpu-operator-e2e
  steps:
    leases:
      - env: REMOTE_HOST
        resource_type: amd-gpu-quota-slice
    pre:
    - ref: amd-gpu-operator-provision
    - ref: amd-gpu-operator-install-operators
    test:
    - ref: amd-gpu-operator-test
    post:
    - ref: amd-gpu-operator-deprovision

