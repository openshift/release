ref:
  as: image-based-install-operator-setup
  from: src
  commands: image-based-install-operator-setup-commands.sh
  resources:
    requests:
      cpu: 1000m
      memory: 500Mi
  dependencies:
  - name: "image-based-install-operator"
    env: IMG
  documentation: |-
    Deploying Image Based Install Operator and Hive.
