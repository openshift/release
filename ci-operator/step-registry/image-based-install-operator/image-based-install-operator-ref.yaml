ref:
  as: image-based-install-operator
  from: src
  commands: image-based-install-operator-commands.sh
  resources:
    requests:
      cpu: 1000m
      memory: 500Mi
  env:
  - name: BMH_EXTERNALLY_PROVISIONED
    default: "false"
  documentation: |-
    Finish installing a host using ibio.
