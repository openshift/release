ref:
  as: image-based-install-operator-reinstall-create-secrets
  from: src
  commands: image-based-install-operator-reinstall-create-secrets-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 50Mi
  documentation: |-
    Create secrets containing cluster identity for use with image based installation.
    These secrets define the cluster crypto data and are generated during an image based install.
