ref:
  as: image-based-install-operator-post-gather
  from: src
  commands: image-based-install-operator-post-gather-commands.sh
  resources:
    requests:
      cpu: 1000m
      memory: 500Mi
  documentation: |-
    Gather info and logs after deleting an ImageClusterInstall.
