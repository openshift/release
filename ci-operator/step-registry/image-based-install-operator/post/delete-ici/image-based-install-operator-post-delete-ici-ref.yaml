ref:
  as: image-based-install-operator-post-delete-ici
  from: src
  commands: image-based-install-operator-post-delete-ici-commands.sh
  resources:
    requests:
      cpu: 1000m
      memory: 500Mi
  documentation: |-
    Delete an ImageClusterInstall.
