ref:
  as: image-based-install-operator-gather
  from: src
  commands: image-based-install-operator-gather-commands.sh
  resources:
    requests:
      cpu: 1000m
      memory: 500Mi
  documentation: |-
    Gather info and logs after installing a host using ibio.