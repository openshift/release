workflow:
  as: gcp-hcp-e2e
  steps:
    pre:
    - ref: gcp-hcp-wif-auth
    test:
    - ref: gcp-hcp-terraform-plan
    post: []
  documentation: |-
    E2E workflow for gcp-hcp-infra. Authenticates to GCP via WIF
    and runs terraform plan on the e2e config.
