ref:
  as: gcp-hcp-check-cleanup-image
  from_image:
    namespace: ocp
    name: "4.22"
    tag: upi-installer
  commands: gcp-hcp-check-cleanup-image-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
  timeout: 2m0s
  documentation: |-
    TEMPORARY: Validates that the cleanup-infrastructure image has all required
    binaries (jq, gcloud, kubectl/oc, curl). Remove after validation.
