ref:
  as: gcp-hcp-verify-argocd-sync
  from_image:
    namespace: ocp
    name: "4.22"
    tag: cli
  commands: gcp-hcp-verify-argocd-sync-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
  timeout: 5m0s
  documentation: |-
    Placeholder test step that validates provision outputs are available.
    Real ArgoCD sync validation will be added in a future story.
