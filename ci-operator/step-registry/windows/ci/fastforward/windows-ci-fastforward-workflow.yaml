workflow:
  as: windows-ci-fastforward
  steps:
    test:
    - ref: windows-ci-fastforward
  documentation: |-
    This workflow fast forwards a source branch to a destination branch in a
    GitHub repo.
