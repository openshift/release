ref:
  as: windows-e2e-test
  from: windows-machine-config-bootstrapper-tests
  commands: windows-e2e-test-commands.sh
  resources:
   requests:
      cpu: 1000m
      memory: 600Mi
  documentation: |-
    The windows-tests step executes the windows/linux hybrid test suite.
