workflow:
  as: abi-workflows-bm--bmc--cluster-health
  steps:
    allow_best_effort_post_steps: true
    pre:
      - chain: abi-chains-bm--bmc
      - chain: cucushift-installer-check-cluster-health
    post: []
  documentation: |-
    This Workflow deploy OpenShift Container Platform (OCP) on Bare Metal with BMC, then perform Cluster Health Check.

    See [ABI overview](https://github.com/openshift/release/blob/main/ci-operator/step-registry/abi/README.md) for details.
