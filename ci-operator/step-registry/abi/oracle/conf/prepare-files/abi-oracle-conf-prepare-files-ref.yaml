ref:
  as: abi-oracle-conf-prepare-files
  from_image:
    namespace: ci
    name: "baremetal-qe-base"
    tag: latest
  commands: abi-oracle-conf-prepare-files-commands.sh
  resources:
    requests:
      cpu: '1'
      memory: 128Mi
  documentation: |-
    The abi-oracle-conf-prepare-files retrieves CLI tools

