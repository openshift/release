ref:
  as: abi-oracle-conf-create-iso
  from_image:
    namespace: ci
    name: "baremetal-qe-base"
    tag: latest
  commands: abi-oracle-conf-create-iso-commands.sh
  resources:
    requests:
      cpu: '1'
      memory: 128Mi
  dependencies:
    - name: "release:latest"
      env: OPENSHIFT_INSTALL_RELEASE_IMAGE_OVERRIDE
  documentation: |-
    The abi-oracle-conf-create-iso retrieves CLI tools

