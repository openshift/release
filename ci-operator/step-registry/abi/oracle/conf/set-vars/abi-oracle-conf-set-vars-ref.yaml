ref:
  as: abi-oracle-conf-set-vars
  from_image:
    namespace: ci
    name: "baremetal-qe-base"
    tag: latest
  commands: abi-oracle-conf-set-vars-commands.sh
  resources:
    requests:
      cpu: '1'
      memory: 128Mi
  credentials:
  - namespace: test-credentials # this entry injects the custom credential
    name: oci-secret-compartment
    mount_path: /var/run/oci-secret-compartment
  - namespace: test-credentials # this entry injects the custom credential
    name: oci-secret-tenancy
    mount_path: /var/run/oci-secret-tenancy
  documentation: |-
    The abi-oracle-conf-set-vars retrieves OCI credentials from https://vault.ci.openshift.org/

