ref:
  as: abi-oracle-conf-download-cli
  from_image:
    namespace: ci
    name: "baremetal-qe-base"
    tag: latest
  commands: abi-oracle-conf-download-cli-commands.sh
  resources:
    requests:
      cpu: '1'
      memory: 128Mi
  documentation: |-
    The abi-oracle-conf-download-cli retrieves CLI tools

