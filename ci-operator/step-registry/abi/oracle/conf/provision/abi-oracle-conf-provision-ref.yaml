ref:
  as: abi-oracle-conf-provision
  from_image:
    namespace: ci
    name: "baremetal-qe-base"
    tag: latest
  commands: abi-oracle-conf-provision-commands.sh
  resources:
    requests:
      cpu: '1'
      memory: 128Mi
  documentation: |-
    The abi-oracle-conf-provision provisions required infrastructure and objects on OCI

