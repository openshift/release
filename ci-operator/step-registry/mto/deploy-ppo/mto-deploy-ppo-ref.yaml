ref:
  as: mto-deploy-ppo
  from: operator-sdk
  # inject oc binary
  cli: latest
  commands: mto-deploy-ppo-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    Deploy Multiarch Tuning Operator to the "openshift-multiarch-tuning-operator" namespace.