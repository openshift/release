ref:
  as: redhat-appstudio-hypershift-gather
  best_effort: true
  optional_on_success: true
  from: cli
  commands: redhat-appstudio-hypershift-gather-commands.sh
  timeout: 900s
  grace_period: 60s
  resources:
    requests:
      cpu: 300m
      memory: 300Mi
  documentation: |-
    The pre-deprovision artifacts step collects like gather extra from ipi-aws.
