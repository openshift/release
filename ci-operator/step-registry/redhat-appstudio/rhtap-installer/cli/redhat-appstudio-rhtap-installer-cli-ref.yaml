ref:
  as: redhat-appstudio-rhtap-installer-cli
  cli: latest
  commands: redhat-appstudio-rhtap-installer-cli-commands.sh
  env:
    - name: GIT_URL
      default: "https://github.com/redhat-appstudio/rhtap-cli.git"
    - name: GIT_REVISION
      default: "main"
  credentials:
    - mount_path: /usr/local/rhtap-ci-secrets/rhtap
      name: rhtap-ci-secrets
      namespace: test-credentials
  from: rhtap-installer
  resources:
    requests:
      cpu: 100m
