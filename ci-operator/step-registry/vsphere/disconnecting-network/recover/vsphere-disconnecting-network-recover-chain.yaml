chain:
  as: vsphere-disconnecting-network-recover
  steps:
  - ref: vsphere-disconnecting-network-recover
  - ref: cucushift-upgrade-healthcheck
  documentation: >-
    This chain revert disconnected network back to connected network in UPI cluster on vSphere platform.
    Only run after chain cucushift-installer-rehearse-vsphere-upi-disconnecting-network-provision if needed.
    And do some healthcheck after network recovers.
