ref:
  as: vsphere-disconnecting-network-recover
  from: upi-installer
  grace_period: 10m
  commands: vsphere-disconnecting-network-recover-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    Recover network from disconnected network in UPI cluster on vSphere platform
