workflow:
  as: vsphere-assisted
  steps:
    allow_best_effort_post_steps: true
    pre:
      - chain: vsphere-assisted-setup
#    test:
#      - ref: baremetalds-assisted-conf
#      - ref: baremetalds-assisted-test
#    post:
#      - ref: baremetalds-assisted-gather
#      - ref: baremetalds-packet-teardown
  documentation: |-
    This workflow installs an OCP cluster by running assisted-installer on a vsphere server.
