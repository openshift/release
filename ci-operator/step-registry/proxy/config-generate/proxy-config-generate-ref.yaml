ref:
  as: proxy-config-generate
  from: 
  from_image:
   namespace: origin
   name: centos
   tag: '8'
  commands: proxy-config-generate-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  credentials:
  documentation: |-
    The proxy generate conf creates the proxy-conf.sh file