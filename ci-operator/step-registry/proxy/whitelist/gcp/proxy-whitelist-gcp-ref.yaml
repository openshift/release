ref:
  as: proxy-whitelist-gcp
  from: cli
  commands: proxy-whitelist-gcp-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  credentials:
  documentation: |-
    Generate particular firewall URLs for GCP cluster.
