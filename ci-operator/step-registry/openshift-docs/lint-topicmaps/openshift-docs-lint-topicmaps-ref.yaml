ref:
  as: openshift-docs-lint-topicmaps
  commands: openshift-docs-lint-topicmaps-commands.sh
  resources:
    requests:
      cpu: 200m
      memory: 200Mi
  from: src
  documentation: |-
    Lint openshift-docs topicmaps YAML with yamllint.
