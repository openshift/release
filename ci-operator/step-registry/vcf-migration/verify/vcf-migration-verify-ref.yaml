ref:
  as: vcf-migration-verify
  from: upi-installer
  commands: vcf-migration-verify-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 200Mi
  timeout: 30m0s
  documentation: |-
    Verifies the migrated cluster is healthy and that cluster and machine
    resources now point only at the target vCenter.
