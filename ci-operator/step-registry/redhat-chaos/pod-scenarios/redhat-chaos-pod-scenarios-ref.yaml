ref:
  as: redhat-chaos-pod-scenarios
  from_image:
    namespace: chaos
    name: krkn
    tag: latest
  commands: redhat-chaos-pod-scenarios-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 100Mi
  documentation: >-
    This step runs the krkn-hub config workload in the deployed cluster
