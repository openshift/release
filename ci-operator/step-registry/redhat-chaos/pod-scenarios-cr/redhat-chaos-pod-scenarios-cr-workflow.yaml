
workflow:
  as: redhat-chaos-pod-scenarios-cr
  steps:
    test:
    - chain: redhat-chaos-pod-scenarios-cr
  documentation: |-
    This workflow executes pod chaos scenarios under load to ensure it is resilient and performant.
