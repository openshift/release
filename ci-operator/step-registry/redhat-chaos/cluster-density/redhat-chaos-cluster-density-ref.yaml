ref:
  as: redhat-chaos-cluster-density
  from_image:
    namespace: chaos
    name: krkn
    tag: latest
  commands: redhat-chaos-cluster-density-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 100Mi
  documentation: >-
    This step runs the perfscale configmap-scale workload in the deployed cluster
