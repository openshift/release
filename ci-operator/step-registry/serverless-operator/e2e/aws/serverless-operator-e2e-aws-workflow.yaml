workflow:
  as: serverless-operator-e2e-aws
  steps:
    pre:
    - chain: ipi-aws-pre
    test:
    - ref: serverless-operator-e2e-test
    post:
    - chain: ipi-aws-post
  documentation: |-
    The Serverless Operator E2E AWS workflow executes the end-to-end test suites on AWS with a default cluster configuration.
