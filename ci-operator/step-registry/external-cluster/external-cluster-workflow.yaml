workflow:
  as: external-cluster
  steps:
    pre:
    - ref: openshift-qe-installer-bm-load-kubeconfig
    post:
    - chain: gather
    env:
      KUBECONFIG_ORIGIN: vault
  documentation: |-
    This workflows imports KUBECONFIG file of a cluster that was deployed
    outside CI Operator.
