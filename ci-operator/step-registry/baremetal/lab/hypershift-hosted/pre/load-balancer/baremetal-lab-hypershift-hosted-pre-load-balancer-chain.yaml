chain:
  as: baremetal-lab-hypershift-hosted-pre-load-balancer
  steps:
    - ref: baremetal-lab-hypershift-hosted-pre-load-balancer
    - ref: baremetal-lab-hypershift-hosted-pre-load-balancer-run
  documentation: |-
    The Baremetal pre load balancer chain will generate the default configuration for HAProxy and will run haproxy
    in the auxiliary host for the hypershift hosted cluster.
