workflow:
  as: baremetal-lab-agent-install-network-dualstack-dynamic-disconnected
  steps:
    pre:
      - chain: baremetal-lab-agent-conf-disconnected
      - ref: baremetal-lab-agent-conf-dynamic
      - ref: baremetal-lab-agent-install
      - ref: enable-qe-catalogsource-disconnected
      - ref: baremetal-lab-nfs-provisioner
    post:
      - chain: baremetal-lab-post
    env:
      DISCONNECTED: "true"
      INTERNAL_NET_CIDR: 192.168.90.0/24
      INTERNAL_NET_CIDR_V6: FE80::/10
      ipv4_enabled: "true"
      ipv6_enabled: "true"
  documentation: |-
    The baremetal-lab-agent-install-network-dualstack-dynamic-disconnected workflow provides pre- and post- steps that provision and
    deprovision an ABI OpenShift cluster with a default configuration on a Baremetal lab,
    allowing job authors to inject their own end-to-end test logic.
