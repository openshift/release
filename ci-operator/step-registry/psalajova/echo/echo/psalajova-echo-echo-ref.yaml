ref:
  as: psalajova-echo-echo
  commands: psalajova-echo-echo-commands.sh
  credentials:
  - collection: psalajova-second-secret
    mount_path: /var/my-secrets
    group: aws
    namespace: test-credentials
  documentation: This ref step is just for local testing of auto-discovery.
  from_image:
    name: centos
    namespace: origin
    tag: "8"
  resources:
    requests:
      cpu: 100m
      memory: 100Mi
