chain:
  as: workers-rhel
  steps:
  - ref: workers-rhel-repos
  - ref: workers-rhel-scaleup
  documentation: |-
    The workers rhel step chain contains steps necessary to prepare and configure OCP on RHEL worker hosts.