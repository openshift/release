ref:
  as: workers-rhel-pre-hook-azure
  from: ansible
  commands: workers-rhel-pre-hook-azure-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 200Mi
  env:
  documentation: |-
    The post-installation repos step configures repos and host settings for new RHEL instances on azure platform.
