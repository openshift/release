ref:
  as: platformoperator-valid-service-mesh
  commands: platformoperator-valid-service-mesh-commands.sh
  from: cli
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    Install an valid Platform Operator during cluster installation. This step only available for Connected clusters and OCP4.12+.
