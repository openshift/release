chain:
  as: leader-worker-set-install
  steps:
  - ref: leader-worker-set-install-catalog
  - ref: leader-worker-set-install-operator
  documentation: |-
    Install the leader-worker-set operator from a catalog source (supports both default and BYO catalogs).
