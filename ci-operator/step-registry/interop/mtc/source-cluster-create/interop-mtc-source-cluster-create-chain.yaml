chain:
  as: interop-mtc-source-cluster-create
  steps:
  env:
      CLUSTER_DURATION: "3600"
      CLUSTER_DURATION_AFTER_FAILURE: "3600"
      CLUSTER_NAME: viv-mtc
      CLUSTER_VERSION: "4.12"
      COMPUTE_MACHINE_TYPE: m5.xlarge
      OCM_LOGIN_URL: https://api.stage.openshift.com
      SUB_CHANNEL: release-v1.7
      SUB_INSTALL_NAMESPACE: openshift-migration
      SUB_PACKAGE: mtc-operator
      SUB_SOURCE: redhat-operators
      SUB_TARGET_NAMESPACES: openshift-migration
  - ref: osd-create-create
  documentation: |-
    Runs the MTC interop scenario