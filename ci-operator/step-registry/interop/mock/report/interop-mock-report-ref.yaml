ref:
  as: interop-mock-report
  from: mock-runner
  commands: interop-mock-report-commands.sh
  resources:
    requests:
      cpu: '1'
      memory: 500Mi
  documentation: |-
    Execute the tests in CSPI-QE/mock_private repo