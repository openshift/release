ref:
  as: aro-ocm-register
  from_image:
    namespace: ocp
    name: "4.14"
    tag: upi-installer
  commands: aro-ocm-register-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    register an aro cluster with OCM using the cloud.redhat.com pull secret included in Prow CI jobs.