ref:
  as: springboot-prepare-cluster
  from: cli
  commands: springboot-prepare-cluster-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    Used to prepare the ephemeral cluster for Springboot interoperability tests.