workflow:
  as: intelliaide-poc
  steps:
    test:
    - ref: intelliaide-poc
  documentation: |-
    Minimal workflow to validate that Intelliaide-compatible Vertex AI
    authentication works in Prow CI using sa-claude-openshift-ci.
