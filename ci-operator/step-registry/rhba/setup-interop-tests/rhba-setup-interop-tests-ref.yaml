ref:
  as: rhba-setup-interop-tests
  from: htpasswd-cli
  commands: rhba-setup-interop-tests-commands.sh
  timeout: 120m0s
  resources:
    requests:
      cpu: 300m
      memory: 2Gi
  documentation: |-
    Setup the environment, create a cluster admin for RHBA interop tests.