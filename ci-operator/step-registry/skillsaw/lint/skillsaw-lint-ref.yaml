ref:
  as: skillsaw-lint
  from_image:
    namespace: ci
    name: skillsaw
    tag: 0.20.0
  commands: skillsaw-lint-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 200Mi
  documentation: |-
    Run Skillsaw and publish its HTML report as an artifact.
