ref:
  as: validatedpatterns-build-docs
  commands: validatedpatterns-build-docs-commands.sh
  credentials:
  - mount_path: /tmp/vault/validatedpatterns-docs-netlify
    name: validatedpatterns-docs-netlify
    namespace: test-credentials
  resources:
    requests:
      cpu: 200m
      memory: 200Mi
  from: src
  env:
  - name: NETLIFY_AUTH_TOKEN_VP
    default: ""
  - name: PREVIEW_SITE
    default: ""
  documentation: |-
    Build and deploy validatedpatterns docs to Netlify.
