chain:
  as: mco-test-e2e-ocl-aws
  steps:
    - ref: mco-conf-day2-add-mcoqe-robot-to-pull-secret
    - ref: mco-conf-day2-enable-ocl
    - ref: openshift-e2e-test
  documentation: |-
    This chain enables On-Cluster Layering (OCL) on the master and worker pools
    and then runs the standard e2e test suite to ensure cluster stability.
