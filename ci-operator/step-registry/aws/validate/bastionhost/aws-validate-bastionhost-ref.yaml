ref:
  as: aws-validate-bastionhost
  from_image:
    namespace: ocp
    name: "4.12"
    tag: upi-installer
  commands: aws-validate-bastionhost-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 200Mi
  documentation: |-
    Validates SSH access and basic functionality of an AWS bastion host.

    Verifies that the bastion host is accessible via SSH and can execute basic commands.
    This ref should only be called from workflows that provision a bastion host.
