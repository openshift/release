ref:
  as: aws-provision-disconnectedusers
  from: upi-installer
  cli: latest
  grace_period: 10m
  commands: aws-provision-disconnectedusers-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    Create static AWS IAM users for disconnected cluster.
