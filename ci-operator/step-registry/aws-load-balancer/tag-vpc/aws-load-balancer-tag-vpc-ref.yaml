ref:
  as: aws-load-balancer-tag-vpc
  from_image:
    namespace: ocp
    name: "4.12"
    tag: upi-installer
  grace_period: 10m
  commands: aws-load-balancer-tag-vpc-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    Tags the shared VPC created by the aws pre proxy step.
