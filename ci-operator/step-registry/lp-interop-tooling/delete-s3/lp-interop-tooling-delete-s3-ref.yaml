ref:
  as: lp-interop-tooling-delete-s3
  from: aws-console
  commands: lp-interop-tooling-delete-s3-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 500Mi
  env:
  - name: BUCKET_NAME
    documentation: The name of the AWS S3 bucket to delete
    default: "interopoadp"
  documentation: |-
    Destroys an AWS S3 bucket