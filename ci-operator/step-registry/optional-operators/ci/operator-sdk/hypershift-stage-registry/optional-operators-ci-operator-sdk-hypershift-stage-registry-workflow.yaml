workflow:
  as: optional-operators-ci-operator-sdk-hypershift-stage-registry
  steps:
    pre:
    - ref: merge-stage-registry-credentials-for-hypershift
    - ref: openshift-cluster-bot-rbac
    - ref: ipi-install-rbac
    - chain: hypershift-hostedcluster-create
    - ref: optional-operators-operator-sdk
    post:
    - chain: hypershift-hostedcluster-dump
    - chain: hypershift-hostedcluster-destroy
  documentation: |-
    Like optional-operators-ci-operator-sdk-hypershift, but merges
    registry.stage.redhat.io credentials into the HostedCluster pull secret
    before cluster creation.
