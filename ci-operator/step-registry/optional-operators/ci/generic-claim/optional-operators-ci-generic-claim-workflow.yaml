workflow:
  as: optional-operators-ci-generic-claim
  steps:
    pre:
    - ref: ipi-install-rbac
    - ref: optional-operators-subscribe
    post:
    - chain: gather
  documentation: |-
    Use an generic cluster claim and installs an
    optional operator using the built index image that refers to build bundle
    images.
