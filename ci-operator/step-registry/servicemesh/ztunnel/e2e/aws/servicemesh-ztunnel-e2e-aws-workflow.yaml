workflow:
  as: servicemesh-ztunnel-e2e-aws
  steps:
    pre:
    - chain: ipi-aws-pre
    - ref: servicemesh-ztunnel-e2e
    post:
    - chain: ipi-deprovision
    env:
      BASE_DOMAIN: "servicemesh.devcluster.openshift.com"
      MAISTRA_BUILDER_IMAGE: "" # value defined in config/maistra/ztunnel tests steps env
  documentation: |-
    The servicemesh-ztunnel-e2e-aws test workflow provides pre- and post- steps that create and
    clean an OpenShift cluster test namespace with a privileged configuration on AWS,
    allowing job authors to inject their own end-to-end test logic.
