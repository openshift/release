ref:
  as: servicemesh-kiali-cypress-tests-execute
  from: kiali-cypress-tests-runner
  commands: servicemesh-kiali-cypress-tests-execute-commands.sh
  grace_period: 10m
  resources:
    requests:
      cpu: '1'
      memory: 1Gi
  env:
  - name: ISTIO_NAMESPACE
    default: "istio-system"
    documentation: |-
      Namespace where the SMCP/Istio is installed.
  - name: ISTIO_SAMPLE_APP_VERSION
    default: "v1.24.5"
    documentation: |-
      Version of istio samples (istio upstream branch tag) to download for demo apps
  - name: MAP_TESTS
    default: "false"
    documentation: |-
      When true, updates the test suite name for reporting tools.
  - name: DR__RP__CR_COMP_NAME
    default: lp-ocp-compat--ServiceMesh
    documentation: |-
      ReportPortal component name used when MAP_TESTS is enabled.
  documentation: |-
    Executes kiali cypress tests.
