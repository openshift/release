ref:
  as: aro-e2e-config-secrets
  from_image:
    namespace: ocp
    name: "4.18"
    tag: upi-installer
  commands: aro-e2e-config-secrets-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    Download secrets and store them in SHARED_DIR
