workflow:
  as: openshift-hyperfleet-e2e
  steps:
    pre:
      - ref: openshift-hyperfleet-e2e-setup
    test:
      - ref: openshift-hyperfleet-e2e-test
    post:
      - ref: openshift-hyperfleet-e2e-cleanup-cluster-resources
      - ref: openshift-hyperfleet-e2e-cleanup-cloud-provider
  documentation: |-
    This workflow will deploy Hyperfleet platform and then call hyperfleet E2E tests.After finish testing, all the prepared resources will be cleaned up.
