workflow:
  as: kubevirt-libvirt-vpn
  steps:
    # #NEED?
    allow_best_effort_post_steps: true
    test:
    - ref: kubevirt-libvirt-vpn
    # dnsConfig removed
  documentation: |-
    Minimal workflow to validate VPN connectivity to a s390x orange-zone
    libvirt host using cluster profile libvirt-s390x-vpn-virt
    Curl-only smoke (external_ip + internal_ip :7001)
