workflow:
  as: kubevirt-libvirt-vpn
  steps:
    # #NEED?
    allow_best_effort_post_steps: true
    test:
    - ref: kubevirt-libvirt-vpn
    # #CORRECT? Nameservers copied from #72780 (10.0.0.1 / 9.9.9.9).
    # may differ or be unnecessary?
    dnsConfig:
      nameservers:
      - 10.0.0.1
      - 9.9.9.9
  documentation: |-
    Minimal workflow to validate VPN connectivity to the SSP s390x orange-zone
    libvirt hosts (kubevirt06/kubevirt07) using cluster profile
    libvirt-s390x-vpn-virt. Pattern follows openshift-e2e-libvirt-vpn from
    multiarch PR #72780, under kubevirt OWNERS.
