chain:
  as: sandboxed-containers-operator-experimental-cluster-preparation
  steps:
    - ref: sandboxed-containers-operator-experimental-cluster-preparation-install-argocd
  documentation: |-
    Sets up the Kubernetes cluster for OSC testing.
    Includes ArgoCD installation for declarative deployment.
    Any other cluster-level setup should be added here. 
