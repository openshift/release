workflow:
  as: sandboxed-containers-operator-bm-coco-sno-e2e
  steps:
    env:
      TEST_PARALLEL: 1
    pre:
    - ref: sandboxed-containers-operator-bm-sno-setup
    - ref: sandboxed-containers-operator-bm-coco-setup
    post:
    - ref: sandboxed-containers-operator-gather-must-gather
    test:
    - ref: openshift-extended-test
  documentation: |-
    This workflow run openshift-extented-test on a bare metal SNO cluster with
    confidential containers deployed.
