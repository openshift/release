ref:
  as: gcp-provision-public-dns-zone-api-records
  from_image:
    namespace: ocp
    name: "4.15"
    tag: upi-installer
  grace_period: 10m
  commands: gcp-provision-public-dns-zone-api-records-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  env:
  - name: BASE_DOMAIN
    default: ""
    documentation: The base domain.
  - name: BASE_DOMAIN_ZONE_NAME
    default: ""
    documentation: The public DNS zone name.
  - name: BASE_DOMAIN_ZONE_PROJECT
    default: ""
    documentation: The GCP project name, which the public DNS zone belongs to.
  documentation: |-
    The step adds "api.<cluster name>.<base domain>." DNS records in public DNS zone.
