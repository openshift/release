ref:
  as: gcp-deprovision-iam-creds-xpn-roles
  from_image:
    namespace: ocp
    name: "4.15"
    tag: upi-installer
  commands: gcp-deprovision-iam-creds-xpn-roles-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    The GCP step removes the roles/permissions from the IAM service accounts in the GCP host project.
