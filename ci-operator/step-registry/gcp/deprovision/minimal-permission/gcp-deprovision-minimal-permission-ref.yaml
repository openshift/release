ref:
  as: gcp-deprovision-minimal-permission
  from_image:
    namespace: ocp
    name: "4.15"
    tag: upi-installer
  commands: gcp-deprovision-minimal-permission-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    The step destroys all resources configured by "gcp-provision-minimal-permission". 
