ref:
  as: gcp-deprovision-public-dns-zone-record-sets
  from_image:
    namespace: ocp
    name: "4.18"
    tag: upi-installer
  grace_period: 10m
  commands: gcp-deprovision-public-dns-zone-record-sets-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    The step will run record-sets-destroy.sh script to destroy record sets.
