ref:
  as: gcp-deprovision-public-dns-zone-api-records
  from_image:
    namespace: ocp
    name: "4.15"
    tag: upi-installer
  commands: gcp-deprovision-public-dns-zone-api-records-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    The step destroys all resources configured by "gcp-provision-public-dns-zone-api-records". 
