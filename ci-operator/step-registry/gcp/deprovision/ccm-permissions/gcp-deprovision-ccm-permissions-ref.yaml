ref:
  as: gcp-deprovision-ccm-permissions
  from_image:
    namespace: ocp
    name: "4.20"
    tag: upi-installer
  commands: gcp-deprovision-ccm-permissions-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    The GCP step removes the permissions for CCM service account.
