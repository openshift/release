ref:
  as: gcp-deprovision-dns-peering-zone-sa
  from_image:
    namespace: ocp
    name: "4.15"
    tag: upi-installer
  commands: gcp-deprovision-dns-peering-zone-sa-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    The GCP step removed the temporarily granted roles from the DNS peering zone service account. 
