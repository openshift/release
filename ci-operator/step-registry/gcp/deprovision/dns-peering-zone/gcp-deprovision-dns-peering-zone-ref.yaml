ref:
  as: gcp-deprovision-dns-peering-zone
  from_image:
    namespace: ocp
    name: "4.15"
    tag: upi-installer
  commands: gcp-deprovision-dns-peering-zone-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    The GCP step deletes the DNS peering zone. 
