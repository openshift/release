ref:
  as: gcp-deprovision-machine-api-controller-permissions
  from_image:
    namespace: ocp
    name: "4.20"
    tag: upi-installer
  commands: gcp-deprovision-machine-api-controller-permissions-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    The GCP step removes the permissions for machine-api-controller service account.
