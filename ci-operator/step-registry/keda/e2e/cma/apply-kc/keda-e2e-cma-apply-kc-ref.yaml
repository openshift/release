ref:
  as: keda-e2e-cma-apply-kc
  cli: latest
  commands: keda-e2e-cma-apply-kc-commands.sh
  from: src
  resources:
    requests:
      cpu: 100m
  documentation: |-
    Applies the KedaController custom resource and waits for all KEDA
    deployments (admission, metrics-apiserver, operator) to become
    available.
