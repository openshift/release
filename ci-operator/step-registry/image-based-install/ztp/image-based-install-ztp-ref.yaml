ref:
  as: image-based-install-ztp
  from: src
  commands: image-based-install-ztp-commands.sh
  resources:
    requests:
      cpu: 1000m
      memory: 500Mi
  documentation: |-
    Finish installing a host using ibio.
