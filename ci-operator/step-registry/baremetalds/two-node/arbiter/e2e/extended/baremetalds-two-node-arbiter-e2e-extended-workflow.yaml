workflow:
  as: baremetalds-two-node-arbiter-e2e-extended
  steps:
    pre:
    - chain: baremetalds-ofcir-pre
    test:
    - ref: openshift-extended-test
    post:
    - chain: baremetalds-ofcir-post
    env:
      CLUSTERTYPE: baremetal
      DEVSCRIPTS_CONFIG: |
        IP_STACK=v4
        NUM_MASTERS=2
        NUM_WORKERS=0
        ENABLE_ARBITER=true
        FEATURE_SET="TechPreviewNoUpgrade"
      TEST_SCENARIOS: "MCO|ETCD|Cluster_Infrastructure|INSTALLER_GENERAL|Authentication|Workloads"
      TEST_IMPORTANCE: "Critical"
      FORCE_SUCCESS_EXIT: "no"
  documentation: |-
    The Baremetalds arbiter E2E extended workflow executes `critical cases for components mco, etcd, authentication` on  two node arbiter that was installed during cluster setup.

