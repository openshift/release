ref:
  as: baremetalds-assisted-setup
  from: dev-scripts
  commands: baremetalds-assisted-setup-commands.sh
  resources:
    requests:
      cpu: 1000m
      memory: 500Mi
  documentation: |-
    Checkout and execute dev-scripts on a packet server with assisted installer
