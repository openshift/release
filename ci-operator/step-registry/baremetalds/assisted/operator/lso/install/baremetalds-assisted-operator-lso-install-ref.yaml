ref:
  as: baremetalds-assisted-operator-lso-install
  from: assisted-service-scripts
  commands: baremetalds-assisted-operator-lso-install-commands.sh
  resources:
    requests:
      cpu: 1000m
      memory: 500Mi
  env:
  - name: DISCONNECTED
    default: "false"
    documentation: Should we work on top of a disconnected environment
  documentation: |-
    Installing Local Storage Operator and creating local volumes.
