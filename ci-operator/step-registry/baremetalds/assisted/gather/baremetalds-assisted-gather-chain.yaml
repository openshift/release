chain:
  as: baremetalds-assisted-gather
  steps:
    - ref: baremetalds-assisted-gather
    - chain: gather
    - ref: baremetalds-packet-teardown
  documentation: |-
    Steps for gathering and teardown after an assisted-installer cluster was installed on a packet server.
