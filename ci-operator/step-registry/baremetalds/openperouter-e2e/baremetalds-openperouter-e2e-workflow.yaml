workflow:
  as: baremetalds-openperouter-e2e
  steps:
    pre:
      - chain: baremetalds-ofcir-pre
    test:
      - ref: baremetalds-openperouter-e2e-test
    post:
      - chain: baremetalds-ofcir-post
  documentation: |-
    Run OpenPerOuter E2E tests on a baremetal OCP cluster deployed via dev-scripts
    with extra networks for the EVPN/SRV6 fabric topology.
