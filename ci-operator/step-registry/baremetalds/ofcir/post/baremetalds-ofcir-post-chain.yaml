chain:
  as: baremetalds-ofcir-post
  steps:
  - ref: baremetalds-devscripts-gather
  - chain: gather
  - ref: ofcir-release
  documentation: |-
    The Baremetalds IPI cleanup chain contains all steps that gather and release an OpenShift cluster on Baremetal, provisioned by the `ofcir-pre` chain.
