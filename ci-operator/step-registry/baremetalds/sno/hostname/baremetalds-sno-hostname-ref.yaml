ref:
  as: baremetalds-sno-hostname
  from: assisted-test-infra
  commands: baremetalds-sno-hostname-commands.sh
  resources:
    requests:
      cpu: 1000m
      memory: 500Mi
  documentation: |-
    Change single-node OpenShift node hostname and wait for cluster reconciliation.
