chain:
  as: baremetalds-ipi-test
  steps:
  - ref: wait
  - ref: baremetalds-e2e-test
  documentation: |-
    The Baremetald IPI test chain encapsulates the baremetalds-e2e-test refs and any future steps.
