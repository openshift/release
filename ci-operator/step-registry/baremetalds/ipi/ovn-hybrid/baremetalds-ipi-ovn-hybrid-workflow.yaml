workflow:
  as: baremetalds-ipi-ovn-hybrid
  steps:
    allow_skip_on_success: true
    pre:
    - ref: baremetalds-packet-setup
    - ref: ipi-install-rbac
    - ref: ovn-conf
    - ref: ovn-conf-hybrid-manifest
    - ref: baremetalds-devscripts-setup
    post:
    - ref: baremetalds-devscripts-gather
    - ref: baremetalds-packet-teardown
  documentation: |-
    This workflow provisions an OpenShift cluster with hybrid OVN networking on bare metal.
