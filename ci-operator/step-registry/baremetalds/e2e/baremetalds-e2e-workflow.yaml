workflow:
  as: baremetalds-e2e
  steps:
    pre:
    - ref: baremetalds-nss-wrapper-hack
#    - ref: baremetalds-setup
#    test:
#    - ref: baremetalds-e2e-test
    post:
    - ref: baremetalds-teardown
  documentation: |-
    The Baremetal DS E2E workflow executes the common end-to-end test suite on a cluster provisioned by running dev-scripts on a packet server.
