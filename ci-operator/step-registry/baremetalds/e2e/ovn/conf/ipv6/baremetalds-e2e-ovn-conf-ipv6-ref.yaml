ref:
  as: baremetalds-e2e-ovn-conf-ipv6
  from: tests
  commands: baremetalds-e2e-ovn-conf-ipv6-commands.sh
  timeout: 600s
  resources:
    requests:
      cpu: "1"
      memory: 100Mi
  documentation: |-
      Uses steps found in published docs [0] to configure cluster to use IPv6. Uses the oc patch cluster command
      on network.config.openshift.io.
      [0] https://docs.openshift.com/container-platform/4.10/networking/ovn_kubernetes_network_provider/converting-to-dual-stack.html

