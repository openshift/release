ref:
  as: baremetalds-devscripts-conf-dualstack
  from: baremetal-installer
  commands: baremetalds-devscripts-conf-dualstack-commands.sh
  resources:
    requests:
      cpu: 1000m
      memory: 500Mi
  documentation: |-
    Enrich the default dev-scripts configuration with the dual stack (IPv4 + IPv6) support.
