ref:
  as: baremetalds-setup
  from: stable:upi-installer
  commands: baremetalds-setup-commands.sh
  resources:
    requests:
      cpu: 1000m
      memory: 500Mi
  documentation: |-
    This step performs the setup of the packet server that will host and execute dev-scripts
