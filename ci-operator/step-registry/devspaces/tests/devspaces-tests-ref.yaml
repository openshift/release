ref:
  as: devspaces-tests
  from: devspaces-runner
  commands: devspaces-tests-commands.sh
  resources:
    requests:
      cpu: "1"
      memory: 2Gi
  documentation: |-
    Execute the devspaces-tests using the provided arguments. All XML results will be combined into "$SHARED_DIR/".