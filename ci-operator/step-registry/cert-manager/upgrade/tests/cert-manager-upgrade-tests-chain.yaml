chain:
  as: cert-manager-upgrade-tests
  steps:
    - ref: cert-manager-install
    - ref: cert-manager-upgrade
  documentation: |-
    Perform upgrade tests for the OpenShift cert-manager Operator.
