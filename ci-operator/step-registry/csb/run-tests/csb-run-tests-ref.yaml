ref:
  as: csb-run-tests
  from_image:
    namespace: ocp
    name: cli-jq
    tag: latest
  commands: csb-run-tests-commands.sh
  grace_period: 10m
  resources:
    requests:
      cpu: 100m
      memory: 1000Mi
  documentation: |-
    This ref step is used for running the tests of CSB product.

