chain:
  as: quay-tests-quay-e2e-test
  steps:
  - chain: quay-tests-deploy-quay-odf
  - ref: quay-tests-test-quay-e2e
  documentation: |-
        This is Quay E2E test Chain including deploy quay testing env and execute quay E2E testing cases
        