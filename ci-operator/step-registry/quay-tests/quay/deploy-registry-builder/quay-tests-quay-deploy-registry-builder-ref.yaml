ref:
  as: quay-tests-quay-deploy-registry-builder
  cli: latest
  from_image:
    name: quay-test-console
    namespace: ci
    tag: latest
  commands: quay-tests-quay-deploy-registry-builder-commands.sh
  timeout: 32400s
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  credentials:
  - collection: quay-tests
    group: aws
    mount_path: /var/run/quay-qe-aws-secret
  - collection: quay-tests
    group: quay
    mount_path: /var/run/quay-qe-quay-secret
  documentation: |-
    Deploy Quay registry with unmanged tls component
  env:
  - name: TLS
    documentation: The quay registry tls comoponent managed status - "false" or "true"
    default: "false"
  - name: QUAYREGISTRY
    documentation: The quay registry name
    default: "quay"
  - name: QUAYNAMESPACE
    documentation: The Quay installed namespace
    default: "quay-enterprise"    