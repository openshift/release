ref:
  as: quay-tests-recycle-quay-gcp
  cli: latest
  from_image:
    name: quay-test-console
    namespace: ci
    tag: latest
  commands: quay-tests-recycle-quay-gcp-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  credentials:
  - namespace: test-credentials
    name: quay-qe-gcp-secret
    mount_path: /var/run/quay-qe-gcp-secret
  documentation: |-
    Recycle Quay GCP Bucket
