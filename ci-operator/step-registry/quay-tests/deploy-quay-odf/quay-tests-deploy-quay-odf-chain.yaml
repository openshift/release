chain:
  as: quay-tests-deploy-quay-odf
  steps:
  - ref: quay-tests-install-odf-noobaa
  - ref: quay-tests-install-quay-on-odf
  documentation: |-
    This is Quay provisioning chain including deploy odf operator with noobaa and quay operator & registry
        