chain:
  as: quay-tests-deploy-quay-odf
  steps:
  - ref: quay-tests-install-quay-on-odf
  - ref: quay-tests-install-odf-noobaa
  documentation: |-
    This is Quay provisioning chain including deploy quay operator, quay registry and odf operator with noobaa
        