ref:
  as: quay-tests-deploy-quay-netapp-storagegrid
  cli: latest
  from_image:
    name: quay-test-console
    namespace: ci
    tag: latest
  commands: quay-tests-deploy-quay-netapp-storagegrid-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  credentials:
  - collection: quay-tests
    group: netapp
    mount_path: /var/run/quay-qe-netapp-secret
  documentation: |-
    Deploy Quay Operator and Quay with Netapp StorageGRID
  env:
  - name: QUAY_OPERATOR_CHANNEL
    documentation: The quay operator channel
    default: "stable-3.10"
  - name: QUAY_OPERATOR_SOURCE
    documentation: The quay operator source
    default: "redhat-operators"