ref:
  as: quay-tests-deploy-quay-aws-rds-postgresql
  cli: latest
  from_image:
    name: quay-test-console
    namespace: ci
    tag: latest
  commands: quay-tests-deploy-quay-aws-rds-postgresql-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  credentials:
  - collection: quay-tests
    group: aws
    mount_path: /var/run/quay-qe-aws-secret
  - collection: quay-tests
    group: aws_rds_postgresql
    mount_path: /var/run/quay-qe-aws-rds-postgresql-secret
  documentation: |-
    Deploy Quay Operator and Quay
  env:
  - name: QUAY_OPERATOR_CHANNEL
    documentation: The quay operator channel
    default: "stable-3.11"
  - name: QUAY_OPERATOR_SOURCE
    documentation: The quay operator source
    default: "redhat-operators"
  - name: POSTGRESQL_VERSION
    documentation: Support Postgresql Version 16/15/14/13
    default: "16"