chain:
  as: jobset-install
  steps:
  - ref: jobset-install-catalog
  - ref: jobset-install-operator
  documentation: |-
    Install the jobset operator from a catalog source (supports both default and BYO catalogs).