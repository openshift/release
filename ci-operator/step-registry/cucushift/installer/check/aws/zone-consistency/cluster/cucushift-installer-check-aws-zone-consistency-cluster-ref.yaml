ref:
  as: cucushift-installer-check-aws-zone-consistency-cluster
  from_image:
    namespace: ocp
    name: "4.16"
    tag: upi-installer
  grace_period: 10m
  commands: cucushift-installer-check-aws-zone-consistency-cluster-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: >-
    Verify control plane machine zone consistency in installed cluster (OCPBUGS-69923).
    This step verifies that actual cluster machines have consistent zone information
    (zone label, providerID zone, and spec zone) after cluster installation is complete.
