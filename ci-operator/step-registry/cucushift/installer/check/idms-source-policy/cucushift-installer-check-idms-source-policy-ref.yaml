ref:
  as: cucushift-installer-check-idms-source-policy
  from_image:
    namespace: ocp
    name: "4.14"
    tag: upi-installer
  grace_period: 10m
  commands: cucushift-installer-check-idms-source-policy-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: >-
    The step checks that the sourcePolicy configured in imageDigestSources in install-config.yaml
    is correctly applied to ImageDigestMirrorSet resources in the cluster.
