ref:
  as: cucushift-installer-check-fips
  from_image:
   namespace: ocp
   name: "4.14"
   tag: upi-installer
  grace_period: 10m
  commands: cucushift-installer-check-fips-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: >-
    The step checks that if fips is enabled on each node when configuring fips in install-config.yaml
