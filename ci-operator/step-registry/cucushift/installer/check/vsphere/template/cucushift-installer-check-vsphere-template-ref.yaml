ref:
  as: cucushift-installer-check-vsphere-template
  from_image:
   namespace: ocp
   name: "4.14"
   tag: upi-installer
  commands: cucushift-installer-check-vsphere-template-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    Check the rhcos template used in vsphere was same as specified in install-config.
