ref:
  as: cucushift-installer-check-trust-policy
  from_image:
    namespace: ocp
    name: "4.14"
    tag: upi-installer
  commands: cucushift-installer-check-trust-policy-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  env:
  documentation: >-
    The step is to check the ca trust policy configured in install-config works well.
