ref:
  as: cucushift-installer-check-scaleup
  from_image:
   namespace: ocp
   name: "4.16"
   tag: upi-installer
  commands: cucushift-installer-check-scaleup-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    Scale up a new worker node via machine API
