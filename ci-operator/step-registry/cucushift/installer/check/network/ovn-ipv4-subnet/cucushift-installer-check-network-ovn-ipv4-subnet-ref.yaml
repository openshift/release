ref:
  as: cucushift-installer-check-network-ovn-ipv4-subnet
  from_image:
   namespace: ocp
   name: "4.18"
   tag: upi-installer
  commands: cucushift-installer-check-network-ovn-ipv4-subnet-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    The step checks networking.ovnKubernetesConfig.ipv4.internalJoinSubnet setting
    correctly in cluster.
