workflow:
  as: cucushift-installer-rehearse-aws-ipi-sno
  steps:
    pre:
    - chain: ipi-conf-aws
    - ref: single-node-conf-aws
    - chain: ipi-install
    test:
    - ref: cucushift-installer-rehearse-aws-ipi-sno-conf
    - ref: cucushift-installer-rehearse-aws-ipi-sno-test
    post:
    - chain: ipi-aws-post
  documentation: |-
    Installs a cluster with an SNO configuration on AWS and verify the default configuration the installer uses when the control-plane is a single node.
