workflow:
  as: cucushift-installer-rehearse-aws-ipi-rootvolume
  steps:
    pre:
      - chain: cucushift-installer-rehearse-aws-ipi-rootvolume-provision
      - ref: cucushift-installer-reportportal-marker
    post:
      - chain: cucushift-installer-rehearse-aws-ipi-deprovision
      - ref: send-results-to-reportportal
  documentation: |-
    This is the workflow to trigger Prow's rehearsal test for AWS root volume
    configuration tests. It uses a custom provision chain that includes the
    root volume configuration step.
