workflow:
  as: cucushift-installer-rehearse-gcp-upi-private
  steps: 
    pre:
    - chain: upi-gcp-pre
    post:
    - chain: upi-gcp-post
    env:
      DISCONNECTED_NETWORK: "no"
      PUBLISH_STRATEGY: "Internal"
  documentation: |-
    This is the workflow to trigger Prow's rehearsal test when submitting installer steps/chain/workflow
