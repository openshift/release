workflow:
  as: cucushift-installer-rehearse-gcp-upi
  steps: 
    pre:
    - chain: upi-gcp-pre
    post:
    - chain: upi-gcp-post
    env:
      CREATE_SSH_BASTION: "no"
  documentation: |-
    This is the workflow to trigger Prow's rehearsal test when submitting installer steps/chain/workflow
