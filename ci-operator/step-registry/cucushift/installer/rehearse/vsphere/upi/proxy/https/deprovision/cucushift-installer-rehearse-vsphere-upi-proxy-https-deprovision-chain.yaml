chain:
  as: cucushift-installer-rehearse-vsphere-upi-proxy-https-deprovision
  steps:
  - chain: gather-core-dump
  - chain: gather
  - ref: upi-deprovision-vsphere
  documentation: |-
    Destroy an UPI cluster behind https proxy in a disconnected network on vSphere
