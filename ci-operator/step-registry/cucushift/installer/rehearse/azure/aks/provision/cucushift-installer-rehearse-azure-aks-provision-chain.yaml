chain:
  as: cucushift-installer-rehearse-azure-aks-provision
  steps:
  - ref: ipi-install-rbac
  - ref: azure-provision-resourcegroup
  - ref: aks-provision
  documentation: |-
    This chain provisions an aks cluster. 
