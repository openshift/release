workflow:
  as: cucushift-installer-rehearse-azure-hypershift
  steps:
    pre:
      - chain: cucushift-installer-rehearse-azure-hypershift-provision
      - ref: cucushift-installer-reportportal-marker
    post:
      - chain: cucushift-installer-rehearse-azure-hypershift-deprovision
      - ref: send-results-to-reportportal
  documentation: |-
    Installs the Hypershift Operator and creates a hosted cluster in azure cloud provider, do the test for hcp mgmt cluster, then destroys hosted clsuter and mgmt cluster.
