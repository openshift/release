ref:
  as: cucushift-hypershift-extended-registry-overrides-health-check
  from: tools
  grace_period: 2m
  timeout: 10m0s
  commands: cucushift-hypershift-extended-registry-overrides-health-check-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 100Mi
  documentation: |-
    Performs health checks on a hosted cluster with registry overrides. 
    Assumes that KUBECONFIG points to the management cluster.
