chain:
  as: openshift-qe-egress-ip-bastion
  steps:
  - ref: openshift-qe-egress-ip-bastion-echo-deploy
  - ref: openshift-qe-egress-ip-setup
  - ref: openshift-qe-egress-ip-tests
  documentation: |-
    Execute egress IP tests with external bastion-hosted validation service.
    This chain includes:
    1. Deploy external IP echo service on bastion host
    2. Configure egress IP resources with proper namespace selectors
    3. Run egress IP validation tests using external service validation