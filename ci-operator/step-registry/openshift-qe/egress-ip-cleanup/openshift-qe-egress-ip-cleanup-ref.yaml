ref:
  as: openshift-qe-egress-ip-cleanup
  from_image:
    namespace: ocp
    name: "4.18"
    tag: cli
  commands: openshift-qe-egress-ip-cleanup-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    Clean up egress IP test resources and configuration.
    Removes EgressIP custom resources, test namespaces, and node labels.