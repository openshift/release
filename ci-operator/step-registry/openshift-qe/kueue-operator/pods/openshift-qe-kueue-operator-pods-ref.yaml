ref:
  as: openshift-qe-kueue-operator-pods
  from_image:
    namespace: ci
    name: ocp-qe-perfscale-ci
    tag: latest
  cli: latest
  credentials:
  - namespace: test-credentials
    name: ocp-qe-perfscale-es
    mount_path: /secret
  resources:
    requests:
      cpu: 100m
      memory: 100Mi
  env:
    - name: WORKLOAD
      default: kueue-operator-pods
    - name: EXTRA_FLAGS
      default: "--pod-replicas=2000 --workload-runtime=10s"
    - name: E2E_VERSION
      default: "v2.6.4"
      documentation: |-
        Override the e2e version
    - name: KUBE_BURNER_VERSION
      default: "1.7.5"
      documentation: |-
        Override the kube burner version
    - name: QPS
      default: "5"
      documentation: |-
       QPS for kube-burner
    - name: BURST
      default: "5"
      documentation: |-
        Burst for kube-burner
  commands: openshift-qe-kueue-operator-pods-commands.sh
  documentation: |-
     This chain executes kueue operator workload for jobs-shared using kube-burner ocp wrapper
