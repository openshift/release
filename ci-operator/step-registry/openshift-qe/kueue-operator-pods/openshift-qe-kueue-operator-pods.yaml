chain:
  as: openshift-qe-kueue-operator-pods
  env:
    WORKLOAD_TYPE: pods
  steps:
  - ref: openshift-qe-kueue-operator
  documentation: |-
     This chain executes kueue operator workload for pods using kube-burner ocp wrapper
