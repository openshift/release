workflow:
  as: openshift-qe-network-vm-bridge-perf
  steps:
    test:
      - chain: openshift-qe-network-vm-bridge-perf
  documentation: |-
    This workflow executes network VM bridge perf workloads.
