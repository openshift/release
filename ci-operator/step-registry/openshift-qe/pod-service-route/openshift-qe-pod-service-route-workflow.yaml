
workflow:
  as: openshift-qe-pod-service-route
  steps:
    env:
      OUTPUT_FILE: kube-burner-ocp.out
    test:
      - ref: openshift-qe-pod-service-route
      - ref: openshift-qe-write-perfscale-results
  documentation: |-
    This workflow executes pod service route workload.
