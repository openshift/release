ref:
  as: openshift-qe-build-kube-burner-test
  from: cli
  commands: openshift-qe-build-kube-burner-test-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
  documentation: >-
    Temporary step to validate kube-burner-ocp builds from fork.
    Remove after confirming the build works in CI.
