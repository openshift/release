ref:
  as: openshift-qe-build-kube-burner-test
  from: cli
  commands: openshift-qe-build-kube-burner-test-commands.sh
  resources:
    requests:
      cpu: "2"
      memory: 2Gi
  documentation: >-
    Temporary build-validation step for PERFSCALE-5323 investigation.
    Validates that Go 1.25.9 install + kube-burner-ocp fork build works
    in the CI container before provisioning a cluster.
