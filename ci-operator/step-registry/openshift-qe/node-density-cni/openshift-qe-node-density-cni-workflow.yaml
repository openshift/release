
workflow:
  as: openshift-qe-node-density-cni
  steps:
    env:
      OUTPUT_FILE: kube-burner-ocp.out
    test:
      - chain: openshift-qe-node-density-cni
  documentation: |-
    This workflow executes node density cni workload.
