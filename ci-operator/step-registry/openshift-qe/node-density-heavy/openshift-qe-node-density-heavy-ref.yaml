ref:
  as: openshift-qe-node-density-heavy
  from_image:
    namespace: ci
    name: ocp-qe-perfscale-ci
    tag: latest
  commands: openshift-qe-node-density-heavy-commands.sh
  credentials:
  - namespace: test-credentials # this entry injects the custom credential
    name: ocp-qe-perfscale-es
    mount_path: /secret
  - namespace: test-credentials
    name: ocp-qe-gcp-sa
    mount_path: /ga-gsheet
  env:
  - name: KUBE_BURNER_VERSION
    default: "1.7.12"
    documentation: |-
      Override the kube burner version
  - name: WORKLOAD
    default: "node-density-heavy"
    documentation: |-
      Workload to select
  - name: GC
    default: "true"
    documentation: |-
      Default is true, which means clean up the pod/resource that kube-burner ocp created, you can set it to false to keep the resouce
  - name: GC_METRICS
    default: "true"
    documentation: |-
      Default is true, which means collect metrics during garbage collection
  - name: PODS_PER_NODE
    default: "100"
    documentation: |-
      Define the maximum number of pods to deploy on each labeled node.  
  - name: NAMESPACED_ITERATIONS
    default: "true"
    documentation: |-
      Run each number of jobs iterations in a distinct namespace. 
  - name: ITERATIONS_PER_NAMESPACE
    default: "1000"
    documentation: |-
      Iterations per namespace. 
  - name: EXTRA_FLAGS
    default: ""
    documentation: |-
      Extra flags that will be appended to the underlying kube-burner ocp command, by default empty. 
  resources:
    requests:
      cpu: 100m
      memory: 100Mi
  documentation: >-
    This step runs the perfscale configmap-scale workload in the deployed cluster
