chain:
  as: openshift-qe-servicemesh
  steps: 
  - ref: servicemesh-sail-operator-deploy-controlplane-sidecar
  - ref: openshift-qe-servicemesh-sidecar
  documentation: |-
     This chain deploys the Sail Operator and the Istio control plane in sidecar mode, and executes the servicemesh sidecar workload
