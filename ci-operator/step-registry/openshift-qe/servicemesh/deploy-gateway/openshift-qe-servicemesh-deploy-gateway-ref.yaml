ref:
  as: openshift-qe-servicemesh-deploy-gateway
  from_image:
    namespace: ci
    name: ocp-qe-perfscale-ci
    tag: latest
  cli: latest
  commands: openshift-qe-servicemesh-deploy-gateway-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 100Mi
  documentation: >-
    This step deploys the gateway for the servicemesh workload in the deployed cluster
  timeout: 30m
