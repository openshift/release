ref:
  as: openshift-qe-servicemesh
  from_image:
    namespace: ci
    name: ocp-qe-perfscale-ci
    tag: latest
  cli: latest
  commands: openshift-qe-servicemesh-commands.sh
  credentials:
  - namespace: test-credentials # this entry injects the custom credential
    name: ocp-qe-perfscale-es
    mount_path: /secret
  resources:
    requests:
      cpu: 100m
      memory: 100Mi
  env:
  - name: E2E_VERSION
    default: "default"
    documentation: |-
      Override the e2e version
  - name: KUBE_BURNER_VERSION
    default: "default"
    documentation: |-
      Override the kube burner version
  - name: SERVICE_MESH
    default: "sidecar"
    documentation: |-
      Define the service mesh to use: sidecar or ambient
  documentation: >-
    This step runs the perfscale servicemesh workload in the deployed cluster
  timeout: 2h30m
