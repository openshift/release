chain:
  as: openshift-qe-kueue-operator-jobs-shared
  env:
    WORKLOAD_TYPE: jobs-shared
    PODS_QUOTA: "400"
  steps:
  - ref: openshift-qe-kueue-operator
  documentation: |-
     This chain executes kueue operator workload for jobs-shared using kube-burner ocp wrapper
