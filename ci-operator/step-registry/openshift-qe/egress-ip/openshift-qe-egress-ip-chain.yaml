chain:
  as: openshift-qe-egress-ip
  steps:
  - ref: openshift-qe-egress-ip-setup
  - ref: openshift-qe-egress-ip-validate
  - ref: openshift-qe-egress-ip-cleanup
  documentation: |-
    Basic egress IP functionality testing chain using Huiran's proven methodology.
    This chain sets up egress IP configuration and validates it using internal
    routing verification instead of external services. Suitable for 3-node clusters.