ref:
  as: openshift-qe-egress-ip-targeted-chaos-node-setup
  from_image:
    namespace: ocp
    name: "4.21"
    tag: cli
  commands: openshift-qe-egress-ip-targeted-chaos-node-setup-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 100Mi
  documentation: >-
    This step configures the environment for targeted node disruption testing.
    It reads the target discovery results and sets up environment variables
    to ensure the subsequent node chaos step targets the specific node that
    is currently hosting the egress IP assignment.