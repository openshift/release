ref:
  as: openshift-qe-egress-ip-validate
  from_image:
    namespace: ocp
    name: "4.18"
    tag: cli
  commands: openshift-qe-egress-ip-validate-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 200Mi
  documentation: |-
    Validate egress IP functionality using internal routing verification.
    This step uses Huiran's proven methodology to test egress IP assignment
    and routing behavior without relying on external services.