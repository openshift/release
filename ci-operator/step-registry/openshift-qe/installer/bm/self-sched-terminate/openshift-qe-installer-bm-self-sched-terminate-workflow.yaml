workflow:
  as:  openshift-qe-installer-bm-self-sched-terminate
  steps:
    test:
    - chain: openshift-qe-installer-bm-self-sched-terminate
  documentation: |-
    Terminates a temporary lab allocation
