
chain:
  as: openshift-qe-cudn-density
  steps:
  - ref: openshift-qe-cudn-density
  - ref: openshift-qe-orion-cudn-density
  documentation: |-
    This chain executes cudn-density workload using kube-burner ocp wrapper and runs Orion for change detection
