chain:
  as: openshift-qe-hypershift-infra
  steps: 
  - ref: openshift-qe-hypershift-infra
  documentation: |-
     Move prometheus monitoring & ingress components to infra node on Hypershift cluster
