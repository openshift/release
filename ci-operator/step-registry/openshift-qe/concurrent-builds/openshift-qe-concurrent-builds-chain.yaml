
chain:
  as: openshift-qe-concurrent-builds
  steps:
  - ref: openshift-qe-concurrent-builds
  - ref: openshift-qe-write-perfscale-results
  env:
  - name: OUTPUT_FILE
    default: "kube-burner-ocp.out"
  documentation: |-
    This workflow executes cluster density v2 workload using kube-burner ocp wrapper