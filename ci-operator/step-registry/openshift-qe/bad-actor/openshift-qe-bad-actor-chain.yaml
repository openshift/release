
chain:
  as: openshift-qe-bad-actor
  steps:
  - ref: openshift-qe-bad-actor
  env:
  - name: OUTPUT_FILE
    default: "kube-burner-ocp.out"
  documentation: |-
    This workflow executes cluster density v2 workload using kube-burner ocp wrapper
