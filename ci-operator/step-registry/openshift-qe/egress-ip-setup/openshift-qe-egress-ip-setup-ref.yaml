ref:
  as: openshift-qe-egress-ip-setup
  from_image:
    namespace: ocp
    name: "4.18"
    tag: cli
  commands: openshift-qe-egress-ip-setup-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    Setup egress IP configuration for testing based on Huiran's proven methodology.
    This step configures EgressIP custom resources and validates proper assignment
    using internal cluster routing validation instead of external services.