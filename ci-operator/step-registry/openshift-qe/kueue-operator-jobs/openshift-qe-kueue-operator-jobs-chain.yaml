chain:
  as: openshift-qe-kueue-operator-jobs
  env:
    WORKLOAD_TYPE: jobs
  steps:
  - ref: openshift-qe-kueue-operator
  documentation: |-
     This chain executes kueue operator workload for jobs using kube-burner ocp wrapper
