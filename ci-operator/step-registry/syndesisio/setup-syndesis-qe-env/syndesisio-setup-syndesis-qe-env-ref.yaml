ref:
  as: syndesisio-setup-syndesis-qe-env
  from: cli
  commands: syndesisio-setup-syndesis-qe-env-commands.sh
  best_effort: true
  timeout: 5m0s
  resources:
    requests:
      cpu: '1'
      memory: 200Mi
  documentation: |-
    Switch to the default project and add the cluster-admin role to the kubeadmin user.