ref:
  as: global-hub-ci-rbac
  from_image:
    name: builder
    namespace: stolostron
    tag: go1.20-linux
  commands: global-hub-ci-rbac-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 100Mi
  documentation: |-
    This step adds necessary privileges to the Prow job's namespace so that
    images can be accessed outside the Prow clusters.
