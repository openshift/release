workflow:
  as: global-hub-ci-rbac
  steps:
    pre:
    - ref: global-hub-ci-rbac
  documentation: |-
    This workflow adds the necessary privileges to the Prow job's namespace so that
    images can be accessed outside the Prow clusters.
