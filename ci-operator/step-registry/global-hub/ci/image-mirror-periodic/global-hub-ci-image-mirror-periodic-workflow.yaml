workflow:
  as: global-hub-ci-image-mirror-periodic
  steps:
    pre:
    - ref: global-hub-ci-rbac
    - ref: global-hub-ci-image-mirror-periodic
  documentation: |-
    This workflow mirrors an image from the CI Registry to a given
    image ref to be used in a perodic Prow job.
