workflow:
  as: global-hub-ci-image-mirror
  steps:
    pre:
    - ref: global-hub-ci-rbac
    - ref: global-hub-ci-image-mirror
  documentation: |-
    This workflow mirrors an image from the CI Registry to a given
    image ref.
