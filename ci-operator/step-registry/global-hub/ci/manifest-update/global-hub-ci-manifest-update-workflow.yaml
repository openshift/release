workflow:
  as: global-hub-ci-manifest-update
  steps:
    pre:
    - ref: global-hub-ci-rbac
    - ref: global-hub-ci-image-mirror
    - ref: global-hub-ci-manifest-update
  documentation: |-
    This workflow mirrors an image from the CI Registry to a given
    image ref and updates the OCM manifest accordingly.
