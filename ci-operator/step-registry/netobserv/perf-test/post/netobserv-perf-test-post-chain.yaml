chain:
  as: netobserv-perf-test-post
  steps:
  - ref: gather-aws-console
  - chain: gather
  - ref: netobserv-perf-test-cleanup
  - ref: ipi-deprovision-deprovision
  - ref: aws-deprovision-users-and-policies
  documentation: |-
    NetObserv perf test steps for must-gather, cleanup and deprovision.
