chain:
  as: netobserv-perf-test-pre-workload
  steps:
  - ref: netobserv-perf-test-deploy
  - ref: netobserv-perf-test-envdata
  documentation: |-
    This chain netobserv perf test set up steps prior to runnning workload.
