observer:
  name: netobserv-perf-test-ingress-perf-observer
  from_image:
    namespace: ci
    name: ocp-qe-perfscale-ci
    tag: latest
  commands: netobserv-perf-test-ingress-perf-test-observer-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 10Mi
  timeout: 9h
  grace_period: 200s
  documentation: |-
        parallel ingress-perf to run as observer
