chain:
  as: netobserv-perf-test-post-workload
  steps:
  - ref: redhat-chaos-cerberus-one-run
  - ref: netobserv-perf-test-metrics-upload
  - ref: netobserv-perf-test-cleanup
  documentation: |-
    This chain netobserv perf test set up steps prior to runnning workload.
