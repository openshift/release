ref:
  as: netobserv-deploy-nfs-provisioner
  from: cli
  commands: netobserv-deploy-nfs-provisioner-commands.sh
  timeout: 1h0m0s
  resources:
    requests:
      cpu: 100m
      memory: 100Mi
  documentation: |-
    Deploys nfs provisioner
