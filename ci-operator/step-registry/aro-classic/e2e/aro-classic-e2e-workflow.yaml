workflow:
  as: aro-classic-e2e
  steps:
    allow_best_effort_post_steps: true
    test:
      - ref: aro-classic-test-persistent
  documentation: |-
    The ARO Classic e2e workflow runs the full end-to-end suite against an existing environment.
