workflow:
  as: openshift-perfscale-e2e-kubelet-density
  steps:
    pre:
    - chain: ipi-conf-aws
    - chain: ipi-install
    test:
    - ref: openshift-perfscale-kubelet-density
    post:
    - chain: ipi-deprovision
  documentation: |-
    The OpenShift E2E kubelet-density workflows executes a minimal scale test that creates 250 pods in each worker node
