ref:
  as: capz-test-check-dependencies
  from: src
  commands: capz-test-check-dependencies-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 200Mi
  credentials:
  - namespace: test-credentials
    name: azure-capz-credentials
    mount_path: /var/run/capz-azure-credentials
  timeout: 10m
  documentation: |-
    Runs CAPZ test suite Phase 01 (check dependencies) to validate
    tool availability, authentication, and naming constraints.
