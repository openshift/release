ref:
  as: capz-test-setup
  from: src
  commands: capz-test-setup-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 200Mi
  timeout: 15m
  documentation: |-
    Runs CAPZ test suite Phase 02 (setup) to clone the
    cluster-api-installer repository and verify required scripts.
