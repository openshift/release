ref:
  as: dpf-hypervisor-deploy-cluster
  commands: dpf-hypervisor-deploy-cluster-commands.sh
  documentation: |-
    Deploy OpenShift cluster with DPF using the automation scripts on the
    hypervisor. This step executes 'make ${DPF_DEPLOY_MAKE_TARGET}' (default
    'all') which performs cluster creation and, for the default target,
    DPF operator installation.
    
    This step:
    1. Executes 'make ${DPF_DEPLOY_MAKE_TARGET}' on the hypervisor
    2. Monitors deployment progress with logs
    3. Waits for cluster to become ready
    4. Validates DPF operator deployment
    5. Copies kubeconfig back to CI environment
    
    The deployment includes:
    - OpenShift cluster creation via Assisted Installer
    - Virtual machine provisioning on hypervisor
    - DPF operator and prerequisites installation
    - HyperShift hosted cluster setup
    - Network configuration for DPUs
    
    Required Vault secrets:
    - private-key: SSH key for hypervisor
    - remote-host: IP address of the hypervisor bastion host

    Expected duration: 4-6 hours depending on cluster size
  env:
  - default: "all"
    documentation: |-
      Make target to run on the hypervisor after 'make clean-all'. DPF jobs
      keep the default ('all'). Set to a thinner target (for example
      'create-base-cluster') when only cluster/VM bring-up is required.
    name: DPF_DEPLOY_MAKE_TARGET
  - default: "main"
    name: OPENSHIFT_DPF_BRANCH
  - default: "60"
    documentation: |-
      Passed through to the hypervisor .env as VERIFY_MAX_RETRIES. Each
      retry sleeps VERIFY_SLEEP_SECONDS (default 30s). Used by
      'make verify-workers' to wait until non-control-plane nodes are Ready.
    name: VERIFY_MAX_RETRIES
  cli: latest
  from: dpf-ci
  resources:
    requests:
      cpu: 500m
      memory: 1Gi
  timeout: 5h0m0s