build_root:
  image_stream_tag:
    name: release
    namespace: openshift
    tag: golang-1.22
releases:
  latest:
    release:
      architecture: amd64
      channel: fast
      version: "4.18"
resources:
  '*':
    limits:
      memory: 4Gi
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: e2e-test
  cluster_claim:
    architecture: amd64
    cloud: aws
    owner: openshift-ci
    product: ocp
    timeout: 1h0m0s
    version: "4.18"
  steps:
    test:
    - ref: insights-onprem-minio-deploy
    - ref: insights-onprem-ros-ocp-backend-e2e-test
    workflow: generic-claim
zz_generated_metadata:
  branch: main
  org: insights-onprem
  repo: ros-ocp-backend
