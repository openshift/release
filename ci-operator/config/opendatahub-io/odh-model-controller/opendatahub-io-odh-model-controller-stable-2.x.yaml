base_images:
  ubi_minimal:
    name: ubi-minimal
    namespace: ocp
    tag: "9"
build_root:
  image_stream_tag:
    name: release
    namespace: openshift
    tag: rhel-9-release-golang-1.24-openshift-4.20
images:
- context_dir: .
  dockerfile_path: Containerfile
  from: ubi_minimal
  to: odh-model-controller
- dockerfile_literal: |-
      FROM registry.access.redhat.com/ubi9/python-311:1

      # Prow expects to be able to check out a repo under /go
      USER 0:0
      WORKDIR /go
      RUN chown 1001:0 .

      USER 1001:0

      # The VENV that is activated by default conflicts with KServe
      # scripts. These variables are set to empty to not activate any
      # VENV by default.
      ENV BASH_ENV="" ENV="" PROMPT_COMMAND=""
  to: odhmc-kserve-tests
promotion:
  to:
  - namespace: opendatahub-io
    tag: stable-2.x
releases:
  latest:
    release:
      architecture: multi
      channel: stable
      version: "4.18"
resources:
  '*':
    limits:
      memory: 4Gi
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: unit
  commands: HOME=/go GOFLAGS=-mod=mod make test
  container:
    from: src
  skip_if_only_changed: ^(?:config|\.github|hack)/|\.md$|^(?:\.gitignore|\.pre-commit-config\.yaml|OWNERS|PROJECT|LICENSE|\.dockerignore|Makefile)$
- always_run: false
  as: e2e-kserve-path-based-routing
  skip_if_only_changed: ^(?:config|\.github|hack)/|\.md$|^(?:\.*/)?(?:\.gitignore|\.pre-commit-config\.yaml|OWNERS|PROJECT|LICENSE|\.dockerignore|Makefile)$
  steps:
    cluster_profile: aws-opendatahub
    workflow: hypershift-hostedcluster-workflow
    env:
      BASE_DOMAIN: openshift-ci-aws.rhaiseng.com
      COMPUTE_NODE_TYPE: m5.2xlarge
      HYPERSHIFT_AWS_REGION: us-west-2
      HYPERSHIFT_NODE_COUNT: "3"
    test:
      - as: kserve-path-based-routing
        cli: latest
        from: odhmc-kserve-tests
        dependencies:
          - env: ODH_MODEL_CONTROLLER_IMAGE
            name: odh-model-controller
        commands: |
          set -euxo pipefail

          echo "ODH_MODEL_CONTROLLER_IMAGE=${ODH_MODEL_CONTROLLER_IMAGE}"

          git clone https://github.com/opendatahub-io/kserve.git /tmp/kserve
          pushd /tmp/kserve
            git fetch --all --tags
            git checkout "${KSERVE_REF:-main}"
            export GITHUB_SHA="${PULL_BASE_SHA:-stable}"
            export ODH_MODEL_CONTROLLER_IMAGE="${ODH_MODEL_CONTROLLER_IMAGE}"

            ./test/scripts/gh-actions/setup-uv.sh || true
            ./test/scripts/openshift-ci/setup-e2e-tests.sh
            ./test/scripts/openshift-ci/run-e2e-tests.sh path_based_routing
          popd
        resources:
          requests: 
            cpu: 100m 
      - as: testlog-gather
        best_effort: true
        cli: latest
        commands: |
          cp -v ${SHARED_DIR}/debuglog-*.log ${SHARED_DIR}/stdout-*.log ${SHARED_DIR}/stderr-*.log "${ARTIFACT_DIR}/" || true
        from: odhmc-kserve-tests
        resources:
          requests:
            cpu: 100m
        timeout: 1m0s
      - as: kserve-must-gather
        best_effort: true
        cli: latest
        commands: |
          oc adm must-gather \
            --image=quay.io/modh/must-gather:rhoai-2.24 \
            --dest-dir "${ARTIFACT_DIR}/gather-kserve" \
            -- "export COMPONENT=kserve; /usr/bin/gather"
        from: odhmc-kserve-tests
        resources:
          requests:
            cpu: 100m 
        timeout: 20m0s
      - as: openshift-must-gather
        best_effort: true
        cli: latest
        commands: oc adm must-gather --dest-dir "${ARTIFACT_DIR}/gather-openshift"
        from: odhmc-kserve-tests
        resources:
          requests: { cpu: 100m }
        timeout: 20m0s
- always_run: false
  as: e2e-kserve-predictor
  skip_if_only_changed: ^(?:config|\.github|hack)/|\.md$|^(?:\.*/)?(?:\.gitignore|\.pre-commit-config\.yaml|OWNERS|PROJECT|LICENSE|\.dockerignore|Makefile)$
  steps:
    cluster_profile: aws-opendatahub
    workflow: hypershift-hostedcluster-workflow
    env:
      BASE_DOMAIN: openshift-ci-aws.rhaiseng.com
      COMPUTE_NODE_TYPE: m5.2xlarge
      HYPERSHIFT_AWS_REGION: us-west-2
      HYPERSHIFT_NODE_COUNT: "3"
    test:
      - as: kserve-predictor
        cli: latest
        from: odhmc-kserve-tests
        dependencies:
          - env: ODH_MODEL_CONTROLLER_IMAGE
            name: odh-model-controller
        commands: |
          set -euxo pipefail
          echo "ODH_MODEL_CONTROLLER_IMAGE=${ODH_MODEL_CONTROLLER_IMAGE}"

          git clone https://github.com/opendatahub-io/kserve.git /tmp/kserve
          pushd /tmp/kserve
            git fetch --all --tags
            git checkout "${KSERVE_REF:-main}"
            export GITHUB_SHA="${PULL_BASE_SHA:-stable}"
            export ODH_MODEL_CONTROLLER_IMAGE="${ODH_MODEL_CONTROLLER_IMAGE}"
            
            ./test/scripts/gh-actions/setup-uv.sh || true
            ./test/scripts/openshift-ci/setup-e2e-tests.sh
            ./test/scripts/openshift-ci/run-e2e-tests.sh predictor
          popd
        resources:
          requests:
            cpu: 100m
      - as: testlog-gather
        best_effort: true
        cli: latest
        commands: |
          cp -v ${SHARED_DIR}/debuglog-*.log ${SHARED_DIR}/stdout-*.log ${SHARED_DIR}/stderr-*.log "${ARTIFACT_DIR}/" || true
        from: odhmc-kserve-tests
        resources:
          requests:
            cpu: 100m 
        timeout: 1m0s
      - as: kserve-must-gather
        best_effort: true
        cli: latest
        commands: |
          oc adm must-gather \
            --image=quay.io/modh/must-gather:rhoai-2.24 \
            --dest-dir "${ARTIFACT_DIR}/gather-kserve" \
            -- "export COMPONENT=kserve; /usr/bin/gather"
        from: odhmc-kserve-tests
        resources:
          requests: { cpu: 100m }
        timeout: 20m0s
      - as: openshift-must-gather
        best_effort: true
        cli: latest
        commands: oc adm must-gather --dest-dir "${ARTIFACT_DIR}/gather-openshift"
        from: odhmc-kserve-tests
        resources:
          requests:
            cpu: 100m
        timeout: 20m0s
- always_run: false
  as: e2e-kserve-graph
  skip_if_only_changed: ^(?:config|\.github|hack)/|\.md$|^(?:\.*/)?(?:\.gitignore|\.pre-commit-config\.yaml|OWNERS|PROJECT|LICENSE|\.dockerignore|Makefile)$
  steps:
    cluster_profile: aws-opendatahub
    workflow: hypershift-hostedcluster-workflow
    env:
      BASE_DOMAIN: openshift-ci-aws.rhaiseng.com
      COMPUTE_NODE_TYPE: m5.2xlarge
      HYPERSHIFT_AWS_REGION: us-west-2
      HYPERSHIFT_NODE_COUNT: "3"
    test:
      - as: kserve-graph
        cli: latest
        from: odhmc-kserve-tests
        dependencies:
          - env: ODH_MODEL_CONTROLLER_IMAGE
            name: odh-model-controller
        commands: |
          set -euxo pipefail
          echo "ODH_MODEL_CONTROLLER_IMAGE=${ODH_MODEL_CONTROLLER_IMAGE}"

          git clone https://github.com/opendatahub-io/kserve.git /tmp/kserve
          pushd /tmp/kserve
            git fetch --all --tags
            git checkout "${KSERVE_REF:-main}"
            export GITHUB_SHA="${PULL_BASE_SHA:-stable}"
            export ODH_MODEL_CONTROLLER_IMAGE="${ODH_MODEL_CONTROLLER_IMAGE}"
            
            ./test/scripts/gh-actions/setup-uv.sh || true
            ./test/scripts/openshift-ci/setup-e2e-tests.sh
            ./test/scripts/openshift-ci/run-e2e-tests.sh graph
          popd
        resources:
          requests:
            cpu: 100m
      - as: testlog-gather
        best_effort: true
        cli: latest
        commands: |
          cp -v ${SHARED_DIR}/debuglog-*.log ${SHARED_DIR}/stdout-*.log ${SHARED_DIR}/stderr-*.log "${ARTIFACT_DIR}/" || true
        from: odhmc-kserve-tests
        resources:
          requests:
            cpu: 100m
        timeout: 1m0s
      - as: kserve-must-gather
        best_effort: true
        cli: latest
        commands: |
          oc adm must-gather \
            --image=quay.io/modh/must-gather:rhoai-2.24 \
            --dest-dir "${ARTIFACT_DIR}/gather-kserve" \
            -- "export COMPONENT=kserve; /usr/bin/gather"
        from: odhmc-kserve-tests
        resources:
          requests:
            cpu: 100m
        timeout: 20m0s
      - as: openshift-must-gather
        best_effort: true
        cli: latest
        commands: oc adm must-gather --dest-dir "${ARTIFACT_DIR}/gather-openshift"
        from: odhmc-kserve-tests
        resources:
          requests:
            cpu: 100m
        timeout: 20m0s
- always_run: false
  as: e2e-kserve-raw
  optional: true
  skip_if_only_changed: ^(?:config|\.github|hack)/|\.md$|^(?:\.*/)?(?:\.gitignore|\.pre-commit-config\.yaml|OWNERS|PROJECT|LICENSE|\.dockerignore|Makefile)$
  steps:
    cluster_profile: aws-opendatahub
    workflow: hypershift-hostedcluster-workflow
    env:
      BASE_DOMAIN: openshift-ci-aws.rhaiseng.com
      COMPUTE_NODE_TYPE: m5.2xlarge
      HYPERSHIFT_AWS_REGION: us-west-2
      HYPERSHIFT_NODE_COUNT: "3"
    test:
      - as: kserve-raw
        cli: latest
        from: odhmc-kserve-tests
        dependencies:
          - env: ODH_MODEL_CONTROLLER_IMAGE
            name: odh-model-controller
        commands: |
          set -euxo pipefail
          echo "ODH_MODEL_CONTROLLER_IMAGE=${ODH_MODEL_CONTROLLER_IMAGE}"

          git clone https://github.com/opendatahub-io/kserve.git /tmp/kserve
          pushd /tmp/kserve
            git fetch --all --tags
            git checkout "${KSERVE_REF:-main}"
            export GITHUB_SHA="${PULL_BASE_SHA:-stable}"
            export ODH_MODEL_CONTROLLER_IMAGE="${ODH_MODEL_CONTROLLER_IMAGE}"
            
            ./test/scripts/gh-actions/setup-uv.sh || true
            ./test/scripts/openshift-ci/setup-e2e-tests.sh
            ./test/scripts/openshift-ci/run-e2e-tests.sh raw
          popd
        resources:
          requests:
            cpu: 100m
      - as: testlog-gather
        best_effort: true
        cli: latest
        commands: |
          cp -v ${SHARED_DIR}/debuglog-*.log ${SHARED_DIR}/stdout-*.log ${SHARED_DIR}/stderr-*.log "${ARTIFACT_DIR}/" || true
        from: odhmc-kserve-tests
        resources:
          requests:
            cpu: 100m
        timeout: 1m0s
      - as: kserve-must-gather
        best_effort: true
        cli: latest
        commands: |
          oc adm must-gather \
            --image=quay.io/modh/must-gather:rhoai-2.24 \
            --dest-dir "${ARTIFACT_DIR}/gather-kserve" \
            -- "export COMPONENT=kserve; /usr/bin/gather"
        from: odhmc-kserve-tests
        resources:
          requests:
            cpu: 100m
        timeout: 20m0s
      - as: openshift-must-gather
        best_effort: true
        cli: latest
        commands: oc adm must-gather --dest-dir "${ARTIFACT_DIR}/gather-openshift"
        from: odhmc-kserve-tests
        resources:
          requests:
            cpu: 100m
        timeout: 20m0s
- as: pr-image-mirror
  skip_if_only_changed: ^(?:config|\.github|hack)/|\.md$|^(?:\.gitignore|\.pre-commit-config\.yaml|OWNERS|PROJECT|LICENSE|\.dockerignore|Makefile)$
  steps:
    dependencies:
      SOURCE_IMAGE_REF: odh-model-controller
    env:
      IMAGE_REPO: odh-model-controller
    workflow: opendatahub-io-ci-image-mirror
- as: stable-image-mirror
  postsubmit: true
  skip_if_only_changed: ^(?:config|\.github|hack)/|\.md$|^(?:\.gitignore|\.pre-commit-config\.yaml|OWNERS|PROJECT|LICENSE|\.dockerignore|Makefile)$
  steps:
    dependencies:
      SOURCE_IMAGE_REF: odh-model-controller
    env:
      IMAGE_REPO: odh-model-controller
      RELEASE_VERSION: stable-2.x
    workflow: opendatahub-io-ci-image-mirror
zz_generated_metadata:
  branch: stable-2.x
  org: opendatahub-io
  repo: odh-model-controller
