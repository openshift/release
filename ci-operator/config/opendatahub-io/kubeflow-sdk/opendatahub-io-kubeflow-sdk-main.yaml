build_root:
  image_stream_tag:
    name: centos
    namespace: openshift
    tag: stream9
resources:
  '*':
    limits:
      memory: 4Gi
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: verify
  commands: |
    python3 -m pip install --upgrade pip # minimal tests here as we have other PR checks already
    python3 -m pip install .
  container:
    from: src
zz_generated_metadata:
  branch: main
  org: opendatahub-io
  repo: kubeflow-sdk
