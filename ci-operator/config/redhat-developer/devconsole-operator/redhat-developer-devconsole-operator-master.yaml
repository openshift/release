canonical_go_repository: github.com/redhat-developer/devconsole-operator

base_images:
  os:
    cluster: https://api.ci.openshift.org
    name: centos
    namespace: openshift
    tag: '7'

resources:
  '*':
    limits:
      memory: 3Gi
    requests:
      memory: 250Mi
      cpu: 100m

    
build_root:
  project_image:
    dockerfile_path: openshift-ci/Dockerfile.tools

binary_build_commands: make build

images:
- from: os
  inputs:
    bin:
      paths:
      - destination_dir: /usr/local/bin/devconsole-operator
        source_path: /tmp/go/src/github.com/redhat-developer/devconsole-operator/out/operator 
  to: devconsole-operator

tests:
- as: test
  commands: make test
  container:
    from: src
