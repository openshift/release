build_root:
  image_stream_tag:
    name: release
    namespace: openshift
    tag: golang-1.19
images:
- dockerfile_literal: |
    FROM fedora:latest
    RUN mkdir /oadp-apps-deployer
    WORKDIR /oadp-apps-deployer
    COPY . .
    CMD ["/bin/bash"]
  to: master
promotion:
  name: oadp-apps-deployer
  namespace: oadp-qe
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
zz_generated_metadata:
  branch: master
  org: oadp-qe
  repo: oadp-apps-deployer
