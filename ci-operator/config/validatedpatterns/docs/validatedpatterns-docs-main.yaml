build_root:
  image_stream_tag:
    name: validatedpatterns-docs-hugo
    namespace: ci
    tag: main
  use_build_cache: true
resources:
  '*':
    limits:
      memory: 4Gi
    requests:
      cpu: 400m
      memory: 800Mi
tests:
- as: build-preview
  steps:
    env:
      PREVIEW_SITE: patternsdocs-pr
    test:
    - ref: validatedpatterns-build-docs
    - ref: validatedpatterns-preview-comment
zz_generated_metadata:
  branch: main
  org: validatedpatterns
  repo: docs
