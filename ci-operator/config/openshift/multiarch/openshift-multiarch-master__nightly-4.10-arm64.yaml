base_images:
  base:
    name: "4.10"
    namespace: ocp
    tag: base-arm64
releases:
  initial:
    candidate:
      architecture: arm64
      product: ocp
      relative: 1
      stream: nightly
      version: "4.10"
  latest:
    candidate:
      architecture: arm64
      product: ocp
      stream: nightly
      version: "4.10"
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: ocp-e2e-aws-arm64
  cluster: arm01
  interval: 24h
  steps:
    cluster_profile: aws-arm64
    test:
      - as: hello
        from: base
        commands: |
          echo "Hello world"
        resources:
          requests:
            cpu: 100m
            memory: 200m
zz_generated_metadata:
  branch: master
  org: openshift
  repo: multiarch
  variant: nightly-4.10-arm64
