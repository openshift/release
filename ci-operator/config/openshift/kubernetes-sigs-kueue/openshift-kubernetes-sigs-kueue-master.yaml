binary_build_commands: make kind-image-build
build_root:
  image_stream_tag:
    name: release
    namespace: openshift
    tag: rhel-9-release-golang-1.23-openshift-4.19
resources:
  '*':
    limits:
      memory: 4Gi
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: e2e
  commands: make test-e2e
  container:
    from: bin
zz_generated_metadata:
  branch: main
  org: openshift
  repo: kubernetes-sigs-kueue
