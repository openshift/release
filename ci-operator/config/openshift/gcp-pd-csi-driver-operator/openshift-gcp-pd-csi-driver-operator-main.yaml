build_root:
  from_repository: true
resources:
  '*':
    limits:
      memory: 4Gi
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: dead-branch-warning
  commands: |
    echo "ERROR: This branch of the repository has been moved to github.com/openshift/csi-operator. Please open your PR there."
    exit 1
  container:
    from: src
zz_generated_metadata:
  branch: main
  org: openshift
  repo: gcp-pd-csi-driver-operator
