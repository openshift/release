base_images:
  cli:
    name: "4.19"
    namespace: ocp
    tag: cli
  cli-jq:
    name: cli-jq
    namespace: ocp
    tag: latest
  dev-scripts:
    name: test
    namespace: ocp-kni
    tag: dev-scripts
  e2e-integration-tests-pf5:
    name: e2e-integration-tests-pf5
    namespace: netobserv
    tag: ci
  e2etests:
    name: e2etests
    namespace: netobserv
    tag: ci
  upi-installer:
    name: "4.19"
    namespace: ocp
    tag: upi-installer
releases:
  arm64-latest:
    candidate:
      architecture: arm64
      product: ocp
      stream: nightly
      version: "4.19"
  latest:
    candidate:
      product: ocp
      stream: nightly
      version: "4.19"
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: netobserv-aws-ipi-arm-f28
  cron: 36 16 8 * *
  reporter_config:
    channel: '#team-qe-netobserv'
    job_states_to_report:
    - aborted
    - error
    - failure
    - success
    report_template: '{{ if eq .Status.State "success" }} :green_jenkins_circle: {{
      else }} :red_jenkins_circle: {{ end }} Job *{{.Spec.Job}}* ended with *{{.Status.State}}*.
      <{{.Status.URL}}|View logs>'
  steps:
    cluster_profile: aws-qe
    env:
      BASE_DOMAIN: qe.devcluster.openshift.com
      COMPUTE_NODE_TYPE: m6gd.2xlarge
      OCP_ARCH: arm64
      SKIP_QE_APP_REGISTRY: "true"
    test:
    - chain: openshift-e2e-test-netobserv-pf5
    workflow: cucushift-installer-rehearse-aws-ipi
  timeout: 10h0m0s
- as: netobserv-gcp-ipi-amd-f14
  cron: 38 11 5,19 * *
  reporter_config:
    channel: '#team-qe-netobserv'
    job_states_to_report:
    - aborted
    - error
    - failure
    - success
    report_template: '{{ if eq .Status.State "success" }} :green_jenkins_circle: {{
      else }} :red_jenkins_circle: {{ end }} Job *{{.Spec.Job}}* ended with *{{.Status.State}}*.
      <{{.Status.URL}}|View logs>'
  steps:
    cluster_profile: gcp-qe
    env:
      COMPUTE_NODE_TYPE: c2-standard-8
      SKIP_QE_APP_REGISTRY: "true"
    test:
    - chain: openshift-e2e-test-netobserv-pf5
    workflow: cucushift-installer-rehearse-gcp-ipi
  timeout: 10h0m0s
- as: netobserv-metal-ds-ipi-ovn-dualstack-primaryv6-f28-ipv6
  capabilities:
  - intranet
  cron: 4 18 20 * *
  reporter_config:
    channel: '#team-qe-netobserv'
    job_states_to_report:
    - aborted
    - error
    - failure
    - success
    report_template: '{{ if eq .Status.State "success" }} :green_jenkins_circle: {{
      else }} :red_jenkins_circle: {{ end }} Job *{{.Spec.Job}}* ended with *{{.Status.State}}*.
      <{{.Status.URL}}|View logs>'
  steps:
    cluster_profile: equinix-ocp-metal-qe
    env:
      CYPRESS_GREP_TAGS: '@netobserv-critical'
      DEVSCRIPTS_CONFIG: |
        IP_STACK=v6v4
        MIRROR_IMAGES=true
        INSTALLER_PROXY=true
        NETWORK_TYPE=OVNKubernetes
      GINKGO_FOCUS_FILTER: "53844"
      SKIP_QE_APP_REGISTRY: "true"
    test:
    - chain: openshift-e2e-test-netobserv-prepare-bm-cluster
    - chain: openshift-e2e-test-netobserv-pf5
    workflow: cucushift-installer-rehearse-baremetalds-ipi-ovn
- as: netobserv-metal-ds-ipi-ovn-ipv4-f28-virt
  capabilities:
  - intranet
  cron: 57 22 8 * *
  reporter_config:
    channel: '#team-qe-netobserv'
    job_states_to_report:
    - aborted
    - error
    - failure
    - success
    report_template: '{{ if eq .Status.State "success" }} :green_jenkins_circle: {{
      else }} :red_jenkins_circle: {{ end }} Job *{{.Spec.Job}}* ended with *{{.Status.State}}*.
      <{{.Status.URL}}|View logs>'
  steps:
    cluster_profile: equinix-ocp-metal-qe
    env:
      DEVSCRIPTS_CONFIG: |
        IP_STACK=v4
        MIRROR_IMAGES=true
        INSTALLER_PROXY=true
        NETWORK_TYPE=OVNKubernetes
      GINKGO_FOCUS_FILTER: with VMs
      SKIP_FRONTEND_TESTS: "true"
      SKIP_QE_APP_REGISTRY: "true"
    test:
    - chain: openshift-e2e-test-netobserv-prepare-bm-cluster
    - chain: openshift-e2e-test-netobserv-pf5
    workflow: cucushift-installer-rehearse-baremetalds-ipi-ovn
- as: netobserv-vsphere-ipi-amd-f28
  cron: 25 6 23 * *
  reporter_config:
    channel: '#team-qe-netobserv'
    job_states_to_report:
    - aborted
    - error
    - failure
    - success
    report_template: '{{ if eq .Status.State "success" }} :green_jenkins_circle: {{
      else }} :red_jenkins_circle: {{ end }} Job *{{.Spec.Job}}* ended with *{{.Status.State}}*.
      <{{.Status.URL}}|View logs>'
  steps:
    cluster_profile: vsphere-elastic
    env:
      FEATURE_SET: TechPreviewNoUpgrade
      GINKGO_FOCUS_FILTER: 82637|77894
      SIZE_VARIANT: large
      SKIP_FRONTEND_TESTS: "true"
      SKIP_QE_APP_REGISTRY: "true"
    test:
    - chain: openshift-e2e-test-netobserv-pf5
    workflow: cucushift-installer-rehearse-vsphere-ipi-ovn-ipsec
zz_generated_metadata:
  branch: release-2.0
  org: openshift
  repo: network-observability-operator
  variant: nightly-4.19
