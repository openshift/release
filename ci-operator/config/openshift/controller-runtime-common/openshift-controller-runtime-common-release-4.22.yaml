build_root:
  from_repository: true
promotion:
  to:
  - name: "4.22"
    namespace: ocp
releases:
  initial:
    integration:
      name: "4.22"
      namespace: ocp
  latest:
    integration:
      include_built_images: true
      name: "4.22"
      namespace: ocp
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: lint
  commands: |
    make lint
  container:
    from: src
- as: verify
  commands: |
    make verify
  container:
    from: src
- as: test
  commands: |
    make test
  container:
    from: src
zz_generated_metadata:
  branch: release-4.22
  org: openshift
  repo: controller-runtime-common
