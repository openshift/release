base_images:
  claude-ai-helpers:
    name: claude-ai-helpers
    namespace: ci
    tag: latest
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: periodic-jira-agent
  cron: 0 9 * * *
  steps:
    env:
      JIRA_AGENT_TEAM: all
    workflow: jira-agent
zz_generated_metadata:
  branch: main
  org: openshift
  repo: release
  variant: jira-agent
