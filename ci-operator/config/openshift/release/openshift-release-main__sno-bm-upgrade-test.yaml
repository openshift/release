base_images:
  dev-scripts:
    name: test
    namespace: ocp-kni
    tag: dev-scripts
  tests:
    name: "5.0"
    namespace: ocp
    tag: tests
  tools:
    name: "5.0"
    namespace: ocp
    tag: tools
releases:
  latest:
    candidate:
      product: ocp
      stream: nightly
      version: "5.0"
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- always_run: false
  as: e2e-metal-ovn-single-node-upgrade-test
  optional: true
  steps:
    cluster_profile: equinix-edge-enablement
    dependencies:
      OPENSHIFT_INSTALL_RELEASE_IMAGE: "release:latest"
      OPENSHIFT_INSTALL_RELEASE_IMAGE_OVERRIDE: "release:latest"
    env:
      TIMEOUT: "+6 hours"
    test:
    - ref: ingress-metallb-conf
    - ref: wait
    - ref: baremetalds-sno-test
    workflow: baremetalds-sno
  timeout: 10h0m0s
zz_generated_metadata:
  branch: main
  org: openshift
  repo: release
  variant: sno-bm-upgrade-test
