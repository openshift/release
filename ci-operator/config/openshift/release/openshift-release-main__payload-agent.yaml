base_images:
  claude-ai-helpers:
    name: claude-ai-helpers
    namespace: ci
    tag: latest
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: analyze
  cron: '@yearly'
  steps:
    env:
      PAYLOAD_TAG: ""
    workflow: openshift-claude-payload
zz_generated_metadata:
  branch: main
  org: openshift
  repo: release
  variant: payload-agent
