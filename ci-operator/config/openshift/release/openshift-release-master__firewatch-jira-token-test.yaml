resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: firewatch-jira-cloud-token-validation
  cron: '@daily'
  steps:
    test:
    - ref: firewatch-test-jira-token
  timeout: 10m0s
zz_generated_metadata:
  branch: master
  org: openshift
  repo: release
  variant: firewatch-jira-token-test
