base_images:
  ocp_builder_rhel-9-golang-1.26-openshift-5.0:
    name: builder
    namespace: ocp
    tag: rhel-9-golang-1.26-openshift-5.0
  ocp_builder_stream9:
    name: builder
    namespace: ocp
    tag: stream9
build_root:
  image_stream_tag:
    name: builder
    namespace: ocp
    tag: rhel-9-golang-1.26-openshift-5.0
images:
  items:
  - build_args:
    - name: TAGS
      value: scos
    dockerfile_path: images/router/haproxy/Dockerfile.ocp
    from: haproxy-router-base
    to: haproxy-router
  - build_args:
    - name: TAGS
      value: scos
    dockerfile_path: images/router/base/Dockerfile.ocp
    from: ocp_builder_stream9
    inputs:
      ocp_builder_rhel-9-golang-1.26-openshift-5.0:
        as:
        - builder
        - registry.ci.openshift.org/ocp/builder:rhel-9-golang-1.26-openshift-5.0
    to: haproxy-router-base
  - build_args:
    - name: TAGS
      value: scos
    dockerfile_path: images/router/haproxy28/Dockerfile.ocp
    from: ocp_builder_stream9
    to: haproxy-router-haproxy28
  - build_args:
    - name: TAGS
      value: scos
    dockerfile_path: images/router/haproxy32/Dockerfile.ocp
    from: ocp_builder_stream9
    to: haproxy-router-haproxy32
promotion:
  to:
  - name: scos-5.0
    namespace: origin
releases:
  latest:
    integration:
      name: scos-5.0
      namespace: origin
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
zz_generated_metadata:
  branch: release-5.0
  org: openshift
  repo: router
  variant: okd-scos
