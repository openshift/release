releases:
  latest:
    integration:
      name: "5.0"
      namespace: ocp
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: azure-deprovision
  cluster: build01
  interval: 4h
  steps:
    test:
    - ref: hypershift-azure-deprovision
zz_generated_metadata:
  branch: main
  org: openshift
  repo: hypershift
  variant: azure-deprovision
