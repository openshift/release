base_images:
  claude-ai-helpers:
    name: claude-ai-helpers
    namespace: ci
    tag: latest
prowgen:
  enable_secrets_store_csi_driver: true
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: ready-to-solve
  cron: 0 8 * * *
  steps:
    workflow: hypershift-ready-to-solve
zz_generated_metadata:
  branch: main
  org: openshift
  repo: hypershift
  variant: ready-to-solve
