base_images:
  claude-ai-helpers:
    name: claude-ai-helpers
    namespace: ci
    tag: latest
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: ready-to-solve
  cron: 0 8 * * *
  steps:
    workflow: hypershift-ready-to-solve
zz_generated_metadata:
  branch: main
  org: openshift
  repo: hypershift
  variant: ready-to-solve
