base_images:
  ansible:
    name: "5.0"
    namespace: ocp
    tag: ansible
  assisted-installer:
    name: assisted
    namespace: ocp-kni
    tag: assisted-installer
  assisted-installer-agent:
    name: assisted
    namespace: ocp-kni
    tag: assisted-installer-agent
  assisted-installer-controller:
    name: assisted
    namespace: ocp-kni
    tag: assisted-installer-controller
  assisted-service:
    name: assisted
    namespace: ocp-kni
    tag: assisted-service
  assisted-test-infra:
    name: assisted-test
    namespace: ocp-kni
    tag: assisted-test-infra
  aws-ebs-csi-driver-operator-test:
    name: "5.0"
    namespace: ocp
    tag: aws-ebs-csi-driver-operator-test
  azure-disk-csi-driver-operator-test:
    name: "5.0"
    namespace: ocp
    tag: azure-disk-csi-driver-operator-test
  base:
    name: "5.0"
    namespace: ocp
    tag: base
  console-tests:
    name: "5.0"
    namespace: ocpopenshift-multiarch-master__nightly-5.0
    tag: console-tests
  dev-scripts:
    name: test
    namespace: ocp-kni
    tag: dev-scripts
  gcp-pd-csi-driver-operator-test:
    name: "5.0"
    namespace: ocp
    tag: gcp-pd-csi-driver-operator-test
  libvirt-installer:
    name: "5.0"
    namespace: ocp
    tag: libvirt-installer
  openstack-installer:
    name: "5.0"
    namespace: ocp
    tag: openstack-installer
  upi-installer:
    name: "5.0"
    namespace: ocp
    tag: upi-installer
  vsphere-csi-driver-operator-test:
    name: "5.0"
    namespace: ocp
    tag: vsphere-csi-driver-operator-test
releases:
  arm64-initial:
    candidate:
      architecture: arm64
      product: ocp
      relative: 1
      stream: nightly
      version: "5.0"
  arm64-latest:
    candidate:
      architecture: arm64
      product: ocp
      stream: nightly
      version: "5.0"
  initial:
    candidate:
      architecture: multi
      product: ocp
      relative: 1
      stream: nightly
      version: "5.0"
  latest:
    candidate:
      architecture: multi
      product: ocp
      stream: nightly
      version: "5.0"
  multi-initial:
    candidate:
      architecture: multi
      product: ocp
      relative: 1
      stream: nightly
      version: "5.0"
  multi-latest:
    candidate:
      architecture: multi
      product: ocp
      stream: nightly
      version: "5.0"
  ppc64le-latest:
    candidate:
      architecture: ppc64le
      product: ocp
      stream: nightly
      version: "5.0"
  s390x-latest:
    candidate:
      architecture: s390x
      product: ocp
      stream: nightly
      version: "5.0"
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: e2e-ibmz-s390x-mgmt-ovn-conformance-virt-extinfra
  capabilities:
  - intranet
  cron: 0 8 * * 1-4
  steps:
    cluster_profile: libvirt-s390x-vpn
    dependencies:
      OPENSHIFT_INSTALL_TARGET: release:initial
    env:
      ARCH: s390x
      BRANCH: "4.21"
      ETCD_DISK_SPEED: slow
      NODE_TUNING: "true"
      TEST_TYPE: upgrade
      USE_EXTERNAL_DNS: "true"
    workflow: hypershift-mce-ibmz-conformance-s390xmgmt-kubevirt-extinfra
zz_generated_metadata:
  branch: release-5.0
  org: openshift
  repo: hypershift
  variant: periodics
