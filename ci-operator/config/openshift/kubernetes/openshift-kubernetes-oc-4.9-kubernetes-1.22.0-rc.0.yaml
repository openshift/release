binary_build_commands: make
build_root:
  image_stream_tag:
    name: release
    namespace: openshift
    tag: rhel-8-release-golang-1.16-openshift-4.9
resources:
  '*':
    requests:
      cpu: "3"
      memory: 12Gi
tests:
- as: unit
  commands: |
    make test KUBE_JUNIT_REPORT_DIR=${ARTIFACT_DIR}/
  container:
    from: bin
zz_generated_metadata:
  branch: oc-4.9-kubernetes-1.22.0-rc.0
  org: openshift
  repo: kubernetes
