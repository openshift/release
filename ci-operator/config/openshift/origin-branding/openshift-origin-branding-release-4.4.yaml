base_images:
  base:
    cluster: https://api.ci.openshift.org
    name: "4.4"
    namespace: ocp
    tag: base
build_root:
  image_stream_tag:
    cluster: https://api.ci.openshift.org
    name: release
    namespace: openshift
    tag: golang-1.10
images:
- from: base
  to: branding
promotion:
  name: "4.4"
  namespace: origin
resources:
  '*':
    requests:
      cpu: 100m
      memory: 4Gi
tag_specification:
  cluster: https://api.ci.openshift.org
  name: "4.4"
  namespace: ocp
tests:
- as: e2e-aws
  steps:
    cluster_profile: aws
    workflow: origin-e2e-aws
zz_generated_metadata:
  branch: release-4.4
  org: openshift
  repo: origin-branding
