base_images:
  openshift-mcp-server:
    name: "5.1"
    namespace: ocp
    tag: openshift-mcp-server
  tls-scanner-tool:
    name: tls-scanner
    namespace: tls-scanner
    tag: tls-scanner-tool
resources:
  '*':
    requests:
      cpu: 500m
      memory: 200Mi
tag_specification:
  name: "5.1"
  namespace: ocp
tests:
- as: tls-scanner-odh-latest
  cron: 0 9 * * 1
  steps:
    cluster_profile: openshift-org-aws
    env:
      SCAN_NAMESPACE: ocp-mcp-server
    test:
    - ref: openshift-mcp-server-install-odh-latest
    - ref: openshift-mcp-server-deploy
    - ref: tls-scanner-run
    workflow: ipi-aws
zz_generated_metadata:
  branch: main
  org: openshift
  repo: openshift-mcp-server
  variant: periodics
