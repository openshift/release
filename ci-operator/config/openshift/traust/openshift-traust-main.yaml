resources:
  '*':
    limits:
      memory: 4Gi
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: lint
  steps:
    test:
    - ref: skillsaw-lint
zz_generated_metadata:
  branch: main
  org: openshift
  repo: traust
