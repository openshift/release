base_images:
  base:
    name: "4.3"
    namespace: ocp
    tag: base
build_root:
  image_stream_tag:
    name: release
    namespace: openshift
    tag: golang-1.12
promotion:
  disabled: true
  name: "4.3"
  namespace: ocp
resources:
  '*':
    limits:
      memory: 4Gi
    requests:
      cpu: 100m
      memory: 200Mi
tag_specification:
  name: "4.3"
  namespace: ocp
test_binary_build_commands: make build GOFLAGS='-race' --warn-undefined-variables
tests:
- as: build
  commands: |
    make build
    make build-tools
  container:
    from: src
- as: verify
  commands: make verify-all
  container:
    from: src
# This is commented out as jobs were generated by this command, but were
# manually changed to have them run on a OVNKubernetes cluster.
#
#- as: e2e-wmcb
#  commands: make run-wmcb-ci-e2e-test
#  openshift_installer_src:
#    cluster_profile: aws
#- as: e2e-wsu
#  commands: make run-wsu-ci-e2e-test
#  openshift_installer_src:
#    cluster_profile: aws
