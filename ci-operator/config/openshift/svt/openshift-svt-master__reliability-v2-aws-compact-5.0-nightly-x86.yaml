base_images:
  upi-installer:
    name: "5.0"
    namespace: ocp
    tag: upi-installer
build_root:
  image_stream_tag:
    name: ci-tools-build-root
    namespace: ci
    tag: latest
releases:
  latest:
    candidate:
      product: ocp
      stream: nightly
      version: "5.0"
resources:
  '*':
    limits:
      memory: 4Gi
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- always_run: false
  as: reliability-v2-68h
  steps:
    allow_skip_on_success: true
    cluster_profile: aws-perfscale-qe
    env:
      BASE_DOMAIN: qe.devcluster.openshift.com
      CONTROL_PLANE_INSTANCE_TYPE: m6i.2xlarge
      EXTRA_DISKS_SIZE: "256"
      NODE_ROLE: master
      PROW_CLUSTER_TYPE: self-managed
      RELIABILITY_DURATION: 68h
    test:
    - ref: idp-htpasswd
    - ref: storage-create-aws-extra-disks
    - ref: openshift-qe-provision-etcd-on-new-disk
    - ref: openshift-qe-deploy-dittybopper
    - chain: openshift-svt-reliability-v2
    workflow: openshift-qe-installer-aws-compact
  timeout: 72h0m0s
- always_run: false
  as: reliability-v2-20h
  steps:
    allow_skip_on_success: true
    cluster_profile: aws-perfscale-qe
    env:
      BASE_DOMAIN: qe.devcluster.openshift.com
      CONTROL_PLANE_INSTANCE_TYPE: m6i.2xlarge
      EXTRA_DISKS_SIZE: "256"
      NODE_ROLE: master
      PROW_CLUSTER_TYPE: self-managed
      RELIABILITY_DURATION: 20h
    test:
    - ref: idp-htpasswd
    - ref: storage-create-aws-extra-disks
    - ref: openshift-qe-provision-etcd-on-new-disk
    - ref: openshift-qe-deploy-dittybopper
    - chain: openshift-svt-reliability-v2
    workflow: openshift-qe-installer-aws-compact
- always_run: false
  as: reliability-v2-1h
  steps:
    cluster_profile: aws-perfscale-qe
    env:
      BASE_DOMAIN: qe.devcluster.openshift.com
      CONTROL_PLANE_INSTANCE_TYPE: m6i.2xlarge
      EXTRA_DISKS_SIZE: "256"
      NODE_ROLE: master
      PROW_CLUSTER_TYPE: self-managed
      RELIABILITY_DURATION: 1h
    test:
    - ref: idp-htpasswd
    - ref: storage-create-aws-extra-disks
    - ref: openshift-qe-provision-etcd-on-new-disk
    - ref: openshift-qe-deploy-dittybopper
    - chain: openshift-svt-reliability-v2
    workflow: openshift-qe-installer-aws-compact
zz_generated_metadata:
  branch: master
  org: openshift
  repo: svt
  variant: reliability-v2-aws-compact-5.0-nightly-x86
