slack_reporter:
- channel: "#alerts-ocp-ert"
  job_states_to_report:
  - failure
  - error
  report_template: 'Job *{{.Spec.Job}}* ended with *{{.Status.State}}*. <{{.Status.URL}}|View logs> @errata-reliability-team Please check!'
  job_names:
  - jira-notificator
