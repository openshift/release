binary_build_commands: echo no-op
build_root:
  image_stream_tag:
    name: release
    namespace: openshift
    tag: golang-1.20
resources:
  '*':
    limits:
      memory: 1Gi
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: security
  cron: 0 0 * * 0,2,4
  steps:
    env:
      PROJECT_NAME: jetstack-cert-manager
      TARGET_REFERENCE: release-1.14
    workflow: openshift-ci-security
zz_generated_metadata:
  branch: release-1.14
  org: openshift
  repo: jetstack-cert-manager
