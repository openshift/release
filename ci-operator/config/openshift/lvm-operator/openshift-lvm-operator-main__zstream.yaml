releases:
  latest:
    candidate:
      product: ocp
      stream: nightly
      version: "4.21"
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: trigger
  cron: 0 6 * * *
  steps:
    test:
    - ref: lvms-zstream-trigger
zz_generated_metadata:
  branch: main
  org: openshift
  repo: lvm-operator
  variant: zstream
