base_images:
  ocp_4.19_base-rhel9:
    name: "4.19"
    namespace: ocp
    tag: base-rhel9
build_root:
  image_stream_tag:
    name: builder
    namespace: ocp
    tag: rhel-9-golang-1.23-openshift-4.19
images:
  items:
  - dockerfile_literal: |-
      FROM golang:1.23 AS builder
      RUN git clone --depth 1 --branch release-4.19 https://github.com/openshift/lvm-operator.git /go/src/github.com/openshift/lvm-operator
      WORKDIR /go/src/github.com/openshift/lvm-operator/test/integration
      RUN go mod download
      RUN GO_COMPLIANCE_POLICY=exempt_all CGO_ENABLED=0 go build -o /tmp/integration-test .
      FROM registry.access.redhat.com/ubi9/ubi-minimal:latest
      COPY --from=builder /tmp/integration-test /integration-test
      ENTRYPOINT ["/integration-test"]
    inputs:
      ocp_4.19_base-rhel9:
        as:
        - registry.access.redhat.com/ubi9/ubi-minimal:latest
      root:
        as:
        - golang:1.23
    to: lvm-operator-integration-test
releases:
  initial:
    candidate:
      product: ocp
      stream: nightly
      version: "4.19"
  latest:
    candidate:
      product: ocp
      stream: nightly
      version: "4.19"
  multi-latest:
    candidate:
      architecture: multi
      product: ocp
      stream: nightly
      version: "4.19"
resources:
  '*':
    limits:
      memory: 4Gi
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: e2e-aws-sno-qe-integration-tests
  run_if_changed: releases/lvm-operator-(catalog-4-19|rhba-v4\.19).*
  steps:
    cluster_profile: openshift-org-aws
    env:
      LVM_OPERATOR_SUB_INSTALL_NAMESPACE: openshift-lvm-storage
      LVM_OPERATOR_SUB_SOURCE: lvm-catalogsource
      ZSTREAM_VERSION: "4.19"
    test:
    - as: lvms-sno-integration-test
      cli: latest
      commands: |
        ./integration-test run-suite -c 1 --junit-path ${ARTIFACT_DIR}/junit_results.xml openshift/lvm-operator/test/integration/qe_tests/sno
      from: lvm-operator-integration-test
      resources:
        requests:
          cpu: "1"
          memory: 1Gi
      timeout: 4h0m0s
    workflow: cucushift-installer-rehearse-aws-ipi-sno-lvms
- as: e2e-aws-sno-arm-qe-integration-tests
  run_if_changed: releases/lvm-operator-(catalog-4-19|rhba-v4\.19).*
  steps:
    cluster_profile: openshift-org-aws
    dependencies:
      OPENSHIFT_INSTALL_RELEASE_IMAGE_OVERRIDE: release:multi-latest
    env:
      COMPUTE_NODE_TYPE: m6g.4xlarge
      CONTROL_PLANE_INSTANCE_TYPE: m6g.4xlarge
      LVM_OPERATOR_SUB_INSTALL_NAMESPACE: openshift-lvm-storage
      LVM_OPERATOR_SUB_SOURCE: lvm-catalogsource
      OCP_ARCH: arm64
      ZSTREAM_VERSION: "4.19"
    test:
    - as: lvms-sno-integration-test
      cli: latest
      commands: |
        ./integration-test run-suite -c 1 --junit-path ${ARTIFACT_DIR}/junit_results.xml openshift/lvm-operator/test/integration/qe_tests/sno
      from: lvm-operator-integration-test
      resources:
        requests:
          cpu: "1"
          memory: 1Gi
      timeout: 4h0m0s
    workflow: cucushift-installer-rehearse-aws-ipi-sno-lvms
- as: e2e-aws-mno-qe-integration-tests
  run_if_changed: releases/lvm-operator-(catalog-4-19|rhba-v4\.19).*
  steps:
    cluster_profile: openshift-org-aws
    env:
      LVM_OPERATOR_SUB_INSTALL_NAMESPACE: openshift-lvm-storage
      LVM_OPERATOR_SUB_SOURCE: lvm-catalogsource
      ZSTREAM_VERSION: "4.19"
    test:
    - as: lvms-mno-integration-test
      cli: latest
      commands: |
        ./integration-test run-suite -c 1 --junit-path ${ARTIFACT_DIR}/junit_results.xml openshift/lvm-operator/test/integration/qe_tests/mno
      from: lvm-operator-integration-test
      resources:
        requests:
          cpu: "1"
          memory: 1Gi
      timeout: 4h0m0s
    workflow: cucushift-installer-rehearse-aws-ipi-mno-lvms
- as: e2e-aws-mno-arm-qe-integration-tests
  run_if_changed: releases/lvm-operator-(catalog-4-19|rhba-v4\.19).*
  steps:
    cluster_profile: openshift-org-aws
    dependencies:
      OPENSHIFT_INSTALL_RELEASE_IMAGE_OVERRIDE: release:multi-latest
    env:
      COMPUTE_NODE_TYPE: m6g.4xlarge
      CONTROL_PLANE_INSTANCE_TYPE: m6g.4xlarge
      LVM_OPERATOR_SUB_INSTALL_NAMESPACE: openshift-lvm-storage
      LVM_OPERATOR_SUB_SOURCE: lvm-catalogsource
      OCP_ARCH: arm64
      ZSTREAM_VERSION: "4.19"
    test:
    - as: lvms-mno-integration-test
      cli: latest
      commands: |
        ./integration-test run-suite -c 1 --junit-path ${ARTIFACT_DIR}/junit_results.xml openshift/lvm-operator/test/integration/qe_tests/mno
      from: lvm-operator-integration-test
      resources:
        requests:
          cpu: "1"
          memory: 1Gi
      timeout: 4h0m0s
    workflow: cucushift-installer-rehearse-aws-ipi-mno-lvms
- as: e2e-baremetalds-sno-dualstack-qe-integration-tests
  capabilities:
  - intranet
  run_if_changed: releases/lvm-operator-(catalog-4-19|rhba-v4\.19).*
  steps:
    cluster_profile: equinix-ocp-metal-qe
    env:
      AUX_HOST: openshift-qe-metal-ci.arm.eng.rdu2.redhat.com
      LVM_OPERATOR_SUB_INSTALL_NAMESPACE: openshift-lvm-storage
      LVM_OPERATOR_SUB_SOURCE: lvm-catalogsource
      RESERVE_BOOTSTRAP: "false"
      ZSTREAM_VERSION: "4.19"
      architecture: amd64
      masters: "1"
      workers: "0"
    test:
    - as: lvms-sno-integration-test
      cli: latest
      commands: |
        ./integration-test run-suite -c 1 --junit-path ${ARTIFACT_DIR}/junit_results.xml openshift/lvm-operator/test/integration/qe_tests/sno
      from: lvm-operator-integration-test
      resources:
        requests:
          cpu: "1"
          memory: 1Gi
    workflow: cucushift-installer-rehearse-baremetalds-ipi-ovn-dualstack-sno-lvms
- as: e2e-baremetalds-mno-dualstack-qe-integration-tests
  run_if_changed: releases/lvm-operator-(catalog-4-19|rhba-v4\.19).*
  steps:
    cluster_profile: equinix-ocp-metal-qe
    env:
      LVM_OPERATOR_SUB_INSTALL_NAMESPACE: openshift-lvm-storage
      LVM_OPERATOR_SUB_SOURCE: lvm-catalogsource
      ZSTREAM_VERSION: "4.19"
    test:
    - as: lvms-mno-integration-test
      cli: latest
      commands: |
        ./integration-test run-suite -c 1 --junit-path ${ARTIFACT_DIR}/junit_results.xml openshift/lvm-operator/test/integration/qe_tests/mno
      from: lvm-operator-integration-test
      resources:
        requests:
          cpu: "1"
          memory: 1Gi
    workflow: cucushift-installer-rehearse-baremetalds-ipi-ovn-dualstack-mno-lvms
zz_generated_metadata:
  branch: release-management
  org: openshift
  repo: lvm-operator
  variant: zstream-4.19
