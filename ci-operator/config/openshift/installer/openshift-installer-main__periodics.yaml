build_root:
  from_repository: true
releases:
  latest:
    candidate:
      product: ocp
      stream: ci
      version: "5.1"
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: e2e-vsphere-manual-credentials-mode
  cron: 0 0 1 1 *
  steps:
    cluster_profile: vsphere-elastic
    workflow: openshift-e2e-vsphere-manual-credentials-mode
zz_generated_metadata:
  branch: main
  org: openshift
  repo: installer
  variant: periodics
