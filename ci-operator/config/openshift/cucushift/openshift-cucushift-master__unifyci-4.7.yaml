base_images:
  openstack-installer:
    name: "4.7"
    namespace: ocp
    tag: openstack-installer
releases:
  initial:
    candidate:
      product: ocp
      relative: 1
      stream: ci
      version: "4.7"
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: unify-ci-upgrade-4.7
  cron: 0 6 * * *
  steps:
    cluster_profile: aws
    workflow: openshift-unify-ci-cucushift
    env:
      TO_VERSION: "4.7.24"
zz_generated_metadata:
  branch: master
  org: openshift
