build_root:
  project_image:
    dockerfile_path: DockerCITest

tests:
  - as: create-cluster
    commands: ./deploy_ci.sh
    container:
      from: root
    secret:
      name: 'ibm-gate-test-secrets'
      mount_path: ''

resources:
  '*':
    requests:
      memory: 8Gi
      cpu: "4"