base_images:
  osde2e:
    name: osde2e
    namespace: ci
    tag: latest
releases:
  latest:
    candidate:
      product: ocp
      stream: nightly
      version: "4.15"
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: rosa-classic-sts-blocking
  cron: 0 0 1 1 *
  steps:
    env:
      CONFIGS: rosa,sts,stage
      INSTALL_LATEST_NIGHTLY: "4.15"
      SECRET_LOCATIONS: /usr/local/osde2e-common,/usr/local/ritu-temp
      SKIP_CLUSTER_HEALTH_CHECKS: "true"
      SKIP_DESTROY_CLUSTER: "true"
      SKIP_MUST_GATHER: "true"
      TEST_CONFIG: configs/blocking-suite.yaml
    post:
    - chain: gather
    - ref: osde2e-cleanup
    pre:
    - ref: osde2e-provision
    test:
    - ref: osde2e-test
zz_generated_metadata:
  branch: main
  org: openshift
  repo: osde2e
  variant: nightly_blocking-4.15
