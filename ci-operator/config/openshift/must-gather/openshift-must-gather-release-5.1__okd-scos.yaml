base_images:
  ocp_builder_rhel-9-golang-1.26-openshift-5.1:
    name: builder
    namespace: ocp
    tag: rhel-9-golang-1.26-openshift-5.1
  origin_scos-5.1_cli:
    name: scos-5.1
    namespace: origin
    tag: cli
build_root:
  image_stream_tag:
    name: builder
    namespace: ocp
    tag: rhel-9-golang-1.26-openshift-5.1
images:
  items:
  - build_args:
    - name: TAGS
      value: scos
    dockerfile_path: Dockerfile.ocp
    from: origin_scos-5.1_cli
    inputs:
      ocp_builder_rhel-9-golang-1.26-openshift-5.1:
        as:
        - builder
        - registry.ci.openshift.org/ocp/builder:rhel-9-golang-1.26-openshift-5.0
    to: must-gather
promotion:
  to:
  - name: scos-5.1
    namespace: origin
releases:
  latest:
    integration:
      name: scos-5.1
      namespace: origin
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
zz_generated_metadata:
  branch: release-5.1
  org: openshift
  repo: must-gather
  variant: okd-scos
