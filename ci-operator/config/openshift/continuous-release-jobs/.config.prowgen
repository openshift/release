private: true
expose: true
slack_reporter:
- channel: '#ops-crt'
  job_states_to_report:
  - error
  - failure
  report_template: ':warning: Job *{{.Spec.Job}}* ended with *{{.Status.State}}*. <{{.Status.URL}}|View logs> :warning:'
  job_names:
  - daily-cluster-apply
