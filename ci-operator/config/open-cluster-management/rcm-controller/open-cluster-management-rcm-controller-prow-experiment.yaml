base_images:
  base:
    name: ubi-minimal
    namespace: ocp
    tag: "8"
  registration:
    name: "4.5"
    namespace: open-cluster-management
    tag: registration
  registration-operator:
    name: "4.5"
    namespace: open-cluster-management
    tag: registration-operator
  registration-operator-src:
    name: "4.5"
    namespace: open-cluster-management
    tag: registration-operator-src
  work:
    name: "4.5"
    namespace: open-cluster-management
    tag: work
build_root:
  image_stream_tag:
    name: release
    namespace: openshift
    tag: golang-1.13
images:
- dockerfile_path: build/Dockerfile
  from: base
  inputs:
    bin:
      paths:
      - destination_dir: .
        source_path: /go/src/github.com/open-cluster-management/rcm-controller
  to: latest
promotion:
  name: "4.5"
  namespace: open-cluster-management
resources:
  '*':
    limits:
      memory: 4Gi
    requests:
      cpu: 100m
      memory: 200Mi
tag_specification:
  name: "4.5"
  namespace: open-cluster-management
tests:
- as: unit
  commands: |
    GOFLAGS="-mod=vendor" go build ./cmd/manager
  container:
    from: bin
zz_generated_metadata:
  branch: prow-experiment
  org: open-cluster-management
  repo: rcm-controller
