base_images:
  cli:
    name: "4.22"
    namespace: ocp
    tag: cli
build_root:
  image_stream_tag:
    name: maistra-builder
    namespace: ci
    tag: upstream-1.28
  use_build_cache: true
releases:
  latest:
    release:
      architecture: multi
      channel: stable
      version: "4.22"
resources:
  '*':
    limits:
      memory: 16Gi
    requests:
      cpu: "4"
      memory: 4Gi
tests:
- as: lint
  commands: |
    make lint \
    ARTIFACTS="${ARTIFACT_DIR:-}" \
    BUILD_WITH_CONTAINER="0" \
    GOBIN="/gobin" \
    GOCACHE="/tmp/cache" \
    GOMODCACHE="/tmp/cache" \
    XDG_CACHE_HOME="/tmp/cache"
  container:
    from: src
- always_run: false
  as: integ-helm
  pipeline_skip_if_only_changed: (^(docs|\.github|\.claude|\.cursor)/)|(\.md$)|((^|/)OWNERS$)|(^\.gitignore$)|(^LICENSE$)
  steps:
    cluster_profile: ossm-aws
    env:
      BASE_DOMAIN: servicemesh.devcluster.openshift.com
      INSTALLATION_METHOD: helm
      MAISTRA_BUILDER_IMAGE: registry.istio.io/testing/build-tools:release-1.28-2cff97d3b27d82d78c5d4e468a467f520cf2fbf3
      MAISTRA_NAMESPACE: maistra-e2e-test
      MAISTRA_SC_POD: maistra-e2e-test-sc-pod
      REPORT_TO_REPORT_PORTAL: "true"
      SKIP_BUILD: "true"
      TEST_FILE_NAME: junit.xml
      TEST_HUB: quay.io/sail-dev/istio-testing
      TEST_SUITE: helm
      TEST_SUITE_DESC: Istio Integration helm
    test:
    - ref: servicemesh-istio-images-build
    - ref: servicemesh-istio-int-tests
    workflow: servicemesh-istio-e2e-profile
- always_run: false
  as: integ-security
  pipeline_skip_if_only_changed: (^(docs|\.github|\.claude|\.cursor)/)|(\.md$)|((^|/)OWNERS$)|(^\.gitignore$)|(^LICENSE$)
  steps:
    cluster_profile: ossm-aws
    env:
      BASE_DOMAIN: servicemesh.devcluster.openshift.com
      COMPUTE_NODE_TYPE: m5.2xlarge
      INSTALLATION_METHOD: helm
      MAISTRA_BUILDER_IMAGE: registry.istio.io/testing/build-tools:release-1.28-2cff97d3b27d82d78c5d4e468a467f520cf2fbf3
      MAISTRA_NAMESPACE: maistra-e2e-test
      MAISTRA_SC_POD: maistra-e2e-test-sc-pod
      REPORT_TO_REPORT_PORTAL: "true"
      SKIP_BUILD: "true"
      TEST_FILE_NAME: junit.xml
      TEST_HUB: quay.io/sail-dev/istio-testing
      TEST_SUITE: security
      TEST_SUITE_DESC: Istio Integration security
    test:
    - ref: servicemesh-istio-images-build
    - ref: servicemesh-istio-int-tests
    workflow: servicemesh-istio-e2e-profile
- always_run: false
  as: integ-pilot
  pipeline_skip_if_only_changed: (^(docs|\.github|\.claude|\.cursor)/)|(\.md$)|((^|/)OWNERS$)|(^\.gitignore$)|(^LICENSE$)
  steps:
    cluster_profile: ossm-aws
    env:
      BASE_DOMAIN: servicemesh.devcluster.openshift.com
      INSTALLATION_METHOD: helm
      MAISTRA_BUILDER_IMAGE: registry.istio.io/testing/build-tools:release-1.28-2cff97d3b27d82d78c5d4e468a467f520cf2fbf3
      MAISTRA_NAMESPACE: maistra-e2e-test
      MAISTRA_SC_POD: maistra-e2e-test-sc-pod
      REPORT_TO_REPORT_PORTAL: "true"
      SKIP_BUILD: "true"
      TEST_FILE_NAME: junit.xml
      TEST_HUB: quay.io/sail-dev/istio-testing
      TEST_SUITE: pilot
      TEST_SUITE_DESC: Istio Integration pilot
    test:
    - ref: servicemesh-istio-images-build
    - ref: servicemesh-istio-int-tests
    workflow: servicemesh-istio-e2e-profile
- always_run: false
  as: integ-telemetry
  pipeline_skip_if_only_changed: (^(docs|\.github|\.claude|\.cursor)/)|(\.md$)|((^|/)OWNERS$)|(^\.gitignore$)|(^LICENSE$)
  steps:
    cluster_profile: ossm-aws
    env:
      BASE_DOMAIN: servicemesh.devcluster.openshift.com
      INSTALLATION_METHOD: helm
      MAISTRA_BUILDER_IMAGE: registry.istio.io/testing/build-tools:release-1.28-2cff97d3b27d82d78c5d4e468a467f520cf2fbf3
      MAISTRA_NAMESPACE: maistra-e2e-test
      MAISTRA_SC_POD: maistra-e2e-test-sc-pod
      REPORT_TO_REPORT_PORTAL: "true"
      SKIP_BUILD: "true"
      TEST_FILE_NAME: junit.xml
      TEST_HUB: quay.io/sail-dev/istio-testing
      TEST_SUITE: telemetry
      TEST_SUITE_DESC: Istio Integration telemetry
    test:
    - ref: servicemesh-istio-images-build
    - ref: servicemesh-istio-int-tests
    workflow: servicemesh-istio-e2e-profile
- always_run: false
  as: integ-ambient
  pipeline_skip_if_only_changed: (^(docs|\.github|\.claude|\.cursor)/)|(\.md$)|((^|/)OWNERS$)|(^\.gitignore$)|(^LICENSE$)
  steps:
    cluster_profile: ossm-aws
    env:
      AMBIENT: "true"
      BASE_DOMAIN: servicemesh.devcluster.openshift.com
      INSTALLATION_METHOD: helm
      MAISTRA_BUILDER_IMAGE: registry.istio.io/testing/build-tools:release-1.28-2cff97d3b27d82d78c5d4e468a467f520cf2fbf3
      MAISTRA_NAMESPACE: maistra-e2e-test
      MAISTRA_SC_POD: maistra-e2e-test-sc-pod
      REPORT_TO_REPORT_PORTAL: "true"
      SKIP_BUILD: "true"
      TEST_FILE_NAME: junit.xml
      TEST_HUB: quay.io/sail-dev/istio-testing
      TEST_SUITE: ambient
      TEST_SUITE_DESC: Istio Integration ambient
    test:
    - ref: servicemesh-istio-images-build
    - ref: servicemesh-istio-int-tests
    workflow: servicemesh-istio-e2e-profile
- as: update-istio-module
  commands: |
    export BUILD_WITH_CONTAINER="0"
    git clone https://github.com/maistra/test-infra.git
    cd test-infra
    ./tools/automator.sh \
      -o openshift-service-mesh \
      -r sail-operator \
      -f /creds-github/token \
      "-c go mod edit -replace="istio.io/istio=github.com/openshift-service-mesh/istio@release-1.28";go mod tidy;make gen" \
      '-t [release-3.3] Automator: Update Istio module' \
      -m bump-istio-mod-1.28 \
      -b release-3.3
  container:
    from: src
  postsubmit: true
  reporter_config:
    channel: '#team-ossm-release-maintenance'
    job_states_to_report:
    - failure
    - error
    report_template: ':failed: Job *{{.Spec.Job}}* ended with *{{.Status.State}}*.
      <{{.Status.URL}}|View logs>'
  secrets:
  - mount_path: /creds-github
    name: ossm-github-simple-job
zz_generated_metadata:
  branch: release-1.28
  org: openshift-service-mesh
  repo: istio
