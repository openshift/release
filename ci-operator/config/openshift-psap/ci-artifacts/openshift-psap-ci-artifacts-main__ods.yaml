base_images:
  os:
    name: ubi
    namespace: ocp
    tag: "8"
build_root:
  image_stream_tag:
    name: release
    namespace: openshift
    tag: golang-1.16
images:
- dockerfile_path: build/Dockerfile
  from: os
  to: ci-artifacts
resources:
  '*':
    limits:
      memory: 2Gi
    requests:
      cpu: 1000m
      memory: 2Gi
tests:
- as: nb-ux-on-private
  cluster_claim:
    architecture: amd64
    cloud: aws
    owner: openshift-ci
    product: ocp
    timeout: 1h30m0s
    version: "4.11"
  steps:
    test:
    - as: test
      commands: |
        export PSAP_ODS_SECRET_PATH=/var/run/psap-ods-secret-1
        run ods run_private_notebook_ux_e2e_scale_test
      credentials:
      - mount_path: /var/run/psap-ods-secret-1
        name: psap-ods-secret-1
        namespace: test-credentials
      from: ci-artifacts
      resources:
        requests:
          cpu: 1000m
          memory: 500Mi
    workflow: generic-claim
- as: nb-ux-on-customer
  steps:
    post:
    - as: destroy-clusters
      commands: |
        export PSAP_ODS_SECRET_PATH=/var/run/psap-ods-secret-1
        run ods clusters destroy single
      credentials:
      - mount_path: /var/run/psap-ods-secret-1
        name: psap-ods-secret-1
        namespace: test-credentials
      from: ci-artifacts
      resources:
        requests:
          cpu: 1000m
          memory: 500Mi
    pre:
    - as: create-clusters
      commands: |
        export PSAP_ODS_SECRET_PATH=/var/run/psap-ods-secret-1
        run ods clusters create single
      credentials:
      - mount_path: /var/run/psap-ods-secret-1
        name: psap-ods-secret-1
        namespace: test-credentials
      from: ci-artifacts
      resources:
        requests:
          cpu: 1000m
          memory: 500Mi
    test:
    - as: test
      commands: |
        export PSAP_ODS_SECRET_PATH=/var/run/psap-ods-secret-1
        run ods run_customer_notebook_ux_e2e_scale_test
      credentials:
      - mount_path: /var/run/psap-ods-secret-1
        name: psap-ods-secret-1
        namespace: test-credentials
      from: ci-artifacts
      resources:
        requests:
          cpu: 1000m
          memory: 500Mi
- as: nb-ux-on-ocp
  steps:
    post:
    - as: destroy-clusters
      commands: |
        export PSAP_ODS_SECRET_PATH=/var/run/psap-ods-secret-1
        run ods clusters destroy ocp
      credentials:
      - mount_path: /var/run/psap-ods-secret-1
        name: psap-ods-secret-1
        namespace: test-credentials
      from: ci-artifacts
      resources:
        requests:
          cpu: 1000m
          memory: 500Mi
    pre:
    - as: create-clusters
      commands: |
        export PSAP_ODS_SECRET_PATH=/var/run/psap-ods-secret-1
        run ods clusters create ocp
      credentials:
      - mount_path: /var/run/psap-ods-secret-1
        name: psap-ods-secret-1
        namespace: test-credentials
      from: ci-artifacts
      resources:
        requests:
          cpu: 1000m
          memory: 500Mi
    test:
    - as: test
      commands: |
        export PSAP_ODS_SECRET_PATH=/var/run/psap-ods-secret-1
        run ods notebook_ux_e2e_scale_test run_ci_e2e_test
      credentials:
      - mount_path: /var/run/psap-ods-secret-1
        name: psap-ods-secret-1
        namespace: test-credentials
      from: ci-artifacts
      resources:
        requests:
          cpu: 1000m
          memory: 500Mi
- as: nb-ux-on-osd
  steps:
    post:
    - as: destroy-clusters
      commands: |
        export PSAP_ODS_SECRET_PATH=/var/run/psap-ods-secret-1
        run ods clusters destroy osd
      credentials:
      - mount_path: /var/run/psap-ods-secret-1
        name: psap-ods-secret-1
        namespace: test-credentials
      from: ci-artifacts
      resources:
        requests:
          cpu: 1000m
          memory: 500Mi
    pre:
    - as: create-clusters
      commands: |
        export PSAP_ODS_SECRET_PATH=/var/run/psap-ods-secret-1
        run ods clusters create osd
      credentials:
      - mount_path: /var/run/psap-ods-secret-1
        name: psap-ods-secret-1
        namespace: test-credentials
      from: ci-artifacts
      resources:
        requests:
          cpu: 1000m
          memory: 500Mi
    test:
    - as: test
      commands: |
        export PSAP_ODS_SECRET_PATH=/var/run/psap-ods-secret-1
        run ods notebook_ux_e2e_scale_test run_ci_e2e_test
      credentials:
      - mount_path: /var/run/psap-ods-secret-1
        name: psap-ods-secret-1
        namespace: test-credentials
      from: ci-artifacts
      resources:
        requests:
          cpu: 1000m
          memory: 500Mi
- as: nb-ux-on-single
  steps:
    post:
    - as: destroy-clusters
      commands: |
        export PSAP_ODS_SECRET_PATH=/var/run/psap-ods-secret-1
        run ods clusters destroy single
      credentials:
      - mount_path: /var/run/psap-ods-secret-1
        name: psap-ods-secret-1
        namespace: test-credentials
      from: ci-artifacts
      resources:
        requests:
          cpu: 1000m
          memory: 500Mi
    pre:
    - as: create-clusters
      commands: |
        export PSAP_ODS_SECRET_PATH=/var/run/psap-ods-secret-1
        run ods clusters create single
      credentials:
      - mount_path: /var/run/psap-ods-secret-1
        name: psap-ods-secret-1
        namespace: test-credentials
      from: ci-artifacts
      resources:
        requests:
          cpu: 1000m
          memory: 500Mi
    test:
    - as: test
      commands: |
        export PSAP_ODS_SECRET_PATH=/var/run/psap-ods-secret-1
        run ods notebook_ux_e2e_scale_test run_ci_e2e_test
      credentials:
      - mount_path: /var/run/psap-ods-secret-1
        name: psap-ods-secret-1
        namespace: test-credentials
      from: ci-artifacts
      resources:
        requests:
          cpu: 1000m
          memory: 500Mi
- as: get-cluster
  steps:
    post:
    - as: destroy-clusters
      commands: |
        export PSAP_ODS_SECRET_PATH=/var/run/psap-ods-secret-1
        run ods clusters destroy single # destroyed only if creation failed
      credentials:
      - mount_path: /var/run/psap-ods-secret-1
        name: psap-ods-secret-1
        namespace: test-credentials
      from: ci-artifacts
      resources:
        requests:
          cpu: 1000m
          memory: 500Mi
    pre:
    - as: create-clusters
      commands: |
        export PSAP_ODS_SECRET_PATH=/var/run/psap-ods-secret-1
        run ods clusters create single keep
      credentials:
      - mount_path: /var/run/psap-ods-secret-1
        name: psap-ods-secret-1
        namespace: test-credentials
      from: ci-artifacts
      resources:
        requests:
          cpu: 1000m
          memory: 500Mi
    test:
    - as: test
      commands: |
        export PSAP_ODS_SECRET_PATH=/var/run/psap-ods-secret-1
        run ods notebook_ux_e2e_scale_test run_ci_e2e_test
      credentials:
      - mount_path: /var/run/psap-ods-secret-1
        name: psap-ods-secret-1
        namespace: test-credentials
      from: ci-artifacts
      resources:
        requests:
          cpu: 1000m
          memory: 500Mi
- as: plot-nb-ux-on-osd
  steps:
    test:
    - as: test
      commands: run ods generate_matrix-benchmarking osd
      from: ci-artifacts
      resources:
        requests:
          cpu: 1000m
          memory: 1Gi
- as: plot-nb-ux-on-ocp
  steps:
    test:
    - as: test
      commands: run ods generate_matrix-benchmarking ocp
      from: ci-artifacts
      resources:
        requests:
          cpu: 1000m
          memory: 1Gi
- as: plot-nb-ux-on-single
  steps:
    test:
    - as: test
      commands: run ods generate_matrix-benchmarking single
      from: ci-artifacts
      resources:
        requests:
          cpu: 1000m
          memory: 1Gi
- as: cleanup-velero-buckets
  cron: 0 6 * * *
  steps:
    test:
    - as: test
      commands: |
        export PSAP_ODS_SECRET_PATH=/var/run/psap-ods-secret-1
        run ods cleanup/velero-buckets
      credentials:
      - mount_path: /var/run/psap-ods-secret-1
        name: psap-ods-secret-1
        namespace: test-credentials
      from: ci-artifacts
      resources:
        requests:
          cpu: 1000m
          memory: 1Gi
- as: cleanup-zombies
  cron: 0 3 * * *
  steps:
    test:
    - as: test
      commands: |
        export PSAP_ODS_SECRET_PATH=/var/run/psap-ods-secret-1
        run ods cleanup/zombies
      credentials:
      - mount_path: /var/run/psap-ods-secret-1
        name: psap-ods-secret-1
        namespace: test-credentials
      from: ci-artifacts
      resources:
        requests:
          cpu: 1000m
          memory: 1Gi
zz_generated_metadata:
  branch: main
  org: openshift-psap
  repo: ci-artifacts
  variant: ods
