base_images:
  cli:
    name: "4.14"
    namespace: ocp
    tag: cli
  operator-sdk:
    name: "4.14"
    namespace: origin
    tag: operator-sdk
build_root:
  image_stream_tag:
    name: release
    namespace: openshift
    tag: golang-1.21
images:
- context_dir: .
  dockerfile_path: Dockerfile
  to: securesign-operator
- dockerfile_literal: |
    FROM src
    COPY oc /usr/bin/oc
    RUN ln -s /usr/bin/oc /usr/bin/kubectl
  from: src
  inputs:
    cli:
      paths:
      - destination_dir: .
        source_path: /usr/bin/oc
  to: src-oc
operator:
  bundles:
  - as: securesign-bundle
    context_dir: .
    dockerfile_path: bundle.Dockerfile
  substitutions:
    # This must match what's in secure-sign-operator bundle
  - pullspec: quay.io/rcook/securesign-controller:latest 
    with: pipeline:securesign-operator
releases:
  latest:
    candidate:
      product: ocp
      stream: nightly
      version: "4.14"
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: security
  cron: 0 3 * * 0
  steps:
    env:
      PROJECT_NAME: securesign-operator
      SNYK_PRE_EXECUTION_HOOK_CMD: go mod vendor
    workflow: openshift-ci-security
- as: tas-operator-e2e
  cluster_claim:
    architecture: amd64
    cloud: aws
    owner: openshift-ci
    product: ocp
    timeout: 3h0m0s
    version: "4.14"
  steps:
    test:
    - as: install
      cli: latest
      commands: |
        set -x
        oc create ns openshift-rhtas-operator
        operator-sdk run bundle -n openshift-rhtas-operator "$OO_BUNDLE" --timeout 5m0s
        oc get deployments -n openshift-rhtas-operator -o yaml
        #oc wait --for condition=Available -n openshift-rhtas-operator deployment rhtas-operator-controller-manager
        oc wait --for condition=Available -n openshift-rhtas-operator deployment operator-controller-manager
      dependencies:
      - env: OO_BUNDLE
        name: securesign-bundle
      from: operator-sdk
      resources:
        requests:
          cpu: 100m
          memory: 200Mi
    - as: test
      cli: latest
      commands: |
        ./ci/openshift/tas-keycloak-install.sh
        make integration-test
      from: src-oc
      resources:
        requests:
          cpu: 100m
          memory: 200Mi
    workflow: generic-claim
zz_generated_metadata:
  branch: main
  org: securesign
  repo: secure-sign-operator
