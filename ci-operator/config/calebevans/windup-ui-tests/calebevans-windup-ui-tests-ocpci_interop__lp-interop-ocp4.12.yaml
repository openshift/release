base_images:
  cli:
    name: "4.12"
    namespace: ocp
    tag: cli
  os:
    name: ubi
    namespace: ocp
    tag: "8"
  upi-installer:
    name: "4.12"
    namespace: ocp
    tag: upi-installer
build_root:
  image_stream_tag:
    name: release
    namespace: openshift
    tag: golang-1.13
images:
- context_dir: .
  dockerfile_path: dockerfiles/interop/Dockerfile
  to: mtr-runner
releases:
  initial:
    candidate:
      product: ocp
      relative: 1
      stream: nightly
      version: "4.12"
  latest:
    candidate:
      product: ocp
      stream: nightly
      version: "4.12"
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: mtr-interop-aws
  interval: 960h
  steps:
    cluster_profile: aws-cspi-qe
    env:
      BASE_DOMAIN: aws.interop.ccitredhat.com
      RESULTS_FILE: windup-ui-results.xml
      SUB_CHANNEL: alpha
    test:
    - chain: mtr-install
    - ref: mtr-deploy-windup
    - chain: mtr-execute-interop-ui-tests
    - ref: lp-interop-tooling-archive-results
    workflow: ipi-aws
zz_generated_metadata:
  branch: ocpci_interop
  org: calebevans
  repo: windup-ui-tests
  variant: lp-interop-ocp4.12
