base_images:
  upi-installer:
    name: "4.12"
    namespace: ocp
    tag: upi-installer
binary_build_commands: make linux-amd64
build_root:
  image_stream_tag:
    name: release
    namespace: openshift
    tag: golang-1.17
releases:
  latest:
    release:
      architecture: amd64
      channel: candidate
      version: "4.12"
resources:
  '*':
    limits:
      memory: 4Gi
    requests:
      cpu: 100m
      memory: 200Mi
  upi-installer:
    requests:
      cpu: "3"
      memory: 5Gi
tests:
- as: provider-certification-tool-vsphere-upi-platform-none
  cron: 0 0 * * 2,4
  steps:
    cluster_profile: vsphere
    workflow: provider-certification-tool-vsphere-upi-platform-none
zz_generated_metadata:
  branch: main
  org: redhat-openshift-ecosystem
  repo: provider-certification-tool
  variant: "4.12"
