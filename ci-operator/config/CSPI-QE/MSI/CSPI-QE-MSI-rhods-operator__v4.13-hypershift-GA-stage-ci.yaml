base_images:
  cli:
    name: "4.14"
    namespace: ocp
    tag: cli
  upi-installer:
    name: "4.14"
    namespace: ocp
    tag: upi-installer
releases:
  latest:
    candidate:
      product: ocp
      stream: ci
      version: "4.14"
resources:
  '*':
    limits:
      memory: 4Gi
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: rosa-ci-hypershift
  steps:
    cluster_profile: aws-managed-cspi-qe
    env:
      CHANNEL_GROUP: candidate
      OPENSHIFT_VERSION: "4.14"
      REGION: us-west-2
    workflow: rosa-aws-sts-hypershift
zz_generated_metadata:
  branch: rhods-operator
  org: CSPI-QE
  repo: MSI
  variant: v4.13-hypershift-GA-stage-ci
