base_images:
  managed-services-integration-tests:
    name: managed-services-integration-tests
    namespace: ci
    tag: latest
  openshift-cli-installer:
    name: openshift-cli-installer
    namespace: ci
    tag: latest
releases:
  latest:
    candidate:
      product: ocp
      stream: ci
      version: "4.14"
resources:
  '*':
    limits:
      memory: 4Gi
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: singlecluster-rosa-4-14-candidate-smoke
  steps:
    cluster_profile: aws-managed-cspi-qe
    env:
      CLUSTER_NAME_PREFIX: ci-smoke
      CLUSTER1_CONFIG: name=ci-rosa;platform=rosa;region=us-west-2;version=4.14;channel-group=candidate;compute-machine-type=m5.4xlarge;replicas=2;expiration-time=8h;timeout=2h
      PULL_SECRET_NAME: pull-secret
      S3_BUCKET_NAME: openshift-cli-installer
      S3_BUCKET_PATH: openshift-ci
    test:
    - ref: cluster-msi-cluster-sanity
    workflow: cluster
zz_generated_metadata:
