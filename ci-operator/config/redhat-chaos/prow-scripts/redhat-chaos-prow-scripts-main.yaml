base_images:
  prow.scripts:
    name: prow-scripts
    namespace: chaos
    tag: latest
build_root:
  from_repository: true
images:
- dockerfile_path: Dockerfile
  from: prow.scripts
  to: prow-scripts
promotion:
  to:
  - namespace: chaos
    tag: latest
resources:
  '*':
    limits:
      memory: 4Gi
    requests:
      cpu: 100m
      memory: 200Mi
zz_generated_metadata:
  branch: main
  org: redhat-chaos
  repo: prow-scripts
