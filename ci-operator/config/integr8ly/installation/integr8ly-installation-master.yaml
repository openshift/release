build_root:
  image_stream_tag:
    cluster: https://api.ci.openshift.org
    name: installation-base-image
    namespace: integr8ly
    tag: latest
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: format
  commands: make test/format
  container:
    from: src
