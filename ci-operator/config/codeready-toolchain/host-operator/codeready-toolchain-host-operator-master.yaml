build_root:
  project_image:
    dockerfile_path: openshift-ci/Dockerfile.tools
prowgen:
  enable_secrets_store_csi_driver: true
releases:
  initial:
    integration:
      name: "4.22"
      namespace: ocp
  latest:
    integration:
      include_built_images: true
      name: "4.22"
      namespace: ocp
resources:
  '*':
    requests:
      cpu: "3"
      memory: 250Mi
tests:
- as: e2e
  steps:
    cluster_profile: devsandboxci-aws
    env:
      BASE_DOMAIN: devsandboxci.devcluster.openshift.com
    test:
    - as: test
      cli: latest
      commands: make test-e2e
      credentials:
      - collection: sandbox-secrets
        group: sandbox-devsso
        mount_path: /usr/local/sandbox-secrets
      from: src
      resources:
        requests:
          cpu: "3"
          memory: 250Mi
    workflow: codeready-toolchain-aws
zz_generated_metadata:
  branch: master
  org: codeready-toolchain
  repo: host-operator
