build_root:
  image_stream_tag:
    name: builder
    namespace: konveyor
    tag: ubi9-v1.25
images:
- additional_architectures:
  - arm64
  dockerfile_path: Containerfile.download
  to: oadp-cli-oadp-1.6
promotion:
  to:
  - name: oadp-cli
    namespace: konveyor
resources:
  '*':
    limits:
      memory: 4Gi
    requests:
      cpu: 100m
      memory: 200Mi
zz_generated_metadata:
  branch: oadp-1.6
  org: migtools
  repo: oadp-cli
