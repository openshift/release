base_images:
  base:
    name: "4.8"
    namespace: ocp
    tag: base
binary_build_commands: make build
build_root:
  image_stream_tag:
    name: release
    namespace: openshift
    tag: golang-1.15
images:
- dockerfile_path: build/Dockerfile
  from: base
  to: node-maintenance-operator
operator:
  bundles:
  - as: nmo-bundle
    dockerfile_path: build/bundle.Dockerfile
  substitutions:
  - pullspec: quay.io/kubevirt/node-maintenance-operator:latest
    with: pipeline:node-maintenance-operator
promotion:
  name: "4.8"
  namespace: kubevirt
releases:
  latest:
    candidate:
      product: ocp
      stream: nightly
      version: "4.8"
resources:
  '*':
    requests:
      cpu: 500m
      memory: 1000Mi
tests:
- as: openshift-e2e
  steps:
    cluster_profile: aws
    dependencies:
      OO_BUNDLE_IMG: nmo-bundle
    env:
      OO_INSTALL_NAMESPACE: default
      OO_TARGET_NAMESPACES: '!install'
    pre:
    - chain: ipi-aws-pre
    - ref: optional-operators-ci-run-bundle
    test:
    - as: e2e
      commands: make cluster-functest
      from: src
      resources:
        requests:
          cpu: 100m
          memory: 200Mi
    workflow: optional-operators-ci-aws
zz_generated_metadata:
  branch: master
  org: kubevirt
  repo: node-maintenance-operator
