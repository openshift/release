slack_reporter:
- channel: '#team-ocp-edge-collab'
  job_states_to_report:
  - failure
  - error
  report_template: >
    :warning: Job *{{.Spec.Job}}* ended with *{{.Status.State}}*.
    <{{.Status.URL}}|View logs>
  job_names:
  - pr-notifier
  - ocp-ci-monitor