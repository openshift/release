build_root:
  from_repository: true
  use_build_cache: true
resources:
  '*':
    limits:
      memory: 4Gi
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: lint
  commands: |
    echo "Placeholder for lint job"
  container:
    from: src
zz_generated_metadata:
  branch: master
  org: openshift-eng
  repo: cyborg
