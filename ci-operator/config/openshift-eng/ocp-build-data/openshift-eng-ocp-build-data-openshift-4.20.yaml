build_root:
  image_stream_tag:
    name: release
    namespace: openshift
    tag: golang-1.13
resources:
  '*':
    limits:
      memory: 4Gi
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: image-check
  steps:
    workflow: ocp-art
zz_generated_metadata:
  branch: openshift-4.20
  org: openshift-eng
  repo: ocp-build-data
