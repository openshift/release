build_root:
  image_stream_tag:
    name: release
    namespace: openshift
    tag: golang-1.13
resources:
  '*':
    limits:
      memory: 4Gi
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: image-check
  capabilities:
  - intranet
  optional: true
  restrict_network_access: false
  steps:
    env:
      OCP_VERSION: "4.19"
    workflow: ocp-art
zz_generated_metadata:
  branch: openshift-4.19
  org: openshift-eng
  repo: ocp-build-data
