build_root:
  project_image:
    dockerfile_literal: FROM ghcr.io/stbenjam/claudelint:main
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: lint
  commands: claudelint -v --strict
  container:
    from: src
zz_generated_metadata:
  branch: main
  org: openshift-eng
  repo: olm-team-ai-helpers
