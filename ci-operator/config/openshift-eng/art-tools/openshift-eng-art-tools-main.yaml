build_root:
  image_stream_tag:
    name: release
    namespace: openshift
    tag: rhel-9-release-golang-1.23-openshift-4.20
resources:
  '*':
    limits:
      memory: 4Gi
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: art-pre-commit-check
  optional: true
  steps:
    test:
    - ref: ocp-art-validate-pre-commit-hooks
- as: security
  optional: true
  steps:
    env:
      PROJECT_NAME: openshift-eng/art-tools
      SNYK_CODE_ADDITIONAL_ARGS: --severity-threshold=high
    workflow: openshift-ci-security
zz_generated_metadata:
  branch: main
  org: openshift-eng
  repo: art-tools
