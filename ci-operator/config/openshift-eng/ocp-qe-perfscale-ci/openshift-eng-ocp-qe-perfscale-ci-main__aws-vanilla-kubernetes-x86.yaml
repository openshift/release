build_root:
  image_stream_tag:
    name: ci-tools-build-root
    namespace: ci
    tag: latest
releases:
  latest:
    candidate:
      product: ocp
      stream: nightly
      version: "4.20"
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- always_run: false
  as: vanilla-kubernetes-2nodes
  steps:
    cluster_profile: aws-perfscale-qe
    test:
    - ref: openshift-qe-cluster-health
    workflow: openshift-qe-installer-aws-vanilla-kubernetes
zz_generated_metadata:
  branch: main
  org: openshift-eng
  repo: ocp-qe-perfscale-ci
  variant: aws-vanilla-kubernetes-x86
