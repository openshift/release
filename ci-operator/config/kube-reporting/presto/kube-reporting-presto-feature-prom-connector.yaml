base_images:
  base:
    name: ubi
    namespace: ocp
    tag: "8"
build_root:
  project_image:
    dockerfile_path: Dockerfile.build
images:
- dockerfile_path: Dockerfile
  from: base
  to: metering-presto
promotion:
  disabled: true
  name: "4.6"
  namespace: ocp
resources:
  '*':
    requests:
      cpu: "2"
      memory: 2Gi
  unit:
    requests:
      cpu: "1"
      memory: 4Gi
tag_specification:
  name: "4.6"
  namespace: ocp
tests:
- artifact_dir: /tmp/artifacts
  as: unit
  commands: |
    set -x
    export ARTIFACT_DIR=/tmp/artifacts
    ./test.sh
  container:
    from: src
zz_generated_metadata:
  branch: feature-prom-connector
  org: kube-reporting
  repo: presto
