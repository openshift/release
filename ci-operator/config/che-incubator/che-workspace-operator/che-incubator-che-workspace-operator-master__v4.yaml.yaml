base_images:
  base:
    name: "4.4"
    namespace: ocp
    tag: base
  cli:
    name: "4.5"
    namespace: ocp
    tag: cli
build_root:
  image_stream_tag:
    name: release
    namespace: openshift
    tag: golang-1.13
resources:
  '*':
    requests:
      cpu: 200m
      memory: 4Gi
tag_specification:
  name: "4.4"
  namespace: ocp
tests:
- as: openshift-web-terminal-e2e
  commands: |
    ls -lrtha
    pwd
    ls -lrtha /tmp/artifacts
    ls -lrtha /tmp/artifacts/installer/auth
    oc version
  openshift_installer_src:
    cluster_profile: aws
zz_generated_metadata:
  branch: master
  org: che-incubator
  repo: che-workspace-operator
  variant: v4.yaml
