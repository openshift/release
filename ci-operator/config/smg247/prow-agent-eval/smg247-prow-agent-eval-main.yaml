build_root:
  image_stream_tag:
    name: release
    namespace: openshift
    tag: rhel-9
images:
  items:
  - dockerfile_path: Dockerfile
    to: prow-agent-eval
promotion:
  to:
  - name: prow-agent-eval
    namespace: ci
resources:
  '*':
    limits:
      memory: 2Gi
    requests:
      cpu: "1"
      memory: 2Gi
zz_generated_metadata:
  branch: main
  org: smg247
  repo: prow-agent-eval
