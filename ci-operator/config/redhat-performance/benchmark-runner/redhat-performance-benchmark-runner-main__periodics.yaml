releases:
  latest:
    candidate:
      product: ocp
      stream: nightly
      version: "4.22"
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: weekly-e2e
  capabilities:
  - intranet
  cron: 0 8 * * 6
  steps:
    env:
      TIMEOUT: "8000"
      WORKLOAD: all
    workflow: redhat-performance-benchmark-runner-e2e
zz_generated_metadata:
  branch: main
  org: redhat-performance
  repo: benchmark-runner
  variant: periodics
