releases:
  latest:
    candidate:
      product: ocp
      stream: nightly
      version: "4.22"
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: all-ephemeral-ibm
  cron: 0 0 * * 6
  steps:
    test:
    - ref: redhat-performance-benchmark-runner-ibm
  timeout: 24h0m0s
zz_generated_metadata:
  branch: main
  org: redhat-performance
  repo: benchmark-runner
  variant: periodics
