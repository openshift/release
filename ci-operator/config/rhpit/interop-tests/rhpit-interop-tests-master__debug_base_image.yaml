base_images:
  clc-ui-e2e:
    name: "2.7"
    namespace: acm-qe
    tag: clc-ui-e2e
build_root:
  image_stream_tag:
    name: release
    namespace: openshift
    tag: golang-1.19
images:
- context_dir: .
  dockerfile_path: Dockerfile.interops
  to: test-clc
releases:
  latest:
    candidate:
      product: ocp
      stream: nightly
      version: "4.12"
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: acm-debug-clc-image
  cron: 0 1 * * 1
  steps:
    cluster_profile: aws-cspi-qe
    test:
    - ref: acm-tests-clc-create
zz_generated_metadata:
  branch: master
  org: rhpit
  repo: interop-tests
  variant: debug_base_image
