base_images:
  cli:
    name: "4.14"
    namespace: ocp
    tag: cli
  openshift-cli-installer:
    name: openshift-cli-installer
    namespace: ci
    tag: latest
releases:
  latest:
    candidate:
      product: ocp
      stream: nightly
      version: "4.14"
resources:
  '*':
    limits:
      memory: 4Gi
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: multicluster-poc
  cron: 0 6 * * 1
  steps:
    cluster_profile: aws-cspi-qe
    env:
      CLUSTER1_CONFIG: name=ci-aws-ipi;base_domain=cspilp.interop.ccitredhat.com;platform=aws;region=us-east-2;version=4.14;stream=nightly;worker_flavor=m5.xlarge
      CLUSTER2_CONFIG: name=ci-aws-ipi;base_domain=cspilp.interop.ccitredhat.com;platform=aws;region=us-east-2;version=4.14;stream=nightly;worker_flavor=m5.xlarge
      PULL_SECRET_NAME: pull-secret
    test:
    - ref: mtc-poc-sleep
    workflow: cluster
zz_generated_metadata:
  branch: main_mtc-test-multi-cluster-4.14
  org: rhpit
  repo: interop-tests
