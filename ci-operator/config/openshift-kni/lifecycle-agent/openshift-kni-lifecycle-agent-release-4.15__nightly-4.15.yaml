base_images:
  lifecycle-agent-operator:
    name: "4.15"
    namespace: ocp-kni
    tag: lifecycle-agent-operator
  lifecycle-agent-operator-bundle:
    name: "4.15"
    namespace: ocp-kni
    tag: lifecycle-agent-operator-bundle
  recert:
    name: recert
    namespace: edge-infrastructure
    tag: release-4.15
  tests:
    name: "4.15"
    namespace: ocp
    tag: tests
releases:
  latest:
    integration:
      name: "4.15"
      namespace: ocp
resources:
  '*':
    requests:
      cpu: 10m
      memory: 100Mi
tests:
- as: seedgen-aws-ocp-4-15
  cron: 45 8 26 6 *
  steps:
    cluster_profile: aws-3
    dependencies:
      LCA_PULL_REF: pipeline:lifecycle-agent-operator
      OO_BUNDLE: pipeline:lifecycle-agent-operator-bundle
      RECERT_IMAGE: pipeline:recert
    env:
      OCP_BASE_IMAGE_SOURCE: release
      OCP_BASE_VERSION: "4.15"
      OO_BUNDLE_OVERRIDE: quay.io/openshift-kni/lifecycle-agent-operator-bundle:4.15
      RECERT_IMAGE_OVERRIDE: quay.io/edge-infrastructure/recert:release-4.15
      SEED_FLOATING_TAG: "4.15"
      SEED_IMAGE_TAG_FORMAT: nightly
    workflow: openshift-image-based-upgrade-seed-create
zz_generated_metadata:
  branch: release-4.15
  org: openshift-kni
  repo: lifecycle-agent
  variant: nightly-4.15
