base_images:
  bip-orchestrate-vm:
    name: bip-orchestrate-vm
    namespace: edge-infrastructure
    tag: latest
  ib-orchestrate-vm:
    name: ib-orchestrate-vm
    namespace: edge-infrastructure
    tag: latest
  installer:
    name: "4.21"
    namespace: ocp
    tag: installer
  lifecycle-agent-operator:
    name: "4.21"
    namespace: ocp-kni
    tag: lifecycle-agent-operator
  lifecycle-agent-operator-bundle:
    name: "4.21"
    namespace: ocp-kni
    tag: lifecycle-agent-operator-bundle
  recert:
    name: recert
    namespace: edge-infrastructure
    tag: latest
  tests:
    name: "4.21"
    namespace: ocp
    tag: tests
releases:
  latest:
    integration:
      name: "4.21"
      namespace: ocp
resources:
  '*':
    requests:
      cpu: 10m
      memory: 100Mi
tests:
- as: seedgen-aws-ocp-4-21
  interval: 24h
  steps:
    cluster_profile: aws
    dependencies:
      BIP_ORCHESTRATE_VM_REF: pipeline:bip-orchestrate-vm
      IB_ORCHESTRATE_VM_REF: pipeline:ib-orchestrate-vm
      LCA_PULL_REF: pipeline:lifecycle-agent-operator
      OO_BUNDLE: pipeline:lifecycle-agent-operator-bundle
      RECERT_IMAGE: pipeline:recert
    env:
      OCP_BASE_IMAGE_SOURCE: ci
      OCP_BASE_VERSION: "4.21"
      OO_BUNDLE_OVERRIDE: quay.io/openshift-kni/lifecycle-agent-operator-bundle:4.21
      RECERT_IMAGE_OVERRIDE: quay.io/edge-infrastructure/recert:v0
      SEED_FLOATING_TAG: "4.21"
      SEED_IMAGE_TAG_FORMAT: nightly
    workflow: openshift-image-based-upgrade-seed-create
- as: image-based-install
  interval: 24h
  steps:
    cluster_profile: aws
    dependencies:
      BIP_ORCHESTRATE_VM_REF: pipeline:bip-orchestrate-vm
      IB_ORCHESTRATE_VM_REF: pipeline:ib-orchestrate-vm
      INSTALLER_PULL_REF: pipeline:installer
      LCA_PULL_REF: pipeline:lifecycle-agent-operator
      OO_BUNDLE: pipeline:lifecycle-agent-operator-bundle
      RECERT_IMAGE: pipeline:recert
    env:
      OCP_BASE_IMAGE_SOURCE: ci
      OCP_BASE_VERSION: "4.21"
      SEED_IMAGE_TAG_FORMAT: nightly
      SEED_IMAGE_TAG_PREFIX: ibi
    workflow: openshift-image-based-install
zz_generated_metadata:
  branch: release-4.21
  org: openshift-kni
  repo: lifecycle-agent
  variant: nightly-4.21
