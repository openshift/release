base_images:
  ansible:
    name: "4.20"
    namespace: ocp
    tag: ansible
  base:
    name: "4.20"
    namespace: ocp
    tag: base
releases:
  latest:
    release:
      channel: stable
      version: "4.20"
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: e2e-telcov10n-functional-cnf-compute-hcp-nto-stable
  capabilities:
  - intranet
  cron: 00 16 * * 4
  steps:
    env:
      T5CI_JOB_HCP_RELEASE_TYPE: stable
      T5CI_JOB_MGMT_RELEASE_TYPE: stable
      T5CI_JOB_TYPE: hcp-cnftests
      T5CI_VERSION: "4.20"
    observers:
      enable:
      - observers-resource-watch
    workflow: telcov10n-functional-cnf-compute-hcp-nto
zz_generated_metadata:
  branch: main
  org: openshift-kni
  repo: eco-ci-cd
  variant: cnf-compute-hcp-nto-4.20
