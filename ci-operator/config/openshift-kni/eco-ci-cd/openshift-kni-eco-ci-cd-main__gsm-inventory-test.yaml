prowgen:
  enable_secrets_store_csi_driver: true
build_root:
  project_image:
    dockerfile_literal: |-
      FROM quay-proxy.ci.openshift.org/openshift/ci:ci_telco-runner_latest
resources:
  '*':
    limits:
      memory: 1Gi
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: gsm-inventory-test
  cluster: build13
  steps:
    test:
    - ref: telcov10n-functional-eco-ci-cd-tests-gsm-inventory
zz_generated_metadata:
  branch: main
  org: openshift-kni
  repo: eco-ci-cd
  variant: gsm-inventory-test
