base_images:
  cli:
    name: "4.20"
    namespace: ocp
    tag: cli
  ocp_builder_rhel-9-golang-1.23-openshift-4.20:
    name: builder
    namespace: ocp
    tag: rhel-9-golang-1.23-openshift-4.20
build_root:
  from_repository: true
images:
- dockerfile_literal: |
    FROM this-is-ignored
    RUN dnf install -y git
    WORKDIR /go/src/github.com/openshift-kni/commatrix
    RUN mkdir -p /go/src/github.com/openshift-kni/commatrix/vendor && \
        chmod -R a+w /go/src/github.com/openshift-kni/commatrix

    RUN mkdir -p /tmp/bin && \
        chgrp root /tmp/bin && \
        chmod -R a+w /tmp/bin

    COPY oc /usr/bin/oc
    COPY oc /tmp/bin/
    COPY app/ .
  from: ocp_builder_rhel-9-golang-1.23-openshift-4.20
  inputs:
    cli:
      paths:
      - destination_dir: .
        source_path: /usr/bin/oc
    src:
      paths:
      - destination_dir: app
        source_path: /go/src/github.com/openshift-kni/commatrix/.
  optional: true
  to: cli-with-git
promotion:
  to:
  - name: "4.20"
    namespace: ocp
releases:
  initial:
    integration:
      name: "4.20"
      namespace: ocp
  latest:
    candidate:
      product: ocp
      stream: ci
      version: "4.20"
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: unit-test
  commands: make test
  container:
    from: src
- as: lint
  commands: make lint
  container:
    from: src
- as: commatrix-e2e-tests
  shard_count: 2
  steps:
    cluster_profile: aws-telco
    env:
      BASE_DOMAIN: telco5g-ci.devcluster.openshift.com
      USER_TAGS: |
        ci-job-source prowci
        ci-job-branch main
        ci-job-fullname openshift-kni-commatrix-e2e-tests-main
        ci-job-type commatrix-e2e-tests
        ci-repo-name commatrix
        ci-org-name openshift-kni
    test:
    - as: commatrix-e2e-tests
      commands: |
        # Install krew
        OS="$(uname | tr '[:upper:]' '[:lower:]')"
        ARCH="$(uname -m | sed -e 's/x86_64/amd64/' -e 's/aarch64/arm64/' -e 's/armv7l/arm/')"
        KREW="krew-${OS}_${ARCH}"

        mkdir -p /tmp
        curl -fsSL "https://github.com/kubernetes-sigs/krew/releases/latest/download/${KREW}.tar.gz" -o /tmp/${KREW}.tar.gz
        mkdir -p /tmp/krew
        tar -xvzf /tmp/${KREW}.tar.gz -C /tmp/krew

        /tmp/krew/${KREW} install krew
        export KREW_ROOT="${KREW_ROOT:-$HOME/.krew}"
        export PATH="${KREW_ROOT}/bin:$PATH"

        export ADDITIONAL_NFTABLES_RULES_FILE_PATH="/tmp/additional-nftables-rules"

        echo "# Allow host level services dynamic port range
        tcp dport 9000-9999 accept
        udp dport 9000-9999 accept
        # Allow Kubernetes node ports dynamic port range
        tcp dport 30000-32767 accept
        udp dport 30000-32767 accept
        # Keep port open for origin test
        # https://github.com/openshift/origin/blob/master/vendor/k8s.io/kubernetes/test/e2e/network/service.go#L2622
        tcp dport 10180 accept
        udp dport 10180 accept

        tcp dport 10256 accept

        tcp dport 443 accept
        udp dport 443 accept

        # Keep port open for origin test
        # https://github.com/openshift/origin/blob/master/vendor/k8s.io/kubernetes/test/e2e/network/service.go#L2724
        tcp dport 80 accept
        udp dport 80 accept" > ${ADDITIONAL_NFTABLES_RULES_FILE_PATH}

        # Install the commatrix krew plugin
        go mod vendor
        make build
        make install INSTALL_DIR=/tmp/bin/
        # run the make e2e-test
        PATH="/tmp/bin:$PATH" EXTRA_NFTABLES_MASTER_FILE="${ADDITIONAL_NFTABLES_RULES_FILE_PATH}" EXTRA_NFTABLES_WORKER_FILE="${ADDITIONAL_NFTABLES_RULES_FILE_PATH}"  make e2e-test
        oc delete ns openshift-commatrix-debug
      from: cli-with-git
      resources:
        requests:
          cpu: 100m
          memory: 200Mi
    - ref: openshift-e2e-test
    workflow: openshift-e2e-aws-ovn-serial
  timeout: 6h0m0s
- as: security
  optional: true
  steps:
    env:
      PROJECT_NAME: commatrix
      SNYK_CODE_ADDITIONAL_ARGS: --severity-threshold=high --policy-path=.snyk
    workflow: openshift-ci-security
zz_generated_metadata:
  branch: main
  org: openshift-kni
  repo: commatrix
