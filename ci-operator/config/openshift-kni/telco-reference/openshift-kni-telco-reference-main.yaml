base_images:
  os-min9:
    name: ubi-minimal
    namespace: ocp
    tag: "9"
build_root:
  from_repository: true
resources:
  '*':
    requests:
      cpu: 150m
      memory: 250Mi
tests:
- as: ci-lint
  commands: make lintCheck
  container:
    from: src
zz_generated_metadata:
  branch: main
  org: openshift-kni
  repo: telco-reference
