build_root:
  image_stream_tag:
    name: ci-tools-build-root
    namespace: ci
    tag: latest
images:
- dockerfile_path: prow/Dockerfile
  to: ocp-qe-perfscale-ci-4.13-stable-prod
promotion:
  namespace: ci
  tag: latest
releases:
  latest:
    release:
      channel: candidate
      version: "4.13"
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: ocp-qe-perfscale-ci-tests-rosa-hcp-cluster-density-v2
  cron: 0 12 * * 4
  steps:
    cluster_profile: aws-perf-qe
    env:
      OCM_LOGIN_ENV: production
      OPENSHIFT_VERSION: "4.13"
      REGION: us-east-2
      ROSA_LOGIN_ENV: production
      COMPUTE_MACHINE_TYPE: m5.2xlarge
      DISABLE_WORKLOAD_MONITORING: "false"
      ENABLE_AUTOSCALING: "false"
      HOSTED_CP: "true"
      MULTI_AZ: "true"
      READY_WAIT_TIMEOUT: 20m
      REPLICAS: "24"
      ZONES_COUNT: "3"
    test:
    - ref: openshift-qe-cluster-density-v2
    workflow: rosa-aws-sts-hypershift
zz_generated_metadata:
  branch: main
  org: openshift-qe
  repo: ocp-qe-perfscale-ci
  variant: rosa-4.13-stable-prod
