build_root:
  image_stream_tag:
    name: ci-tools-build-root
    namespace: ci
    tag: latest
images:
- dockerfile_path: prow/Dockerfile
  to: ocp-qe-perfscale-ci-write-jobs-to-gsheet
promotion:
  to:
  - namespace: ci
    tag: latest
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: write-jobs-to-google-sheet-sprintly
  cron: 0 0 */21 * *
  steps:
    test:
    - ref: openshift-qe-write-jobs-to-gsheet
- as: get-orion-results-416
  cluster: build05
  cron: 0 0 * * MON
  steps:
    env:
      ES_TYPE: internal
      HUNTER_ANALYZE: "true"
      CONFIG: "examples/payload-scale-416.yaml"
      ORION_REPO: "https://github.com/paigerube14/orion.git"
      BRANCH: "payload_examples"
    test:
    - ref: openshift-qe-orion
- as: get-orion-results-415
  cluster: build05
  cron: 0 0 * * MON
  steps:
    env:
      ES_TYPE: internal
      HUNTER_ANALYZE: "true"
      CONFIG: "examples/payload-scale-415.yaml"
      ORION_REPO: "https://github.com/paigerube14/orion.git"
      BRANCH: "payload_examples"
    test:
    - ref: openshift-qe-orion
zz_generated_metadata:
  branch: main
  org: openshift-qe
  repo: ocp-qe-perfscale-ci
  variant: write_jobs_to_gsheet
