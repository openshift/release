build_root:
  image_stream_tag:
    name: builder
    namespace: ocp
    tag: rhel-9-golang-1.26-openshift-5.0
prowgen:
  expose: true
  private: true
resources:
  '*':
    requests:
      cpu: "1"
      memory: 1Gi
tests:
- as: integration
  commands: |
    export HOME=/tmp
    export GOFLAGS=-mod=readonly
    make test-envtest
  container:
    from: src
zz_generated_metadata:
  branch: main
  org: openshift-hyperfleet
  repo: hyperfleet-applier
  variant: presubmits
