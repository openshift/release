base_images:
  cli:
    name: "4.14"
    namespace: ocp
    tag: cli
releases:
  latest:
    candidate:
      product: ocp
      stream: nightly
      version: "4.14"
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: csb-interop-aws-latest
  cron: 0 6 25 12 *
  steps:
    cluster_profile: aws-cspi-qe
    env:
      BASE_DOMAIN: cspilp.interop.ccitredhat.com
      FIREWATCH_CONFIG: |
        {
          "failure_rules":
            [
              {"step": "ipi-*", "failure_type": "all", "classification": "Infrastructure Provisioning - Cluster"},
              {"step": "gather-*", "failure_type": "pod_failure", "classification": "Other"},
              {"step": "gather-*", "failure_type": "test_failure", "classification": "NONE", "jira_project": "NONE", "ignore": "true"},
              {"step": "openshift-*", "failure_type": "all", "classification": "Other"},
              {"step": "csb-*", "failure_type": "test_failure", "classification": "Test Failure-latest", "jira_project": "CSB", "jira_epic": "CSB-2628"}
            ]
        }
      FIREWATCH_DEFAULT_JIRA_PROJECT: LPINTEROP
    test:
    - ref: csb-deploy-resources
    - ref: csb-run-tests
    workflow: firewatch-ipi-aws
zz_generated_metadata:
  branch: main
  org: jboss-fuse
  repo: csb-tnb-tests-container
  variant: csb-ocp4.14-lp-interop
