base_images:
  base:
    name: "4.12" # Specify a suitable OpenShift base image version
    namespace: ocp
    tag: base
build_root:
  from_repository: true
canonical_go_repository: github.com/korrel8r/korrel8r
images:
- dockerfile_path: Dockerfile.ocp # If a specific Dockerfile is needed for CI
  from: base
  to: korrel8r-image
tests:
- as: unit
  commands: |
    # Example commands to run unit tests
    go test ./...
  container: test
