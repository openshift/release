base_images:
  cli:
    name: "4.16"
    namespace: ocp
    tag: cli
releases:
  latest:
    candidate:
      product: ocp
      stream: nightly
      version: "4.16"
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: security-test
  cron: 0 0 * * *
  steps:
    cluster_profile: aws
    env:
      SLEEP_DURATION: 1h
    test:
    - ref: ipi-conf
    - ref: wait
zz_generated_metadata:
  branch: master
  org: 3scale
  repo: 3scale-operator
  variant: security
