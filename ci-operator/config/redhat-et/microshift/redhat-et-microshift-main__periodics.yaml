base_images:
  cli:
    name: "4.8"
    namespace: ocp
    tag: cli
  libvirt-installer:
    name: "4.10"
    namespace: ocp
    tag: libvirt-installer
  test-bin:
    name: "4.8"
    namespace: ocp
    tag: tests
build_root:
  image_stream_tag:
    name: release
    namespace: openshift
    tag: rhel-8-release-golang-1.16-openshift-4.10
promotion:
  name: microshift
  namespace: microshift
resources:
  '*':
    limits:
      memory: 4Gi
    requests:
      cpu: 100m
      memory: 200Mi
tag_specification:
  name: "4.8"
  namespace: ocp
tests:
- as: copr-install-periodic-health-check
  cron: 0 0 * * *
  steps:
    cluster_profile: gcp
    workflow: redhat-et-microshift-e2e-copr-rpm-validation
- as: copr-install-periodic-health-check-debug
  steps:
    cluster_profile: gcp
    workflow: redhat-et-microshift-e2e-copr-rpm-validation
zz_generated_metadata:
  branch: main
  org: redhat-et
  repo: microshift
  variant: periodics
