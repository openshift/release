build_root:
  image_stream_tag:
    name: release
    namespace: openshift
    tag: golang-1.19
canonical_go_repository: github.com/openshift/velero-plugin-for-gcp
images:
- dockerfile_path: Dockerfile.ubi
  to: velero-plugin-for-gcp-oadp-1.0
promotion:
  to:
  - disabled: true
    name: velero-plugin-for-gcp
    namespace: konveyor
releases:
  latest:
    release:
      channel: fast
      version: "4.11"
resources:
  '*':
    limits:
      memory: 4Gi
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: unit-test
  optional: true
  steps:
    test:
    - as: unit
      commands: go mod vendor && go test -v ./...
      from: src
      resources:
        requests:
          cpu: 100m
          memory: 200Mi
zz_generated_metadata:
  branch: oadp-1.0
  org: openshift-priv
  repo: velero-plugin-for-gcp
