build_root:
  from_repository: true
canonical_go_repository: github.com/openshift/run-once-duration-override
images:
- dockerfile_path: Dockerfile.rhel7
  to: run-once-duration-override-webhook
promotion:
  to:
  - name: 4.16-priv
    namespace: ocp-private
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
tag_specification:
  name: 4.16-priv
  namespace: ocp-private
tests:
- as: verify
  commands: make verify-gofmt
  container:
    from: src
- as: unit
  commands: make test-unit
  container:
    from: src
- as: e2e-aws-operator
  steps:
    cluster_profile: aws
    test:
    - as: test
      cli: latest
      commands: make test-e2e
      from: src
      resources:
        requests:
          cpu: 100m
    workflow: ipi-aws
- as: security
  optional: true
  steps:
    workflow: openshift-ci-security
zz_generated_metadata:
  branch: release-4.16
  org: openshift-priv
  repo: run-once-duration-override
