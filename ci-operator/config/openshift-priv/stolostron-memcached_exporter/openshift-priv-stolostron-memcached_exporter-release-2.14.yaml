base_images:
  base:
    name: ubi-minimal
    namespace: ocp
    tag: "9"
build_root:
  image_stream_tag:
    name: builder
    namespace: stolostron
    tag: go1.23-linux
canonical_go_repository: github.com/stolostron/memcached_exporter
images:
  items:
  - dockerfile_path: Dockerfile.prow
    from: base
    to: memcached-exporter
promotion:
  to:
  - disabled: true
    name: "2.14"
    namespace: stolostron
prowgen:
  private: true
resources:
  '*':
    limits:
      memory: 6Gi
    requests:
      cpu: 100m
      memory: 1Gi
test_binary_build_commands: "true"
tests:
- as: pr-image-mirror
  steps:
    dependencies:
      SOURCE_IMAGE_REF: memcached-exporter
    env:
      IMAGE_REPO: memcached-exporter
      REGISTRY_ORG: stolostron
    workflow: ocm-ci-image-mirror
zz_generated_metadata:
  branch: release-2.14
  org: openshift-priv
  repo: stolostron-memcached_exporter
