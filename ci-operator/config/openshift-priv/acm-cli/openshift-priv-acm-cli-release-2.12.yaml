base_images:
  base:
    name: ubi-minimal
    namespace: ocp
    tag: "9"
build_root:
  from_repository: true
canonical_go_repository: github.com/stolostron/acm-cli
images:
  items:
  - dockerfile_path: Dockerfile
    from: base
    optional: true
    to: acm-cli
promotion:
  to:
  - disabled: true
    name: "2.12"
    namespace: stolostron
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
zz_generated_metadata:
  branch: release-2.12
  org: openshift-priv
  repo: acm-cli
