build_root:
  image_stream_tag:
    name: release
    namespace: openshift
    tag: golang-1.25
canonical_go_repository: github.com/openshift/vcf-migration-operator
promotion:
  to:
  - name: 5.0-priv
    namespace: ocp-private
releases:
  initial:
    integration:
      name: 5.0-priv
      namespace: ocp-private
  latest:
    integration:
      include_built_images: true
      name: 5.0-priv
      namespace: ocp-private
resources:
  '*':
    limits:
      memory: 4Gi
    requests:
      cpu: 100m
      memory: 200Mi
zz_generated_metadata:
  branch: release-5.0
  org: openshift-priv
  repo: vcf-migration-operator
