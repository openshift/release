base_images:
  base:
    name: ubi-minimal
    namespace: ocp
    tag: "9"
binary_build_commands: make build
build_root:
  from_repository: true
canonical_go_repository: github.com/stolostron/governance-policy-addon-controller
images:
  items:
  - dockerfile_path: build/Dockerfile
    from: base
    optional: true
    to: governance-policy-addon-controller
promotion:
  to:
  - disabled: true
    name: "2.13"
    namespace: stolostron
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
zz_generated_metadata:
  branch: release-2.13
  org: openshift-priv
  repo: stolostron-governance-policy-addon-controller
