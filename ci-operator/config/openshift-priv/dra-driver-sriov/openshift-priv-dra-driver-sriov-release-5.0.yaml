build_root:
  from_repository: true
canonical_go_repository: github.com/openshift/dra-driver-sriov
images:
  items:
  - dockerfile_path: Dockerfile.ocp
    to: dra-driver-sriov
promotion:
  to:
  - disabled: true
    name: sriov-5.0
    namespace: origin
  - name: "5.0"
    namespace: ocp-priv
prowgen:
  private: true
releases:
  initial:
    integration:
      name: "5.0"
      namespace: ocp-priv
  latest:
    integration:
      include_built_images: true
      name: "5.0"
      namespace: ocp-priv
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: unit
  commands: GOFLAGS="-mod=mod" make test
  container:
    from: src
zz_generated_metadata:
  branch: release-5.0
  org: openshift-priv
  repo: dra-driver-sriov
