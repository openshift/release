base_images:
  base:
    name: ubi-minimal
    namespace: ocp
    tag: "9"
build_root:
  image_stream_tag:
    name: builder
    namespace: stolostron
    tag: go1.25-linux
canonical_go_repository: github.com/stolostron/backplane-must-gather
images:
- dockerfile_path: Dockerfile
  from: base
  to: backplane-must-gather
promotion:
  to:
  - disabled: true
    name: "2.7"
    namespace: stolostron
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
zz_generated_metadata:
  branch: backplane-2.7
  org: openshift-priv
  repo: backplane-must-gather
