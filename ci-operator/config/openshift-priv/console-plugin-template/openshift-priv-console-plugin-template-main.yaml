build_root:
  from_repository: true
canonical_go_repository: github.com/openshift/console-plugin-template
images:
  items:
  - dockerfile_path: Dockerfile
    to: console-plugin-template
promotion:
  to:
  - name: 5.0-priv
    namespace: ocp-private
prowgen:
  private: true
releases:
  initial:
    integration:
      name: 5.0-priv
      namespace: ocp-private
  latest:
    integration:
      include_built_images: true
      name: 5.0-priv
      namespace: ocp-private
resources:
  '*':
    limits:
      memory: 4Gi
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: frontend
  commands: |
    git config --global --add safe.directory /go/src/github.com/openshift/console-plugin-template
    ./test-frontend.sh
  container:
    from: src
- as: e2e-aws-plugin-template
  steps:
    cluster_profile: openshift-org-aws
    test:
    - as: test
      cli: latest
      commands: ./test-prow-e2e.sh
      dependencies:
      - env: PLUGIN_TEMPLATE_PULL_SPEC
        name: console-plugin-template
      from: src
      grace_period: 30m0s
      resources:
        requests:
          cpu: 100m
      timeout: 3h0m0s
    workflow: openshift-e2e-aws
zz_generated_metadata:
  branch: main
  org: openshift-priv
  repo: console-plugin-template
