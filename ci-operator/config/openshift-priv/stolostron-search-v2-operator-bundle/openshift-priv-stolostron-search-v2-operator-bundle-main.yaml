base_images:
  base:
    name: ubi-minimal
    namespace: ocp
    tag: "8"
build_root:
  image_stream_tag:
    name: builder
    namespace: stolostron
    tag: go1.18-linux
canonical_go_repository: github.com/stolostron/search-v2-operator-bundle
images:
  items:
  - dockerfile_path: Dockerfile
    from: base
    to: search-operator-bundle
promotion:
  to:
  - disabled: true
    name: "2.12"
    namespace: stolostron
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: pr-image-mirror
  steps:
    dependencies:
      SOURCE_IMAGE_REF: search-operator-bundle
    env:
      IMAGE_REPO: search-operator-bundle
      REGISTRY_ORG: stolostron
    workflow: ocm-ci-image-mirror
zz_generated_metadata:
  branch: main
  org: openshift-priv
  repo: stolostron-search-v2-operator-bundle
