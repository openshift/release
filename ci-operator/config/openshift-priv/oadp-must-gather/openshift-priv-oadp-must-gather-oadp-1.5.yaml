build_root:
  image_stream_tag:
    name: builder
    namespace: konveyor
    tag: ubi9-v1.23
canonical_go_repository: github.com/openshift/oadp-must-gather
images:
- additional_architectures:
  - arm64
  dockerfile_path: Dockerfile
  to: oadp-must-gather-1.5
promotion:
  to:
  - disabled: true
    name: oadp-must-gather
    namespace: konveyor
resources:
  '*':
    limits:
      memory: 4Gi
    requests:
      cpu: 100m
      memory: 200Mi
zz_generated_metadata:
  branch: oadp-1.5
  org: openshift-priv
  repo: oadp-must-gather
