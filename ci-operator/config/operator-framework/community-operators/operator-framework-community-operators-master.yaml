base_images:
  cli:
    name: "4.6"
    namespace: ocp
    tag: cli
build_root:
  project_image:
    dockerfile_path: scripts/ci/Dockerfile.ci-operator
resources:
  '*':
    requests:
      cpu: 1000m
      memory: 4Gi
tag_specification:
  name: "4.6"
  namespace: ocp
tests:
- as: free-test1
  commands: |
    echo
  openshift_installer_src:
    cluster_profile: aws
- as: free-test2
  commands: |
    echo
  openshift_installer_src:
    cluster_profile: aws
- as: free-test3
  commands: |
    echo
    echo "git log"
    git --no-pager log --oneline|head
    echo
    echo "finished:)"
  openshift_installer_src:
    cluster_profile: aws
- as: free-test4
  commands: |
    echo
    echo "git log"
    GIT_PAGER="cut -c 1-${COLUMNS-80}" git --no-pager log --oneline|head
    echo
    echo "finished:)"
  openshift_installer_src:
    cluster_profile: aws
- as: src-test
  commands: |
    echo "Need to clone default branch, cloning..."
    mkdir -p /tmp/oper
    cd /tmp/oper
    git clone https://github.com/J0zi/community-operators.git
    cd community-operators
    git checkout RHO-716-release-on-openshift
    ls
    scripts/ci/openshift-deploy.sh
  openshift_installer_src:
    cluster_profile: aws
zz_generated_metadata:
  branch: master
  org: operator-framework
  repo: community-operators
