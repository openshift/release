binary_build_commands: make build
build_root:
  from_repository: true
resources:
  '*':
    limits:
      memory: 4Gi
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: check-format
  commands: make check-format
  container:
    from: src
zz_generated_metadata:
  branch: main
  org: kubev2v
  repo: migration-planner
