build_root:
  image_stream_tag:
    name: release
    namespace: openshift
    tag: rhel-9-release-golang-1.25-openshift-5.0
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: echo
  commands: echo ok
  container:
    from: src
zz_generated_metadata:
  branch: main
  org: cyborg-verse
  repo: bp-parity-checks
