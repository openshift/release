base_images:
  os:
    name: centos
    namespace: openshift
    tag: "7"
releases:
  latest:
    release:
      channel: stable
      version: "4.7"
resources:
  '*':
    limits:
      memory: 4Gi
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: test-e2e
  cron: '@yearly'
  steps:
    test:
    - as: test-e2e
      cli: latest
      commands: |
        oc get nodes
      dependencies:
      - env: RELEASE_IMAGE_LATEST
        name: registry.ci.openshift.org/ocp/release:4.6
      from: os
      resources:
        requests:
          cpu: 100m
    workflow: upi-ibmz-eco
zz_generated_metadata:
  branch: master
  org: ibmz-eco
  repo: isv-operator
