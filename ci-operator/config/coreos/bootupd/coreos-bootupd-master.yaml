binary_build_commands: make
build_root:
  image_stream_tag:
    name: cosa-buildroot
    namespace: coreos
    tag: latest
resources:
  '*':
    requests:
      cpu: 1000m
      memory: 3Gi
tests:
- as: test
  commands: ./target/release/bootupd --help
  container:
    from: bin
zz_generated_metadata:
  branch: master
  org: coreos
  repo: bootupd
