build_root:
  image_stream_tag:
    name: release
    namespace: openshift
    tag: rhel-9-release-golang-1.24-openshift-4.21
resources:
  '*':
    limits:
      memory: 4Gi
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: temp
  commands: echo "Test"
  container:
    from: src
zz_generated_metadata:
  branch: main
  org: osac-project
  repo: osac-operator
