# quay/quay CI config

The files listed in `_generator/matrix.yaml.in`'s `managed_files` are generated; do not hand-edit them.

Regenerate with `make -C ci-operator/config/quay/quay/_generator generate`, then run `make update` from the repo root. See `_generator/README.md` for the generator itself.
