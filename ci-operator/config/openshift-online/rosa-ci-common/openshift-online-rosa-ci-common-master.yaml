build_root:
  image_stream_tag:
    name: release
    namespace: openshift
    tag: rhel-9-release-golang-1.24-openshift-4.22
prowgen:
  expose: true
  private: true
resources:
  '*':
    limits:
      memory: 4Gi
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: test
  commands: go test -mod=mod ./test/...
  container:
    from: src
zz_generated_metadata:
  branch: master
  org: openshift-online
  repo: rosa-ci-common
