build_root:
  image_stream_tag:
    name: release
    namespace: openshift
    tag: rhel-9-release-golang-1.23-openshift-4.18
prowgen:
  expose: true
  private: true
resources:
  '*':
    limits:
      memory: 4Gi
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: test
  commands: unset GOFLAGS && make test
  container:
    from: src
zz_generated_metadata:
  branch: master
  org: openshift-online
  repo: rosa-clusters-cleaner
