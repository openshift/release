build_root:
  image_stream_tag:
    name: release
    namespace: openshift
    tag: rhel-9-release-golang-1.25-openshift-4.23
prowgen:
  enable_secrets_store_csi_driver: true
  private: true
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: validate-dashboards
  commands: |
    python3 hack/validate-dashboards.py
  container:
    from: src
zz_generated_metadata:
  branch: main
  org: openshift-online
  repo: rosa-rhobs-dashboards
