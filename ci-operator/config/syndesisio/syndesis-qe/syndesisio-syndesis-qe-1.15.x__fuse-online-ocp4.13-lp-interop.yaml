base_images:
  cli:
    name: "4.13"
    namespace: ocp
    tag: cli
build_root:
  image_stream_tag:
    name: release
    namespace: openshift
    tag: golang-1.19
images:
- context_dir: docker
  to: base
- context_dir: docker
  inputs:
    syndesis-build:
      as:
      - pipeline:buildSyndesis
  to: new-base
- dockerfile_literal: |-
    FROM cli
    RUN echo "test replacing build step"
  from: cli
  to: syndesis-build
releases:
  latest:
    candidate:
      product: ocp
      stream: nightly
      version: "4.13"
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: fuse-online-aws
  cron: 0 1 * * 1
  steps:
    cluster_profile: aws-cspi-qe
    env:
      BASE_DOMAIN: cspilp.interop.ccitredhat.com
    workflow: ipi-aws
zz_generated_metadata:
  branch: 1.15.x
  org: syndesisio
  repo: syndesis-qe
  variant: fuse-online-ocp4.13-lp-interop
