build_root:
  project_image:
    context_dir: kernel-crawler
    dockerfile_path: Dockerfile.ubi
resources:
  '*':
    limits:
      memory: 4Gi
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: unittests
  commands: go test -mod=mod -v ./...
  container:
    from: src
- as: crawl-kernels
  steps:
    test:
    - as: crawl
      commands: .openshift-ci/crawl/main.sh
      credentials:
      - mount_path: /tmp/secret/stackrox-kernel-packer-crawl
        name: stackrox-kernel-packer-crawl-test
        namespace: test-credentials
      from: src
      resources:
        requests:
          cpu: 4000m
          memory: 4000Mi
  timeout: 6h0m0s
zz_generated_metadata:
  branch: ddolgov-feature-crawling
  org: stackrox
  repo: kernel-packer
