build_root:
  project_image:
    dockerfile_path: .openshift-ci/Dockerfile.build_root
resources:
  '*':
    requests:
      cpu: 2000m
      memory: 4000Mi
tests:
- as: go-postgres-tests
  steps:
    test:
    - ref: stackrox-initial
zz_generated_metadata:
  branch: gavin-OSCI-onboard-go-postgres-tests
  org: stackrox
  repo: stackrox
