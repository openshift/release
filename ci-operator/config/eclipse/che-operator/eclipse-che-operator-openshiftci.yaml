base_images:
  cli:
    name: "4.5"
    namespace: ocp
    tag: cli
  cli-jq:
    name: cli-jq
    namespace: ocp
    tag: latest
resources:
  '*':
    limits:
      memory: 4Gi
    requests:
      cpu: 100m
      memory: 200Mi
tag_specification:
  name: "4.5"
  namespace: ocp
tests:
- as: e2e-operator
  commands: |
    jq --version
    yq --version
  openshift_installer_src:
    cluster_profile: aws
zz_generated_metadata:
  branch: openshiftci
  org: eclipse
  repo: che-operator
