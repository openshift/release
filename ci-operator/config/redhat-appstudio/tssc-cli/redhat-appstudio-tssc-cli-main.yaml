build_root:
  image_stream_tag:
    name: release
    namespace: openshift
    tag: golang-1.22
resources:
  '*':
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: test
  commands: echo "Hello, World!"
  container:
    from: src
  cron: 0 6 1 1 *
zz_generated_metadata:
  branch: main
  org: redhat-appstudio
  repo: tssc-cli
