base_images:
  yamllint:
    name: yamllint
    namespace: ci
    tag: latest
build_root:
  image_stream_tag:
    name: release
    namespace: openshift
    tag: rhel-9-release-golang-1.24-openshift-4.23
prowgen:
  enable_secrets_store_csi_driver: true
resources:
  '*':
    limits:
      memory: 4Gi
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: yamllint
  commands: yamllint .
  container:
    from: yamllint
zz_generated_metadata:
  branch: main
  org: redhat-appstudio
  repo: infra-common-deployments
