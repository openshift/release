presubmits:
  openshift/kube-state-metrics:
  - agent: kubernetes
    always_run: true
    branches:
    - openshift-master
    context: ci/prow/images
    decorate: true
    name: pull-ci-openshift-kube-state-metrics-openshift-master-images
    rerun_command: /test images
    skip_cloning: true
    spec:
      containers:
      - args:
        - --artifact-dir=$(ARTIFACTS)
        - --give-pr-author-access-to-namespace=true
        - --target=[images]
        - --target=[release:latest]
        command:
        - ci-operator
        env:
        - name: CONFIG_SPEC
          valueFrom:
            configMapKeyRef:
              key: openshift-kube-state-metrics-openshift-master-presubmits.yaml
              name: ci-operator-openshift-kube-state-metrics
        image: ci-operator:latest
        imagePullPolicy: Always
        name: ""
        resources:
          limits:
            cpu: 500m
          requests:
            cpu: 10m
      serviceAccountName: ci-operator
    trigger: ((?m)^/test( all| images),?(\s+|$))
