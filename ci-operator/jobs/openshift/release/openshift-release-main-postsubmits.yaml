postsubmits:
  openshift/release:
  - agent: kubernetes
    branches:
    - ^main$
    cluster: app.ci
    decorate: true
    labels:
      ci.openshift.io/role: infra
    max_concurrency: 1
    name: branch-ci-openshift-release-main-config-change-trigger
    spec:
      containers:
      - args:
        - --candidate-path=.
        - --github-token-path=/etc/github/oauth
        - --github-endpoint=http://ghproxy
        - --github-endpoint=https://api.github.com
        - --github-graphql-endpoint=http://ghproxy/graphql
        - --github-graphql-endpoint=https://api.github.com/graphql
        - --dry-run=false
        command:
        - config-change-trigger
        image: quay-proxy.ci.openshift.org/openshift/ci:ci_config-change-trigger_latest
        imagePullPolicy: Always
        name: ""
        resources:
          requests:
            cpu: 10m
        volumeMounts:
        - mountPath: /etc/github
          name: token
          readOnly: true
      serviceAccountName: hook
      volumes:
      - name: token
        secret:
          secretName: github-credentials-openshift-ci-robot
  - agent: kubernetes
    branches:
    - ^main$
    cluster: app.ci
    decorate: true
    decoration_config:
      grace_period: 0s
      timeout: 1h0m0s
    labels:
      ci.openshift.io/role: infra
    max_concurrency: 1
    name: branch-ci-openshift-release-main-gsm-secrets-reconciler
    run_if_changed: core-services/sync-rover-groups/_config.yaml
    spec:
      containers:
      - args:
        - --config=./core-services/sync-rover-groups/_config.yaml
        - --log-level=info
        - --gcp-service-account-key-file=/tmp/gcp-credentials/key.json
        command:
        - gsm-secret-sync
        env:
        - name: GCP_PROJECT_ID
          value: openshift-ci-secrets
        - name: GCP_PROJECT_NUMBER
          value: "384486694155"
        image: quay-proxy.ci.openshift.org/openshift/ci:ci_gsm-secret-sync_latest
        imagePullPolicy: Always
        name: ""
        resources:
          requests:
            cpu: 100m
        volumeMounts:
        - mountPath: /tmp/gcp-credentials
          name: gsm-secrets-reconciler-sa-key
          readOnly: true
      volumes:
      - csi:
          driver: secrets-store.csi.k8s.io
          readOnly: true
          volumeAttributes:
            secretProviderClass: gsm-secrets-reconciler
        name: gsm-secrets-reconciler-sa-key
  - agent: kubernetes
    branches:
    - ^main$
    cluster: app.ci
    decorate: true
    decoration_config:
      grace_period: 0s
      timeout: 8h0m0s
    labels:
      ci.openshift.io/role: infra
    max_concurrency: 1
    name: branch-ci-openshift-release-main-label-sync
    run_if_changed: core-services/prow/02_config/_labels.yaml
    spec:
      containers:
      - args:
        - --config=./core-services/prow/02_config/_labels.yaml
        - --confirm=true
        - --orgs=openshift,cri-o,operator-framework,openshift-priv,redhat-openshift-ecosystem,openshift-psap,openshift-metal3,shipwright-io,openshift-assisted,openshift-eng
        - --token=/etc/github/oauth
        - --endpoint=http://ghproxy
        - --debug
        command:
        - /ko-app/label_sync
        image: quay.io/openshift/ci:ci_label-sync_latest
        imagePullPolicy: Always
        name: ""
        resources:
          requests:
            cpu: 500m
        volumeMounts:
        - mountPath: /etc/github
          name: token
          readOnly: true
      volumes:
      - name: token
        secret:
          secretName: github-credentials-openshift-ci-robot
  - agent: kubernetes
    always_run: true
    branches:
    - ^main$
    cluster: build02
    decorate: true
    decoration_config:
      skip_cloning: true
    labels:
      ci-operator.openshift.io/is-promotion: "true"
      ci-operator.openshift.io/variant: ptp-upstream-images
      ci.openshift.io/generator: prowgen
    max_concurrency: 1
    name: branch-ci-openshift-release-main-ptp-upstream-images-images
    spec:
      containers:
      - args:
        - --gcs-upload-secret=/secrets/gcs/service-account.json
        - --image-import-pull-secret=/etc/pull-secret/.dockerconfigjson
        - --image-mirror-push-secret=/etc/push-secret/.dockerconfigjson
        - --promote
        - --report-credentials-file=/etc/report/credentials
        - --target=[images]
        - --variant=ptp-upstream-images
        command:
        - ci-operator
        image: quay-proxy.ci.openshift.org/openshift/ci:ci_ci-operator_latest
        imagePullPolicy: Always
        name: ""
        resources:
          requests:
            cpu: 10m
        volumeMounts:
        - mountPath: /secrets/gcs
          name: gcs-credentials
          readOnly: true
        - mountPath: /secrets/manifest-tool
          name: manifest-tool-local-pusher
          readOnly: true
        - mountPath: /etc/pull-secret
          name: pull-secret
          readOnly: true
        - mountPath: /etc/push-secret
          name: push-secret
          readOnly: true
        - mountPath: /etc/report
          name: result-aggregator
          readOnly: true
      serviceAccountName: ci-operator
      volumes:
      - name: manifest-tool-local-pusher
        secret:
          secretName: manifest-tool-local-pusher
      - name: pull-secret
        secret:
          secretName: registry-pull-credentials
      - name: push-secret
        secret:
          secretName: registry-push-credentials-ci-central
      - name: result-aggregator
        secret:
          secretName: result-aggregator
  - agent: kubernetes
    branches:
    - ^main$
    cluster: core-ci
    decorate: true
    labels:
      ci.openshift.io/role: infra
    max_concurrency: 1
    name: branch-ci-openshift-release-main-release-controller-annotate
    spec:
      containers:
      - command:
        - hack/annotate.sh
        env:
        - name: KUBECONFIG
          value: /etc/build-farm-credentials/kubeconfig
        image: quay-proxy.ci.openshift.org/openshift/ci:ocp_cli-jq_latest
        imagePullPolicy: Always
        name: ""
        resources:
          requests:
            cpu: 10m
        volumeMounts:
        - mountPath: /etc/build-farm-credentials
          name: build-farm-credentials
          readOnly: true
      volumes:
      - name: build-farm-credentials
        secret:
          items:
          - key: sa.config-updater.app.ci.config
            path: kubeconfig
          secretName: config-updater
  - agent: kubernetes
    branches:
    - ^main$
    cluster: core-ci
    decorate: true
    labels:
      ci.openshift.io/build-farm: vsphere02
      ci.openshift.io/generator: cluster-init
      ci.openshift.io/role: infra
    max_concurrency: 1
    name: branch-ci-openshift-release-main-vsphere02-apply
    spec:
      containers:
      - args:
        - --as=
        - --config-dir=clusters/build-clusters/vsphere02
        - --confirm=true
        - --kubeconfig=/etc/build-farm-credentials/kubeconfig
        command:
        - applyconfig
        image: quay.io/openshift/ci-public:ci_applyconfig_latest
        imagePullPolicy: Always
        name: ""
        resources:
          requests:
            cpu: 10m
        volumeMounts:
        - mountPath: /etc/build-farm-credentials
          name: build-farm-credentials
          readOnly: true
      volumes:
      - name: build-farm-credentials
        secret:
          items:
          - key: sa.config-updater.vsphere02.config
            path: kubeconfig
          secretName: config-updater
