presubmits:
  kubevirt/kubevirt:
  - agent: kubernetes
    always_run: true
    branches:
    - master
    context: ci/prow/kubevirt-e2e-test
    decorate: true
    decoration_config:
      skip_cloning: true
    labels:
      ci-operator.openshift.io/prowgen-controlled: "true"
      pj-rehearse.openshift.io/can-be-rehearsed: "true"
    name: pull-ci-kubevirt-kubevirt-master-kubevirt-e2e-test
    path_alias: kubevirt.io/kubevirt
    rerun_command: /test kubevirt-e2e-test
    spec:
      containers:
      - args:
        - --artifact-dir=$(ARTIFACTS)
        - --branch=master
        - --give-pr-author-access-to-namespace=true
        - --lease-server=http://boskos
        - --org=kubevirt
        - --repo=kubevirt
        - --resolver-address=http://ci-operator-configresolver-ci.svc.ci.openshift.org
        - --secret-dir=/usr/local/kubevirt-e2e-test-cluster-profile
        - --sentry-dsn-path=/etc/sentry-dsn/ci-operator
        - --target=kubevirt-e2e-test
        - --template=/usr/local/kubevirt-e2e-test
        command:
        - ci-operator
        env:
        - name: CLUSTER_TYPE
          value: gcp
        - name: CONFIG_SPEC
          valueFrom:
            configMapKeyRef:
              key: kubevirt-kubevirt-master.yaml
              name: ci-operator-master-configs
        - name: JOB_NAME_SAFE
          value: kubevirt-e2e-test
        - name: TEST_COMMAND
          value: |2

            set -euo pipefail

            echo "fetching oc and kubectl for deployment"
            export oc_filename='oc.tar.gz'
            export oc_dirname="${oc_filename%.tar.gz}"
            export oc_download_url_base='https://mirror.openshift.com/pub/openshift-v4/clients/oc/4.2/linux/'
            (
              mkdir ${oc_dirname}
              cd ${oc_dirname}
              curl -L -O -o ${oc_filename} ${oc_download_url_base}/${oc_filename}
              tar zxvf ${oc_filename}
              ln -s $(pwd)/${oc_dirname} kubectl
            ) || ( echo "failed to download ${oc_filename}"; exit 1 )
            export PATH=$(pwd)/${oc_dirname}:$PATH
            which kubectl
            which oc

            echo "checking nodes for cluster"
            oc get nodes

            echo "checking configuration"
            env|grep KUBE
            kubectl config view

            export DOCKER_PREFIX='dhiller'
            export DOCKER_TAG="latest"
            export KUBEVIRT_PROVIDER=external

            export GIMME_GO_VERSION=1.12.8
            export GOPATH="/go"
            export GOBIN="/usr/bin"
            source /etc/profile.d/gimme.sh

            echo "creating manifests"
            bash -x ./hack/build-manifests.sh

            echo "checking configuration location"
            echo "KUBECONFIG: ${KUBECONFIG}"

            echo "patching cluster-up external provider"
            curl -L -O -o cluster-up/cluster/external/provider.sh https://github.com/dhiller/kubevirtci/blob/fix-external-provider/cluster-up/cluster/external/provider.sh

            echo "calling cluster-up to prepare config and check whether cluster is reachable"
            bash -x ./cluster-up/up.sh

            echo "checking cluster configuration after config prep"
            kubectl config view

            echo "deploying"
            bash -x ./hack/cluster-deploy.sh

            echo "checking pods for kubevirt"
            oc get pods -n kubevirt

            echo "testing"
            go get -u github.com/onsi/ginkgo/ginkgo
            bash -x ./hack/build-func-tests.sh
            bash -x ./hack/functests.sh
        image: ci-operator:latest
        imagePullPolicy: Always
        name: ""
        resources:
          requests:
            cpu: 10m
        volumeMounts:
        - mountPath: /usr/local/kubevirt-e2e-test-cluster-profile
          name: cluster-profile
        - mountPath: /usr/local/kubevirt-e2e-test
          name: job-definition
          subPath: cluster-launch-installer-src.yaml
        - mountPath: /etc/sentry-dsn
          name: sentry-dsn
          readOnly: true
      serviceAccountName: ci-operator
      volumes:
      - name: cluster-profile
        projected:
          sources:
          - secret:
              name: cluster-secrets-gcp
          - configMap:
              name: cluster-profile-gcp
      - configMap:
          name: prow-job-cluster-launch-installer-src
        name: job-definition
      - name: sentry-dsn
        secret:
          secretName: sentry-dsn
    trigger: (?m)^/test( | .* )kubevirt-e2e-test,?($|\s.*)
