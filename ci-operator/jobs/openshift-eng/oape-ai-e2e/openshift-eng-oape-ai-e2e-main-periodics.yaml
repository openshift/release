periodics:
- agent: kubernetes
  cluster: build13
  cron: 0 6 * * 1-5
  decorate: true
  decoration_config:
    sparse_checkout_files:
    - images/agent-worker.Dockerfile
    - images/gh-token-minter.Dockerfile
    - images/go-server.Dockerfile
    - images/review-handler.Dockerfile
    - prow-workflow/input.Dockerfile
  extra_refs:
  - base_ref: main
    org: openshift-eng
    repo: oape-ai-e2e
    sparse_checkout_files:
    - images/agent-worker.Dockerfile
    - images/gh-token-minter.Dockerfile
    - images/go-server.Dockerfile
    - images/review-handler.Dockerfile
    - prow-workflow/input.Dockerfile
  labels:
    ci.openshift.io/generator: prowgen
    pj-rehearse.openshift.io/can-be-rehearsed: "true"
  name: periodic-ci-openshift-eng-oape-ai-e2e-main-periodic-cve-agent
  spec:
    containers:
    - args:
      - --gcs-upload-secret=/secrets/gcs/service-account.json
      - --image-import-pull-secret=/etc/pull-secret/.dockerconfigjson
      - --lease-server-credentials-file=/etc/boskos/credentials
      - --report-credentials-file=/etc/report/credentials
      - --target=periodic-cve-agent
      command:
      - ci-operator
      env:
      - name: HTTP_SERVER_IP
        valueFrom:
          fieldRef:
            fieldPath: status.podIP
      image: quay-proxy.ci.openshift.org/openshift/ci:ci_ci-operator_latest
      imagePullPolicy: Always
      name: ""
      ports:
      - containerPort: 8080
        name: http
      resources:
        requests:
          cpu: 10m
      volumeMounts:
      - mountPath: /etc/boskos
        name: boskos
        readOnly: true
      - mountPath: /secrets/gcs
        name: gcs-credentials
        readOnly: true
      - mountPath: /secrets/manifest-tool
        name: manifest-tool-local-pusher
        readOnly: true
      - mountPath: /etc/pull-secret
        name: pull-secret
        readOnly: true
      - mountPath: /etc/report
        name: result-aggregator
        readOnly: true
    serviceAccountName: ci-operator
    volumes:
    - name: boskos
      secret:
        items:
        - key: credentials
          path: credentials
        secretName: boskos-credentials
    - name: manifest-tool-local-pusher
      secret:
        secretName: manifest-tool-local-pusher
    - name: pull-secret
      secret:
        secretName: registry-pull-credentials
    - name: result-aggregator
      secret:
        secretName: result-aggregator
- agent: kubernetes
  cluster: build13
  cron: '@yearly'
  decorate: true
  decoration_config:
    sparse_checkout_files:
    - images/agent-worker.Dockerfile
    - images/gh-token-minter.Dockerfile
    - images/go-server.Dockerfile
    - images/review-handler.Dockerfile
    - prow-workflow/input.Dockerfile
  extra_refs:
  - base_ref: main
    org: openshift-eng
    repo: oape-ai-e2e
    sparse_checkout_files:
    - images/agent-worker.Dockerfile
    - images/gh-token-minter.Dockerfile
    - images/go-server.Dockerfile
    - images/review-handler.Dockerfile
    - prow-workflow/input.Dockerfile
  labels:
    ci.openshift.io/generator: prowgen
    pj-rehearse.openshift.io/can-be-rehearsed: "true"
  name: periodic-ci-openshift-eng-oape-ai-e2e-main-periodic-cve-agent-ondemand
  spec:
    containers:
    - args:
      - --gcs-upload-secret=/secrets/gcs/service-account.json
      - --image-import-pull-secret=/etc/pull-secret/.dockerconfigjson
      - --lease-server-credentials-file=/etc/boskos/credentials
      - --report-credentials-file=/etc/report/credentials
      - --target=periodic-cve-agent-ondemand
      command:
      - ci-operator
      env:
      - name: HTTP_SERVER_IP
        valueFrom:
          fieldRef:
            fieldPath: status.podIP
      image: quay-proxy.ci.openshift.org/openshift/ci:ci_ci-operator_latest
      imagePullPolicy: Always
      name: ""
      ports:
      - containerPort: 8080
        name: http
      resources:
        requests:
          cpu: 10m
      volumeMounts:
      - mountPath: /etc/boskos
        name: boskos
        readOnly: true
      - mountPath: /secrets/gcs
        name: gcs-credentials
        readOnly: true
      - mountPath: /secrets/manifest-tool
        name: manifest-tool-local-pusher
        readOnly: true
      - mountPath: /etc/pull-secret
        name: pull-secret
        readOnly: true
      - mountPath: /etc/report
        name: result-aggregator
        readOnly: true
    serviceAccountName: ci-operator
    volumes:
    - name: boskos
      secret:
        items:
        - key: credentials
          path: credentials
        secretName: boskos-credentials
    - name: manifest-tool-local-pusher
      secret:
        secretName: manifest-tool-local-pusher
    - name: pull-secret
      secret:
        secretName: registry-pull-credentials
    - name: result-aggregator
      secret:
        secretName: result-aggregator
