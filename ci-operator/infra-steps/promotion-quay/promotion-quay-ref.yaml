ref:
  as: promotion-quay
  from: cli
  commands: promotion-quay-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 200Mi
  documentation: Quay promotion step that mirrors images to quay.io and creates quay-proxy ImageStream tags. This is an infrastructure step managed by DPTP and cannot be used in user workflows.
