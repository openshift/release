retester:
  max_retests_for_sha_and_base: 1
  max_retests_for_sha: 3
  orgs:
    openshift:
      enabled: true
    openshift-priv:
      enabled: true
    openshift-knative:
      enabled: true
    operator-framework:
      repos:
        operator-marketplace:
          enabled: true
        operator-registry:
          enabled: true
    cri-o:
      repos:
        cri-o:
          enabled: true
    kubevirt-ui:
      repos:
        kubevirt-plugin:
          enabled: true
