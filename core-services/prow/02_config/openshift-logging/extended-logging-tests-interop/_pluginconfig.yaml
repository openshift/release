plugins:
  openshift-logging/extended-logging-tests-interop:
    plugins:
    - trigger
triggers:
- org_invite:
    prominent: {}
  repos:
  - openshift-logging/extended-logging-tests-interop
  trusted_apps:
  - openshift-merge-bot
