branch-protection:
  orgs:
    openstack-k8s-operators:
      repos:
        osp-director-operator:
          required_status_checks:
            contexts:
            - continuous-integration/jenkins/pr-merge
            - dpulls
            - Golang lint, vet and unit test pipeline / github (govet, golint and
              gotest)
            - Golang lint, vet and unit test pipeline / pre-commit (pull_request)
            - ci/prow/unit
tide:
  queries:
  - labels:
    - approved
    - lgtm
    missingLabels:
    - backports/unvalidated-commits
    - do-not-merge/hold
    - do-not-merge/invalid-owners-file
    - do-not-merge/work-in-progress
    - jira/invalid-bug
    - needs-rebase
    repos:
    - openstack-k8s-operators/osp-director-operator
