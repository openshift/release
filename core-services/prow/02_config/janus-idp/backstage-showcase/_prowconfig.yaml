branch-protection:
  orgs:
    janus-idp:
      repos:
        backstage-showcase:
          include:
          - main
          - 0.*
          - 1.*
          protect: true
          required_status_checks:
            contexts:
            - CI / Main Job (pull_request)
            - PR Docker Build / PR Docker Build (pull_request_target)
