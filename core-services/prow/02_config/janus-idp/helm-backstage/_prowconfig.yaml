branch-protection:
  orgs:
    janus-idp:
      repos:
        helm-backstage:
          include:
          - main
          protect: true
          required_status_checks:
            contexts:
            - Lint Metadata
            - Test Latest Release
