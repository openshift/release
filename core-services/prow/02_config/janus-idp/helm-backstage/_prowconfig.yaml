branch-protection:
  orgs:
    janus-idp:
      repos:
        helm-backstage:
          include:
          - main
          protect: true
          required_status_checks:
            contexts:
            - Test Charts / Lint Metadata
            - Test Charts / Test Latest Release
