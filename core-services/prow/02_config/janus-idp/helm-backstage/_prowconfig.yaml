branch-protection:
  orgs:
    janus-idp:
      repos:
        helm-backstage:
          include:
          - main
          protect: true
          required_status_checks:
            contexts:
            - Test Charts / Lint Metadata (pull_request)
            - Test Charts / Test Latest Release (pull_request)
