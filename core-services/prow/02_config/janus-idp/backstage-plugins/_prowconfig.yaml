branch-protection:
  orgs:
    janus-idp:
      repos:
        backstage-plugins:
          include:
          - main
          protect: true
          required_status_checks:
            contexts:
            - Conventional Commits
            - Test with Node.js 20
            - Build all packages with Node.js 20
