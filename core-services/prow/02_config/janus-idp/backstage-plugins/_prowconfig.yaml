branch-protection:
  orgs:
    janus-idp:
      repos:
        backstage-plugins:
          include:
          - main
          protect: true
          required_status_checks:
            contexts:
            - Pull request / Test (pull_request)
            - Semantic Release / Conventional Commits (pull_request_target)
            - Pull request / Build all packages (pull_request)
