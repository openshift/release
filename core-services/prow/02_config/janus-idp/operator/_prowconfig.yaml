branch-protection:
  orgs:
    janus-idp:
      repos:
        operator:
          include:
          - main
          protect: true
          required_status_checks:
            contexts:
            - Makefile CI / build
            - PR Build operator, bundle, and catalog images / PR Docker Build
