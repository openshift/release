branch-protection:
  orgs:
    janus-idp:
      repos:
        operator:
          include:
          - main
          protect: true
          required_status_checks:
            contexts:
            - Makefile CI / build (pull_request)
            - PR Build operator, bundle, and catalog images / PR Build (pull_request_target)
