plugins:
  kubevirt/kubevirt-template-validator:
    plugins:
    - trigger
triggers:
- org_invite:
    prominent: {}
  repos:
  - kubevirt/kubevirt-template-validator
  trusted_apps:
  - openshift-merge-bot
