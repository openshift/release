plugins:
  kubevirt/node-maintenance-operator:
    plugins:
    - trigger
triggers:
- org_invite:
    prominent: {}
  repos:
  - kubevirt/node-maintenance-operator
  trusted_apps:
  - openshift-merge-bot
