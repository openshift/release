plugins:
  kubevirt/hyperconverged-cluster-operator:
    plugins:
    - trigger
triggers:
- org_invite:
    prominent: {}
  repos:
  - kubevirt/hyperconverged-cluster-operator
  trusted_apps:
  - openshift-merge-bot
