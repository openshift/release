plugins:
  lightspeed-core/lightspeed-stack:
    plugins:
    - trigger
    - verify-owners
    - owners-label
triggers:
- org_invite:
    prominent: {}
  repos:
  - lightspeed-core/lightspeed-stack
  trusted_apps:
  - openshift-merge-bot
