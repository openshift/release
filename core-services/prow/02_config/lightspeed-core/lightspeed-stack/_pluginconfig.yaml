plugins:
  lightspeed-core/lightspeed-stack:
    plugins:
    - trigger
    - verify-owners
    - owners-label
triggers:
- repos:
  - lightspeed-core/lightspeed-stack
  trusted_apps:
  - openshift-merge-bot
