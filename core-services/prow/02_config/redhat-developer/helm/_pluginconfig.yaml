external_plugins:
  redhat-developer/helm:
  - endpoint: http://refresh
    events:
    - issue_comment
    name: refresh
  - endpoint: http://needs-rebase
    events:
    - pull_request
    name: needs-rebase
plugins:
  redhat-developer/helm:
    plugins:
    - approve
