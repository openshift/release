external_plugins:
  redhat-developer/jenkins-operator-bundle:
  - endpoint: http://refresh
    events:
    - issue_comment
    name: refresh
  - endpoint: http://needs-rebase
    events:
    - pull_request
    name: needs-rebase
plugins:
  redhat-developer/jenkins-operator-bundle:
    plugins:
    - approve
