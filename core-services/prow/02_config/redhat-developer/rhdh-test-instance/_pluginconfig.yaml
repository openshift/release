external_plugins:
  redhat-developer/rhdh-test-instance:
  - endpoint: http://refresh
    events:
    - issue_comment
    name: refresh
  - endpoint: http://needs-rebase
    events:
    - issue_comment
    - pull_request
    name: needs-rebase
plugins:
  redhat-developer/rhdh-test-instance:
    plugins:
    - owners-label
