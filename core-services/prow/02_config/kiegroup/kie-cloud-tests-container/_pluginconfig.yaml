plugins:
  kiegroup/kie-cloud-tests-container:
    plugins:
    - trigger
triggers:
- org_invite:
    prominent: {}
  repos:
  - kiegroup/kie-cloud-tests-container
  trusted_apps:
  - openshift-merge-bot
