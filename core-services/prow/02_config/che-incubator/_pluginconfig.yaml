external_plugins:
  che-incubator:
  - endpoint: http://jira-lifecycle-plugin
    events:
    - issue_comment
    - pull_request
    name: jira-lifecycle-plugin
plugins:
  che-incubator:
    plugins:
    - jira
