external_plugins:
  devfile/devworkspace-operator:
  - endpoint: http://jira-lifecycle-plugin
    events:
    - issue_comment
    - pull_request
    name: jira-lifecycle-plugin
plugins:
  devfile/devworkspace-operator:
    plugins:
    - jira
