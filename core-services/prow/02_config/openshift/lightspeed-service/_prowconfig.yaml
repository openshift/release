branch-protection:
  orgs:
    openshift:
      repos:
        lightspeed-service:
          branches:
            main:
              protect: true
              required_status_checks:
                contexts:
                - Red Hat Konflux / lightspeed-service-on-pull-request
tide:
  queries:
  - labels:
    - approved
    - lgtm
    missingLabels:
    - backports/unvalidated-commits
    - do-not-merge/hold
    - do-not-merge/invalid-owners-file
    - do-not-merge/work-in-progress
    - jira/invalid-bug
    - needs-rebase
    repos:
    - openshift/lightspeed-service
