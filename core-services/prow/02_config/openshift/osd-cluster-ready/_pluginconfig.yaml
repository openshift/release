approve:
- commandHelpLink: ""
  repos:
  - openshift/osd-cluster-ready
  require_self_approval: false
lgtm:
- repos:
  - openshift/osd-cluster-ready
  review_acts_as_lgtm: true
plugins:
  openshift/osd-cluster-ready:
    plugins:
    - approve
triggers:
- repos:
  - openshift/osd-cluster-ready
  trusted_apps:
  - red-hat-konflux
