label:
  restricted_labels:
    openshift/image-registry:
    - allowed_users:
      - xiuwang
      - wewang58
      assign_on:
      - label: backport-risk-assessed
      label: cherry-pick-approved
    - allowed_users:
      - dmage
      - syed
      - HammerMeetNail
      - flavianmissi
      label: backport-risk-assessed
plugins:
  openshift/image-registry:
    plugins:
    - approve
triggers:
- repos:
  - openshift/image-registry
  trusted_apps:
  - dependabot
