label:
  restricted_labels:
    openshift/jenkins-sync-plugin:
    - allowed_teams:
      - openshift-patch-managers
      allowed_users:
      - adambkaplan
      - coreydaley
      label: backport-risk-assessed
plugins:
  openshift/jenkins-sync-plugin:
    plugins:
    - approve
