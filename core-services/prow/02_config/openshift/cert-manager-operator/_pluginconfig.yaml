label:
  restricted_labels:
    openshift/cert-manager-operator:
    - allowed_users:
      - stlaz
      label: backport-risk-assessed
plugins:
  openshift/cert-manager-operator:
    plugins:
    - approve
