label:
  restricted_labels:
    openshift/cluster-image-registry-operator:
    - allowed_users:
      - xiuwang
      - wewang58
      label: cherry-pick-approved
    - allowed_users:
      - dmage
      - syed
      - HammerMeetNail
      label: backport-risk-assessed
plugins:
  openshift/cluster-image-registry-operator:
    plugins:
    - approve
