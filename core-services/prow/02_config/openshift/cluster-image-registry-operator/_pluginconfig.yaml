label:
  restricted_labels:
    openshift/cluster-image-registry-operator:
    - allowed_users:
      - xiuwang
      - wewang58
      assign_on:
      - label: backport-risk-assessed
      label: cherry-pick-approved
    - allowed_users:
      - dmage
      - syed
      - HammerMeetNail
      - flavianmissi
      label: backport-risk-assessed
plugins:
  openshift/cluster-image-registry-operator:
    plugins:
    - approve
triggers:
- repos:
  - openshift/cluster-image-registry-operator
  trusted_apps:
  - dependabot
