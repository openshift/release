label:
  restricted_labels:
    openshift/cluster-image-registry-operator:
    - allowed_users:
      - everettraven
      - benluddy
      - sjenning
      - tjungblu
      - ardaguclu
      - p0lyn0mial
      - xiuwang
      assign_on:
      - label: backport-risk-assessed
      label: cherry-pick-approved
    - allowed_users:
      - everettraven
      - benluddy
      - sjenning
      - tjungblu
      - ardaguclu
      - p0lyn0mial
      - flavianmissi
      - ricardomaraschini
      label: backport-risk-assessed
plugins:
  openshift/cluster-image-registry-operator:
    plugins:
    - approve
triggers:
- repos:
  - openshift/cluster-image-registry-operator
  trusted_apps:
  - dependabot
  - openshift-merge-bot
