branch-protection:
  orgs:
    openshift:
      repos:
        ocm-agent:
          branches:
            master:
              protect: true
              required_status_checks:
                contexts:
                - Konflux kflux-prd-rh03 / ocm-agent-operator-on-pull-request
                - ci/prow/coverage
                - ci/prow/images
                - ci/prow/lint
                - ci/prow/test
tide:
  queries:
  - labels:
    - approved
    - lgtm
    missingLabels:
    - backports/unvalidated-commits
    - do-not-merge/hold
    - do-not-merge/invalid-owners-file
    - do-not-merge/work-in-progress
    - jira/invalid-bug
    - needs-rebase
    repos:
    - openshift/ocm-agent-operator
