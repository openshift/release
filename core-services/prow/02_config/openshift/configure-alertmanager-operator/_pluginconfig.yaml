plugins:
  openshift/configure-alertmanager-operator:
    plugins:
    - approve
triggers:
- org_invite:
    prominent: {}
  repos:
  - openshift/configure-alertmanager-operator
  trusted_apps:
  - red-hat-konflux
  - openshift-merge-bot
