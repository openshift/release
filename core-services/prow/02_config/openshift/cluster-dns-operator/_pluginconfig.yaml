label:
  restricted_labels:
    openshift/cluster-dns-operator:
    - allowed_users:
      - candita
      - Miciah
      label: backport-risk-assessed
plugins:
  openshift/cluster-dns-operator:
    plugins:
    - approve
