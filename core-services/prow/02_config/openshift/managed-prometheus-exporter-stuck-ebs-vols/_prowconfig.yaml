branch-protection:
  orgs:
    openshift:
      repos:
        managed-prometheus-exporter-stuck-ebs-vols:
          required_status_checks:
            contexts:
            - ci.ext.devshift.net PR build
