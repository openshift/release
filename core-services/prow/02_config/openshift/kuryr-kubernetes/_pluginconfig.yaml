label:
  restricted_labels:
    openshift/kuryr-kubernetes:
    - allowed_teams:
      - kuryr-kubernetes
      label: backport-risk-assessed
    - allowed_users:
      - eurijon
      - itzikb-redhat
      label: cherry-pick-approved
plugins:
  openshift/kuryr-kubernetes:
    plugins:
    - approve
