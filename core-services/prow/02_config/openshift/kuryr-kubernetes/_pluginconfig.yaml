label:
  restricted_labels:
    openshift/kuryr-kubernetes:
    - allowed_teams:
      - kuryr-kubernetes
      label: backport-risk-assessed
plugins:
  openshift/kuryr-kubernetes:
    plugins:
    - approve
