label:
  restricted_labels:
    openshift/kube-rbac-proxy:
    - allowed_users:
      - s-urbaniak
      - stlaz
      label: backport-risk-assessed
plugins:
  openshift/kube-rbac-proxy:
    plugins:
    - approve
