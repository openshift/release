label:
  restricted_labels:
    openshift/template-service-broker:
    - allowed_users:
      - emmajiafan
      - cuipinghuo
      label: cherry-pick-approved
plugins:
  openshift/template-service-broker:
    plugins:
    - approve
