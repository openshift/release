label:
  restricted_labels:
    openshift/cloud-provider-openstack:
    - allowed_teams:
      - openshift-patch-managers
      allowed_users:
      - mandre
      label: backport-risk-assessed
plugins:
  openshift/cloud-provider-openstack:
    plugins:
    - approve
