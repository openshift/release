branch-protection:
  orgs:
    openshift:
      repos:
        tektoncd-triggers:
          branches:
            release-next:
              protect: false
            release-next-ci:
              protect: false
