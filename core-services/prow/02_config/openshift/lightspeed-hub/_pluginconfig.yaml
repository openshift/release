approve:
- ignore_review_state: true
  repos:
  - openshift/lightspeed-hub
  require_self_approval: true
lgtm:
- repos:
  - openshift/lightspeed-hub
plugins:
  openshift/lightspeed-hub:
    plugins:
    - approve
triggers:
- repos:
  - openshift/lightspeed-hub
  trusted_apps:
  - dependabot
  - red-hat-konflux
  - openshift-merge-bot
