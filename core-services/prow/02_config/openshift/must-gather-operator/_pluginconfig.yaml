approve:
- repos:
  - openshift/must-gather-operator
  require_self_approval: false
label:
  restricted_labels:
    openshift/must-gather-operator:
    - allowed_teams:
      - openshift-application-platform-maintainers
      - openshift-staff-engineers
      allowed_users:
      - Prashanth684
      - shivprakashmuley
      - swghosh
      label: backport-risk-assessed
lgtm:
- repos:
  - openshift/must-gather-operator
  review_acts_as_lgtm: true
plugins:
  openshift/must-gather-operator:
    plugins:
    - approve
