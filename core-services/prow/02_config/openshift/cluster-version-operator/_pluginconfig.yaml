label:
  restricted_labels:
    openshift/cluster-version-operator:
    - allowed_users:
      - LalatenduMohanty
      label: backport-risk-assessed
    - allowed_users:
      - jianlinliu
      - jiajliu
      label: cherry-pick-approved
plugins:
  openshift/cluster-version-operator:
    plugins:
    - approve
