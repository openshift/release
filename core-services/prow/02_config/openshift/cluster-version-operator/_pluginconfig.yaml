label:
  restricted_labels:
    openshift/cluster-version-operator:
    - allowed_users:
      - LalatenduMohanty
      - jianlinliu
      - jiajliu
      label: backport-risk-assessed
plugins:
  openshift/cluster-version-operator:
    plugins:
    - approve
