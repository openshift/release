label:
  restricted_labels:
    openshift/ovn-kubernetes:
    - allowed_teams:
      - openshift-staff-engineers
      allowed_users:
      - jcaamano
      - kyrtapz
      - martinkennelly
      - pperiyasamy
      - tssurya
      label: backport-risk-assessed
plugins:
  openshift/ovn-kubernetes:
    plugins:
    - approve
triggers:
- repos:
  - openshift/ovn-kubernetes
  trusted_apps:
  - openshift-pr-manager
  - openshift-merge-bot
