label:
  restricted_labels:
    openshift/ocp-release-operator-sdk:
    - allowed_users:
      - emmajiafan
      - cuipinghuo
      label: cherry-pick-approved
plugins:
  openshift/ocp-release-operator-sdk:
    plugins:
    - approve
