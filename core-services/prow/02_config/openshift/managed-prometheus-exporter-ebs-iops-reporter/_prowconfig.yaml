branch-protection:
  orgs:
    openshift:
      repos:
        managed-prometheus-exporter-ebs-iops-reporter:
          required_status_checks:
            contexts:
            - ci.ext.devshift.net PR build
