approve:
- repos:
  - openshift/functions-operator
  require_self_approval: false
plugins:
  openshift/functions-operator:
    plugins:
    - approve
    - size
triggers:
- repos:
  - openshift/functions-operator
  trusted_apps:
  - red-hat-konflux
  - red-hat-konflux-kflux-prd-rh02
  - openshift-merge-bot
  - openshift-rebasebot
