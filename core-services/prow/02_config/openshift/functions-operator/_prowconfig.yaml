branch-protection:
  orgs:
    openshift:
      repos:
        functions-operator:
          branches:
            main:
              protect: true
              required_status_checks:
                contexts:
                - ci/prow/unit
                - ci/prow/lint
                - ci/prow/e2e-install-aws
tide:
  queries:
  - labels:
    - approved
    - lgtm
    missingLabels:
    - backports/unvalidated-commits
    - do-not-merge/hold
    - do-not-merge/invalid-owners-file
    - do-not-merge/work-in-progress
    - jira/invalid-bug
    - needs-rebase
    repos:
    - openshift/functions-operator
