branch-protection:
  orgs:
    openshift:
      repos:
        managed-velero-operator:
          branches:
            master:
              protect: true
              required_status_checks:
                contexts:
                - Konflux kflux-prd-rh03 / managed-velero-operator-on-pull-request
                - ci/prow/images
                - ci/prow/lint
                - ci/prow/test
                - ci/prow/validate
tide:
  queries:
  - labels:
    - approved
    - lgtm
    missingLabels:
    - backports/unvalidated-commits
    - do-not-merge/hold
    - do-not-merge/invalid-owners-file
    - do-not-merge/work-in-progress
    - jira/invalid-bug
    - needs-rebase
    repos:
    - openshift/managed-velero-operator
