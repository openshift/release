branch-protection:
  orgs:
    openshift:
      repos:
        configuration-anomaly-detection:
          branches:
            main:
              protect: true
              required_status_checks:
                contexts:
                - Konflux kflux-prd-rh02 / configuration-anomaly-detection-on-pull-request
                - ci/prow/coverage
                - ci/prow/lint
                - ci/prow/test
                - ci/prow/validate
tide:
  queries:
  - labels:
    - approved
    - lgtm
    missingLabels:
    - backports/unvalidated-commits
    - do-not-merge/hold
    - do-not-merge/invalid-owners-file
    - do-not-merge/work-in-progress
    - jira/invalid-bug
    - needs-rebase
    repos:
    - openshift/configuration-anomaly-detection
