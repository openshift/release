label:
  restricted_labels:
    openshift/cluster-authentication-operator:
    - allowed_users:
      - stlaz
      label: backport-risk-assessed
plugins:
  openshift/cluster-authentication-operator:
    plugins:
    - approve
