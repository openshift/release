label:
  restricted_labels:
    openshift/template-service-broker-operator:
    - allowed_users:
      - emmajiafan
      - cuipinghuo
plugins:
  openshift/template-service-broker-operator:
    plugins:
    - approve
