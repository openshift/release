label:
  restricted_labels:
    openshift/ansible-service-broker:
    - allowed_users:
      - emmajiafan
      - cuipinghuo
      label: cherry-pick-approved
plugins:
  openshift/ansible-service-broker:
    plugins:
    - approve
