branch-protection:
  orgs:
    openshift-service-mesh:
      repos:
        ztunnel:
          include:
          - master
          - main
          - release-*
          protect: true
          required_pull_request_reviews:
            required_approving_review_count: 1
tide:
  merge_method:
    openshift-service-mesh/ztunnel: squash
  queries:
  - author: openshift-service-mesh-bot
    labels:
    - auto-merge
    missingLabels:
    - do-not-merge
    - do-not-merge/hold
    - do-not-merge/work-in-progress
    repos:
    - openshift-service-mesh/ztunnel
  - labels:
    - okay to merge
    missingLabels:
    - do-not-merge
    - do-not-merge/hold
    - do-not-merge/work-in-progress
    repos:
    - openshift-service-mesh/ztunnel
    reviewApprovedRequired: true
