plugins:
  openshift-priv/k8s-prometheus-adapter:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/k8s-prometheus-adapter
  trusted_apps:
  - openshift-merge-bot
