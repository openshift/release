plugins:
  openshift-priv/multus-networkpolicy:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/multus-networkpolicy
  trusted_apps:
  - openshift-merge-bot
