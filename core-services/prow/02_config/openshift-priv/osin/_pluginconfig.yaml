plugins:
  openshift-priv/osin:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/osin
  trusted_apps:
  - openshift-merge-bot
