plugins:
  openshift-priv/gatekeeper-operator:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/gatekeeper-operator
  trusted_apps:
  - openshift-merge-bot
