plugins:
  openshift-priv/external-storage:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/external-storage
  trusted_apps:
  - openshift-merge-bot
