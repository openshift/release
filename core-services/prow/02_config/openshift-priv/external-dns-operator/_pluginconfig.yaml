approve:
- commandHelpLink: ""
  ignore_review_state: true
  repos:
  - openshift-priv/external-dns-operator
  require_self_approval: true
lgtm:
- repos:
  - openshift-priv/external-dns-operator
plugins:
  openshift-priv/external-dns-operator:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/external-dns-operator
  trusted_apps:
  - openshift-merge-bot
