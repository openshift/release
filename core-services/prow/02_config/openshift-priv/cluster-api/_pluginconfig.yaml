approve:
- commandHelpLink: ""
  repos:
  - openshift-priv/cluster-api
  require_self_approval: false
plugins:
  openshift-priv/cluster-api:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/cluster-api
  trusted_apps:
  - openshift-merge-bot
