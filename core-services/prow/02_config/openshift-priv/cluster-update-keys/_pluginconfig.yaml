plugins:
  openshift-priv/cluster-update-keys:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/cluster-update-keys
  trusted_apps:
  - openshift-merge-bot
