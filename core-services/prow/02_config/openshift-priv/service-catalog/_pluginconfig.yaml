plugins:
  openshift-priv/service-catalog:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/service-catalog
  trusted_apps:
  - openshift-merge-bot
