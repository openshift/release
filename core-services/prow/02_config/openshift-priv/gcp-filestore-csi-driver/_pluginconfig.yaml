approve:
- commandHelpLink: ""
  repos:
  - openshift-priv/gcp-filestore-csi-driver
  require_self_approval: false
lgtm:
- repos:
  - openshift-priv/gcp-filestore-csi-driver
  review_acts_as_lgtm: true
plugins:
  openshift-priv/gcp-filestore-csi-driver:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/gcp-filestore-csi-driver
  trusted_apps:
  - openshift-merge-bot
