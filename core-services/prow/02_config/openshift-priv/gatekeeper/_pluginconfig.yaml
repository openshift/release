plugins:
  openshift-priv/gatekeeper:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/gatekeeper
  trusted_apps:
  - openshift-merge-bot
