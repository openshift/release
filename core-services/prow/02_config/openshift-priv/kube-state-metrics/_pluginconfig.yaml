plugins:
  openshift-priv/kube-state-metrics:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/kube-state-metrics
  trusted_apps:
  - openshift-merge-bot
