plugins:
  openshift-priv/runtime-utils:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/runtime-utils
  trusted_apps:
  - openshift-merge-bot
