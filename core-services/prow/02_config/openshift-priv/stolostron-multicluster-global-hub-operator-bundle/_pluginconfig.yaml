plugins:
  openshift-priv/stolostron-multicluster-global-hub-operator-bundle:
    plugins:
    - approve
    - assign
    - golint
    - help
    - hold
    - label
    - lgtm
    - lifecycle
    - override
    - owners-label
    - retitle
    - shrug
    - trigger
    - verify-owners
    - wip
triggers:
- repos:
  - openshift-priv/stolostron-multicluster-global-hub-operator-bundle
  trusted_apps:
  - openshift-merge-bot
