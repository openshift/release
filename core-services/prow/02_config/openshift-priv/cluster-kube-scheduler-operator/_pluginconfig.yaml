plugins:
  openshift-priv/cluster-kube-scheduler-operator:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/cluster-kube-scheduler-operator
  trusted_apps:
  - openshift-merge-bot
