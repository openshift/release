plugins:
  openshift-priv/descheduler:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/descheduler
  trusted_apps:
  - openshift-merge-bot
