plugins:
  openshift-priv/cluster-api-provider-ovirt:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/cluster-api-provider-ovirt
  trusted_apps:
  - openshift-merge-bot
