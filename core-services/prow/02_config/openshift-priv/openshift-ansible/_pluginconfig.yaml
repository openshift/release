plugins:
  openshift-priv/openshift-ansible:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/openshift-ansible
  trusted_apps:
  - openshift-merge-bot
