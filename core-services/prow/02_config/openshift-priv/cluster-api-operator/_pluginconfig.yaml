approve:
- commandHelpLink: ""
  repos:
  - openshift-priv/cluster-api-operator
  require_self_approval: false
lgtm:
- repos:
  - openshift-priv/cluster-api-operator
plugins:
  openshift-priv/cluster-api-operator:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/cluster-api-operator
  trusted_apps:
  - openshift-merge-bot
