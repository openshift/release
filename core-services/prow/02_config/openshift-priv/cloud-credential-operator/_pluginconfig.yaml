plugins:
  openshift-priv/cloud-credential-operator:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/cloud-credential-operator
  trusted_apps:
  - openshift-merge-bot
