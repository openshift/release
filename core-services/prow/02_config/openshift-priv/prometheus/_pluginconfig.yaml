plugins:
  openshift-priv/prometheus:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/prometheus
  trusted_apps:
  - openshift-merge-bot
