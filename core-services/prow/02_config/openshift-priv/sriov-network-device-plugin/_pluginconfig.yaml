plugins:
  openshift-priv/sriov-network-device-plugin:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/sriov-network-device-plugin
  trusted_apps:
  - openshift-merge-bot
