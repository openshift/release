plugins:
  openshift-priv/apiserver-library-go:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/apiserver-library-go
  trusted_apps:
  - openshift-merge-bot
