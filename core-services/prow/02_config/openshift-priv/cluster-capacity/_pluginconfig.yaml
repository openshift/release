plugins:
  openshift-priv/cluster-capacity:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/cluster-capacity
  trusted_apps:
  - openshift-merge-bot
