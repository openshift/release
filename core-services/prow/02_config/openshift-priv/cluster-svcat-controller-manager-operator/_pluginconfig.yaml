plugins:
  openshift-priv/cluster-svcat-controller-manager-operator:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/cluster-svcat-controller-manager-operator
  trusted_apps:
  - openshift-merge-bot
