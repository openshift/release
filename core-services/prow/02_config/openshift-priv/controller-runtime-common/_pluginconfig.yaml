plugins:
  openshift-priv/controller-runtime-common:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/controller-runtime-common
  trusted_apps:
  - openshift-merge-bot
