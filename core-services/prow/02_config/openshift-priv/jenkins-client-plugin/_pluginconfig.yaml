plugins:
  openshift-priv/jenkins-client-plugin:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/jenkins-client-plugin
  trusted_apps:
  - openshift-merge-bot
