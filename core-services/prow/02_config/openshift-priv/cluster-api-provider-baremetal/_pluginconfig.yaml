approve:
- commandHelpLink: ""
  repos:
  - openshift-priv/cluster-api-provider-baremetal
  require_self_approval: false
plugins:
  openshift-priv/cluster-api-provider-baremetal:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/cluster-api-provider-baremetal
  trusted_apps:
  - openshift-merge-bot
