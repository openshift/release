plugins:
  openshift-priv/cluster-openshift-apiserver-operator:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/cluster-openshift-apiserver-operator
  trusted_apps:
  - openshift-merge-bot
