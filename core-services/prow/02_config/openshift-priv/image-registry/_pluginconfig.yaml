plugins:
  openshift-priv/image-registry:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/image-registry
  trusted_apps:
  - openshift-merge-bot
