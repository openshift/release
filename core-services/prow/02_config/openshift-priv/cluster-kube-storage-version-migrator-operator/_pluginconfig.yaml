plugins:
  openshift-priv/cluster-kube-storage-version-migrator-operator:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/cluster-kube-storage-version-migrator-operator
  trusted_apps:
  - openshift-merge-bot
