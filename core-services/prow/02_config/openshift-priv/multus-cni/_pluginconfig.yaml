plugins:
  openshift-priv/multus-cni:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/multus-cni
  trusted_apps:
  - openshift-merge-bot
