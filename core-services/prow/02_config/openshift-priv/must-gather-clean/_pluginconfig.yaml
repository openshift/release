approve:
- repos:
  - openshift-priv/must-gather-clean
  require_self_approval: false
lgtm:
- repos:
  - openshift-priv/must-gather-clean
  review_acts_as_lgtm: true
plugins:
  openshift-priv/must-gather-clean:
    plugins:
    - approve
    - assign
    - blunderbuss
    - cat
    - dog
    - golint
    - goose
    - heart
    - help
    - hold
    - jira
    - label
    - lgtm
    - lifecycle
    - override
    - owners-label
    - pony
    - retitle
    - shrug
    - sigmention
    - skip
    - trigger
    - verify-owners
    - wip
    - yuks
triggers:
- repos:
  - openshift-priv/must-gather-clean
  trusted_apps:
  - openshift-merge-bot
