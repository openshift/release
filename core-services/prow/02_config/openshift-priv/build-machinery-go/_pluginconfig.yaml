plugins:
  openshift-priv/build-machinery-go:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/build-machinery-go
  trusted_apps:
  - openshift-merge-bot
