plugins:
  openshift-priv/prometheus-operator:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/prometheus-operator
  trusted_apps:
  - openshift-merge-bot
