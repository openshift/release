plugins:
  openshift-priv/oauth-proxy:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/oauth-proxy
  trusted_apps:
  - openshift-merge-bot
