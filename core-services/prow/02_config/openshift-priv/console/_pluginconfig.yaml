plugins:
  openshift-priv/console:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/console
  trusted_apps:
  - openshift-merge-bot
