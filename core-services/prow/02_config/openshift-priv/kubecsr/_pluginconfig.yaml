plugins:
  openshift-priv/kubecsr:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/kubecsr
  trusted_apps:
  - openshift-merge-bot
