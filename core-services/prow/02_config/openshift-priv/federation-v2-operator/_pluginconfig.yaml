plugins:
  openshift-priv/federation-v2-operator:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/federation-v2-operator
  trusted_apps:
  - openshift-merge-bot
