plugins:
  openshift-priv/build-test-images:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/build-test-images
  trusted_apps:
  - openshift-merge-bot
