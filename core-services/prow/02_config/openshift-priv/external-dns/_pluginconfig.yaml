approve:
- commandHelpLink: ""
  ignore_review_state: true
  repos:
  - openshift-priv/external-dns
  require_self_approval: true
lgtm:
- repos:
  - openshift-priv/external-dns
plugins:
  openshift-priv/external-dns:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/external-dns
  trusted_apps:
  - openshift-merge-bot
