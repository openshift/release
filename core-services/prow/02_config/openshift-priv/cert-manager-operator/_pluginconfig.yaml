plugins:
  openshift-priv/cert-manager-operator:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/cert-manager-operator
  trusted_apps:
  - openshift-merge-bot
