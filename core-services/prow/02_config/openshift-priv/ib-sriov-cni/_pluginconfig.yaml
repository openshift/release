plugins:
  openshift-priv/ib-sriov-cni:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/ib-sriov-cni
  trusted_apps:
  - openshift-merge-bot
