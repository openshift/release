plugins:
  openshift-priv/telemeter:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/telemeter
  trusted_apps:
  - openshift-merge-bot
