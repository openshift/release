plugins:
  openshift-priv/must-gather:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/must-gather
  trusted_apps:
  - openshift-merge-bot
