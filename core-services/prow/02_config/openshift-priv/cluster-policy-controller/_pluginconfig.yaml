plugins:
  openshift-priv/cluster-policy-controller:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/cluster-policy-controller
  trusted_apps:
  - openshift-merge-bot
