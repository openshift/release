plugins:
  openshift-priv/cluster-kube-descheduler-operator:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/cluster-kube-descheduler-operator
  trusted_apps:
  - openshift-merge-bot
