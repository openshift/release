plugins:
  openshift-priv/route-controller-manager:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/route-controller-manager
  trusted_apps:
  - openshift-merge-bot
