plugins:
  openshift-priv/vertical-pod-autoscaler-operator:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/vertical-pod-autoscaler-operator
  trusted_apps:
  - openshift-merge-bot
