plugins:
  openshift-priv/kubefed-operator:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/kubefed-operator
  trusted_apps:
  - openshift-merge-bot
