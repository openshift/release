plugins:
  openshift-priv/aws-pod-identity-webhook:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/aws-pod-identity-webhook
  trusted_apps:
  - openshift-merge-bot
