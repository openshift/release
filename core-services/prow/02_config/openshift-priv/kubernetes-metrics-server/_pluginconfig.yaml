plugins:
  openshift-priv/kubernetes-metrics-server:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/kubernetes-metrics-server
  trusted_apps:
  - openshift-merge-bot
