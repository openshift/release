plugins:
  openshift-priv/service-ca-operator:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/service-ca-operator
  trusted_apps:
  - openshift-merge-bot
