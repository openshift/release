approve:
- commandHelpLink: ""
  ignore_review_state: true
  repos:
  - openshift-priv/coredns-ocp-dnsnameresolver
  require_self_approval: true
lgtm:
- repos:
  - openshift-priv/coredns-ocp-dnsnameresolver
plugins:
  openshift-priv/coredns-ocp-dnsnameresolver:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/coredns-ocp-dnsnameresolver
  trusted_apps:
  - openshift-merge-bot
