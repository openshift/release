plugins:
  openshift-priv/cluster-samples-operator:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/cluster-samples-operator
  trusted_apps:
  - openshift-merge-bot
