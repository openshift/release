plugins:
  openshift-priv/cluster-resource-override-admission:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/cluster-resource-override-admission
  trusted_apps:
  - openshift-merge-bot
