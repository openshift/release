plugins:
  openshift-priv/project-request-limit:
    plugins:
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/project-request-limit
  trusted_apps:
  - openshift-merge-bot
