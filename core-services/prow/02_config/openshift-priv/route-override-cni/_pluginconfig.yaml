plugins:
  openshift-priv/route-override-cni:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/route-override-cni
  trusted_apps:
  - openshift-merge-bot
