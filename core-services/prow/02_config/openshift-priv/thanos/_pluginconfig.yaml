plugins:
  openshift-priv/thanos:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/thanos
  trusted_apps:
  - openshift-merge-bot
