approve:
- commandHelpLink: ""
  repos:
  - openshift-priv/cloud-provider-aws
  require_self_approval: true
lgtm:
- repos:
  - openshift-priv/cloud-provider-aws
plugins:
  openshift-priv/cloud-provider-aws:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/cloud-provider-aws
  trusted_apps:
  - openshift-merge-bot
