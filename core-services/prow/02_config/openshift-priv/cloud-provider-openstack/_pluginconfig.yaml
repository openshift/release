plugins:
  openshift-priv/cloud-provider-openstack:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/cloud-provider-openstack
  trusted_apps:
  - openshift-merge-bot
