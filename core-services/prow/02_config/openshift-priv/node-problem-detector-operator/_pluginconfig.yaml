plugins:
  openshift-priv/node-problem-detector-operator:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/node-problem-detector-operator
  trusted_apps:
  - openshift-merge-bot
