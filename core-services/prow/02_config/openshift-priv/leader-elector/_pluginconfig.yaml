plugins:
  openshift-priv/leader-elector:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/leader-elector
  trusted_apps:
  - openshift-merge-bot
