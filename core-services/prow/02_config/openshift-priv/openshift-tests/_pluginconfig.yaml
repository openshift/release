bugzilla:
  orgs:
    openshift-priv:
      repos:
        openshift-tests:
          branches:
            '*':
              allowed_groups:
              - redhat
              - qe_staff
              validate_by_default: false
plugins:
  openshift-priv/openshift-tests:
    plugins:
    - approve
