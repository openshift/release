plugins:
  openshift-priv/ansible-service-broker:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/ansible-service-broker
  trusted_apps:
  - openshift-merge-bot
