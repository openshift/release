plugins:
  openshift-priv/cluster-network-operator:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/cluster-network-operator
  trusted_apps:
  - openshift-merge-bot
