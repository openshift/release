approve:
- commandHelpLink: ""
  repos:
  - openshift-priv/cloud-event-proxy
  require_self_approval: false
lgtm:
- repos:
  - openshift-priv/cloud-event-proxy
  review_acts_as_lgtm: true
plugins:
  openshift-priv/cloud-event-proxy:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/cloud-event-proxy
  trusted_apps:
  - openshift-merge-bot
