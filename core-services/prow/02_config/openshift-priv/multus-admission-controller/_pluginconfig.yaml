plugins:
  openshift-priv/multus-admission-controller:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/multus-admission-controller
  trusted_apps:
  - openshift-merge-bot
