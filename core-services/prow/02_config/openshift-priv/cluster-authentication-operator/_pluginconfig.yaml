plugins:
  openshift-priv/cluster-authentication-operator:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/cluster-authentication-operator
  trusted_apps:
  - openshift-merge-bot
