plugins:
  openshift-priv/cluster-bootstrap:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/cluster-bootstrap
  trusted_apps:
  - openshift-merge-bot
