approve:
- commandHelpLink: ""
  repos:
  - openshift-priv/cluster-baremetal-operator
  require_self_approval: false
plugins:
  openshift-priv/cluster-baremetal-operator:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/cluster-baremetal-operator
  trusted_apps:
  - openshift-merge-bot
