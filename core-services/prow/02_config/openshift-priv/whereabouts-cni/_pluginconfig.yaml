plugins:
  openshift-priv/whereabouts-cni:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/whereabouts-cni
  trusted_apps:
  - openshift-merge-bot
