plugins:
  openshift-priv/run-once-duration-override:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/run-once-duration-override
  trusted_apps:
  - openshift-merge-bot
