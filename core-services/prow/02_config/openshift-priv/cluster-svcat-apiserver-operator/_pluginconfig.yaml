plugins:
  openshift-priv/cluster-svcat-apiserver-operator:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/cluster-svcat-apiserver-operator
  trusted_apps:
  - openshift-merge-bot
