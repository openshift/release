plugins:
  openshift-priv/kube-rbac-proxy:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/kube-rbac-proxy
  trusted_apps:
  - openshift-merge-bot
