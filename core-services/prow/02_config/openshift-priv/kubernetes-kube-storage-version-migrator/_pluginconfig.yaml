plugins:
  openshift-priv/kubernetes-kube-storage-version-migrator:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/kubernetes-kube-storage-version-migrator
  trusted_apps:
  - openshift-merge-bot
