plugins:
  openshift-priv/sriov-dp-admission-controller:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/sriov-dp-admission-controller
  trusted_apps:
  - openshift-merge-bot
