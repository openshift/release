plugins:
  openshift-priv/oauth-apiserver:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/oauth-apiserver
  trusted_apps:
  - openshift-merge-bot
