plugins:
  openshift-priv/template-service-broker-operator:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/template-service-broker-operator
  trusted_apps:
  - openshift-merge-bot
