approve:
- commandHelpLink: ""
  repos:
  - openshift-priv/must-gather-operator
  require_self_approval: false
lgtm:
- repos:
  - openshift-priv/must-gather-operator
  review_acts_as_lgtm: true
plugins:
  openshift-priv/must-gather-operator:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/must-gather-operator
  trusted_apps:
  - openshift-merge-bot
