approve:
- repos:
  - openshift-priv/must-gather-operator
  require_self_approval: false
lgtm:
- repos:
  - openshift-priv/must-gather-operator
  review_acts_as_lgtm: true
plugins:
  openshift-priv/must-gather-operator:
    plugins:
    - approve
    - assign
    - blunderbuss
    - cat
    - dog
    - golint
    - goose
    - heart
    - help
    - hold
    - jira
    - label
    - lgtm
    - lifecycle
    - override
    - owners-label
    - pony
    - retitle
    - shrug
    - sigmention
    - skip
    - trigger
    - verify-owners
    - wip
    - yuks
triggers:
- repos:
  - openshift-priv/must-gather-operator
  trusted_apps:
  - openshift-merge-bot
