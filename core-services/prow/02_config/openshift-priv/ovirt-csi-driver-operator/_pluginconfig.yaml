plugins:
  openshift-priv/ovirt-csi-driver-operator:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/ovirt-csi-driver-operator
  trusted_apps:
  - openshift-merge-bot
