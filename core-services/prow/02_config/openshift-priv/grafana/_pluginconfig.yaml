plugins:
  openshift-priv/grafana:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/grafana
  trusted_apps:
  - openshift-merge-bot
