plugins:
  openshift-priv/containernetworking-plugins:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/containernetworking-plugins
  trusted_apps:
  - openshift-merge-bot
