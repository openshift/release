plugins:
  openshift-priv/cluster-monitoring-operator:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/cluster-monitoring-operator
  trusted_apps:
  - openshift-merge-bot
