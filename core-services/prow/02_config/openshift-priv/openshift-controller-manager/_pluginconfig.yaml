plugins:
  openshift-priv/openshift-controller-manager:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/openshift-controller-manager
  trusted_apps:
  - openshift-merge-bot
