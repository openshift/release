plugins:
  openshift-priv/cluster-resource-override-admission-operator:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/cluster-resource-override-admission-operator
  trusted_apps:
  - openshift-merge-bot
