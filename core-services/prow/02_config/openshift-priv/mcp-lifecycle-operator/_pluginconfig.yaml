plugins:
  openshift-priv/mcp-lifecycle-operator:
    plugins:
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/mcp-lifecycle-operator
  trusted_apps:
  - openshift-merge-bot
