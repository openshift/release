plugins:
  openshift-priv/openshift-state-metrics:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/openshift-state-metrics
  trusted_apps:
  - openshift-merge-bot
