plugins:
  openshift-priv/prometheus-alertmanager:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/prometheus-alertmanager
  trusted_apps:
  - openshift-merge-bot
