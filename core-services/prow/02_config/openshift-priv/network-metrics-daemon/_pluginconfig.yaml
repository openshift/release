plugins:
  openshift-priv/network-metrics-daemon:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/network-metrics-daemon
  trusted_apps:
  - openshift-merge-bot
