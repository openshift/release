plugins:
  openshift-priv/console-operator:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/console-operator
  trusted_apps:
  - openshift-merge-bot
