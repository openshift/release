plugins:
  openshift-priv/kuryr-kubernetes:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/kuryr-kubernetes
  trusted_apps:
  - openshift-merge-bot
