plugins:
  openshift-priv/node-problem-detector:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/node-problem-detector
  trusted_apps:
  - openshift-merge-bot
