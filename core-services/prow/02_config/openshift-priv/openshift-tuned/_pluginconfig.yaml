plugins:
  openshift-priv/openshift-tuned:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/openshift-tuned
  trusted_apps:
  - openshift-merge-bot
