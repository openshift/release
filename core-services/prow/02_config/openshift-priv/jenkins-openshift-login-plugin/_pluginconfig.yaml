plugins:
  openshift-priv/jenkins-openshift-login-plugin:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/jenkins-openshift-login-plugin
  trusted_apps:
  - openshift-merge-bot
