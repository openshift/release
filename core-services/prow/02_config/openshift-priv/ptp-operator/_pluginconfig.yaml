plugins:
  openshift-priv/ptp-operator:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/ptp-operator
  trusted_apps:
  - openshift-merge-bot
