plugins:
  openshift-priv/cluster-config-operator:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/cluster-config-operator
  trusted_apps:
  - openshift-merge-bot
