plugins:
  openshift-priv/openshift-apiserver:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/openshift-apiserver
  trusted_apps:
  - openshift-merge-bot
