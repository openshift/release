plugins:
  openshift-priv/machine-config-operator:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/machine-config-operator
  trusted_apps:
  - openshift-merge-bot
