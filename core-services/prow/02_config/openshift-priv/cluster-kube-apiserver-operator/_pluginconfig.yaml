plugins:
  openshift-priv/cluster-kube-apiserver-operator:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/cluster-kube-apiserver-operator
  trusted_apps:
  - openshift-merge-bot
