plugins:
  openshift-priv/cluster-etcd-operator:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/cluster-etcd-operator
  trusted_apps:
  - openshift-merge-bot
