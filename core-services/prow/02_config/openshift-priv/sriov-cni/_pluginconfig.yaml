plugins:
  openshift-priv/sriov-cni:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/sriov-cni
  trusted_apps:
  - openshift-merge-bot
