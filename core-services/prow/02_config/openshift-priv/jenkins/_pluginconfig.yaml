plugins:
  openshift-priv/jenkins:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/jenkins
  trusted_apps:
  - openshift-merge-bot
