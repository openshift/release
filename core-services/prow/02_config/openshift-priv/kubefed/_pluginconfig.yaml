plugins:
  openshift-priv/kubefed:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/kubefed
  trusted_apps:
  - openshift-merge-bot
