plugins:
  openshift-priv/network-tools:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/network-tools
  trusted_apps:
  - openshift-merge-bot
