plugins:
  openshift-priv/etcd:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/etcd
  trusted_apps:
  - openshift-merge-bot
