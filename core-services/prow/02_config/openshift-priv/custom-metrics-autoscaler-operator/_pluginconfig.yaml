plugins:
  openshift-priv/custom-metrics-autoscaler-operator:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/custom-metrics-autoscaler-operator
  trusted_apps:
  - openshift-merge-bot
