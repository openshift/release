plugins:
  openshift-priv/client-go:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/client-go
  trusted_apps:
  - openshift-merge-bot
