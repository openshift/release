plugins:
  openshift-priv/oauth-server:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/oauth-server
  trusted_apps:
  - openshift-merge-bot
