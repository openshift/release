plugins:
  openshift-priv/sriov-network-operator:
    plugins:
    - approve
    - golint
    - goose
    - jira
    - owners-label
    - pony
    - sigmention
triggers:
- repos:
  - openshift-priv/sriov-network-operator
  trusted_apps:
  - openshift-merge-bot
