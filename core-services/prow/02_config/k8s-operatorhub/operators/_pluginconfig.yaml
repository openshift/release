external_plugins:
  k8s-operatorhub/operators:
  - endpoint: http://jira-lifecycle-plugin
    events:
    - issue_comment
    - pull_request
    name: jira-lifecycle-plugin
plugins:
  k8s-operatorhub/operators:
    plugins:
    - owners-label
