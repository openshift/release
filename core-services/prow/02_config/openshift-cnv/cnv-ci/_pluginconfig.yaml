plugins:
  openshift-cnv/cnv-ci:
    plugins:
    - approve
    - assign
    - hold
    - label
    - lgtm
    - trigger
    - override
    - verify-owners
    - wip
triggers:
- repos:
  - openshift-cnv/cnv-ci
  trusted_apps:
  - openshift-merge-bot
