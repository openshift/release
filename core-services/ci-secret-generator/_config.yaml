- fields:
  - cmd: oc --kubeconfig /etc/build-farm-credentials/sa.config-updater.$(cluster).config sa create-kubeconfig --namespace ci $(service_account)
      | sed "s/$(service_account)/$(cluster)/g"
    name: sa.$(service_account).$(cluster).config
  item_name: build_farm
  params:
    cluster:
    - arm01
    - build01
    - build02
    - vsphere
    service_account:
    - config-updater
    - deck
    - sinker
    - hook
    - crier
    - prow-controller-manager
    - dptp-controller-manager
    - ci-operator
- fields:
  - cmd: oc --kubeconfig /etc/build-farm-credentials/sa.config-updater.$(cluster).config sa create-kubeconfig --namespace ci $(service_account)
      | sed "s/$(service_account)/$(cluster)/g"
    name: sa.$(service_account).$(cluster).config
  item_name: build_farm
  params:
    cluster:
    - app.ci
    service_account:
    - config-updater
    - deck
    - sinker
    - hook
    - crier
    - prow-controller-manager
    - pj-rehearse
    - ci-operator
- fields:
  - cmd: oc --kubeconfig /etc/build-farm-credentials/sa.config-updater.$(cluster).config sa create-kubeconfig --namespace ci $(service_account)
      | sed "s/$(service_account)/$(cluster)/g"
    name: sa.$(service_account).$(cluster).config
  item_name: release-controller
  params:
    cluster:
    - app.ci
    service_account:
    - release-controller
    - release-controller-ocp
    - release-controller-ocp-priv
    - release-controller-ocp-ppc64le
    - release-controller-ocp-ppc64le-priv
    - release-controller-ocp-s390x
    - release-controller-ocp-s390x-priv
    - release-controller-ocp-arm64
    - release-controller-ocp-arm64-priv
- fields:
  - cmd: oc --kubeconfig /etc/build-farm-credentials/sa.config-updater.$(cluster).config get secrets --sort-by=.metadata.creationTimestamp
      --namespace ci -o json | jq '.items[] | select(.type=="kubernetes.io/dockercfg")
      | select(.metadata.annotations["kubernetes.io/service-account.name"]=="image-puller")'|
      jq --slurp '.[-1] | .data[".dockercfg"]' --raw-output | base64 --decode | jq
      '.["image-registry.openshift-image-registry.svc:5000"].auth' --raw-output |
      tr -d '\n'
    name: token_image-puller_$(cluster)_reg_auth_value.txt
  item_name: build_farm
  params:
    cluster:
    - app.ci
    - arm01
    - build01
    - build02
    - vsphere
- fields:
  - cmd: oc --kubeconfig /etc/build-farm-credentials/sa.config-updater.$(cluster).config get secrets --sort-by=.metadata.creationTimestamp
      --namespace ci -o json | jq '.items[] | select(.type=="kubernetes.io/dockercfg")
      | select(.metadata.annotations["kubernetes.io/service-account.name"]=="image-pusher")'
      | jq --slurp '.[-1] | .data[".dockercfg"]' --raw-output | base64 --decode |
      jq '.["image-registry.openshift-image-registry.svc.cluster.local:5000"].auth'
      --raw-output | tr -d '\n'
    name: token_image-pusher_$(cluster)_reg_auth_value.txt
  item_name: build_farm
  params:
    cluster:
    - app.ci
- fields:
  - cmd: oc --kubeconfig /etc/build-farm-credentials/sa.config-updater.$(cluster).config sa create-kubeconfig --namespace ci $(service_account)
      | sed "s/$(service_account)/$(cluster)/g"
    name: sa.$(service_account).$(cluster).config
  item_name: ci-chat-bot
  params:
    cluster:
    - arm01
    - build01
    - build02
    - vsphere
    service_account:
    - ci-chat-bot
- fields:
  - cmd: oc --kubeconfig /etc/build-farm-credentials/sa.config-updater.$(cluster).config sa create-kubeconfig --namespace ci $(service_account)
      | sed "s/$(service_account)/$(cluster)/g"
    name: sa.$(service_account).$(cluster).config
  item_name: pod-scaler
  params:
    cluster:
    - build01
    - build02
    - app.ci
    service_account:
    - pod-scaler
- fields:
  - cmd: oc --kubeconfig /etc/build-farm-credentials/sa.config-updater.$(cluster).config sa create-kubeconfig --namespace bparees $(service_account)
      | sed "s/$(service_account)/$(cluster)/g"
    name: sa.$(service_account).$(cluster).config
  item_name: endurance_cluster
  params:
    cluster:
    - app.ci
    service_account:
    - endurance
- fields:
  - cmd: oc --kubeconfig /etc/build-farm-credentials/sa.config-updater.$(cluster).config sa create-kubeconfig --namespace ci $(service_account)
      | sed "s/$(service_account)/$(cluster)/g"
    name: sa.$(service_account).$(cluster).config
  item_name: build_farm
  params:
    cluster:
    - hive
    service_account:
    - cluster-display
    - config-updater
    - dptp-pools-cm
    - hive
