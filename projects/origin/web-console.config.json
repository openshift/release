{
  "tag_specification": {
    "cluster": "https://api.ci.openshift.org",
    "namespace": "openshift",
    "name": "origin-v3.10",
    "tag": "",
    "tag_overrides": {}
  },
  "base_rpm_images": {
    "base": {
      "cluster": "https://api.ci.openshift.org",
      "namespace": "openshift",
      "name": "origin-v3.10",
      "tag": "base"
    }
  },
  "test_base_image": {
    "cluster": "https://api.ci.openshift.org",
    "namespace": "openshift",
    "name": "release",
    "tag": "nodejs-0.10"
  },

  "test_binary_build_commands": "yum install -y java-1.?.0-openjdk Xvfb firefox; yum clean all -y; dbus-uuidgen > /etc/machine-id",

  "tests": [
    {
      "as": "unit",
      "from": "src",
      "commands": "export PATH=$PATH:$(pwd)/node_modules/.bin; export HOME=/tmp; ARTIFACT_DIR=/tmp/artifacts make test",
      "artifact_dir": "/tmp/artifacts"
    }
  ]
}